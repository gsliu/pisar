<?xml version='1.0' encoding='utf-8'?>
<svg xmlns:xlink="http://www.w3.org/1999/xlink" xmlns="http://www.w3.org/2000/svg" id="chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95" class="pygal-chart" viewBox="0 0 1000 600"><!--Generated with pygal 1.1.0 ©Kozea 2011-2013 on 2013-08-25--><!--http://pygal.org--><!--http://github.com/Kozea/pygal--><defs><style type="text/css">#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .title{font-family:monospace;font-size:16px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .legends .legend text{font-family:monospace;font-size:14px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis text{font-family:monospace;font-size:8px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis text.major{font-family:monospace;font-size:10px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .series text{font-family:monospace;font-size:8px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip text{font-family:monospace;font-size:16px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 text.no_data{font-size:64px}
#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95{background-color:black}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 path,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 rect,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 circle{-webkit-transition:1s ease-out;-moz-transition:1s ease-out;transition:1s ease-out}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .graph &gt; .background{fill:black}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .plot &gt; .background{fill:#111}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .graph{fill:#999}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 text.no_data{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .title{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .legends .legend text{fill:#999}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .legends .legend:hover text{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .line{stroke:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .guide.line{stroke:#555}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .major.line{stroke:#999}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis text.major{stroke:#eee;fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .line-graph .axis.x .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .stackedline-graph .axis.x .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .xy-graph .axis.x .guides:hover .guide.line{stroke:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .guides:hover text{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .reactive{fill-opacity:.1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .reactive.active,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .active .reactive{fill-opacity:.75}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .series text{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip rect{fill:#111;stroke:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip text{fill:#eee}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .country{fill:#999;stroke:#111 !important;opacity:.9;stroke-width:3;-webkit-transition:250ms;-moz-transition:250ms;-o-transition:250ms;transition:250ms}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .country:hover{opacity:1;stroke-width:10}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-0{stroke:#ff5995;fill:#ff5995}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-1{stroke:#b6e354;fill:#b6e354}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-2{stroke:#feed6c;fill:#feed6c}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-3{stroke:#8cedff;fill:#8cedff}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-4{stroke:#9e6ffe;fill:#9e6ffe}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-5{stroke:#899ca1;fill:#899ca1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-6{stroke:#f8f8f2;fill:#f8f8f2}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-7{stroke:#bf4646;fill:#bf4646}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-8{stroke:#516083;fill:#516083}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-9{stroke:#f92672;fill:#f92672}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-10{stroke:#82b414;fill:#82b414}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-11{stroke:#fd971f;fill:#fd971f}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-12{stroke:#56c2d6;fill:#56c2d6}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-13{stroke:#808384;fill:#808384}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-14{stroke:#8c54fe;fill:#8c54fe}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .color-15{stroke:#465457;fill:#465457}
#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 text.no_data{text-anchor:middle}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .guide.line{fill-opacity:0}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .centered{text-anchor:middle}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .title{text-anchor:middle}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .legends .legend text{fill-opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.x text{text-anchor:middle}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.x:not(.web) text[transform]{text-anchor:start}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y text{text-anchor:end}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y2 text{text-anchor:start}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y .logarithmic text:not(.major),#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y2 .logarithmic text:not(.major){font-size:50%}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .guide.line{stroke-dasharray:4,4}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .major.guide.line{stroke-dasharray:6,6}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis text.major{stroke-width:0.5px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .horizontal .axis.y .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .horizontal .axis.y2 .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .vertical .axis.x .guide.line{opacity:0}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis.y2 .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .line-graph .axis.x .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .gauge-graph .axis.x .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .stackedline-graph .axis.x .guides:hover .guide.line,#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .xy-graph .axis.x .guides:hover .guide.line{opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .axis .guides:hover text{opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .nofill{fill:none}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .dot{stroke-width:1px;fill-opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .dot.active{stroke-width:5px}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .series text{stroke:none}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 .series text.active{opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip rect{fill-opacity:0.8}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip text{fill-opacity:1}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 #tooltip text tspan.label{fill-opacity:.8}#chart-f3c882a6-2cc0-4851-b743-d6b55cad9a95 a:visited{fill:none}</style><script type="text/javascript">window.config = {"truncate_legend": null, "print_values": true, "tooltip_font_size": 16, "y_title": null, "height": 600, "legend_at_bottom": false, "show_legend": true, "show_dots": true, "explicit_size": false, "legend_font_size": 14, "x_labels_major_count": null, "major_label_font_size": 10, "fill": true, "style": {"opacity": ".1", "foreground": "#999", "plot_background": "#111", "foreground_light": "#eee", "transition": "1s ease-out", "foreground_dark": "#555", "colors": ["#ff5995", "#b6e354", "#feed6c", "#8cedff", "#9e6ffe", "#899ca1", "#f8f8f2", "#bf4646", "#516083", "#f92672", "#82b414", "#fd971f", "#56c2d6", "#808384", "#8c54fe", "#465457"], "background": "black", "opacity_hover": ".75"}, "x_title": null, "zero": 0, "show_minor_x_labels": false, "interpolation_parameters": {}, "x_labels_major": ["00:10:01", "01:10:01", "02:10:01", "03:10:01", "04:10:01", "05:10:01", "06:10:01", "07:10:01", "08:10:01", "09:10:01", "10:10:01", "11:10:01", "12:10:01", "13:10:01", "14:10:01", "15:10:01", "16:10:01", "17:10:02", "18:10:01", "19:10:01", "20:10:01", "21:10:01", "22:10:01", "23:10:01"], "rounded_bars": null, "label_font_size": 8, "show_y_labels": true, "dots_size": 1.5, "x_label_rotation": 40, "x_labels": ["00:10:01", "00:20:02", "00:30:01", "00:40:01", "00:50:01", "01:00:01", "01:10:01", "01:20:01", "01:30:01", "01:40:01", "01:50:01", "02:00:01", "02:10:01", "02:20:01", "02:30:01", "02:40:01", "02:50:01", "03:00:01", "03:10:01", "03:20:01", "03:30:01", "03:40:01", "03:50:01", "04:00:01", "04:10:01", "04:20:01", "04:30:01", "04:40:01", "04:50:01", "05:00:01", "05:10:01", "05:20:02", "05:30:01", "05:40:01", "05:50:01", "06:00:01", "06:10:01", "06:20:01", "06:30:01", "06:40:01", "06:50:01", "07:00:01", "07:10:01", "07:20:01", "07:30:01", "07:40:02", "07:50:01", "08:00:01", "08:10:01", "08:20:01", "08:30:01", "08:40:01", "08:50:01", "09:00:01", "09:10:01", "09:20:01", "09:30:01", "09:40:01", "09:50:01", "10:00:01", "10:10:01", "10:20:01", "10:30:01", "10:40:01", "10:50:01", "11:00:01", "11:10:01", "11:20:02", "11:30:01", "11:40:01", "11:50:01", "12:00:01", "12:10:01", "12:20:01", "12:30:01", "12:40:01", "12:50:01", "13:00:01", "13:10:01", "13:20:01", "13:30:01", "13:40:01", "13:50:01", "14:00:01", "14:10:01", "14:20:01", "14:30:01", "14:40:01", "14:50:01", "15:00:02", "15:10:01", "15:20:01", "15:30:01", "15:40:01", "15:50:01", "16:00:01", "16:10:01", "16:20:01", "16:30:01", "16:40:01", "16:50:01", "17:00:01", "17:10:02", "17:20:03", "17:30:01", "17:40:01", "17:50:01", "18:00:01", "18:10:01", "18:20:01", "18:30:01", "18:40:01", "18:50:01", "19:00:01", "19:10:01", "19:20:01", "19:30:01", "19:40:01", "19:50:01", "20:00:01", "20:10:01", "20:20:01", "20:30:01", "20:40:01", "20:50:01", "21:00:01", "21:10:01", "21:20:01", "21:30:01", "21:40:01", "21:50:01", "22:00:01", "22:10:01", "22:20:01", "22:30:01", "22:40:01", "22:50:01", "23:00:01", "23:10:01", "23:20:01", "23:30:01", "23:40:01", "23:50:01"], "value_font_size": 8, "disable_xml_declaration": false, "strict": false, "css": ["style.css", "graph.css"], "human_readable": false, "spacing": 10, "y_label_rotation": 0, "pretty_print": false, "tooltip_border_radius": 0, "interpolate": null, "js": ["./svg.jquery.js", "./pygal-tooltips.js"], "y_labels": null, "interpolation_precision": 250, "value_formatter": null, "no_data_font_size": 64, "logarithmic": true, "print_zeroes": false, "no_data_text": "No data", "x_labels_major_every": null, "title": "CPU0", "legend_box_size": 12, "no_prefix": false, "stroke": true, "include_x_axis": false, "range": null, "truncate_label": null, "order_min": null, "title_font_size": 16, "margin": 20, "width": 1000}</script><script type="text/javascript">/*!
 * jQuery JavaScript Library v1.7.2 modified by Florian Mounier to work with SVG
 * http://jquery.com/
 *
 * Copyright 2011, John Resig
 * Dual licensed under the MIT or GPL Version 2 licenses.
 * http://jquery.org/license
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 * Copyright 2011, The Dojo Foundation
 * Released under the MIT, BSD, and GPL Licenses.
 *
 * Date: Wed Mar 21 12:46:34 2012 -0700
 */
(function( window, undefined ) {

// Use the correct document accordingly with window argument (sandbox)
var document = window.document,
	navigator = window.navigator,
	location = window.location;
var jQuery = (function() {

// Define a local copy of jQuery
var jQuery = function( selector, context ) {
		// The jQuery object is actually just the init constructor 'enhanced'
		return new jQuery.fn.init( selector, context, rootjQuery );
	},

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$,

	// A central reference to the root jQuery(document)
	rootjQuery,

	// A simple way to check for HTML strings or ID strings
	// Prioritize #id over &lt;tag&gt; to avoid XSS via location.hash (#9521)
	quickExpr = /^(?:[^#&lt;]*(&lt;[\w\W]+&gt;)[^&gt;]*$|#([\w\-]*)$)/,

	// Check if a string has a non-whitespace character in it
	rnotwhite = /\S/,

	// Used for trimming whitespace
	trimLeft = /^\s+/,
	trimRight = /\s+$/,

	// Match a standalone tag
	rsingleTag = /^&lt;(\w+)\s*\/?&gt;(?:&lt;\/\1&gt;)?$/,

	// JSON RegExp
	rvalidchars = /^[\],:{}\s]*$/,
	rvalidescape = /\\(?:["\\\/bfnrt]|u[0-9a-fA-F]{4})/g,
	rvalidtokens = /"[^"\\\n\r]*"|true|false|null|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?/g,
	rvalidbraces = /(?:^|:|,)(?:\s*\[)+/g,

	// Useragent RegExp
	rwebkit = /(webkit)[ \/]([\w.]+)/,
	ropera = /(opera)(?:.*version)?[ \/]([\w.]+)/,
	rmsie = /(msie) ([\w.]+)/,
	rmozilla = /(mozilla)(?:.*? rv:([\w.]+))?/,

	// Matches dashed string for camelizing
	rdashAlpha = /-([a-z]|[0-9])/ig,
	rmsPrefix = /^-ms-/,

	// Used by jQuery.camelCase as callback to replace()
	fcamelCase = function( all, letter ) {
		return ( letter + "" ).toUpperCase();
	},

	// Keep a UserAgent string for use with jQuery.browser
	userAgent = navigator.userAgent,

	// For matching the engine and version of the browser
	browserMatch,

	// The deferred used on DOM ready
	readyList,

	// The ready event handler
	DOMContentLoaded,

	// Save a reference to some core methods
	toString = Object.prototype.toString,
	hasOwn = Object.prototype.hasOwnProperty,
	push = Array.prototype.push,
	slice = Array.prototype.slice,
	trim = String.prototype.trim,
	indexOf = Array.prototype.indexOf,

	// [[Class]] -&gt; type pairs
	class2type = {};

jQuery.fn = jQuery.prototype = {
	constructor: jQuery,
	init: function( selector, context, rootjQuery ) {
		var match, elem, ret, doc;

		// Handle $(""), $(null), or $(undefined)
		if ( !selector ) {
			return this;
		}

		// Handle $(DOMElement)
		if ( selector.nodeType ) {
			this.context = this[0] = selector;
			this.length = 1;
			return this;
		}

		// The body element only exists once, optimize finding it
		if ( selector === "body" &amp;&amp; !context &amp;&amp; document.body ) {
			this.context = document;
			this[0] = document.body;
			this.selector = selector;
			this.length = 1;
			return this;
		}

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			// Are we dealing with HTML string or an ID?
			if ( selector.charAt(0) === "&lt;" &amp;&amp; selector.charAt( selector.length - 1 ) === "&gt;" &amp;&amp; selector.length &gt;= 3 ) {
				// Assume that strings that start and end with &lt;&gt; are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = quickExpr.exec( selector );
			}

			// Verify a match, and that no context was specified for #id
			if ( match &amp;&amp; (match[1] || !context) ) {

				// HANDLE: $(html) -&gt; $(array)
				if ( match[1] ) {
					context = context instanceof jQuery ? context[0] : context;
					doc = ( context ? context.ownerDocument || context : document );

					// If a single string is passed in and it's a single tag
					// just do a createElement and skip the rest
					ret = rsingleTag.exec( selector );

					if ( ret ) {
						if ( jQuery.isPlainObject( context ) ) {
							selector = [ document.createElementNS( 'http://www.w3.org/2000/svg', ret[1] ) ];
							jQuery.fn.attr.call( selector, context, true );

						} else {
							selector = [ doc.createElementNS( 'http://www.w3.org/2000/svg', ret[1] ) ];
						}

					} else {
						ret = jQuery.buildFragment( [ match[1] ], [ doc ] );
						selector = ( ret.cacheable ? jQuery.clone(ret.fragment) : ret.fragment ).childNodes;
					}

					return jQuery.merge( this, selector );

				// HANDLE: $("#id")
				} else {
					elem = document.getElementById( match[2] );

					// Check parentNode to catch when Blackberry 4.6 returns
					// nodes that are no longer in the document #6963
					if ( elem &amp;&amp; elem.parentNode ) {
						// Handle the case where IE and Opera return items
						// by name instead of ID
						if ( elem.id !== match[2] ) {
							return rootjQuery.find( selector );
						}

						// Otherwise, we inject the element directly into the jQuery object
						this.length = 1;
						this[0] = elem;
					}

					this.context = document;
					this.selector = selector;
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || rootjQuery ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( jQuery.isFunction( selector ) ) {
			return rootjQuery.ready( selector );
		}

		if ( selector.selector !== undefined ) {
			this.selector = selector.selector;
			this.context = selector.context;
		}

		return jQuery.makeArray( selector, this );
	},

	// Start with an empty selector
	selector: "",

	// The current version of jQuery being used
	jquery: "1.7.2",

	// The default length of a jQuery object is 0
	length: 0,

	// The number of elements contained in the matched element set
	size: function() {
		return this.length;
	},

	toArray: function() {
		return slice.call( this, 0 );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {
		return num == null ?

			// Return a 'clean' array
			this.toArray() :

			// Return just the object
			( num &lt; 0 ? this[ this.length + num ] : this[ num ] );
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems, name, selector ) {
		// Build a new jQuery matched element set
		var ret = this.constructor();

		if ( jQuery.isArray( elems ) ) {
			push.apply( ret, elems );

		} else {
			jQuery.merge( ret, elems );
		}

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;

		ret.context = this.context;

		if ( name === "find" ) {
			ret.selector = this.selector + ( this.selector ? " " : "" ) + selector;
		} else if ( name ) {
			ret.selector = this.selector + "." + name + "(" + selector + ")";
		}

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	// (You can seed the arguments with an array of args, but this is
	// only used internally.)
	each: function( callback, args ) {
		return jQuery.each( this, callback, args );
	},

	ready: function( fn ) {
		// Attach the listeners
		jQuery.bindReady();

		// Add the callback
		readyList.add( fn );

		return this;
	},

	eq: function( i ) {
		i = +i;
		return i === -1 ?
			this.slice( i ) :
			this.slice( i, i + 1 );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	slice: function() {
		return this.pushStack( slice.apply( this, arguments ),
			"slice", slice.call(arguments).join(",") );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map(this, function( elem, i ) {
			return callback.call( elem, i, elem );
		}));
	},

	end: function() {
		return this.prevObject || this.constructor(null);
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: push,
	sort: [].sort,
	splice: [].splice
};

// Give the init function the jQuery prototype for later instantiation
jQuery.fn.init.prototype = jQuery.fn;

jQuery.extend = jQuery.fn.extend = function() {
	var options, name, src, copy, copyIsArray, clone,
		target = arguments[0] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;
		target = arguments[1] || {};
		// skip the boolean and the target
		i = 2;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" &amp;&amp; !jQuery.isFunction(target) ) {
		target = {};
	}

	// extend jQuery itself if only one argument is passed
	if ( length === i ) {
		target = this;
		--i;
	}

	for ( ; i &lt; length; i++ ) {
		// Only deal with non-null/undefined values
		if ( (options = arguments[ i ]) != null ) {
			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep &amp;&amp; copy &amp;&amp; ( jQuery.isPlainObject(copy) || (copyIsArray = jQuery.isArray(copy)) ) ) {
					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src &amp;&amp; jQuery.isArray(src) ? src : [];

					} else {
						clone = src &amp;&amp; jQuery.isPlainObject(src) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend({
	noConflict: function( deep ) {
		if ( window.$ === jQuery ) {
			window.$ = _$;
		}

		if ( deep &amp;&amp; window.jQuery === jQuery ) {
			window.jQuery = _jQuery;
		}

		return jQuery;
	},

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See #6781
	readyWait: 1,

	// Hold (or release) the ready event
	holdReady: function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	},

	// Handle when the DOM is ready
	ready: function( wait ) {
		// Either a released hold or an DOMready/load event and not yet ready
		if ( (wait === true &amp;&amp; !--jQuery.readyWait) || (wait !== true &amp;&amp; !jQuery.isReady) ) {
			// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).
		  //    if ( !document.body ) {
		  //    	return setTimeout( jQuery.ready, 1 );
		  //    }

			// Remember that the DOM is ready
			jQuery.isReady = true;

			// If a normal DOM Ready event fired, decrement, and wait if need be
			if ( wait !== true &amp;&amp; --jQuery.readyWait &gt; 0 ) {
				return;
			}

			// If there are functions bound, to execute
			readyList.fireWith( document, [ jQuery ] );

			// Trigger any bound ready events
			if ( jQuery.fn.trigger ) {
				jQuery( document ).trigger( "ready" ).off( "ready" );
			}
		}
	},

	bindReady: function() {
		if ( readyList ) {
			return;
		}

		readyList = jQuery.Callbacks( "once memory" );

		// Catch cases where $(document).ready() is called after the
		// browser event has already occurred.
		if ( document.readyState === "complete" ) {
			// Handle it asynchronously to allow scripts the opportunity to delay ready
			return setTimeout( jQuery.ready, 1 );
		}

		// Mozilla, Opera and webkit nightlies currently support this event
		if ( document.addEventListener ) {
			// Use the handy event callback
			document.addEventListener( "DOMContentLoaded", DOMContentLoaded, false );

			// A fallback to window.onload, that will always work
			window.addEventListener( "load", jQuery.ready, false );

		// If IE event model is used
		} else if ( document.attachEvent ) {
			// ensure firing before onload,
			// maybe late but safe also for iframes
			document.attachEvent( "onreadystatechange", DOMContentLoaded );

			// A fallback to window.onload, that will always work
			window.attachEvent( "onload", jQuery.ready );

			// If IE and not a frame
			// continually check to see if the document is ready
			var toplevel = false;

			try {
				toplevel = window.frameElement == null;
			} catch(e) {}

			if ( document.documentElement.doScroll &amp;&amp; toplevel ) {
				doScrollCheck();
			}
		}
	},

	// See test/unit/core.js for details concerning isFunction.
	// Since version 1.3, DOM methods and functions like alert
	// aren't supported. They return false on IE (#2968).
	isFunction: function( obj ) {
		return jQuery.type(obj) === "function";
	},

	isArray: Array.isArray || function( obj ) {
		return jQuery.type(obj) === "array";
	},

	isWindow: function( obj ) {
		return obj != null &amp;&amp; obj == obj.window;
	},

	isNumeric: function( obj ) {
		return !isNaN( parseFloat(obj) ) &amp;&amp; isFinite( obj );
	},

	type: function( obj ) {
		return obj == null ?
			String( obj ) :
			class2type[ toString.call(obj) ] || "object";
	},

	isPlainObject: function( obj ) {
		// Must be an Object.
		// Because of IE, we also have to check the presence of the constructor property.
		// Make sure that DOM nodes and window objects don't pass through, as well
		if ( !obj || jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {
			return false;
		}

		try {
			// Not own constructor property must be Object
			if ( obj.constructor &amp;&amp;
				!hasOwn.call(obj, "constructor") &amp;&amp;
				!hasOwn.call(obj.constructor.prototype, "isPrototypeOf") ) {
				return false;
			}
		} catch ( e ) {
			// IE8,9 Will throw exceptions on certain host objects #9897
			return false;
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own.

		var key;
		for ( key in obj ) {}

		return key === undefined || hasOwn.call( obj, key );
	},

	isEmptyObject: function( obj ) {
		for ( var name in obj ) {
			return false;
		}
		return true;
	},

	error: function( msg ) {
		throw new Error( msg );
	},

	parseJSON: function( data ) {
		if ( typeof data !== "string" || !data ) {
			return null;
		}

		// Make sure leading/trailing whitespace is removed (IE can't handle it)
		data = jQuery.trim( data );

		// Attempt to parse using the native JSON parser first
		if ( window.JSON &amp;&amp; window.JSON.parse ) {
			return window.JSON.parse( data );
		}

		// Make sure the incoming data is actual JSON
		// Logic borrowed from http://json.org/json2.js
		if ( rvalidchars.test( data.replace( rvalidescape, "@" )
			.replace( rvalidtokens, "]" )
			.replace( rvalidbraces, "")) ) {

			return ( new Function( "return " + data ) )();

		}
		jQuery.error( "Invalid JSON: " + data );
	},

	// Cross-browser xml parsing
	parseXML: function( data ) {
		if ( typeof data !== "string" || !data ) {
			return null;
		}
		var xml, tmp;
		try {
			if ( window.DOMParser ) { // Standard
				tmp = new DOMParser();
				xml = tmp.parseFromString( data , "text/xml" );
			} else { // IE
				xml = new ActiveXObject( "Microsoft.XMLDOM" );
				xml.async = "false";
				xml.loadXML( data );
			}
		} catch( e ) {
			xml = undefined;
		}
		if ( !xml || !xml.documentElement || xml.getElementsByTagName( "parsererror" ).length ) {
			jQuery.error( "Invalid XML: " + data );
		}
		return xml;
	},

	noop: function() {},

	// Evaluates a script in a global context
	// Workarounds based on findings by Jim Driscoll
	// http://weblogs.java.net/blog/driscoll/archive/2009/09/08/eval-javascript-global-context
	globalEval: function( data ) {
		if ( data &amp;&amp; rnotwhite.test( data ) ) {
			// We use execScript on Internet Explorer
			// We use an anonymous function so that context is window
			// rather than jQuery in Firefox
			( window.execScript || function( data ) {
				window[ "eval" ].call( window, data );
			} )( data );
		}
	},

	// Convert dashed to camelCase; used by the css and data modules
	// Microsoft forgot to hump their vendor prefix (#9572)
	camelCase: function( string ) {
		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
	},

	nodeName: function( elem, name ) {
		return elem.nodeName &amp;&amp; elem.nodeName.toUpperCase() === name.toUpperCase();
	},

	// args is for internal usage only
	each: function( object, callback, args ) {
		var name, i = 0,
			length = object.length,
			isObj = length === undefined || jQuery.isFunction( object );

		if ( args ) {
			if ( isObj ) {
				for ( name in object ) {
					if ( callback.apply( object[ name ], args ) === false ) {
						break;
					}
				}
			} else {
				for ( ; i &lt; length; ) {
					if ( callback.apply( object[ i++ ], args ) === false ) {
						break;
					}
				}
			}

		// A special, fast, case for the most common use of each
		} else {
			if ( isObj ) {
				for ( name in object ) {
					if ( callback.call( object[ name ], name, object[ name ] ) === false ) {
						break;
					}
				}
			} else {
				for ( ; i &lt; length; ) {
					if ( callback.call( object[ i ], i, object[ i++ ] ) === false ) {
						break;
					}
				}
			}
		}

		return object;
	},

	// Use native String.trim function wherever possible
	trim: trim ?
		function( text ) {
			return text == null ?
				"" :
				trim.call( text );
		} :

		// Otherwise use our own trimming functionality
		function( text ) {
			return text == null ?
				"" :
				text.toString().replace( trimLeft, "" ).replace( trimRight, "" );
		},

	// results is for internal usage only
	makeArray: function( array, results ) {
		var ret = results || [];

		if ( array != null ) {
			// The window, strings (and functions) also have 'length'
			// Tweaked logic slightly to handle Blackberry 4.7 RegExp issues #6930
			var type = jQuery.type( array );

			if ( array.length == null || type === "string" || type === "function" || type === "regexp" || jQuery.isWindow( array ) ) {
				push.call( ret, array );
			} else {
				jQuery.merge( ret, array );
			}
		}

		return ret;
	},

	inArray: function( elem, array, i ) {
		var len;

		if ( array ) {
			if ( indexOf ) {
				return indexOf.call( array, elem, i );
			}

			len = array.length;
			i = i ? i &lt; 0 ? Math.max( 0, len + i ) : i : 0;

			for ( ; i &lt; len; i++ ) {
				// Skip accessing in sparse arrays
				if ( i in array &amp;&amp; array[ i ] === elem ) {
					return i;
				}
			}
		}

		return -1;
	},

	merge: function( first, second ) {
		var i = first.length,
			j = 0;

		if ( typeof second.length === "number" ) {
			for ( var l = second.length; j &lt; l; j++ ) {
				first[ i++ ] = second[ j ];
			}

		} else {
			while ( second[j] !== undefined ) {
				first[ i++ ] = second[ j++ ];
			}
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, inv ) {
		var ret = [], retVal;
		inv = !!inv;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( var i = 0, length = elems.length; i &lt; length; i++ ) {
			retVal = !!callback( elems[ i ], i );
			if ( inv !== retVal ) {
				ret.push( elems[ i ] );
			}
		}

		return ret;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var value, key, ret = [],
			i = 0,
			length = elems.length,
			// jquery objects are treated as arrays
			isArray = elems instanceof jQuery || length !== undefined &amp;&amp; typeof length === "number" &amp;&amp; ( ( length &gt; 0 &amp;&amp; elems[ 0 ] &amp;&amp; elems[ length -1 ] ) || length === 0 || jQuery.isArray( elems ) ) ;

		// Go through the array, translating each of the items to their
		if ( isArray ) {
			for ( ; i &lt; length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}

		// Go through every key on the object,
		} else {
			for ( key in elems ) {
				value = callback( elems[ key ], key, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}
		}

		// Flatten any nested arrays
		return ret.concat.apply( [], ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// Bind a function to a context, optionally partially applying any
	// arguments.
	proxy: function( fn, context ) {
		if ( typeof context === "string" ) {
			var tmp = fn[ context ];
			context = fn;
			fn = tmp;
		}

		// Quick check to determine if target is callable, in the spec
		// this throws a TypeError, but we will just return undefined.
		if ( !jQuery.isFunction( fn ) ) {
			return undefined;
		}

		// Simulated bind
		var args = slice.call( arguments, 2 ),
			proxy = function() {
				return fn.apply( context, args.concat( slice.call( arguments ) ) );
			};

		// Set the guid of unique handler to the same of original handler, so it can be removed
		proxy.guid = fn.guid = fn.guid || proxy.guid || jQuery.guid++;

		return proxy;
	},

	// Mutifunctional method to get and set values to a collection
	// The value/s can optionally be executed if it's a function
	access: function( elems, fn, key, value, chainable, emptyGet, pass ) {
		var exec,
			bulk = key == null,
			i = 0,
			length = elems.length;

		// Sets many values
		if ( key &amp;&amp; typeof key === "object" ) {
			for ( i in key ) {
				jQuery.access( elems, fn, i, key[i], 1, emptyGet, value );
			}
			chainable = 1;

		// Sets one value
		} else if ( value !== undefined ) {
			// Optionally, function values get executed if exec is true
			exec = pass === undefined &amp;&amp; jQuery.isFunction( value );

			if ( bulk ) {
				// Bulk operations only iterate when executing function values
				if ( exec ) {
					exec = fn;
					fn = function( elem, key, value ) {
						return exec.call( jQuery( elem ), value );
					};

				// Otherwise they run against the entire set
				} else {
					fn.call( elems, value );
					fn = null;
				}
			}

			if ( fn ) {
				for (; i &lt; length; i++ ) {
					fn( elems[i], key, exec ? value.call( elems[i], i, fn( elems[i], key ) ) : value, pass );
				}
			}

			chainable = 1;
		}

		return chainable ?
			elems :

			// Gets
			bulk ?
				fn.call( elems ) :
				length ? fn( elems[0], key ) : emptyGet;
	},

	now: function() {
		return ( new Date() ).getTime();
	},

	// Use of jQuery.browser is frowned upon.
	// More details: http://docs.jquery.com/Utilities/jQuery.browser
	uaMatch: function( ua ) {
		ua = ua.toLowerCase();

		var match = rwebkit.exec( ua ) ||
			ropera.exec( ua ) ||
			rmsie.exec( ua ) ||
			ua.indexOf("compatible") &lt; 0 &amp;&amp; rmozilla.exec( ua ) ||
			[];

		return { browser: match[1] || "", version: match[2] || "0" };
	},

	sub: function() {
		function jQuerySub( selector, context ) {
			return new jQuerySub.fn.init( selector, context );
		}
		jQuery.extend( true, jQuerySub, this );
		jQuerySub.superclass = this;
		jQuerySub.fn = jQuerySub.prototype = this();
		jQuerySub.fn.constructor = jQuerySub;
		jQuerySub.sub = this.sub;
		jQuerySub.fn.init = function init( selector, context ) {
			if ( context &amp;&amp; context instanceof jQuery &amp;&amp; !(context instanceof jQuerySub) ) {
				context = jQuerySub( context );
			}

			return jQuery.fn.init.call( this, selector, context, rootjQuerySub );
		};
		jQuerySub.fn.init.prototype = jQuerySub.fn;
		var rootjQuerySub = jQuerySub(document);
		return jQuerySub;
	},

	browser: {}
});

// Populate the class2type map
jQuery.each("Boolean Number String Function Array Date RegExp Object".split(" "), function(i, name) {
	class2type[ "[object " + name + "]" ] = name.toLowerCase();
});

browserMatch = jQuery.uaMatch( userAgent );
if ( browserMatch.browser ) {
	jQuery.browser[ browserMatch.browser ] = true;
	jQuery.browser.version = browserMatch.version;
}

// Deprecated, use jQuery.browser.webkit instead
if ( jQuery.browser.webkit ) {
	jQuery.browser.safari = true;
}

// IE doesn't match non-breaking spaces with \s
if ( rnotwhite.test( "\xA0" ) ) {
	trimLeft = /^[\s\xA0]+/;
	trimRight = /[\s\xA0]+$/;
}

// All jQuery objects should point back to these
rootjQuery = jQuery(document);

// Cleanup functions for the document ready method
if ( document.addEventListener ) {
	DOMContentLoaded = function() {
		document.removeEventListener( "DOMContentLoaded", DOMContentLoaded, false );
		jQuery.ready();
	};

} else if ( document.attachEvent ) {
	DOMContentLoaded = function() {
		// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).
		if ( document.readyState === "complete" ) {
			document.detachEvent( "onreadystatechange", DOMContentLoaded );
			jQuery.ready();
		}
	};
}

// The DOM ready check for Internet Explorer
function doScrollCheck() {
	if ( jQuery.isReady ) {
		return;
	}

	try {
		// If IE is used, use the trick by Diego Perini
		// http://javascript.nwbox.com/IEContentLoaded/
		document.documentElement.doScroll("left");
	} catch(e) {
		setTimeout( doScrollCheck, 1 );
		return;
	}

	// and execute any waiting functions
	jQuery.ready();
}

return jQuery;

})();


// String to Object flags format cache
var flagsCache = {};

// Convert String-formatted flags into Object-formatted ones and store in cache
function createFlags( flags ) {
	var object = flagsCache[ flags ] = {},
		i, length;
	flags = flags.split( /\s+/ );
	for ( i = 0, length = flags.length; i &lt; length; i++ ) {
		object[ flags[i] ] = true;
	}
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	flags:	an optional list of space-separated flags that will change how
 *			the callback list behaves
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible flags:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( flags ) {

	// Convert flags from String-formatted to Object-formatted
	// (we check in cache first)
	flags = flags ? ( flagsCache[ flags ] || createFlags( flags ) ) : {};

	var // Actual callback list
		list = [],
		// Stack of fire calls for repeatable lists
		stack = [],
		// Last fire value (for non-forgettable lists)
		memory,
		// Flag to know if list was already fired
		fired,
		// Flag to know if list is currently firing
		firing,
		// First callback to fire (used internally by add and fireWith)
		firingStart,
		// End of the loop when firing
		firingLength,
		// Index of currently firing callback (modified by remove if needed)
		firingIndex,
		// Add one or several callbacks to the list
		add = function( args ) {
			var i,
				length,
				elem,
				type,
				actual;
			for ( i = 0, length = args.length; i &lt; length; i++ ) {
				elem = args[ i ];
				type = jQuery.type( elem );
				if ( type === "array" ) {
					// Inspect recursively
					add( elem );
				} else if ( type === "function" ) {
					// Add if not in unique mode and callback is not in
					if ( !flags.unique || !self.has( elem ) ) {
						list.push( elem );
					}
				}
			}
		},
		// Fire callbacks
		fire = function( context, args ) {
			args = args || [];
			memory = !flags.memory || [ context, args ];
			fired = true;
			firing = true;
			firingIndex = firingStart || 0;
			firingStart = 0;
			firingLength = list.length;
			for ( ; list &amp;&amp; firingIndex &lt; firingLength; firingIndex++ ) {
				if ( list[ firingIndex ].apply( context, args ) === false &amp;&amp; flags.stopOnFalse ) {
					memory = true; // Mark as halted
					break;
				}
			}
			firing = false;
			if ( list ) {
				if ( !flags.once ) {
					if ( stack &amp;&amp; stack.length ) {
						memory = stack.shift();
						self.fireWith( memory[ 0 ], memory[ 1 ] );
					}
				} else if ( memory === true ) {
					self.disable();
				} else {
					list = [];
				}
			}
		},
		// Actual Callbacks object
		self = {
			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {
					var length = list.length;
					add( arguments );
					// Do we need to add the callbacks to the
					// current firing batch?
					if ( firing ) {
						firingLength = list.length;
					// With memory, if we're not firing then
					// we should call right away, unless previous
					// firing was halted (stopOnFalse)
					} else if ( memory &amp;&amp; memory !== true ) {
						firingStart = length;
						fire( memory[ 0 ], memory[ 1 ] );
					}
				}
				return this;
			},
			// Remove a callback from the list
			remove: function() {
				if ( list ) {
					var args = arguments,
						argIndex = 0,
						argLength = args.length;
					for ( ; argIndex &lt; argLength ; argIndex++ ) {
						for ( var i = 0; i &lt; list.length; i++ ) {
							if ( args[ argIndex ] === list[ i ] ) {
								// Handle firingIndex and firingLength
								if ( firing ) {
									if ( i &lt;= firingLength ) {
										firingLength--;
										if ( i &lt;= firingIndex ) {
											firingIndex--;
										}
									}
								}
								// Remove the element
								list.splice( i--, 1 );
								// If we have some unicity property then
								// we only need to do this once
								if ( flags.unique ) {
									break;
								}
							}
						}
					}
				}
				return this;
			},
			// Control if a given callback is in the list
			has: function( fn ) {
				if ( list ) {
					var i = 0,
						length = list.length;
					for ( ; i &lt; length; i++ ) {
						if ( fn === list[ i ] ) {
							return true;
						}
					}
				}
				return false;
			},
			// Remove all callbacks from the list
			empty: function() {
				list = [];
				return this;
			},
			// Have the list do nothing anymore
			disable: function() {
				list = stack = memory = undefined;
				return this;
			},
			// Is it disabled?
			disabled: function() {
				return !list;
			},
			// Lock the list in its current state
			lock: function() {
				stack = undefined;
				if ( !memory || memory === true ) {
					self.disable();
				}
				return this;
			},
			// Is it locked?
			locked: function() {
				return !stack;
			},
			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				if ( stack ) {
					if ( firing ) {
						if ( !flags.once ) {
							stack.push( [ context, args ] );
						}
					} else if ( !( flags.once &amp;&amp; memory ) ) {
						fire( context, args );
					}
				}
				return this;
			},
			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},
			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};




var // Static reference to slice
	sliceDeferred = [].slice;

jQuery.extend({

	Deferred: function( func ) {
		var doneList = jQuery.Callbacks( "once memory" ),
			failList = jQuery.Callbacks( "once memory" ),
			progressList = jQuery.Callbacks( "memory" ),
			state = "pending",
			lists = {
				resolve: doneList,
				reject: failList,
				notify: progressList
			},
			promise = {
				done: doneList.add,
				fail: failList.add,
				progress: progressList.add,

				state: function() {
					return state;
				},

				// Deprecated
				isResolved: doneList.fired,
				isRejected: failList.fired,

				then: function( doneCallbacks, failCallbacks, progressCallbacks ) {
					deferred.done( doneCallbacks ).fail( failCallbacks ).progress( progressCallbacks );
					return this;
				},
				always: function() {
					deferred.done.apply( deferred, arguments ).fail.apply( deferred, arguments );
					return this;
				},
				pipe: function( fnDone, fnFail, fnProgress ) {
					return jQuery.Deferred(function( newDefer ) {
						jQuery.each( {
							done: [ fnDone, "resolve" ],
							fail: [ fnFail, "reject" ],
							progress: [ fnProgress, "notify" ]
						}, function( handler, data ) {
							var fn = data[ 0 ],
								action = data[ 1 ],
								returned;
							if ( jQuery.isFunction( fn ) ) {
								deferred[ handler ](function() {
									returned = fn.apply( this, arguments );
									if ( returned &amp;&amp; jQuery.isFunction( returned.promise ) ) {
										returned.promise().then( newDefer.resolve, newDefer.reject, newDefer.notify );
									} else {
										newDefer[ action + "With" ]( this === deferred ? newDefer : this, [ returned ] );
									}
								});
							} else {
								deferred[ handler ]( newDefer[ action ] );
							}
						});
					}).promise();
				},
				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					if ( obj == null ) {
						obj = promise;
					} else {
						for ( var key in promise ) {
							obj[ key ] = promise[ key ];
						}
					}
					return obj;
				}
			},
			deferred = promise.promise({}),
			key;

		for ( key in lists ) {
			deferred[ key ] = lists[ key ].fire;
			deferred[ key + "With" ] = lists[ key ].fireWith;
		}

		// Handle state
		deferred.done( function() {
			state = "resolved";
		}, failList.disable, progressList.lock ).fail( function() {
			state = "rejected";
		}, doneList.disable, progressList.lock );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( firstParam ) {
		var args = sliceDeferred.call( arguments, 0 ),
			i = 0,
			length = args.length,
			pValues = new Array( length ),
			count = length,
			pCount = length,
			deferred = length &lt;= 1 &amp;&amp; firstParam &amp;&amp; jQuery.isFunction( firstParam.promise ) ?
				firstParam :
				jQuery.Deferred(),
			promise = deferred.promise();
		function resolveFunc( i ) {
			return function( value ) {
				args[ i ] = arguments.length &gt; 1 ? sliceDeferred.call( arguments, 0 ) : value;
				if ( !( --count ) ) {
					deferred.resolveWith( deferred, args );
				}
			};
		}
		function progressFunc( i ) {
			return function( value ) {
				pValues[ i ] = arguments.length &gt; 1 ? sliceDeferred.call( arguments, 0 ) : value;
				deferred.notifyWith( promise, pValues );
			};
		}
		if ( length &gt; 1 ) {
			for ( ; i &lt; length; i++ ) {
				if ( args[ i ] &amp;&amp; args[ i ].promise &amp;&amp; jQuery.isFunction( args[ i ].promise ) ) {
					args[ i ].promise().then( resolveFunc(i), deferred.reject, progressFunc(i) );
				} else {
					--count;
				}
			}
			if ( !count ) {
				deferred.resolveWith( deferred, args );
			}
		} else if ( deferred !== firstParam ) {
			deferred.resolveWith( deferred, length ? [ firstParam ] : [] );
		}
		return promise;
	}
});




jQuery.support = {
		// IE strips leading whitespace when .innerHTML is used
		leadingWhitespace: true,

		// Make sure that tbody elements aren't automatically inserted
		// IE will insert them into empty tables
		tbody: false,

		// Make sure that link elements get serialized correctly by innerHTML
		// This requires a wrapper element in IE
		htmlSerialize: true,

		// Get the style information from getAttribute
		// (IE uses .cssText instead)
		style: true,

		// Make sure that URLs aren't manipulated
		// (IE normalizes it by default)
		hrefNormalized: true,

		// Make sure that element opacity exists
		// (IE uses filter instead)
		// Use a regex to work around a WebKit issue. See #5145
		opacity: true,

		// Verify style float existence
		// (IE uses styleFloat instead of cssFloat)
		cssFloat: true,

		// Make sure that if no value is specified for a checkbox
		// that it defaults to "on".
		// (WebKit defaults to "" instead)
		checkOn: true,

		// Make sure that a selected-by-default option has a working selected property.
		// (WebKit defaults to false instead of true, IE too, if it's in an optgroup)
		optSelected: true,

		// Test setAttribute on camelCase class. If it works, we need attrFixes when doing get/setAttribute (ie6/7)
		getSetAttribute: true,

		// Tests for enctype support on a form(#6743)
		enctype: true,

		// Makes sure cloning an html5 element does not cause problems
		// Where outerHTML is undefined, this still works
		html5Clone: true,

		// Will be defined later
		submitBubbles: true,
		changeBubbles: true,
		focusinBubbles: false,
		deleteExpando: true,
		noCloneEvent: true,
		inlineBlockNeedsLayout: false,
		shrinkWrapBlocks: false,
		reliableMarginRight: true,
		pixelMargin: true
	};



var rbrace = /^(?:\{.*\}|\[.*\])$/,
	rmultiDash = /([A-Z])/g;

jQuery.extend({
	cache: {},

	// Please use with caution
	uuid: 0,

	// Unique for each copy of jQuery on the page
	// Non-digits removed to match rinlinejQuery
	expando: "jQuery" + ( jQuery.fn.jquery + Math.random() ).replace( /\D/g, "" ),

	// The following elements throw uncatchable exceptions if you
	// attempt to add expando properties to them.
	noData: {
		"embed": true,
		// Ban all objects except for Flash (which handle expandos)
		"object": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",
		"applet": true
	},

	hasData: function( elem ) {
		elem = elem.nodeType ? jQuery.cache[ elem[jQuery.expando] ] : elem[ jQuery.expando ];
		return !!elem &amp;&amp; !isEmptyDataObject( elem );
	},

	data: function( elem, name, data, pvt /* Internal Use Only */ ) {
		if ( !jQuery.acceptData( elem ) ) {
			return;
		}

		var privateCache, thisCache, ret,
			internalKey = jQuery.expando,
			getByName = typeof name === "string",

			// We have to handle DOM nodes and JS objects differently because IE6-7
			// can't GC object references properly across the DOM-JS boundary
			isNode = elem.nodeType,

			// Only DOM nodes need the global jQuery cache; JS object data is
			// attached directly to the object so GC can occur automatically
			cache = isNode ? jQuery.cache : elem,

			// Only defining an ID for JS objects if its cache already exists allows
			// the code to shortcut on the same path as a DOM node with no cache
			id = isNode ? elem[ internalKey ] : elem[ internalKey ] &amp;&amp; internalKey,
			isEvents = name === "events";

		// Avoid doing any more work than we need to when trying to get data on an
		// object that has no data at all
		if ( (!id || !cache[id] || (!isEvents &amp;&amp; !pvt &amp;&amp; !cache[id].data)) &amp;&amp; getByName &amp;&amp; data === undefined ) {
			return;
		}

		if ( !id ) {
			// Only DOM nodes need a new unique ID for each element since their data
			// ends up in the global cache
			if ( isNode ) {
				elem[ internalKey ] = id = ++jQuery.uuid;
			} else {
				id = internalKey;
			}
		}

		if ( !cache[ id ] ) {
			cache[ id ] = {};

			// Avoids exposing jQuery metadata on plain JS objects when the object
			// is serialized using JSON.stringify
			if ( !isNode ) {
				cache[ id ].toJSON = jQuery.noop;
			}
		}

		// An object can be passed to jQuery.data instead of a key/value pair; this gets
		// shallow copied over onto the existing cache
		if ( typeof name === "object" || typeof name === "function" ) {
			if ( pvt ) {
				cache[ id ] = jQuery.extend( cache[ id ], name );
			} else {
				cache[ id ].data = jQuery.extend( cache[ id ].data, name );
			}
		}

		privateCache = thisCache = cache[ id ];

		// jQuery data() is stored in a separate object inside the object's internal data
		// cache in order to avoid key collisions between internal data and user-defined
		// data.
		if ( !pvt ) {
			if ( !thisCache.data ) {
				thisCache.data = {};
			}

			thisCache = thisCache.data;
		}

		if ( data !== undefined ) {
			thisCache[ jQuery.camelCase( name ) ] = data;
		}

		// Users should not attempt to inspect the internal events object using jQuery.data,
		// it is undocumented and subject to change. But does anyone listen? No.
		if ( isEvents &amp;&amp; !thisCache[ name ] ) {
			return privateCache.events;
		}

		// Check for both converted-to-camel and non-converted data property names
		// If a data property was specified
		if ( getByName ) {

			// First Try to find as-is property data
			ret = thisCache[ name ];

			// Test for null|undefined property data
			if ( ret == null ) {

				// Try to find the camelCased property
				ret = thisCache[ jQuery.camelCase( name ) ];
			}
		} else {
			ret = thisCache;
		}

		return ret;
	},

	removeData: function( elem, name, pvt /* Internal Use Only */ ) {
		if ( !jQuery.acceptData( elem ) ) {
			return;
		}

		var thisCache, i, l,

			// Reference to internal data cache key
			internalKey = jQuery.expando,

			isNode = elem.nodeType,

			// See jQuery.data for more information
			cache = isNode ? jQuery.cache : elem,

			// See jQuery.data for more information
			id = isNode ? elem[ internalKey ] : internalKey;

		// If there is already no cache entry for this object, there is no
		// purpose in continuing
		if ( !cache[ id ] ) {
			return;
		}

		if ( name ) {

			thisCache = pvt ? cache[ id ] : cache[ id ].data;

			if ( thisCache ) {

				// Support array or space separated string names for data keys
				if ( !jQuery.isArray( name ) ) {

					// try the string as a key before any manipulation
					if ( name in thisCache ) {
						name = [ name ];
					} else {

						// split the camel cased version by spaces unless a key with the spaces exists
						name = jQuery.camelCase( name );
						if ( name in thisCache ) {
							name = [ name ];
						} else {
							name = name.split( " " );
						}
					}
				}

				for ( i = 0, l = name.length; i &lt; l; i++ ) {
					delete thisCache[ name[i] ];
				}

				// If there is no data left in the cache, we want to continue
				// and let the cache object itself get destroyed
				if ( !( pvt ? isEmptyDataObject : jQuery.isEmptyObject )( thisCache ) ) {
					return;
				}
			}
		}

		// See jQuery.data for more information
		if ( !pvt ) {
			delete cache[ id ].data;

			// Don't destroy the parent cache unless the internal data object
			// had been the only thing left in it
			if ( !isEmptyDataObject(cache[ id ]) ) {
				return;
			}
		}

		// Browsers that fail expando deletion also refuse to delete expandos on
		// the window, but it will allow it on all other JS objects; other browsers
		// don't care
		// Ensure that `cache` is not a window object #10080
		if ( jQuery.support.deleteExpando || !cache.setInterval ) {
			delete cache[ id ];
		} else {
			cache[ id ] = null;
		}

		// We destroyed the cache and need to eliminate the expando on the node to avoid
		// false lookups in the cache for entries that no longer exist
		if ( isNode ) {
			// IE does not allow us to delete expando properties from nodes,
			// nor does it have a removeAttribute function on Document nodes;
			// we must handle all of these cases
			if ( jQuery.support.deleteExpando ) {
				delete elem[ internalKey ];
			} else if ( elem.removeAttribute ) {
				elem.removeAttribute( internalKey );
			} else {
				elem[ internalKey ] = null;
			}
		}
	},

	// For internal use only.
	_data: function( elem, name, data ) {
		return jQuery.data( elem, name, data, true );
	},

	// A method for determining if a DOM node can handle the data expando
	acceptData: function( elem ) {
		if ( elem.nodeName ) {
			var match = jQuery.noData[ elem.nodeName.toLowerCase() ];

			if ( match ) {
				return !(match === true || elem.getAttribute("classid") !== match);
			}
		}

		return true;
	}
});

jQuery.fn.extend({
	data: function( key, value ) {
		var parts, part, attr, name, l,
			elem = this[0],
			i = 0,
			data = null;

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = jQuery.data( elem );

				if ( elem.nodeType === 1 &amp;&amp; !jQuery._data( elem, "parsedAttrs" ) ) {
					attr = elem.attributes;
					for ( l = attr.length; i &lt; l; i++ ) {
						name = attr[i].name;

						if ( name.indexOf( "data-" ) === 0 ) {
							name = jQuery.camelCase( name.substring(5) );

							dataAttr( elem, name, data[ name ] );
						}
					}
					jQuery._data( elem, "parsedAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each(function() {
				jQuery.data( this, key );
			});
		}

		parts = key.split( ".", 2 );
		parts[1] = parts[1] ? "." + parts[1] : "";
		part = parts[1] + "!";

		return jQuery.access( this, function( value ) {

			if ( value === undefined ) {
				data = this.triggerHandler( "getData" + part, [ parts[0] ] );

				// Try to fetch any internally stored data first
				if ( data === undefined &amp;&amp; elem ) {
					data = jQuery.data( elem, key );
					data = dataAttr( elem, key, data );
				}

				return data === undefined &amp;&amp; parts[1] ?
					this.data( parts[0] ) :
					data;
			}

			parts[1] = value;
			this.each(function() {
				var self = jQuery( this );

				self.triggerHandler( "setData" + part, parts );
				jQuery.data( this, key, value );
				self.triggerHandler( "changeData" + part, parts );
			});
		}, null, value, arguments.length &gt; 1, null, false );
	},

	removeData: function( key ) {
		return this.each(function() {
			jQuery.removeData( this, key );
		});
	}
});

function dataAttr( elem, key, data ) {
	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined &amp;&amp; elem.nodeType === 1 ) {

		var name = "data-" + key.replace( rmultiDash, "-$1" ).toLowerCase();

		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = data === "true" ? true :
				data === "false" ? false :
				data === "null" ? null :
				jQuery.isNumeric( data ) ? +data :
					rbrace.test( data ) ? jQuery.parseJSON( data ) :
					data;
			} catch( e ) {}

			// Make sure we set the data so it isn't changed later
			jQuery.data( elem, key, data );

		} else {
			data = undefined;
		}
	}

	return data;
}

// checks a cache object for emptiness
function isEmptyDataObject( obj ) {
	for ( var name in obj ) {

		// if the public data object is empty, the private is still empty
		if ( name === "data" &amp;&amp; jQuery.isEmptyObject( obj[name] ) ) {
			continue;
		}
		if ( name !== "toJSON" ) {
			return false;
		}
	}

	return true;
}




function handleQueueMarkDefer( elem, type, src ) {
	var deferDataKey = type + "defer",
		queueDataKey = type + "queue",
		markDataKey = type + "mark",
		defer = jQuery._data( elem, deferDataKey );
	if ( defer &amp;&amp;
		( src === "queue" || !jQuery._data(elem, queueDataKey) ) &amp;&amp;
		( src === "mark" || !jQuery._data(elem, markDataKey) ) ) {
		// Give room for hard-coded callbacks to fire first
		// and eventually mark/queue something else on the element
		setTimeout( function() {
			if ( !jQuery._data( elem, queueDataKey ) &amp;&amp;
				!jQuery._data( elem, markDataKey ) ) {
				jQuery.removeData( elem, deferDataKey, true );
				defer.fire();
			}
		}, 0 );
	}
}

jQuery.extend({

	_mark: function( elem, type ) {
		if ( elem ) {
			type = ( type || "fx" ) + "mark";
			jQuery._data( elem, type, (jQuery._data( elem, type ) || 0) + 1 );
		}
	},

	_unmark: function( force, elem, type ) {
		if ( force !== true ) {
			type = elem;
			elem = force;
			force = false;
		}
		if ( elem ) {
			type = type || "fx";
			var key = type + "mark",
				count = force ? 0 : ( (jQuery._data( elem, key ) || 1) - 1 );
			if ( count ) {
				jQuery._data( elem, key, count );
			} else {
				jQuery.removeData( elem, key, true );
				handleQueueMarkDefer( elem, type, "mark" );
			}
		}
	},

	queue: function( elem, type, data ) {
		var q;
		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			q = jQuery._data( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !q || jQuery.isArray(data) ) {
					q = jQuery._data( elem, type, jQuery.makeArray(data) );
				} else {
					q.push( data );
				}
			}
			return q || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			fn = queue.shift(),
			hooks = {};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
		}

		if ( fn ) {
			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			jQuery._data( elem, type + ".run", hooks );
			fn.call( elem, function() {
				jQuery.dequeue( elem, type );
			}, hooks );
		}

		if ( !queue.length ) {
			jQuery.removeData( elem, type + "queue " + type + ".run", true );
			handleQueueMarkDefer( elem, type, "queue" );
		}
	}
});

jQuery.fn.extend({
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length &lt; setter ) {
			return jQuery.queue( this[0], type );
		}

		return data === undefined ?
			this :
			this.each(function() {
				var queue = jQuery.queue( this, type, data );

				if ( type === "fx" &amp;&amp; queue[0] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			});
	},
	dequeue: function( type ) {
		return this.each(function() {
			jQuery.dequeue( this, type );
		});
	},
	// Based off of the plugin by Clint Helfers, with permission.
	// http://blindsignals.com/index.php/2009/07/jquery-delay/
	delay: function( time, type ) {
		time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
		type = type || "fx";

		return this.queue( type, function( next, hooks ) {
			var timeout = setTimeout( next, time );
			hooks.stop = function() {
				clearTimeout( timeout );
			};
		});
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},
	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, object ) {
		if ( typeof type !== "string" ) {
			object = type;
			type = undefined;
		}
		type = type || "fx";
		var defer = jQuery.Deferred(),
			elements = this,
			i = elements.length,
			count = 1,
			deferDataKey = type + "defer",
			queueDataKey = type + "queue",
			markDataKey = type + "mark",
			tmp;
		function resolve() {
			if ( !( --count ) ) {
				defer.resolveWith( elements, [ elements ] );
			}
		}
		while( i-- ) {
			if (( tmp = jQuery.data( elements[ i ], deferDataKey, undefined, true ) ||
					( jQuery.data( elements[ i ], queueDataKey, undefined, true ) ||
						jQuery.data( elements[ i ], markDataKey, undefined, true ) ) &amp;&amp;
					jQuery.data( elements[ i ], deferDataKey, jQuery.Callbacks( "once memory" ), true ) )) {
				count++;
				tmp.add( resolve );
			}
		}
		resolve();
		return defer.promise( object );
	}
});




var rclass = /[\n\t\r]/g,
	rspace = /\s+/,
	rreturn = /\r/g,
	rtype = /^(?:button|input)$/i,
	rfocusable = /^(?:button|input|object|select|textarea)$/i,
	rclickable = /^a(?:rea)?$/i,
	rboolean = /^(?:autofocus|autoplay|async|checked|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped|selected)$/i,
	getSetAttribute = jQuery.support.getSetAttribute,
	nodeHook, boolHook, fixSpecified;

jQuery.fn.extend({
	attr: function( name, value ) {
		return jQuery.access( this, jQuery.attr, name, value, arguments.length &gt; 1 );
	},

	removeAttr: function( name ) {
		return this.each(function() {
			jQuery.removeAttr( this, name );
		});
	},

	prop: function( name, value ) {
		return jQuery.access( this, jQuery.prop, name, value, arguments.length &gt; 1 );
	},

	removeProp: function( name ) {
		name = jQuery.propFix[ name ] || name;
		return this.each(function() {
			// try/catch handles cases where IE balks (such as removing a property on window)
			try {
				this[ name ] = undefined;
				delete this[ name ];
			} catch( e ) {}
		});
	},

	addClass: function( value ) {
		var classNames, i, l, elem,
			setClass, c, cl;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).addClass( value.call(this, j, this.className.baseVal) );
			});
		}

		if ( value &amp;&amp; typeof value === "string" ) {
			classNames = value.split( rspace );

			for ( i = 0, l = this.length; i &lt; l; i++ ) {
				elem = this[ i ];

				if ( elem.nodeType === 1 ) {
					if ( !elem.className.baseVal &amp;&amp; classNames.length === 1 ) {
						elem.className.baseVal = value;

					} else {
						setClass = " " + elem.className.baseVal + " ";

						for ( c = 0, cl = classNames.length; c &lt; cl; c++ ) {
							if ( !~setClass.indexOf( " " + classNames[ c ] + " " ) ) {
								setClass += classNames[ c ] + " ";
							}
						}
						elem.className.baseVal = jQuery.trim( setClass );
					}
				}
			}
		}

		return this;
	},

	removeClass: function( value ) {
		var classNames, i, l, elem, className, c, cl;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).removeClass( value.call(this, j, this.className.baseVal) );
			});
		}

		if ( (value &amp;&amp; typeof value === "string") || value === undefined ) {
			classNames = ( value || "" ).split( rspace );

			for ( i = 0, l = this.length; i &lt; l; i++ ) {
				elem = this[ i ];

				if ( elem.nodeType === 1 &amp;&amp; elem.className.baseVal ) {
					if ( value ) {
						className = (" " + elem.className.baseVal + " ").replace( rclass, " " );
						for ( c = 0, cl = classNames.length; c &lt; cl; c++ ) {
							className = className.replace(" " + classNames[ c ] + " ", " ");
						}
						elem.className.baseVal = jQuery.trim( className );

					} else {
						elem.className.baseVal = "";
					}
				}
			}
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var type = typeof value,
			isBool = typeof stateVal === "boolean";

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( i ) {
				jQuery( this ).toggleClass( value.call(this, i, this.className.baseVal, stateVal), stateVal );
			});
		}

		return this.each(function() {
			if ( type === "string" ) {
				// toggle individual class names
				var className,
					i = 0,
					self = jQuery( this ),
					state = stateVal,
					classNames = value.split( rspace );

				while ( (className = classNames[ i++ ]) ) {
					// check each className given, space seperated list
					state = isBool ? state : !self.hasClass( className );
					self[ state ? "addClass" : "removeClass" ]( className );
				}

			} else if ( type === "undefined" || type === "boolean" ) {
				if ( this.className.baseVal ) {
					// store className if set
					jQuery._data( this, "__className__", this.className.baseVal );
				}

				// toggle whole className
				this.className.baseVal = this.className.baseVal || value === false ? "" : jQuery._data( this, "__className__" ) || "";
			}
		});
	},

	hasClass: function( selector ) {
		var className = " " + selector + " ",
			i = 0,
			l = this.length;
		for ( ; i &lt; l; i++ ) {
			if ( this[i].nodeType === 1 &amp;&amp; (" " + this[i].className.baseVal + " ").replace(rclass, " ").indexOf( className ) &gt; -1 ) {
				return true;
			}
		}

		return false;
	},

	val: function( value ) {
		var hooks, ret, isFunction,
			elem = this[0];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] || jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, "value" )) !== undefined ) {
					return ret;
				}

				ret = elem.value;

				return typeof ret === "string" ?
					// handle most common string cases
					ret.replace(rreturn, "") :
					// handle cases where value is null/undef or number
					ret == null ? "" : ret;
			}

			return;
		}

		isFunction = jQuery.isFunction( value );

		return this.each(function( i ) {
			var self = jQuery(this), val;

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( isFunction ) {
				val = value.call( this, i, self.val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";
			} else if ( typeof val === "number" ) {
				val += "";
			} else if ( jQuery.isArray( val ) ) {
				val = jQuery.map(val, function ( value ) {
					return value == null ? "" : value + "";
				});
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !("set" in hooks) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		});
	}
});

jQuery.extend({
	valHooks: {
		option: {
			get: function( elem ) {
				// attributes.value is undefined in Blackberry 4.7 but
				// uses .value. See #6932
				var val = elem.attributes.value;
				return !val || val.specified ? elem.value : elem.text;
			}
		},
		select: {
			get: function( elem ) {
				var value, i, max, option,
					index = elem.selectedIndex,
					values = [],
					options = elem.options,
					one = elem.type === "select-one";

				// Nothing was selected
				if ( index &lt; 0 ) {
					return null;
				}

				// Loop through all the selected options
				i = one ? index : 0;
				max = one ? index + 1 : options.length;
				for ( ; i &lt; max; i++ ) {
					option = options[ i ];

					// Don't return options that are disabled or in a disabled optgroup
					if ( option.selected &amp;&amp; (jQuery.support.optDisabled ? !option.disabled : option.getAttribute("disabled") === null) &amp;&amp;
							(!option.parentNode.disabled || !jQuery.nodeName( option.parentNode, "optgroup" )) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				// Fixes Bug #2551 -- select.val() broken in IE after form.reset()
				if ( one &amp;&amp; !values.length &amp;&amp; options.length ) {
					return jQuery( options[ index ] ).val();
				}

				return values;
			},

			set: function( elem, value ) {
				var values = jQuery.makeArray( value );

				jQuery(elem).find("option").each(function() {
					this.selected = jQuery.inArray( jQuery(this).val(), values ) &gt;= 0;
				});

				if ( !values.length ) {
					elem.selectedIndex = -1;
				}
				return values;
			}
		}
	},

	attrFn: {
		val: true,
		css: true,
		html: true,
		text: true,
		data: true,
		width: true,
		height: true,
		offset: true
	},

	attr: function( elem, name, value, pass ) {
		var ret, hooks, notxml,
			nType = elem.nodeType;

		// don't get/set attributes on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		if ( pass &amp;&amp; name in jQuery.attrFn ) {
			return jQuery( elem )[ name ]( value );
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === "undefined" ) {
			return jQuery.prop( elem, name, value );
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		// All attributes are lowercase
		// Grab necessary hook if one is defined
		if ( notxml ) {
			name = name.toLowerCase();
			hooks = jQuery.attrHooks[ name ] || ( rboolean.test( name ) ? boolHook : nodeHook );
		}

		if ( value !== undefined ) {

			if ( value === null ) {
				jQuery.removeAttr( elem, name );
				return;

			} else if ( hooks &amp;&amp; "set" in hooks &amp;&amp; notxml &amp;&amp; (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				if (name == 'href') {
					elem.setAttributeNS( 'http://www.w3.org/1999/xlink', name, "" + value );
				} else {
					elem.setAttribute( name, "" + value );
				}
				return value;
			}

		} else if ( hooks &amp;&amp; "get" in hooks &amp;&amp; notxml &amp;&amp; (ret = hooks.get( elem, name )) !== null ) {
			return ret;

		} else {

			ret = elem.getAttribute( name );

			// Non-existent attributes return null, we normalize to undefined
			return ret === null ?
				undefined :
				ret;
		}
	},

	removeAttr: function( elem, value ) {
		var propName, attrNames, name, l, isBool,
			i = 0;

		if ( value &amp;&amp; elem.nodeType === 1 ) {
			attrNames = value.toLowerCase().split( rspace );
			l = attrNames.length;

			for ( ; i &lt; l; i++ ) {
				name = attrNames[ i ];

				if ( name ) {
					propName = jQuery.propFix[ name ] || name;
					isBool = rboolean.test( name );

					// See #9699 for explanation of this approach (setting first, then removal)
					// Do not do this for boolean attributes (see #10870)
					if ( !isBool ) {
						jQuery.attr( elem, name, "" );
					}
					elem.removeAttribute( getSetAttribute ? name : propName );

					// Set corresponding property to false for boolean attributes
					if ( isBool &amp;&amp; propName in elem ) {
						elem[ propName ] = false;
					}
				}
			}
		}
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				// We can't allow the type property to be changed (since it causes problems in IE)
				if ( rtype.test( elem.nodeName ) &amp;&amp; elem.parentNode ) {
					jQuery.error( "type property can't be changed" );
				} else if ( !jQuery.support.radioValue &amp;&amp; value === "radio" &amp;&amp; jQuery.nodeName(elem, "input") ) {
					// Setting the type on a radio button after the value resets the value in IE6-9
					// Reset value to it's default in case type is set after value
					// This is for element creation
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}
			}
		},
		// Use the value property for back compat
		// Use the nodeHook for button elements in IE6/7 (#1954)
		value: {
			get: function( elem, name ) {
				if ( nodeHook &amp;&amp; jQuery.nodeName( elem, "button" ) ) {
					return nodeHook.get( elem, name );
				}
				return name in elem ?
					elem.value :
					null;
			},
			set: function( elem, value, name ) {
				if ( nodeHook &amp;&amp; jQuery.nodeName( elem, "button" ) ) {
					return nodeHook.set( elem, value, name );
				}
				// Does not return so that setAttribute is also used
				elem.value = value;
			}
		}
	},

	propFix: {
		tabindex: "tabIndex",
		readonly: "readOnly",
		"for": "htmlFor",
		"class": "className",
		maxlength: "maxLength",
		cellspacing: "cellSpacing",
		cellpadding: "cellPadding",
		rowspan: "rowSpan",
		colspan: "colSpan",
		usemap: "useMap",
		frameborder: "frameBorder",
		contenteditable: "contentEditable"
	},

	prop: function( elem, name, value ) {
		var ret, hooks, notxml,
			nType = elem.nodeType;

		// don't get/set properties on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		if ( notxml ) {
			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks &amp;&amp; "set" in hooks &amp;&amp; (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				return ( elem[ name ] = value );
			}

		} else {
			if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, name )) !== null ) {
				return ret;

			} else {
				return elem[ name ];
			}
		}
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {
				// elem.tabIndex doesn't always return the correct value when it hasn't been explicitly set
				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
				var attributeNode = elem.getAttributeNode("tabindex");

				return attributeNode &amp;&amp; attributeNode.specified ?
					parseInt( attributeNode.value, 10 ) :
					rfocusable.test( elem.nodeName ) || rclickable.test( elem.nodeName ) &amp;&amp; elem.href ?
						0 :
						undefined;
			}
		}
	}
});

// Add the tabIndex propHook to attrHooks for back-compat (different case is intentional)
jQuery.attrHooks.tabindex = jQuery.propHooks.tabIndex;

// Hook for boolean attributes
boolHook = {
	get: function( elem, name ) {
		// Align boolean attributes with corresponding properties
		// Fall back to attribute presence where some booleans are not supported
		var attrNode,
			property = jQuery.prop( elem, name );
		return property === true || typeof property !== "boolean" &amp;&amp; ( attrNode = elem.getAttributeNode(name) ) &amp;&amp; attrNode.nodeValue !== false ?
			name.toLowerCase() :
			undefined;
	},
	set: function( elem, value, name ) {
		var propName;
		if ( value === false ) {
			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else {
			// value is true since we know at this point it's type boolean and not false
			// Set boolean attributes to the same name and set the DOM property
			propName = jQuery.propFix[ name ] || name;
			if ( propName in elem ) {
				// Only set the IDL specifically if it already exists on the element
				elem[ propName ] = true;
			}

			elem.setAttribute( name, name.toLowerCase() );
		}
		return name;
	}
};

// IE6/7 do not support getting/setting some attributes with get/setAttribute
if ( !getSetAttribute ) {

	fixSpecified = {
		name: true,
		id: true,
		coords: true
	};

	// Use this for any attribute in IE6/7
	// This fixes almost every IE6/7 issue
	nodeHook = jQuery.valHooks.button = {
		get: function( elem, name ) {
			var ret;
			ret = elem.getAttributeNode( name );
			return ret &amp;&amp; ( fixSpecified[ name ] ? ret.nodeValue !== "" : ret.specified ) ?
				ret.nodeValue :
				undefined;
		},
		set: function( elem, value, name ) {
			// Set the existing or create a new attribute node
			var ret = elem.getAttributeNode( name );
			if ( !ret ) {
				ret = document.createAttribute( name );
				elem.setAttributeNode( ret );
			}
			return ( ret.nodeValue = value + "" );
		}
	};

	// Apply the nodeHook to tabindex
	jQuery.attrHooks.tabindex.set = nodeHook.set;

	// Set width and height to auto instead of 0 on empty string( Bug #8150 )
	// This is for removals
	jQuery.each([ "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			set: function( elem, value ) {
				if ( value === "" ) {
					elem.setAttribute( name, "auto" );
					return value;
				}
			}
		});
	});

	// Set contenteditable to false on removals(#10429)
	// Setting to empty string throws an error as an invalid value
	jQuery.attrHooks.contenteditable = {
		get: nodeHook.get,
		set: function( elem, value, name ) {
			if ( value === "" ) {
				value = "false";
			}
			nodeHook.set( elem, value, name );
		}
	};
}


// Some attributes require a special call on IE
if ( !jQuery.support.hrefNormalized ) {
	jQuery.each([ "href", "src", "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			get: function( elem ) {
				var ret = elem.getAttribute( name, 2 );
				return ret === null ? undefined : ret;
			}
		});
	});
}

if ( !jQuery.support.style ) {
	jQuery.attrHooks.style = {
		get: function( elem ) {
			// Return undefined in the case of empty string
			// Normalize to lowercase since IE uppercases css property names
			return elem.style.cssText.toLowerCase() || undefined;
		},
		set: function( elem, value ) {
			return ( elem.style.cssText = "" + value );
		}
	};
}

// Safari mis-reports the default selected property of an option
// Accessing the parent's selectedIndex property fixes it
if ( !jQuery.support.optSelected ) {
	jQuery.propHooks.selected = jQuery.extend( jQuery.propHooks.selected, {
		get: function( elem ) {
			var parent = elem.parentNode;

			if ( parent ) {
				parent.selectedIndex;

				// Make sure that it also works with optgroups, see #5701
				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
			return null;
		}
	});
}

// IE6/7 call enctype encoding
if ( !jQuery.support.enctype ) {
	jQuery.propFix.enctype = "encoding";
}

// Radios and checkboxes getter/setter
if ( !jQuery.support.checkOn ) {
	jQuery.each([ "radio", "checkbox" ], function() {
		jQuery.valHooks[ this ] = {
			get: function( elem ) {
				// Handle the case where in Webkit "" is returned instead of "on" if a value isn't specified
				return elem.getAttribute("value") === null ? "on" : elem.value;
			}
		};
	});
}
jQuery.each([ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = jQuery.extend( jQuery.valHooks[ this ], {
		set: function( elem, value ) {
			if ( jQuery.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery(elem).val(), value ) &gt;= 0 );
			}
		}
	});
});




var rformElems = /^(?:textarea|input|select)$/i,
	rtypenamespace = /^([^\.]*)?(?:\.(.+))?$/,
	rhoverHack = /(?:^|\s)hover(\.\S+)?\b/,
	rkeyEvent = /^key/,
	rmouseEvent = /^(?:mouse|contextmenu)|click/,
	rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
	rquickIs = /^(\w*)(?:#([\w\-]+))?(?:\.([\w\-]+))?$/,
	quickParse = function( selector ) {
		var quick = rquickIs.exec( selector );
		if ( quick ) {
			//   0  1    2   3
			// [ _, tag, id, class ]
			quick[1] = ( quick[1] || "" ).toLowerCase();
			quick[3] = quick[3] &amp;&amp; new RegExp( "(?:^|\\s)" + quick[3] + "(?:\\s|$)" );
		}
		return quick;
	},
	quickIs = function( elem, m ) {
		var attrs = elem.attributes || {};
		return (
			(!m[1] || elem.nodeName.toLowerCase() === m[1]) &amp;&amp;
			(!m[2] || (attrs.id || {}).value === m[2]) &amp;&amp;
			(!m[3] || m[3].test( (attrs[ "class" ] || {}).value ))
		);
	},
	hoverHack = function( events ) {
		return jQuery.event.special.hover ? events : events.replace( rhoverHack, "mouseenter$1 mouseleave$1" );
	};

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	add: function( elem, types, handler, data, selector ) {

		var elemData, eventHandle, events,
			t, tns, type, namespaces, handleObj,
			handleObjIn, quick, handlers, special;

		// Don't attach events to noData or text/comment nodes (allow plain objects tho)
		if ( elem.nodeType === 3 || elem.nodeType === 8 || !types || !handler || !(elemData = jQuery._data( elem )) ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		events = elemData.events;
		if ( !events ) {
			elemData.events = events = {};
		}
		eventHandle = elemData.handle;
		if ( !eventHandle ) {
			elemData.handle = eventHandle = function( e ) {
				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== "undefined" &amp;&amp; (!e || jQuery.event.triggered !== e.type) ?
					jQuery.event.dispatch.apply( eventHandle.elem, arguments ) :
					undefined;
			};
			// Add elem as a property of the handle fn to prevent a memory leak with IE non-native events
			eventHandle.elem = elem;
		}

		// Handle multiple events separated by a space
		// jQuery(...).bind("mouseover mouseout", fn);
		types = jQuery.trim( hoverHack(types) ).split( " " );
		for ( t = 0; t &lt; types.length; t++ ) {

			tns = rtypenamespace.exec( types[t] ) || [];
			type = tns[1];
			namespaces = ( tns[2] || "" ).split( "." ).sort();

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend({
				type: type,
				origType: tns[1],
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				quick: selector &amp;&amp; quickParse( selector ),
				namespace: namespaces.join(".")
			}, handleObjIn );

			// Init the event handler queue if we're the first
			handlers = events[ type ];
			if ( !handlers ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener/attachEvent if the special events handler returns false
				if ( !special.setup || special.setup.call( elem, data, namespaces, eventHandle ) === false ) {
					// Bind the global event handler to the element
					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle, false );

					} else if ( elem.attachEvent ) {
						elem.attachEvent( "on" + type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

		// Nullify elem to prevent memory leaks in IE
		elem = null;
	},

	global: {},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {

		var elemData = jQuery.hasData( elem ) &amp;&amp; jQuery._data( elem ),
			t, tns, type, origType, namespaces, origCount,
			j, events, special, handle, eventType, handleObj;

		if ( !elemData || !(events = elemData.events) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = jQuery.trim( hoverHack( types || "" ) ).split(" ");
		for ( t = 0; t &lt; types.length; t++ ) {
			tns = rtypenamespace.exec( types[t] ) || [];
			type = origType = tns[1];
			namespaces = tns[2];

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector? special.delegateType : special.bindType ) || type;
			eventType = events[ type ] || [];
			origCount = eventType.length;
			namespaces = namespaces ? new RegExp("(^|\\.)" + namespaces.split(".").sort().join("\\.(?:.*\\.)?") + "(\\.|$)") : null;

			// Remove matching events
			for ( j = 0; j &lt; eventType.length; j++ ) {
				handleObj = eventType[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &amp;&amp;
					 ( !handler || handler.guid === handleObj.guid ) &amp;&amp;
					 ( !namespaces || namespaces.test( handleObj.namespace ) ) &amp;&amp;
					 ( !selector || selector === handleObj.selector || selector === "**" &amp;&amp; handleObj.selector ) ) {
					eventType.splice( j--, 1 );

					if ( handleObj.selector ) {
						eventType.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( eventType.length === 0 &amp;&amp; origCount !== eventType.length ) {
				if ( !special.teardown || special.teardown.call( elem, namespaces ) === false ) {
					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			handle = elemData.handle;
			if ( handle ) {
				handle.elem = null;
			}

			// removeData also checks for emptiness and clears the expando if empty
			// so use it instead of delete
			jQuery.removeData( elem, [ "events", "handle" ], true );
		}
	},

	// Events that are safe to short-circuit if no handlers are attached.
	// Native DOM events should not be added, they may have inline handlers.
	customEvent: {
		"getData": true,
		"setData": true,
		"changeData": true
	},

	trigger: function( event, data, elem, onlyHandlers ) {
		// Don't do events on text and comment nodes
		if ( elem &amp;&amp; (elem.nodeType === 3 || elem.nodeType === 8) ) {
			return;
		}

		// Event object or event type
		var type = event.type || event,
			namespaces = [],
			cache, exclusive, i, cur, old, ontype, special, handle, eventPath, bubbleType;

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf( "!" ) &gt;= 0 ) {
			// Exclusive events trigger only for the exact event (no namespaces)
			type = type.slice(0, -1);
			exclusive = true;
		}

		if ( type.indexOf( "." ) &gt;= 0 ) {
			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split(".");
			type = namespaces.shift();
			namespaces.sort();
		}

		if ( (!elem || jQuery.event.customEvent[ type ]) &amp;&amp; !jQuery.event.global[ type ] ) {
			// No jQuery handlers for this event type, and it can't have inline handlers
			return;
		}

		// Caller can pass in an Event, Object, or just an event type string
		event = typeof event === "object" ?
			// jQuery.Event object
			event[ jQuery.expando ] ? event :
			// Object literal
			new jQuery.Event( type, event ) :
			// Just the event type (string)
			new jQuery.Event( type );

		event.type = type;
		event.isTrigger = true;
		event.exclusive = exclusive;
		event.namespace = namespaces.join( "." );
		event.namespace_re = event.namespace? new RegExp("(^|\\.)" + namespaces.join("\\.(?:.*\\.)?") + "(\\.|$)") : null;
		ontype = type.indexOf( ":" ) &lt; 0 ? "on" + type : "";

		// Handle a global trigger
		if ( !elem ) {

			// TODO: Stop taunting the data cache; remove global events and always attach to document
			cache = jQuery.cache;
			for ( i in cache ) {
				if ( cache[ i ].events &amp;&amp; cache[ i ].events[ type ] ) {
					jQuery.event.trigger( event, data, cache[ i ].handle.elem, true );
				}
			}
			return;
		}

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data != null ? jQuery.makeArray( data ) : [];
		data.unshift( event );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( special.trigger &amp;&amp; special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (#9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
		eventPath = [[ elem, special.bindType || type ]];
		if ( !onlyHandlers &amp;&amp; !special.noBubble &amp;&amp; !jQuery.isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			cur = rfocusMorph.test( bubbleType + type ) ? elem : elem.parentNode;
			old = null;
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push([ cur, bubbleType ]);
				old = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( old &amp;&amp; old === elem.ownerDocument ) {
				eventPath.push([ old.defaultView || old.parentWindow || window, bubbleType ]);
			}
		}

		// Fire handlers on the event path
		for ( i = 0; i &lt; eventPath.length &amp;&amp; !event.isPropagationStopped(); i++ ) {

			cur = eventPath[i][0];
			event.type = eventPath[i][1];

			handle = ( jQuery._data( cur, "events" ) || {} )[ event.type ] &amp;&amp; jQuery._data( cur, "handle" );
			if ( handle ) {
				handle.apply( cur, data );
			}
			// Note that this is a bare JS function and not a jQuery handler
			handle = ontype &amp;&amp; cur[ ontype ];
			if ( handle &amp;&amp; jQuery.acceptData( cur ) &amp;&amp; handle.apply( cur, data ) === false ) {
				event.preventDefault();
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers &amp;&amp; !event.isDefaultPrevented() ) {

			if ( (!special._default || special._default.apply( elem.ownerDocument, data ) === false) &amp;&amp;
				!(type === "click" &amp;&amp; jQuery.nodeName( elem, "a" )) &amp;&amp; jQuery.acceptData( elem ) ) {

				// Call a native DOM method on the target with the same name name as the event.
				// Can't use an .isFunction() check here because IE6/7 fails that test.
				// Don't do default actions on window, that's where global variables be (#6170)
				// IE&lt;9 dies on focus/blur to hidden element (#1486)
				if ( ontype &amp;&amp; elem[ type ] &amp;&amp; ((type !== "focus" &amp;&amp; type !== "blur") || event.target.offsetWidth !== 0) &amp;&amp; !jQuery.isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					old = elem[ ontype ];

					if ( old ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;
					elem[ type ]();
					jQuery.event.triggered = undefined;

					if ( old ) {
						elem[ ontype ] = old;
					}
				}
			}
		}

		return event.result;
	},

	dispatch: function( event ) {

		// Make a writable jQuery.Event from the native event object
		event = jQuery.event.fix( event || window.event );

		var handlers = ( (jQuery._data( this, "events" ) || {} )[ event.type ] || []),
			delegateCount = handlers.delegateCount,
			args = [].slice.call( arguments, 0 ),
			run_all = !event.exclusive &amp;&amp; !event.namespace,
			special = jQuery.event.special[ event.type ] || {},
			handlerQueue = [],
			i, j, cur, jqcur, ret, selMatch, matched, matches, handleObj, sel, related;

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[0] = event;
		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch &amp;&amp; special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers that should run if there are delegated events
		// Avoid non-left-click bubbling in Firefox (#3861)
		if ( delegateCount &amp;&amp; !(event.button &amp;&amp; event.type === "click") ) {

			// Pregenerate a single jQuery object for reuse with .is()
			jqcur = jQuery(this);
			jqcur.context = this.ownerDocument || this;

			for ( cur = event.target; cur != this; cur = cur.parentNode || this ) {

				// Don't process events on disabled elements (#6911, #8165)
				if ( cur.disabled !== true ) {
					selMatch = {};
					matches = [];
					jqcur[0] = cur;
					for ( i = 0; i &lt; delegateCount; i++ ) {
						handleObj = handlers[ i ];
						sel = handleObj.selector;

						if ( selMatch[ sel ] === undefined ) {
							selMatch[ sel ] = (
								handleObj.quick ? quickIs( cur, handleObj.quick ) : jqcur.is( sel )
							);
						}
						if ( selMatch[ sel ] ) {
							matches.push( handleObj );
						}
					}
					if ( matches.length ) {
						handlerQueue.push({ elem: cur, matches: matches });
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		if ( handlers.length &gt; delegateCount ) {
			handlerQueue.push({ elem: this, matches: handlers.slice( delegateCount ) });
		}

		// Run delegates first; they may want to stop propagation beneath us
		for ( i = 0; i &lt; handlerQueue.length &amp;&amp; !event.isPropagationStopped(); i++ ) {
			matched = handlerQueue[ i ];
			event.currentTarget = matched.elem;

			for ( j = 0; j &lt; matched.matches.length &amp;&amp; !event.isImmediatePropagationStopped(); j++ ) {
				handleObj = matched.matches[ j ];

				// Triggered event must either 1) be non-exclusive and have no namespace, or
				// 2) have namespace(s) a subset or equal to those in the bound event (both can have no namespace).
				if ( run_all || (!event.namespace &amp;&amp; !handleObj.namespace) || event.namespace_re &amp;&amp; event.namespace_re.test( handleObj.namespace ) ) {

					event.data = handleObj.data;
					event.handleObj = handleObj;

					ret = ( (jQuery.event.special[ handleObj.origType ] || {}).handle || handleObj.handler )
							.apply( matched.elem, args );

					if ( ret !== undefined ) {
						event.result = ret;
						if ( ret === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	// Includes some event props shared by KeyEvent and MouseEvent
	// *** attrChange attrName relatedNode srcElement  are not normalized, non-W3C, deprecated, will be removed in 1.8 ***
	props: "attrChange attrName relatedNode srcElement altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),

	fixHooks: {},

	keyHooks: {
		props: "char charCode key keyCode".split(" "),
		filter: function( event, original ) {

			// Add which for key events
			if ( event.which == null ) {
				event.which = original.charCode != null ? original.charCode : original.keyCode;
			}

			return event;
		}
	},

	mouseHooks: {
		props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
		filter: function( event, original ) {
			var eventDoc, doc, body,
				button = original.button,
				fromElement = original.fromElement;

			// Calculate pageX/Y if missing and clientX/Y available
			if ( event.pageX == null &amp;&amp; original.clientX != null ) {
				eventDoc = event.target.ownerDocument || document;
				doc = eventDoc.documentElement;
				body = eventDoc.body;

				event.pageX = original.clientX + ( doc &amp;&amp; doc.scrollLeft || body &amp;&amp; body.scrollLeft || 0 ) - ( doc &amp;&amp; doc.clientLeft || body &amp;&amp; body.clientLeft || 0 );
				event.pageY = original.clientY + ( doc &amp;&amp; doc.scrollTop  || body &amp;&amp; body.scrollTop  || 0 ) - ( doc &amp;&amp; doc.clientTop  || body &amp;&amp; body.clientTop  || 0 );
			}

			// Add relatedTarget, if necessary
			if ( !event.relatedTarget &amp;&amp; fromElement ) {
				event.relatedTarget = fromElement === event.target ? original.toElement : fromElement;
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			// Note: button is not normalized, so don't use it
			if ( !event.which &amp;&amp; button !== undefined ) {
				event.which = ( button &amp; 1 ? 1 : ( button &amp; 2 ? 3 : ( button &amp; 4 ? 2 : 0 ) ) );
			}

			return event;
		}
	},

	fix: function( event ) {
		if ( event[ jQuery.expando ] ) {
			return event;
		}

		// Create a writable copy of the event object and normalize some properties
		var i, prop,
			originalEvent = event,
			fixHook = jQuery.event.fixHooks[ event.type ] || {},
			copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;

		event = jQuery.Event( originalEvent );

		for ( i = copy.length; i; ) {
			prop = copy[ --i ];
			event[ prop ] = originalEvent[ prop ];
		}

		// Fix target property, if necessary (#1925, IE 6/7/8 &amp; Safari2)
		if ( !event.target ) {
			event.target = originalEvent.srcElement || document;
		}

		// Target should not be a text node (#504, Safari)
		if ( event.target.nodeType === 3 ) {
			event.target = event.target.parentNode;
		}

		// For mouse/key events; add metaKey if it's not there (#3368, IE6/7/8)
		if ( event.metaKey === undefined ) {
			event.metaKey = event.ctrlKey;
		}

		return fixHook.filter? fixHook.filter( event, originalEvent ) : event;
	},

	special: {
		ready: {
			// Make sure the ready event is setup
			setup: jQuery.bindReady
		},

		load: {
			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},

		focus: {
			delegateType: "focusin"
		},
		blur: {
			delegateType: "focusout"
		},

		beforeunload: {
			setup: function( data, namespaces, eventHandle ) {
				// We only want to do this special case on windows
				if ( jQuery.isWindow( this ) ) {
					this.onbeforeunload = eventHandle;
				}
			},

			teardown: function( namespaces, eventHandle ) {
				if ( this.onbeforeunload === eventHandle ) {
					this.onbeforeunload = null;
				}
			}
		}
	},

	simulate: function( type, elem, event, bubble ) {
		// Piggyback on a donor event to simulate a different one.
		// Fake originalEvent to avoid donor's stopPropagation, but if the
		// simulated event prevents default then we do the same on the donor.
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{ type: type,
				isSimulated: true,
				originalEvent: {}
			}
		);
		if ( bubble ) {
			jQuery.event.trigger( e, null, elem );
		} else {
			jQuery.event.dispatch.call( elem, e );
		}
		if ( e.isDefaultPrevented() ) {
			event.preventDefault();
		}
	}
};

// Some plugins are using, but it's undocumented/deprecated and will be removed.
// The 1.7 special event interface should provide all the hooks needed now.
jQuery.event.handle = jQuery.event.dispatch;

jQuery.removeEvent = document.removeEventListener ?
	function( elem, type, handle ) {
		if ( elem.removeEventListener ) {
			elem.removeEventListener( type, handle, false );
		}
	} :
	function( elem, type, handle ) {
		if ( elem.detachEvent ) {
			elem.detachEvent( "on" + type, handle );
		}
	};

jQuery.Event = function( src, props ) {
	// Allow instantiation without the 'new' keyword
	if ( !(this instanceof jQuery.Event) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src &amp;&amp; src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = ( src.defaultPrevented || src.returnValue === false ||
			src.getPreventDefault &amp;&amp; src.getPreventDefault() ) ? returnTrue : returnFalse;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src &amp;&amp; src.timeStamp || jQuery.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

function returnFalse() {
	return false;
}
function returnTrue() {
	return true;
}

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	preventDefault: function() {
		this.isDefaultPrevented = returnTrue;

		var e = this.originalEvent;
		if ( !e ) {
			return;
		}

		// if preventDefault exists run it on the original event
		if ( e.preventDefault ) {
			e.preventDefault();

		// otherwise set the returnValue property of the original event to false (IE)
		} else {
			e.returnValue = false;
		}
	},
	stopPropagation: function() {
		this.isPropagationStopped = returnTrue;

		var e = this.originalEvent;
		if ( !e ) {
			return;
		}
		// if stopPropagation exists run it on the original event
		if ( e.stopPropagation ) {
			e.stopPropagation();
		}
		// otherwise set the cancelBubble property of the original event to true (IE)
		e.cancelBubble = true;
	},
	stopImmediatePropagation: function() {
		this.isImmediatePropagationStopped = returnTrue;
		this.stopPropagation();
	},
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse
};

// Create mouseenter/leave events using mouseover/out and event-time checks
jQuery.each({
	mouseenter: "mouseover",
	mouseleave: "mouseout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj,
				selector = handleObj.selector,
				ret;

			// For mousenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || (related !== target &amp;&amp; !jQuery.contains( target, related )) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
});

// IE submit delegation
if ( !jQuery.support.submitBubbles ) {

	jQuery.event.special.submit = {
		setup: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Lazy-add a submit handler when a descendant form may potentially be submitted
			jQuery.event.add( this, "click._submit keypress._submit", function( e ) {
				// Node name check avoids a VML-related crash in IE (#9807)
				var elem = e.target,
					form = jQuery.nodeName( elem, "input" ) || jQuery.nodeName( elem, "button" ) ? elem.form : undefined;
				if ( form &amp;&amp; !form._submit_attached ) {
					jQuery.event.add( form, "submit._submit", function( event ) {
						event._submit_bubble = true;
					});
					form._submit_attached = true;
				}
			});
			// return undefined since we don't need an event listener
		},
		
		postDispatch: function( event ) {
			// If form was submitted by the user, bubble the event up the tree
			if ( event._submit_bubble ) {
				delete event._submit_bubble;
				if ( this.parentNode &amp;&amp; !event.isTrigger ) {
					jQuery.event.simulate( "submit", this.parentNode, event, true );
				}
			}
		},

		teardown: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Remove delegated handlers; cleanData eventually reaps submit handlers attached above
			jQuery.event.remove( this, "._submit" );
		}
	};
}

// IE change delegation and checkbox/radio fix
if ( !jQuery.support.changeBubbles ) {

	jQuery.event.special.change = {

		setup: function() {

			if ( rformElems.test( this.nodeName ) ) {
				// IE doesn't fire change on a check/radio until blur; trigger it on click
				// after a propertychange. Eat the blur-change in special.change.handle.
				// This still fires onchange a second time for check/radio after blur.
				if ( this.type === "checkbox" || this.type === "radio" ) {
					jQuery.event.add( this, "propertychange._change", function( event ) {
						if ( event.originalEvent.propertyName === "checked" ) {
							this._just_changed = true;
						}
					});
					jQuery.event.add( this, "click._change", function( event ) {
						if ( this._just_changed &amp;&amp; !event.isTrigger ) {
							this._just_changed = false;
							jQuery.event.simulate( "change", this, event, true );
						}
					});
				}
				return false;
			}
			// Delegated event; lazy-add a change handler on descendant inputs
			jQuery.event.add( this, "beforeactivate._change", function( e ) {
				var elem = e.target;

				if ( rformElems.test( elem.nodeName ) &amp;&amp; !elem._change_attached ) {
					jQuery.event.add( elem, "change._change", function( event ) {
						if ( this.parentNode &amp;&amp; !event.isSimulated &amp;&amp; !event.isTrigger ) {
							jQuery.event.simulate( "change", this.parentNode, event, true );
						}
					});
					elem._change_attached = true;
				}
			});
		},

		handle: function( event ) {
			var elem = event.target;

			// Swallow native change events from checkbox/radio, we already triggered them above
			if ( this !== elem || event.isSimulated || event.isTrigger || (elem.type !== "radio" &amp;&amp; elem.type !== "checkbox") ) {
				return event.handleObj.handler.apply( this, arguments );
			}
		},

		teardown: function() {
			jQuery.event.remove( this, "._change" );

			return rformElems.test( this.nodeName );
		}
	};
}

// Create "bubbling" focus and blur events
if ( !jQuery.support.focusinBubbles ) {
	jQuery.each({ focus: "focusin", blur: "focusout" }, function( orig, fix ) {

		// Attach a single capturing handler while someone wants focusin/focusout
		var attaches = 0,
			handler = function( event ) {
				jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ), true );
			};

		jQuery.event.special[ fix ] = {
			setup: function() {
				if ( attaches++ === 0 ) {
					document.addEventListener( orig, handler, true );
				}
			},
			teardown: function() {
				if ( --attaches === 0 ) {
					document.removeEventListener( orig, handler, true );
				}
			}
		};
	});
}

jQuery.fn.extend({

	on: function( types, selector, data, fn, /*INTERNAL*/ one ) {
		var origFn, type;

		// Types can be a map of types/handlers
		if ( typeof types === "object" ) {
			// ( types-Object, selector, data )
			if ( typeof selector !== "string" ) { // &amp;&amp; selector != null
				// ( types-Object, data )
				data = data || selector;
				selector = undefined;
			}
			for ( type in types ) {
				this.on( type, selector, data, types[ type ], one );
			}
			return this;
		}

		if ( data == null &amp;&amp; fn == null ) {
			// ( types, fn )
			fn = selector;
			data = selector = undefined;
		} else if ( fn == null ) {
			if ( typeof selector === "string" ) {
				// ( types, selector, fn )
				fn = data;
				data = undefined;
			} else {
				// ( types, data, fn )
				fn = data;
				data = selector;
				selector = undefined;
			}
		}
		if ( fn === false ) {
			fn = returnFalse;
		} else if ( !fn ) {
			return this;
		}

		if ( one === 1 ) {
			origFn = fn;
			fn = function( event ) {
				// Can use an empty set, since event contains the info
				jQuery().off( event );
				return origFn.apply( this, arguments );
			};
			// Use same guid so caller can remove using origFn
			fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
		}
		return this.each( function() {
			jQuery.event.add( this, types, fn, data, selector );
		});
	},
	one: function( types, selector, data, fn ) {
		return this.on( types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		if ( types &amp;&amp; types.preventDefault &amp;&amp; types.handleObj ) {
			// ( event )  dispatched jQuery.Event
			var handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ? handleObj.origType + "." + handleObj.namespace : handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {
			// ( types-object [, selector] )
			for ( var type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {
			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each(function() {
			jQuery.event.remove( this, types, fn, selector );
		});
	},

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	live: function( types, data, fn ) {
		jQuery( this.context ).on( types, this.selector, data, fn );
		return this;
	},
	die: function( types, fn ) {
		jQuery( this.context ).off( types, this.selector || "**", fn );
		return this;
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {
		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length == 1? this.off( selector, "**" ) : this.off( types, selector, fn );
	},

	trigger: function( type, data ) {
		return this.each(function() {
			jQuery.event.trigger( type, data, this );
		});
	},
	triggerHandler: function( type, data ) {
		if ( this[0] ) {
			return jQuery.event.trigger( type, data, this[0], true );
		}
	},

	toggle: function( fn ) {
		// Save reference to arguments for access in closure
		var args = arguments,
			guid = fn.guid || jQuery.guid++,
			i = 0,
			toggler = function( event ) {
				// Figure out which function to execute
				var lastToggle = ( jQuery._data( this, "lastToggle" + fn.guid ) || 0 ) % i;
				jQuery._data( this, "lastToggle" + fn.guid, lastToggle + 1 );

				// Make sure that clicks stop
				event.preventDefault();

				// and execute the function
				return args[ lastToggle ].apply( this, arguments ) || false;
			};

		// link all the functions, so any of them can unbind this click handler
		toggler.guid = guid;
		while ( i &lt; args.length ) {
			args[ i++ ].guid = guid;
		}

		return this.click( toggler );
	},

	hover: function( fnOver, fnOut ) {
		return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
	}
});

jQuery.each( ("blur focus focusin focusout load resize scroll unload click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup error contextmenu").split(" "), function( i, name ) {

	// Handle event binding
	jQuery.fn[ name ] = function( data, fn ) {
		if ( fn == null ) {
			fn = data;
			data = null;
		}

		return arguments.length &gt; 0 ?
			this.on( name, null, data, fn ) :
			this.trigger( name );
	};

	if ( jQuery.attrFn ) {
		jQuery.attrFn[ name ] = true;
	}

	if ( rkeyEvent.test( name ) ) {
		jQuery.event.fixHooks[ name ] = jQuery.event.keyHooks;
	}

	if ( rmouseEvent.test( name ) ) {
		jQuery.event.fixHooks[ name ] = jQuery.event.mouseHooks;
	}
});



/*!
 * Sizzle CSS Selector Engine
 *  Copyright 2011, The Dojo Foundation
 *  Released under the MIT, BSD, and GPL Licenses.
 *  More information: http://sizzlejs.com/
 */
(function(){

var chunker = /((?:\((?:\([^()]+\)|[^()]+)+\)|\[(?:\[[^\[\]]*\]|['"][^'"]*['"]|[^\[\]'"]+)+\]|\\.|[^ &gt;+~,(\[\\]+)+|[&gt;+~])(\s*,\s*)?((?:.|\r|\n)*)/g,
	expando = "sizcache" + (Math.random() + '').replace('.', ''),
	done = 0,
	toString = Object.prototype.toString,
	hasDuplicate = false,
	baseHasDuplicate = true,
	rBackslash = /\\/g,
	rReturn = /\r\n/g,
	rNonWord = /\W/;

// Here we check if the JavaScript engine is using some sort of
// optimization where it does not always call our comparision
// function. If that is the case, discard the hasDuplicate value.
//   Thus far that includes Google Chrome.
[0, 0].sort(function() {
	baseHasDuplicate = false;
	return 0;
});

var Sizzle = function( selector, context, results, seed ) {
	results = results || [];
	context = context || document;

	var origContext = context;

	if ( context.nodeType !== 1 &amp;&amp; context.nodeType !== 9 ) {
		return [];
	}

	if ( !selector || typeof selector !== "string" ) {
		return results;
	}

	var m, set, checkSet, extra, ret, cur, pop, i,
		prune = true,
		contextXML = Sizzle.isXML( context ),
		parts = [],
		soFar = selector;

	// Reset the position of the chunker regexp (start from head)
	do {
		chunker.exec( "" );
		m = chunker.exec( soFar );

		if ( m ) {
			soFar = m[3];

			parts.push( m[1] );

			if ( m[2] ) {
				extra = m[3];
				break;
			}
		}
	} while ( m );

	if ( parts.length &gt; 1 &amp;&amp; origPOS.exec( selector ) ) {

		if ( parts.length === 2 &amp;&amp; Expr.relative[ parts[0] ] ) {
			set = posProcess( parts[0] + parts[1], context, seed );

		} else {
			set = Expr.relative[ parts[0] ] ?
				[ context ] :
				Sizzle( parts.shift(), context );

			while ( parts.length ) {
				selector = parts.shift();

				if ( Expr.relative[ selector ] ) {
					selector += parts.shift();
				}

				set = posProcess( selector, set, seed );
			}
		}

	} else {
		// Take a shortcut and set the context if the root selector is an ID
		// (but not if it'll be faster if the inner selector is an ID)
		if ( !seed &amp;&amp; parts.length &gt; 1 &amp;&amp; context.nodeType === 9 &amp;&amp; !contextXML &amp;&amp;
				Expr.match.ID.test(parts[0]) &amp;&amp; !Expr.match.ID.test(parts[parts.length - 1]) ) {

			ret = Sizzle.find( parts.shift(), context, contextXML );
			context = ret.expr ?
				Sizzle.filter( ret.expr, ret.set )[0] :
				ret.set[0];
		}

		if ( context ) {
			ret = seed ?
				{ expr: parts.pop(), set: makeArray(seed) } :
				Sizzle.find( parts.pop(), parts.length === 1 &amp;&amp; (parts[0] === "~" || parts[0] === "+") &amp;&amp; context.parentNode ? context.parentNode : context, contextXML );

			set = ret.expr ?
				Sizzle.filter( ret.expr, ret.set ) :
				ret.set;

			if ( parts.length &gt; 0 ) {
				checkSet = makeArray( set );

			} else {
				prune = false;
			}

			while ( parts.length ) {
				cur = parts.pop();
				pop = cur;

				if ( !Expr.relative[ cur ] ) {
					cur = "";
				} else {
					pop = parts.pop();
				}

				if ( pop == null ) {
					pop = context;
				}

				Expr.relative[ cur ]( checkSet, pop, contextXML );
			}

		} else {
			checkSet = parts = [];
		}
	}

	if ( !checkSet ) {
		checkSet = set;
	}

	if ( !checkSet ) {
		Sizzle.error( cur || selector );
	}

	if ( toString.call(checkSet) === "[object Array]" ) {
		if ( !prune ) {
			results.push.apply( results, checkSet );

		} else if ( context &amp;&amp; context.nodeType === 1 ) {
			for ( i = 0; checkSet[i] != null; i++ ) {
				if ( checkSet[i] &amp;&amp; (checkSet[i] === true || checkSet[i].nodeType === 1 &amp;&amp; Sizzle.contains(context, checkSet[i])) ) {
					results.push( set[i] );
				}
			}

		} else {
			for ( i = 0; checkSet[i] != null; i++ ) {
				if ( checkSet[i] &amp;&amp; checkSet[i].nodeType === 1 ) {
					results.push( set[i] );
				}
			}
		}

	} else {
		makeArray( checkSet, results );
	}

	if ( extra ) {
		Sizzle( extra, origContext, results, seed );
		Sizzle.uniqueSort( results );
	}

	return results;
};

Sizzle.uniqueSort = function( results ) {
	if ( sortOrder ) {
		hasDuplicate = baseHasDuplicate;
		results.sort( sortOrder );

		if ( hasDuplicate ) {
			for ( var i = 1; i &lt; results.length; i++ ) {
				if ( results[i] === results[ i - 1 ] ) {
					results.splice( i--, 1 );
				}
			}
		}
	}

	return results;
};

Sizzle.matches = function( expr, set ) {
	return Sizzle( expr, null, null, set );
};

Sizzle.matchesSelector = function( node, expr ) {
	return Sizzle( expr, null, null, [node] ).length &gt; 0;
};

Sizzle.find = function( expr, context, isXML ) {
	var set, i, len, match, type, left;

	if ( !expr ) {
		return [];
	}

	for ( i = 0, len = Expr.order.length; i &lt; len; i++ ) {
		type = Expr.order[i];

		if ( (match = Expr.leftMatch[ type ].exec( expr )) ) {
			left = match[1];
			match.splice( 1, 1 );

			if ( left.substr( left.length - 1 ) !== "\\" ) {
				match[1] = (match[1] || "").replace( rBackslash, "" );
				set = Expr.find[ type ]( match, context, isXML );

				if ( set != null ) {
					expr = expr.replace( Expr.match[ type ], "" );
					break;
				}
			}
		}
	}

	if ( !set ) {
		set = typeof context.getElementsByTagName !== "undefined" ?
			context.getElementsByTagName( "*" ) :
			[];
	}

	return { set: set, expr: expr };
};

Sizzle.filter = function( expr, set, inplace, not ) {
	var match, anyFound,
		type, found, item, filter, left,
		i, pass,
		old = expr,
		result = [],
		curLoop = set,
		isXMLFilter = set &amp;&amp; set[0] &amp;&amp; Sizzle.isXML( set[0] );

	while ( expr &amp;&amp; set.length ) {
		for ( type in Expr.filter ) {
			if ( (match = Expr.leftMatch[ type ].exec( expr )) != null &amp;&amp; match[2] ) {
				filter = Expr.filter[ type ];
				left = match[1];

				anyFound = false;

				match.splice(1,1);

				if ( left.substr( left.length - 1 ) === "\\" ) {
					continue;
				}

				if ( curLoop === result ) {
					result = [];
				}

				if ( Expr.preFilter[ type ] ) {
					match = Expr.preFilter[ type ]( match, curLoop, inplace, result, not, isXMLFilter );

					if ( !match ) {
						anyFound = found = true;

					} else if ( match === true ) {
						continue;
					}
				}

				if ( match ) {
					for ( i = 0; (item = curLoop[i]) != null; i++ ) {
						if ( item ) {
							found = filter( item, match, i, curLoop );
							pass = not ^ found;

							if ( inplace &amp;&amp; found != null ) {
								if ( pass ) {
									anyFound = true;

								} else {
									curLoop[i] = false;
								}

							} else if ( pass ) {
								result.push( item );
								anyFound = true;
							}
						}
					}
				}

				if ( found !== undefined ) {
					if ( !inplace ) {
						curLoop = result;
					}

					expr = expr.replace( Expr.match[ type ], "" );

					if ( !anyFound ) {
						return [];
					}

					break;
				}
			}
		}

		// Improper expression
		if ( expr === old ) {
			if ( anyFound == null ) {
				Sizzle.error( expr );

			} else {
				break;
			}
		}

		old = expr;
	}

	return curLoop;
};

Sizzle.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

/**
 * Utility function for retreiving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
var getText = Sizzle.getText = function( elem ) {
    var i, node,
		nodeType = elem.nodeType,
		ret = "";

	if ( nodeType ) {
		if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
			// Use textContent || innerText for elements
			if ( typeof elem.textContent === 'string' ) {
				return elem.textContent;
			} else if ( typeof elem.innerText === 'string' ) {
				// Replace IE's carriage returns
				return elem.innerText.replace( rReturn, '' );
			} else {
				// Traverse it's children
				for ( elem = elem.firstChild; elem; elem = elem.nextSibling) {
					ret += getText( elem );
				}
			}
		} else if ( nodeType === 3 || nodeType === 4 ) {
			return elem.nodeValue;
		}
	} else {

		// If no nodeType, this is expected to be an array
		for ( i = 0; (node = elem[i]); i++ ) {
			// Do not traverse comment nodes
			if ( node.nodeType !== 8 ) {
				ret += getText( node );
			}
		}
	}
	return ret;
};

var Expr = Sizzle.selectors = {
	order: [ "ID", "NAME", "TAG" ],

	match: {
		ID: /#((?:[\w\u00c0-\uFFFF\-]|\\.)+)/,
		CLASS: /\.((?:[\w\u00c0-\uFFFF\-]|\\.)+)/,
		NAME: /\[name=['"]*((?:[\w\u00c0-\uFFFF\-]|\\.)+)['"]*\]/,
		ATTR: /\[\s*((?:[\w\u00c0-\uFFFF\-]|\\.)+)\s*(?:(\S?=)\s*(?:(['"])(.*?)\3|(#?(?:[\w\u00c0-\uFFFF\-]|\\.)*)|)|)\s*\]/,
		TAG: /^((?:[\w\u00c0-\uFFFF\*\-]|\\.)+)/,
		CHILD: /:(only|nth|last|first)-child(?:\(\s*(even|odd|(?:[+\-]?\d+|(?:[+\-]?\d*)?n\s*(?:[+\-]\s*\d+)?))\s*\))?/,
		POS: /:(nth|eq|gt|lt|first|last|even|odd)(?:\((\d*)\))?(?=[^\-]|$)/,
		PSEUDO: /:((?:[\w\u00c0-\uFFFF\-]|\\.)+)(?:\((['"]?)((?:\([^\)]+\)|[^\(\)]*)+)\2\))?/
	},

	leftMatch: {},

	attrMap: {
		"class": "className",
		"for": "htmlFor"
	},

	attrHandle: {
		href: function( elem ) {
			return elem.getAttribute( "href" );
		},
		type: function( elem ) {
			return elem.getAttribute( "type" );
		}
	},

	relative: {
		"+": function(checkSet, part){
			var isPartStr = typeof part === "string",
				isTag = isPartStr &amp;&amp; !rNonWord.test( part ),
				isPartStrNotTag = isPartStr &amp;&amp; !isTag;

			if ( isTag ) {
				part = part.toLowerCase();
			}

			for ( var i = 0, l = checkSet.length, elem; i &lt; l; i++ ) {
				if ( (elem = checkSet[i]) ) {
					while ( (elem = elem.previousSibling) &amp;&amp; elem.nodeType !== 1 ) {}

					checkSet[i] = isPartStrNotTag || elem &amp;&amp; elem.nodeName.toLowerCase() === part ?
						elem || false :
						elem === part;
				}
			}

			if ( isPartStrNotTag ) {
				Sizzle.filter( part, checkSet, true );
			}
		},

		"&gt;": function( checkSet, part ) {
			var elem,
				isPartStr = typeof part === "string",
				i = 0,
				l = checkSet.length;

			if ( isPartStr &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();

				for ( ; i &lt; l; i++ ) {
					elem = checkSet[i];

					if ( elem ) {
						var parent = elem.parentNode;
						checkSet[i] = parent.nodeName.toLowerCase() === part ? parent : false;
					}
				}

			} else {
				for ( ; i &lt; l; i++ ) {
					elem = checkSet[i];

					if ( elem ) {
						checkSet[i] = isPartStr ?
							elem.parentNode :
							elem.parentNode === part;
					}
				}

				if ( isPartStr ) {
					Sizzle.filter( part, checkSet, true );
				}
			}
		},

		"": function(checkSet, part, isXML){
			var nodeCheck,
				doneName = done++,
				checkFn = dirCheck;

			if ( typeof part === "string" &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();
				nodeCheck = part;
				checkFn = dirNodeCheck;
			}

			checkFn( "parentNode", part, doneName, checkSet, nodeCheck, isXML );
		},

		"~": function( checkSet, part, isXML ) {
			var nodeCheck,
				doneName = done++,
				checkFn = dirCheck;

			if ( typeof part === "string" &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();
				nodeCheck = part;
				checkFn = dirNodeCheck;
			}

			checkFn( "previousSibling", part, doneName, checkSet, nodeCheck, isXML );
		}
	},

	find: {
		ID: function( match, context, isXML ) {
			if ( typeof context.getElementById !== "undefined" &amp;&amp; !isXML ) {
				var m = context.getElementById(match[1]);
				// Check parentNode to catch when Blackberry 4.6 returns
				// nodes that are no longer in the document #6963
				return m &amp;&amp; m.parentNode ? [m] : [];
			}
		},

		NAME: function( match, context ) {
			if ( typeof context.getElementsByName !== "undefined" ) {
				var ret = [],
					results = context.getElementsByName( match[1] );

				for ( var i = 0, l = results.length; i &lt; l; i++ ) {
					if ( results[i].getAttribute("name") === match[1] ) {
						ret.push( results[i] );
					}
				}

				return ret.length === 0 ? null : ret;
			}
		},

		TAG: function( match, context ) {
			if ( typeof context.getElementsByTagName !== "undefined" ) {
				return context.getElementsByTagName( match[1] );
			}
		}
	},
	preFilter: {
		CLASS: function( match, curLoop, inplace, result, not, isXML ) {
			match = " " + match[1].replace( rBackslash, "" ) + " ";

			if ( isXML ) {
				return match;
			}

			for ( var i = 0, elem; (elem = curLoop[i]) != null; i++ ) {
				if ( elem ) {
					if ( not ^ (elem.className &amp;&amp; elem.className.baseVal &amp;&amp; (" " + elem.className.baseVal + " ").replace(/[\t\n\r]/g, " ").indexOf(match) &gt;= 0) ) {
						if ( !inplace ) {
							result.push( elem );
						}

					} else if ( inplace ) {
						curLoop[i] = false;
					}
				}
			}

			return false;
		},

		ID: function( match ) {
			return match[1].replace( rBackslash, "" );
		},

		TAG: function( match, curLoop ) {
			return match[1].replace( rBackslash, "" ).toLowerCase();
		},

		CHILD: function( match ) {
			if ( match[1] === "nth" ) {
				if ( !match[2] ) {
					Sizzle.error( match[0] );
				}

				match[2] = match[2].replace(/^\+|\s*/g, '');

				// parse equations like 'even', 'odd', '5', '2n', '3n+2', '4n-1', '-n+6'
				var test = /(-?)(\d*)(?:n([+\-]?\d*))?/.exec(
					match[2] === "even" &amp;&amp; "2n" || match[2] === "odd" &amp;&amp; "2n+1" ||
					!/\D/.test( match[2] ) &amp;&amp; "0n+" + match[2] || match[2]);

				// calculate the numbers (first)n+(last) including if they are negative
				match[2] = (test[1] + (test[2] || 1)) - 0;
				match[3] = test[3] - 0;
			}
			else if ( match[2] ) {
				Sizzle.error( match[0] );
			}

			// TODO: Move to normal caching system
			match[0] = done++;

			return match;
		},

		ATTR: function( match, curLoop, inplace, result, not, isXML ) {
			var name = match[1] = match[1].replace( rBackslash, "" );

			if ( !isXML &amp;&amp; Expr.attrMap[name] ) {
				match[1] = Expr.attrMap[name];
			}

			// Handle if an un-quoted value was used
			match[4] = ( match[4] || match[5] || "" ).replace( rBackslash, "" );

			if ( match[2] === "~=" ) {
				match[4] = " " + match[4] + " ";
			}

			return match;
		},

		PSEUDO: function( match, curLoop, inplace, result, not ) {
			if ( match[1] === "not" ) {
				// If we're dealing with a complex expression, or a simple one
				if ( ( chunker.exec(match[3]) || "" ).length &gt; 1 || /^\w/.test(match[3]) ) {
					match[3] = Sizzle(match[3], null, null, curLoop);

				} else {
					var ret = Sizzle.filter(match[3], curLoop, inplace, true ^ not);

					if ( !inplace ) {
						result.push.apply( result, ret );
					}

					return false;
				}

			} else if ( Expr.match.POS.test( match[0] ) || Expr.match.CHILD.test( match[0] ) ) {
				return true;
			}

			return match;
		},

		POS: function( match ) {
			match.unshift( true );

			return match;
		}
	},

	filters: {
		enabled: function( elem ) {
			return elem.disabled === false &amp;&amp; elem.type !== "hidden";
		},

		disabled: function( elem ) {
			return elem.disabled === true;
		},

		checked: function( elem ) {
			return elem.checked === true;
		},

		selected: function( elem ) {
			// Accessing this property makes selected-by-default
			// options in Safari work properly
			if ( elem.parentNode ) {
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		parent: function( elem ) {
			return !!elem.firstChild;
		},

		empty: function( elem ) {
			return !elem.firstChild;
		},

		has: function( elem, i, match ) {
			return !!Sizzle( match[3], elem ).length;
		},

		header: function( elem ) {
			return (/h\d/i).test( elem.nodeName );
		},

		text: function( elem ) {
			var attr = elem.getAttribute( "type" ), type = elem.type;
			// IE6 and 7 will map elem.type to 'text' for new HTML5 types (search, etc)
			// use getAttribute instead to test this case
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "text" === type &amp;&amp; ( attr === type || attr === null );
		},

		radio: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "radio" === elem.type;
		},

		checkbox: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "checkbox" === elem.type;
		},

		file: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "file" === elem.type;
		},

		password: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "password" === elem.type;
		},

		submit: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return (name === "input" || name === "button") &amp;&amp; "submit" === elem.type;
		},

		image: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "image" === elem.type;
		},

		reset: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return (name === "input" || name === "button") &amp;&amp; "reset" === elem.type;
		},

		button: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" &amp;&amp; "button" === elem.type || name === "button";
		},

		input: function( elem ) {
			return (/input|select|textarea|button/i).test( elem.nodeName );
		},

		focus: function( elem ) {
			return elem === elem.ownerDocument.activeElement;
		}
	},
	setFilters: {
		first: function( elem, i ) {
			return i === 0;
		},

		last: function( elem, i, match, array ) {
			return i === array.length - 1;
		},

		even: function( elem, i ) {
			return i % 2 === 0;
		},

		odd: function( elem, i ) {
			return i % 2 === 1;
		},

		lt: function( elem, i, match ) {
			return i &lt; match[3] - 0;
		},

		gt: function( elem, i, match ) {
			return i &gt; match[3] - 0;
		},

		nth: function( elem, i, match ) {
			return match[3] - 0 === i;
		},

		eq: function( elem, i, match ) {
			return match[3] - 0 === i;
		}
	},
	filter: {
		PSEUDO: function( elem, match, i, array ) {
			var name = match[1],
				filter = Expr.filters[ name ];

			if ( filter ) {
				return filter( elem, i, match, array );

			} else if ( name === "contains" ) {
				return (elem.textContent || elem.innerText || getText([ elem ]) || "").indexOf(match[3]) &gt;= 0;

			} else if ( name === "not" ) {
				var not = match[3];

				for ( var j = 0, l = not.length; j &lt; l; j++ ) {
					if ( not[j] === elem ) {
						return false;
					}
				}

				return true;

			} else {
				Sizzle.error( name );
			}
		},

		CHILD: function( elem, match ) {
			var first, last,
				doneName, parent, cache,
				count, diff,
				type = match[1],
				node = elem;

			switch ( type ) {
				case "only":
				case "first":
					while ( (node = node.previousSibling) ) {
						if ( node.nodeType === 1 ) {
							return false;
						}
					}

					if ( type === "first" ) {
						return true;
					}

					node = elem;

					/* falls through */
				case "last":
					while ( (node = node.nextSibling) ) {
						if ( node.nodeType === 1 ) {
							return false;
						}
					}

					return true;

				case "nth":
					first = match[2];
					last = match[3];

					if ( first === 1 &amp;&amp; last === 0 ) {
						return true;
					}

					doneName = match[0];
					parent = elem.parentNode;

					if ( parent &amp;&amp; (parent[ expando ] !== doneName || !elem.nodeIndex) ) {
						count = 0;

						for ( node = parent.firstChild; node; node = node.nextSibling ) {
							if ( node.nodeType === 1 ) {
								node.nodeIndex = ++count;
							}
						}

						parent[ expando ] = doneName;
					}

					diff = elem.nodeIndex - last;

					if ( first === 0 ) {
						return diff === 0;

					} else {
						return ( diff % first === 0 &amp;&amp; diff / first &gt;= 0 );
					}
			}
		},

		ID: function( elem, match ) {
			return elem.nodeType === 1 &amp;&amp; elem.getAttribute("id") === match;
		},

		TAG: function( elem, match ) {
			return (match === "*" &amp;&amp; elem.nodeType === 1) || !!elem.nodeName &amp;&amp; elem.nodeName.toLowerCase() === match;
		},

		CLASS: function( elem, match ) {
			return (" " + ((elem.className &amp;&amp; elem.className.baseVal) || elem.getAttribute("class")) + " ")
				.indexOf( match ) &gt; -1;
		},

		ATTR: function( elem, match ) {
			var name = match[1],
				result = Sizzle.attr ?
					Sizzle.attr( elem, name ) :
					Expr.attrHandle[ name ] ?
					Expr.attrHandle[ name ]( elem ) :
					elem[ name ] != null ?
						elem[ name ] :
						elem.getAttribute( name ),
				value = result + "",
				type = match[2],
				check = match[4];

			return result == null ?
				type === "!=" :
				!type &amp;&amp; Sizzle.attr ?
				result != null :
				type === "=" ?
				value === check :
				type === "*=" ?
				value.indexOf(check) &gt;= 0 :
				type === "~=" ?
				(" " + value + " ").indexOf(check) &gt;= 0 :
				!check ?
				value &amp;&amp; result !== false :
				type === "!=" ?
				value !== check :
				type === "^=" ?
				value.indexOf(check) === 0 :
				type === "$=" ?
				value.substr(value.length - check.length) === check :
				type === "|=" ?
				value === check || value.substr(0, check.length + 1) === check + "-" :
				false;
		},

		POS: function( elem, match, i, array ) {
			var name = match[2],
				filter = Expr.setFilters[ name ];

			if ( filter ) {
				return filter( elem, i, match, array );
			}
		}
	}
};

var origPOS = Expr.match.POS,
	fescape = function(all, num){
		return "\\" + (num - 0 + 1);
	};

for ( var type in Expr.match ) {
	Expr.match[ type ] = new RegExp( Expr.match[ type ].source + (/(?![^\[]*\])(?![^\(]*\))/.source) );
	Expr.leftMatch[ type ] = new RegExp( /(^(?:.|\r|\n)*?)/.source + Expr.match[ type ].source.replace(/\\(\d+)/g, fescape) );
}
// Expose origPOS
// "global" as in regardless of relation to brackets/parens
Expr.match.globalPOS = origPOS;

var makeArray = function( array, results ) {
	array = Array.prototype.slice.call( array, 0 );

	if ( results ) {
		results.push.apply( results, array );
		return results;
	}

	return array;
};

// Perform a simple check to determine if the browser is capable of
// converting a NodeList to an array using builtin methods.
// Also verifies that the returned array holds DOM nodes
// (which is not the case in the Blackberry browser)
try {
	Array.prototype.slice.call( document.documentElement.childNodes, 0 )[0].nodeType;

// Provide a fallback method if it does not work
} catch( e ) {
	makeArray = function( array, results ) {
		var i = 0,
			ret = results || [];

		if ( toString.call(array) === "[object Array]" ) {
			Array.prototype.push.apply( ret, array );

		} else {
			if ( typeof array.length === "number" ) {
				for ( var l = array.length; i &lt; l; i++ ) {
					ret.push( array[i] );
				}

			} else {
				for ( ; array[i]; i++ ) {
					ret.push( array[i] );
				}
			}
		}

		return ret;
	};
}

var sortOrder, siblingCheck;

if ( document.documentElement.compareDocumentPosition ) {
	sortOrder = function( a, b ) {
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		if ( !a.compareDocumentPosition || !b.compareDocumentPosition ) {
			return a.compareDocumentPosition ? -1 : 1;
		}

		return a.compareDocumentPosition(b) &amp; 4 ? -1 : 1;
	};

} else {
	sortOrder = function( a, b ) {
		// The nodes are identical, we can exit early
		if ( a === b ) {
			hasDuplicate = true;
			return 0;

		// Fallback to using sourceIndex (in IE) if it's available on both nodes
		} else if ( a.sourceIndex &amp;&amp; b.sourceIndex ) {
			return a.sourceIndex - b.sourceIndex;
		}

		var al, bl,
			ap = [],
			bp = [],
			aup = a.parentNode,
			bup = b.parentNode,
			cur = aup;

		// If the nodes are siblings (or identical) we can do a quick check
		if ( aup === bup ) {
			return siblingCheck( a, b );

		// If no parents were found then the nodes are disconnected
		} else if ( !aup ) {
			return -1;

		} else if ( !bup ) {
			return 1;
		}

		// Otherwise they're somewhere else in the tree so we need
		// to build up a full list of the parentNodes for comparison
		while ( cur ) {
			ap.unshift( cur );
			cur = cur.parentNode;
		}

		cur = bup;

		while ( cur ) {
			bp.unshift( cur );
			cur = cur.parentNode;
		}

		al = ap.length;
		bl = bp.length;

		// Start walking down the tree looking for a discrepancy
		for ( var i = 0; i &lt; al &amp;&amp; i &lt; bl; i++ ) {
			if ( ap[i] !== bp[i] ) {
				return siblingCheck( ap[i], bp[i] );
			}
		}

		// We ended someplace up the tree so do a sibling check
		return i === al ?
			siblingCheck( a, bp[i], -1 ) :
			siblingCheck( ap[i], b, 1 );
	};

	siblingCheck = function( a, b, ret ) {
		if ( a === b ) {
			return ret;
		}

		var cur = a.nextSibling;

		while ( cur ) {
			if ( cur === b ) {
				return -1;
			}

			cur = cur.nextSibling;
		}

		return 1;
	};
}

// Check to see if the browser returns elements by name when
// querying by getElementById (and provide a workaround)
(function(){
	// We're going to inject a fake input element with a specified name
	var form = document.createElement("div"),
		id = "script" + (new Date()).getTime(),
		root = document.documentElement;

	form.innerHTML = "&lt;a name='" + id + "'/&gt;";

	// Inject it into the root element, check its status, and remove it quickly
	root.insertBefore( form, root.firstChild );

	// The workaround has to do additional checks after a getElementById
	// Which slows things down for other browsers (hence the branching)
	if ( document.getElementById( id ) ) {
		Expr.find.ID = function( match, context, isXML ) {
			if ( typeof context.getElementById !== "undefined" &amp;&amp; !isXML ) {
				var m = context.getElementById(match[1]);

				return m ?
					m.id === match[1] || typeof m.getAttributeNode !== "undefined" &amp;&amp; m.getAttributeNode("id").nodeValue === match[1] ?
						[m] :
						undefined :
					[];
			}
		};

		Expr.filter.ID = function( elem, match ) {
			var node = typeof elem.getAttributeNode !== "undefined" &amp;&amp; elem.getAttributeNode("id");

			return elem.nodeType === 1 &amp;&amp; node &amp;&amp; node.nodeValue === match;
		};
	}

	root.removeChild( form );

	// release memory in IE
	root = form = null;
})();

(function(){
	// Check to see if the browser returns only elements
	// when doing getElementsByTagName("*")

	// Create a fake element
	var div = document.createElement("div");
	div.appendChild( document.createComment("") );

	// Make sure no comments are found
	if ( div.getElementsByTagName("*").length &gt; 0 ) {
		Expr.find.TAG = function( match, context ) {
			var results = context.getElementsByTagName( match[1] );

			// Filter out possible comments
			if ( match[1] === "*" ) {
				var tmp = [];

				for ( var i = 0; results[i]; i++ ) {
					if ( results[i].nodeType === 1 ) {
						tmp.push( results[i] );
					}
				}

				results = tmp;
			}

			return results;
		};
	}

	// Check to see if an attribute returns normalized href attributes
	div.innerHTML = "&lt;a href='#'&gt;&lt;/a&gt;";

	if ( div.firstChild &amp;&amp; typeof div.firstChild.getAttribute !== "undefined" &amp;&amp;
			div.firstChild.getAttribute("href") !== "#" ) {

		Expr.attrHandle.href = function( elem ) {
			return elem.getAttribute( "href", 2 );
		};
	}

	// release memory in IE
	div = null;
})();

if ( document.querySelectorAll ) {
	(function(){
		var oldSizzle = Sizzle,
			div = document.createElement("div"),
			id = "__sizzle__";

		div.innerHTML = "&lt;p class='TEST'&gt;&lt;/p&gt;";

		// Safari can't handle uppercase or unicode characters when
		// in quirks mode.
		if ( div.querySelectorAll &amp;&amp; div.querySelectorAll(".TEST").length === 0 ) {
			return;
		}

		Sizzle = function( query, context, extra, seed ) {
			context = context || document;

			// Only use querySelectorAll on non-XML documents
			// (ID selectors don't work in non-HTML documents)
			if ( !seed &amp;&amp; !Sizzle.isXML(context) ) {
				// See if we find a selector to speed up
				var match = /^(\w+$)|^\.([\w\-]+$)|^#([\w\-]+$)/.exec( query );

				if ( match &amp;&amp; (context.nodeType === 1 || context.nodeType === 9) ) {
					// Speed-up: Sizzle("TAG")
					if ( match[1] ) {
						return makeArray( context.getElementsByTagName( query ), extra );

					// Speed-up: Sizzle(".CLASS")
					} else if ( match[2] &amp;&amp; Expr.find.CLASS &amp;&amp; context.getElementsByClassName ) {
						return makeArray( context.getElementsByClassName( match[2] ), extra );
					}
				}

				if ( context.nodeType === 9 ) {
					// Speed-up: Sizzle("body")
					// The body element only exists once, optimize finding it
					if ( query === "body" &amp;&amp; context.body ) {
						return makeArray( [ context.body ], extra );

					// Speed-up: Sizzle("#ID")
					} else if ( match &amp;&amp; match[3] ) {
						var elem = context.getElementById( match[3] );

						// Check parentNode to catch when Blackberry 4.6 returns
						// nodes that are no longer in the document #6963
						if ( elem &amp;&amp; elem.parentNode ) {
							// Handle the case where IE and Opera return items
							// by name instead of ID
							if ( elem.id === match[3] ) {
								return makeArray( [ elem ], extra );
							}

						} else {
							return makeArray( [], extra );
						}
					}

					try {
						return makeArray( context.querySelectorAll(query), extra );
					} catch(qsaError) {}

				// qSA works strangely on Element-rooted queries
				// We can work around this by specifying an extra ID on the root
				// and working up from there (Thanks to Andrew Dupont for the technique)
				// IE 8 doesn't work on object elements
				} else if ( context.nodeType === 1 &amp;&amp; context.nodeName.toLowerCase() !== "object" ) {
					var oldContext = context,
						old = context.getAttribute( "id" ),
						nid = old || id,
						hasParent = context.parentNode,
						relativeHierarchySelector = /^\s*[+~]/.test( query );

					if ( !old ) {
						context.setAttribute( "id", nid );
					} else {
						nid = nid.replace( /'/g, "\\$&amp;" );
					}
					if ( relativeHierarchySelector &amp;&amp; hasParent ) {
						context = context.parentNode;
					}

					try {
						if ( !relativeHierarchySelector || hasParent ) {
							return makeArray( context.querySelectorAll( "[id='" + nid + "'] " + query ), extra );
						}

					} catch(pseudoError) {
					} finally {
						if ( !old ) {
							oldContext.removeAttribute( "id" );
						}
					}
				}
			}

			return oldSizzle(query, context, extra, seed);
		};

		for ( var prop in oldSizzle ) {
			Sizzle[ prop ] = oldSizzle[ prop ];
		}

		// release memory in IE
		div = null;
	})();
}

(function(){
	var html = document.documentElement,
		matches = html.matchesSelector || html.mozMatchesSelector || html.webkitMatchesSelector || html.msMatchesSelector;

	if ( matches ) {
		// Check to see if it's possible to do matchesSelector
		// on a disconnected node (IE 9 fails this)
		var disconnectedMatch = !matches.call( document.createElement( "div" ), "div" ),
			pseudoWorks = false;

		try {
			// This should fail with an exception
			// Gecko does not error, returns false instead
			matches.call( document.documentElement, "[test!='']:sizzle" );

		} catch( pseudoError ) {
			pseudoWorks = true;
		}

		Sizzle.matchesSelector = function( node, expr ) {
			// Make sure that attribute selectors are quoted
			expr = expr.replace(/\=\s*([^'"\]]*)\s*\]/g, "='$1']");

			if ( !Sizzle.isXML( node ) ) {
				try {
					if ( pseudoWorks || !Expr.match.PSEUDO.test( expr ) &amp;&amp; !/!=/.test( expr ) ) {
						var ret = matches.call( node, expr );

						// IE 9's matchesSelector returns false on disconnected nodes
						if ( ret || !disconnectedMatch ||
								// As well, disconnected nodes are said to be in a document
								// fragment in IE 9, so check for that
								node.document &amp;&amp; node.document.nodeType !== 11 ) {
							return ret;
						}
					}
				} catch(e) {}
			}

			return Sizzle(expr, null, null, [node]).length &gt; 0;
		};
	}
})();

(function(){
	var div = document.createElement("div");

	div.innerHTML = "&lt;div class='test e'&gt;&lt;/div&gt;&lt;div class='test'&gt;&lt;/div&gt;";

	// Opera can't find a second classname (in 9.6)
	// Also, make sure that getElementsByClassName actually exists
	if ( !div.getElementsByClassName || div.getElementsByClassName("e").length === 0 ) {
		return;
	}

	// Safari caches class attributes, doesn't catch changes (in 3.2)
	div.lastChild.className.baseVal = "e";

	if ( div.getElementsByClassName("e").length === 1 ) {
		return;
	}

	Expr.order.splice(1, 0, "CLASS");
	Expr.find.CLASS = function( match, context, isXML ) {
		if ( typeof context.getElementsByClassName !== "undefined" &amp;&amp; !isXML ) {
			return context.getElementsByClassName(match[1]);
		}
	};

	// release memory in IE
	div = null;
})();

function dirNodeCheck( dir, cur, doneName, checkSet, nodeCheck, isXML ) {
	for ( var i = 0, l = checkSet.length; i &lt; l; i++ ) {
		var elem = checkSet[i];

		if ( elem ) {
			var match = false;

			elem = elem[dir];

			while ( elem ) {
				if ( elem[ expando ] === doneName ) {
					match = checkSet[elem.sizset];
					break;
				}

				if ( elem.nodeType === 1 &amp;&amp; !isXML ){
					elem[ expando ] = doneName;
					elem.sizset = i;
				}

				if ( elem.nodeName.toLowerCase() === cur ) {
					match = elem;
					break;
				}

				elem = elem[dir];
			}

			checkSet[i] = match;
		}
	}
}

function dirCheck( dir, cur, doneName, checkSet, nodeCheck, isXML ) {
	for ( var i = 0, l = checkSet.length; i &lt; l; i++ ) {
		var elem = checkSet[i];

		if ( elem ) {
			var match = false;

			elem = elem[dir];

			while ( elem ) {
				if ( elem[ expando ] === doneName ) {
					match = checkSet[elem.sizset];
					break;
				}

				if ( elem.nodeType === 1 ) {
					if ( !isXML ) {
						elem[ expando ] = doneName;
						elem.sizset = i;
					}

					if ( typeof cur !== "string" ) {
						if ( elem === cur ) {
							match = true;
							break;
						}

					} else if ( Sizzle.filter( cur, [elem] ).length &gt; 0 ) {
						match = elem;
						break;
					}
				}

				elem = elem[dir];
			}

			checkSet[i] = match;
		}
	}
}

if ( document.documentElement.contains ) {
	Sizzle.contains = function( a, b ) {
		return a !== b &amp;&amp; (a.contains ? a.contains(b) : true);
	};

} else if ( document.documentElement.compareDocumentPosition ) {
	Sizzle.contains = function( a, b ) {
		return !!(a.compareDocumentPosition(b) &amp; 16);
	};

} else {
	Sizzle.contains = function() {
		return false;
	};
}

Sizzle.isXML = function( elem ) {
	// documentElement is verified for cases where it doesn't yet exist
	// (such as loading iframes in IE - #4833)
	var documentElement = (elem ? elem.ownerDocument || elem : 0).documentElement;

	return documentElement ? documentElement.nodeName !== "HTML" : false;
};

var posProcess = function( selector, context, seed ) {
	var match,
		tmpSet = [],
		later = "",
		root = context.nodeType ? [context] : context;

	// Position selectors must be done after the filter
	// And so must :not(positional) so we move all PSEUDOs to the end
	while ( (match = Expr.match.PSEUDO.exec( selector )) ) {
		later += match[0];
		selector = selector.replace( Expr.match.PSEUDO, "" );
	}

	selector = Expr.relative[selector] ? selector + "*" : selector;

	for ( var i = 0, l = root.length; i &lt; l; i++ ) {
		Sizzle( selector, root[i], tmpSet, seed );
	}

	return Sizzle.filter( later, tmpSet );
};

// EXPOSE
// Override sizzle attribute retrieval
Sizzle.attr = jQuery.attr;
Sizzle.selectors.attrMap = {};
jQuery.find = Sizzle;
jQuery.expr = Sizzle.selectors;
jQuery.expr[":"] = jQuery.expr.filters;
jQuery.unique = Sizzle.uniqueSort;
jQuery.text = Sizzle.getText;
jQuery.isXMLDoc = Sizzle.isXML;
jQuery.contains = Sizzle.contains;


})();


var runtil = /Until$/,
	rparentsprev = /^(?:parents|prevUntil|prevAll)/,
	// Note: This RegExp should be improved, or likely pulled from Sizzle
	rmultiselector = /,/,
	isSimple = /^.[^:#\[\.,]*$/,
	slice = Array.prototype.slice,
	POS = jQuery.expr.match.globalPOS,
	// methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend({
	find: function( selector ) {
		var self = this,
			i, l;

		if ( typeof selector !== "string" ) {
			return jQuery( selector ).filter(function() {
				for ( i = 0, l = self.length; i &lt; l; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			});
		}

		var ret = this.pushStack( "", "find", selector ),
			length, n, r;

		for ( i = 0, l = this.length; i &lt; l; i++ ) {
			length = ret.length;
			jQuery.find( selector, this[i], ret );

			if ( i &gt; 0 ) {
				// Make sure that the results are unique
				for ( n = length; n &lt; ret.length; n++ ) {
					for ( r = 0; r &lt; length; r++ ) {
						if ( ret[r] === ret[n] ) {
							ret.splice(n--, 1);
							break;
						}
					}
				}
			}
		}

		return ret;
	},

	has: function( target ) {
		var targets = jQuery( target );
		return this.filter(function() {
			for ( var i = 0, l = targets.length; i &lt; l; i++ ) {
				if ( jQuery.contains( this, targets[i] ) ) {
					return true;
				}
			}
		});
	},

	not: function( selector ) {
		return this.pushStack( winnow(this, selector, false), "not", selector);
	},

	filter: function( selector ) {
		return this.pushStack( winnow(this, selector, true), "filter", selector );
	},

	is: function( selector ) {
		return !!selector &amp;&amp; (
			typeof selector === "string" ?
				// If this is a positional selector, check membership in the returned set
				// so $("p:first").is("p:last") won't return true for a doc with two "p".
				POS.test( selector ) ?
					jQuery( selector, this.context ).index( this[0] ) &gt;= 0 :
					jQuery.filter( selector, this ).length &gt; 0 :
				this.filter( selector ).length &gt; 0 );
	},

	closest: function( selectors, context ) {
		var ret = [], i, l, cur = this[0];

		// Array (deprecated as of jQuery 1.7)
		if ( jQuery.isArray( selectors ) ) {
			var level = 1;

			while ( cur &amp;&amp; cur.ownerDocument &amp;&amp; cur !== context ) {
				for ( i = 0; i &lt; selectors.length; i++ ) {

					if ( jQuery( cur ).is( selectors[ i ] ) ) {
						ret.push({ selector: selectors[ i ], elem: cur, level: level });
					}
				}

				cur = cur.parentNode;
				level++;
			}

			return ret;
		}

		// String
		var pos = POS.test( selectors ) || typeof selectors !== "string" ?
				jQuery( selectors, context || this.context ) :
				0;

		for ( i = 0, l = this.length; i &lt; l; i++ ) {
			cur = this[i];

			while ( cur ) {
				if ( pos ? pos.index(cur) &gt; -1 : jQuery.find.matchesSelector(cur, selectors) ) {
					ret.push( cur );
					break;

				} else {
					cur = cur.parentNode;
					if ( !cur || !cur.ownerDocument || cur === context || cur.nodeType === 11 ) {
						break;
					}
				}
			}
		}

		ret = ret.length &gt; 1 ? jQuery.unique( ret ) : ret;

		return this.pushStack( ret, "closest", selectors );
	},

	// Determine the position of an element within
	// the matched set of elements
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[0] &amp;&amp; this[0].parentNode ) ? this.prevAll().length : -1;
		}

		// index in selector
		if ( typeof elem === "string" ) {
			return jQuery.inArray( this[0], jQuery( elem ) );
		}

		// Locate the position of the desired element
		return jQuery.inArray(
			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[0] : elem, this );
	},

	add: function( selector, context ) {
		var set = typeof selector === "string" ?
				jQuery( selector, context ) :
				jQuery.makeArray( selector &amp;&amp; selector.nodeType ? [ selector ] : selector ),
			all = jQuery.merge( this.get(), set );

		return this.pushStack( isDisconnected( set[0] ) || isDisconnected( all[0] ) ?
			all :
			jQuery.unique( all ) );
	},

	andSelf: function() {
		return this.add( this.prevObject );
	}
});

// A painfully simple check to see if an element is disconnected
// from a document (should be improved, where feasible).
function isDisconnected( node ) {
	return !node || !node.parentNode || node.parentNode.nodeType === 11;
}

jQuery.each({
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent &amp;&amp; parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return jQuery.dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return jQuery.nth( elem, 2, "nextSibling" );
	},
	prev: function( elem ) {
		return jQuery.nth( elem, 2, "previousSibling" );
	},
	nextAll: function( elem ) {
		return jQuery.dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return jQuery.dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return jQuery.sibling( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return jQuery.sibling( elem.firstChild );
	},
	contents: function( elem ) {
		return jQuery.nodeName( elem, "iframe" ) ?
			elem.contentDocument || elem.contentWindow.document :
			jQuery.makeArray( elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var ret = jQuery.map( this, fn, until );

		if ( !runtil.test( name ) ) {
			selector = until;
		}

		if ( selector &amp;&amp; typeof selector === "string" ) {
			ret = jQuery.filter( selector, ret );
		}

		ret = this.length &gt; 1 &amp;&amp; !guaranteedUnique[ name ] ? jQuery.unique( ret ) : ret;

		if ( (this.length &gt; 1 || rmultiselector.test( selector )) &amp;&amp; rparentsprev.test( name ) ) {
			ret = ret.reverse();
		}

		return this.pushStack( ret, name, slice.call( arguments ).join(",") );
	};
});

jQuery.extend({
	filter: function( expr, elems, not ) {
		if ( not ) {
			expr = ":not(" + expr + ")";
		}

		return elems.length === 1 ?
			jQuery.find.matchesSelector(elems[0], expr) ? [ elems[0] ] : [] :
			jQuery.find.matches(expr, elems);
	},

	dir: function( elem, dir, until ) {
		var matched = [],
			cur = elem[ dir ];

		while ( cur &amp;&amp; cur.nodeType !== 9 &amp;&amp; (until === undefined || cur.nodeType !== 1 || !jQuery( cur ).is( until )) ) {
			if ( cur.nodeType === 1 ) {
				matched.push( cur );
			}
			cur = cur[dir];
		}
		return matched;
	},

	nth: function( cur, result, dir, elem ) {
		result = result || 1;
		var num = 0;

		for ( ; cur; cur = cur[dir] ) {
			if ( cur.nodeType === 1 &amp;&amp; ++num === result ) {
				break;
			}
		}

		return cur;
	},

	sibling: function( n, elem ) {
		var r = [];

		for ( ; n; n = n.nextSibling ) {
			if ( n.nodeType === 1 &amp;&amp; n !== elem ) {
				r.push( n );
			}
		}

		return r;
	}
});

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, keep ) {

	// Can't pass null or undefined to indexOf in Firefox 4
	// Set to 0 to skip string check
	qualifier = qualifier || 0;

	if ( jQuery.isFunction( qualifier ) ) {
		return jQuery.grep(elements, function( elem, i ) {
			var retVal = !!qualifier.call( elem, i, elem );
			return retVal === keep;
		});

	} else if ( qualifier.nodeType ) {
		return jQuery.grep(elements, function( elem, i ) {
			return ( elem === qualifier ) === keep;
		});

	} else if ( typeof qualifier === "string" ) {
		var filtered = jQuery.grep(elements, function( elem ) {
			return elem.nodeType === 1;
		});

		if ( isSimple.test( qualifier ) ) {
			return jQuery.filter(qualifier, filtered, !keep);
		} else {
			qualifier = jQuery.filter( qualifier, filtered );
		}
	}

	return jQuery.grep(elements, function( elem, i ) {
		return ( jQuery.inArray( elem, qualifier ) &gt;= 0 ) === keep;
	});
}




function createSafeFragment( document ) {
	var list = nodeNames.split( "|" ),
	safeFrag = document.createDocumentFragment();

	if ( safeFrag.createElement ) {
		while ( list.length ) {
			safeFrag.createElement(
				list.pop()
			);
		}
	}
	return safeFrag;
}

var nodeNames = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|" +
		"header|hgroup|mark|meter|nav|output|progress|section|summary|time|video",
	rinlinejQuery = / jQuery\d+="(?:\d+|null)"/g,
	rleadingWhitespace = /^\s+/,
	rxhtmlTag = /&lt;(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^&gt;]*)\/&gt;/ig,
	rtagName = /&lt;([\w:]+)/,
	rtbody = /&lt;tbody/i,
	rhtml = /&lt;|&amp;#?\w+;/,
	rnoInnerhtml = /&lt;(?:script|style)/i,
	rnocache = /&lt;(?:script|object|embed|option|style)/i,
	rnoshimcache = new RegExp("&lt;(?:" + nodeNames + ")[\\s/&gt;]", "i"),
	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
	rscriptType = /\/(java|ecma)script/i,
	rcleanScript = /^\s*&lt;!(?:\[CDATA\[|\-\-)/,
	wrapMap = {
		option: [ 1, "&lt;select multiple='multiple'&gt;", "&lt;/select&gt;" ],
		legend: [ 1, "&lt;fieldset&gt;", "&lt;/fieldset&gt;" ],
		thead: [ 1, "&lt;table&gt;", "&lt;/table&gt;" ],
		tr: [ 2, "&lt;table&gt;&lt;tbody&gt;", "&lt;/tbody&gt;&lt;/table&gt;" ],
		td: [ 3, "&lt;table&gt;&lt;tbody&gt;&lt;tr&gt;", "&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;" ],
		col: [ 2, "&lt;table&gt;&lt;tbody&gt;&lt;/tbody&gt;&lt;colgroup&gt;", "&lt;/colgroup&gt;&lt;/table&gt;" ],
		area: [ 1, "&lt;map&gt;", "&lt;/map&gt;" ],
		_default: [ 0, "", "" ]
	},
	safeFragment = createSafeFragment( document );

wrapMap.optgroup = wrapMap.option;
wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;

// IE can't serialize &lt;link&gt; and &lt;script&gt; tags normally
if ( !jQuery.support.htmlSerialize ) {
	wrapMap._default = [ 1, "div&lt;div&gt;", "&lt;/div&gt;" ];
}

jQuery.fn.extend({
	text: function( value ) {
		return jQuery.access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().append( ( this[0] &amp;&amp; this[0].ownerDocument || document ).createTextNode( value ) );
		}, null, value, arguments.length );
	},

	wrapAll: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapAll( html.call(this, i) );
			});
		}

		if ( this[0] ) {
			// The elements to wrap the target around
			var wrap = jQuery( html, this[0].ownerDocument ).eq(0).clone(true);

			if ( this[0].parentNode ) {
				wrap.insertBefore( this[0] );
			}

			wrap.map(function() {
				var elem = this;

				while ( elem.firstChild &amp;&amp; elem.firstChild.nodeType === 1 ) {
					elem = elem.firstChild;
				}

				return elem;
			}).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapInner( html.call(this, i) );
			});
		}

		return this.each(function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		});
	},

	wrap: function( html ) {
		var isFunction = jQuery.isFunction( html );

		return this.each(function(i) {
			jQuery( this ).wrapAll( isFunction ? html.call(this, i) : html );
		});
	},

	unwrap: function() {
		return this.parent().each(function() {
			if ( !jQuery.nodeName( this, "body" ) ) {
				jQuery( this ).replaceWith( this.childNodes );
			}
		}).end();
	},

	append: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 ) {
				this.appendChild( elem );
			}
		});
	},

	prepend: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 ) {
				this.insertBefore( elem, this.firstChild );
			}
		});
	},

	before: function() {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			return this.domManip(arguments, false, function( elem ) {
				this.parentNode.insertBefore( elem, this );
			});
		} else if ( arguments.length ) {
			var set = jQuery.clean( arguments );
			set.push.apply( set, this.toArray() );
			return this.pushStack( set, "before", arguments );
		}
	},

	after: function() {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			return this.domManip(arguments, false, function( elem ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			});
		} else if ( arguments.length ) {
			var set = this.pushStack( this, "after", arguments );
			set.push.apply( set, jQuery.clean(arguments) );
			return set;
		}
	},

	// keepData is for internal use only--do not document
	remove: function( selector, keepData ) {
		for ( var i = 0, elem; (elem = this[i]) != null; i++ ) {
			if ( !selector || jQuery.filter( selector, [ elem ] ).length ) {
				if ( !keepData &amp;&amp; elem.nodeType === 1 ) {
					jQuery.cleanData( elem.getElementsByTagName("*") );
					jQuery.cleanData( [ elem ] );
				}

				if ( elem.parentNode ) {
					elem.parentNode.removeChild( elem );
				}
			}
		}

		return this;
	},

	empty: function() {
		for ( var i = 0, elem; (elem = this[i]) != null; i++ ) {
			// Remove element nodes and prevent memory leaks
			if ( elem.nodeType === 1 ) {
				jQuery.cleanData( elem.getElementsByTagName("*") );
			}

			// Remove any remaining nodes
			while ( elem.firstChild ) {
				elem.removeChild( elem.firstChild );
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function () {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		});
	},

	html: function( value ) {
		return jQuery.access( this, function( value ) {
			var elem = this[0] || {},
				i = 0,
				l = this.length;

			if ( value === undefined ) {
				return elem.nodeType === 1 ?
					elem.innerHTML.replace( rinlinejQuery, "" ) :
					null;
			}


			if ( typeof value === "string" &amp;&amp; !rnoInnerhtml.test( value ) &amp;&amp;
				( jQuery.support.leadingWhitespace || !rleadingWhitespace.test( value ) ) &amp;&amp;
				!wrapMap[ ( rtagName.exec( value ) || ["", ""] )[1].toLowerCase() ] ) {

				value = value.replace( rxhtmlTag, "&lt;$1&gt;&lt;/$2&gt;" );

				try {
					for (; i &lt; l; i++ ) {
						// Remove element nodes and prevent memory leaks
						elem = this[i] || {};
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( elem.getElementsByTagName( "*" ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch(e) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function( value ) {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			// Make sure that the elements are removed from the DOM before they are inserted
			// this can help fix replacing a parent with child elements
			if ( jQuery.isFunction( value ) ) {
				return this.each(function(i) {
					var self = jQuery(this), old = self.html();
					self.replaceWith( value.call( this, i, old ) );
				});
			}

			if ( typeof value !== "string" ) {
				value = jQuery( value ).detach();
			}

			return this.each(function() {
				var next = this.nextSibling,
					parent = this.parentNode;

				jQuery( this ).remove();

				if ( next ) {
					jQuery(next).before( value );
				} else {
					jQuery(parent).append( value );
				}
			});
		} else {
			return this.length ?
				this.pushStack( jQuery(jQuery.isFunction(value) ? value() : value), "replaceWith", value ) :
				this;
		}
	},

	detach: function( selector ) {
		return this.remove( selector, true );
	},

	domManip: function( args, table, callback ) {
		var results, first, fragment, parent,
			value = args[0],
			scripts = [];

		// We can't cloneNode fragments that contain checked, in WebKit
		if ( !jQuery.support.checkClone &amp;&amp; arguments.length === 3 &amp;&amp; typeof value === "string" &amp;&amp; rchecked.test( value ) ) {
			return this.each(function() {
				jQuery(this).domManip( args, table, callback, true );
			});
		}

		if ( jQuery.isFunction(value) ) {
			return this.each(function(i) {
				var self = jQuery(this);
				args[0] = value.call(this, i, table ? self.html() : undefined);
				self.domManip( args, table, callback );
			});
		}

		if ( this[0] ) {
			parent = value &amp;&amp; value.parentNode;

			// If we're in a fragment, just use that instead of building a new one
			if ( jQuery.support.parentNode &amp;&amp; parent &amp;&amp; parent.nodeType === 11 &amp;&amp; parent.childNodes.length === this.length ) {
				results = { fragment: parent };

			} else {
				results = jQuery.buildFragment( args, this, scripts );
			}

			fragment = results.fragment;

			if ( fragment.childNodes.length === 1 ) {
				first = fragment = fragment.firstChild;
			} else {
				first = fragment.firstChild;
			}

			if ( first ) {
				table = table &amp;&amp; jQuery.nodeName( first, "tr" );

				for ( var i = 0, l = this.length, lastIndex = l - 1; i &lt; l; i++ ) {
					callback.call(
						table ?
							root(this[i], first) :
							this[i],
						// Make sure that we do not leak memory by inadvertently discarding
						// the original fragment (which might have attached data) instead of
						// using it; in addition, use the original fragment object for the last
						// item instead of first because it can end up being emptied incorrectly
						// in certain situations (Bug #8070).
						// Fragments from the fragment cache must always be cloned and never used
						// in place.
						results.cacheable || ( l &gt; 1 &amp;&amp; i &lt; lastIndex ) ?
							jQuery.clone( fragment, true, true ) :
							fragment
					);
				}
			}

			if ( scripts.length ) {
				jQuery.each( scripts, function( i, elem ) {
					if ( elem.src ) {
						jQuery.ajax({
							type: "GET",
							global: false,
							url: elem.src,
							async: false,
							dataType: "script"
						});
					} else {
						jQuery.globalEval( ( elem.text || elem.textContent || elem.innerHTML || "" ).replace( rcleanScript, "/*$0*/" ) );
					}

					if ( elem.parentNode ) {
						elem.parentNode.removeChild( elem );
					}
				});
			}
		}

		return this;
	}
});

function root( elem, cur ) {
	return jQuery.nodeName(elem, "table") ?
		(elem.getElementsByTagName("tbody")[0] ||
		elem.appendChild(elem.ownerDocument.createElement("tbody"))) :
		elem;
}

function cloneCopyEvent( src, dest ) {

	if ( dest.nodeType !== 1 || !jQuery.hasData( src ) ) {
		return;
	}

	var type, i, l,
		oldData = jQuery._data( src ),
		curData = jQuery._data( dest, oldData ),
		events = oldData.events;

	if ( events ) {
		delete curData.handle;
		curData.events = {};

		for ( type in events ) {
			for ( i = 0, l = events[ type ].length; i &lt; l; i++ ) {
				jQuery.event.add( dest, type, events[ type ][ i ] );
			}
		}
	}

	// make the cloned public data object a copy from the original
	if ( curData.data ) {
		curData.data = jQuery.extend( {}, curData.data );
	}
}

function cloneFixAttributes( src, dest ) {
	var nodeName;

	// We do not need to do anything for non-Elements
	if ( dest.nodeType !== 1 ) {
		return;
	}

	// clearAttributes removes the attributes, which we don't want,
	// but also removes the attachEvent events, which we *do* want
	if ( dest.clearAttributes ) {
		dest.clearAttributes();
	}

	// mergeAttributes, in contrast, only merges back on the
	// original attributes, not the events
	if ( dest.mergeAttributes ) {
		dest.mergeAttributes( src );
	}

	nodeName = dest.nodeName.toLowerCase();

	// IE6-8 fail to clone children inside object elements that use
	// the proprietary classid attribute value (rather than the type
	// attribute) to identify the type of content to display
	if ( nodeName === "object" ) {
		dest.outerHTML = src.outerHTML;

	} else if ( nodeName === "input" &amp;&amp; (src.type === "checkbox" || src.type === "radio") ) {
		// IE6-8 fails to persist the checked state of a cloned checkbox
		// or radio button. Worse, IE6-7 fail to give the cloned element
		// a checked appearance if the defaultChecked value isn't also set
		if ( src.checked ) {
			dest.defaultChecked = dest.checked = src.checked;
		}

		// IE6-7 get confused and end up setting the value of a cloned
		// checkbox/radio button to an empty string instead of "on"
		if ( dest.value !== src.value ) {
			dest.value = src.value;
		}

	// IE6-8 fails to return the selected option to the default selected
	// state when cloning options
	} else if ( nodeName === "option" ) {
		dest.selected = src.defaultSelected;

	// IE6-8 fails to set the defaultValue to the correct value when
	// cloning other types of input fields
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;

	// IE blanks contents when cloning scripts
	} else if ( nodeName === "script" &amp;&amp; dest.text !== src.text ) {
		dest.text = src.text;
	}

	// Event data gets referenced instead of copied if the expando
	// gets copied too
	dest.removeAttribute( jQuery.expando );

	// Clear flags for bubbling special change/submit events, they must
	// be reattached when the newly cloned events are first activated
	dest.removeAttribute( "_submit_attached" );
	dest.removeAttribute( "_change_attached" );
}

jQuery.buildFragment = function( args, nodes, scripts ) {
	var fragment, cacheable, cacheresults, doc,
	first = args[ 0 ];

	// nodes may contain either an explicit document object,
	// a jQuery collection or context object.
	// If nodes[0] contains a valid object to assign to doc
	if ( nodes &amp;&amp; nodes[0] ) {
		doc = nodes[0].ownerDocument || nodes[0];
	}

	// Ensure that an attr object doesn't incorrectly stand in as a document object
	// Chrome and Firefox seem to allow this to occur and will throw exception
	// Fixes #8950
	if ( !doc.createDocumentFragment ) {
		doc = document;
	}

	// Only cache "small" (1/2 KB) HTML strings that are associated with the main document
	// Cloning options loses the selected state, so don't cache them
	// IE 6 doesn't like it when you put &lt;object&gt; or &lt;embed&gt; elements in a fragment
	// Also, WebKit does not clone 'checked' attributes on cloneNode, so don't cache
	// Lastly, IE6,7,8 will not correctly reuse cached fragments that were created from unknown elems #10501
	if ( args.length === 1 &amp;&amp; typeof first === "string" &amp;&amp; first.length &lt; 512 &amp;&amp; doc === document &amp;&amp;
		first.charAt(0) === "&lt;" &amp;&amp; !rnocache.test( first ) &amp;&amp;
		(jQuery.support.checkClone || !rchecked.test( first )) &amp;&amp;
		(jQuery.support.html5Clone || !rnoshimcache.test( first )) ) {

		cacheable = true;

		cacheresults = jQuery.fragments[ first ];
		if ( cacheresults &amp;&amp; cacheresults !== 1 ) {
			fragment = cacheresults;
		}
	}

	if ( !fragment ) {
		fragment = doc.createDocumentFragment();
		jQuery.clean( args, doc, fragment, scripts );
	}

	if ( cacheable ) {
		jQuery.fragments[ first ] = cacheresults ? fragment : 1;
	}

	return { fragment: fragment, cacheable: cacheable };
};

jQuery.fragments = {};

jQuery.each({
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var ret = [],
			insert = jQuery( selector ),
			parent = this.length === 1 &amp;&amp; this[0].parentNode;

		if ( parent &amp;&amp; parent.nodeType === 11 &amp;&amp; parent.childNodes.length === 1 &amp;&amp; insert.length === 1 ) {
			insert[ original ]( this[0] );
			return this;

		} else {
			for ( var i = 0, l = insert.length; i &lt; l; i++ ) {
				var elems = ( i &gt; 0 ? this.clone(true) : this ).get();
				jQuery( insert[i] )[ original ]( elems );
				ret = ret.concat( elems );
			}

			return this.pushStack( ret, name, insert.selector );
		}
	};
});

function getAll( elem ) {
	if ( typeof elem.getElementsByTagName !== "undefined" ) {
		return elem.getElementsByTagName( "*" );

	} else if ( typeof elem.querySelectorAll !== "undefined" ) {
		return elem.querySelectorAll( "*" );

	} else {
		return [];
	}
}

// Used in clean, fixes the defaultChecked property
function fixDefaultChecked( elem ) {
	if ( elem.type === "checkbox" || elem.type === "radio" ) {
		elem.defaultChecked = elem.checked;
	}
}
// Finds all inputs and passes them to fixDefaultChecked
function findInputs( elem ) {
	var nodeName = ( elem.nodeName || "" ).toLowerCase();
	if ( nodeName === "input" ) {
		fixDefaultChecked( elem );
	// Skip scripts, get other children
	} else if ( nodeName !== "script" &amp;&amp; typeof elem.getElementsByTagName !== "undefined" ) {
		jQuery.grep( elem.getElementsByTagName("input"), fixDefaultChecked );
	}
}

// Derived From: http://www.iecss.com/shimprove/javascript/shimprove.1-0-1.js
function shimCloneNode( elem ) {
	var div = document.createElement( "div" );
	safeFragment.appendChild( div );

	div.innerHTML = elem.outerHTML;
	return div.firstChild;
}

jQuery.extend({
	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var srcElements,
			destElements,
			i,
			// IE&lt;=8 does not properly clone detached, unknown element nodes
			clone = jQuery.support.html5Clone || jQuery.isXMLDoc(elem) || !rnoshimcache.test( "&lt;" + elem.nodeName + "&gt;" ) ?
				elem.cloneNode( true ) :
				shimCloneNode( elem );

		if ( (!jQuery.support.noCloneEvent || !jQuery.support.noCloneChecked) &amp;&amp;
				(elem.nodeType === 1 || elem.nodeType === 11) &amp;&amp; !jQuery.isXMLDoc(elem) ) {
			// IE copies events bound via attachEvent when using cloneNode.
			// Calling detachEvent on the clone will also remove the events
			// from the original. In order to get around this, we use some
			// proprietary methods to clear the events. Thanks to MooTools
			// guys for this hotness.

			cloneFixAttributes( elem, clone );

			// Using Sizzle here is crazy slow, so we use getElementsByTagName instead
			srcElements = getAll( elem );
			destElements = getAll( clone );

			// Weird iteration because IE will replace the length property
			// with an element if you are cloning the body and one of the
			// elements on the page has a name or id of "length"
			for ( i = 0; srcElements[i]; ++i ) {
				// Ensure that the destination node is not null; Fixes #9587
				if ( destElements[i] ) {
					cloneFixAttributes( srcElements[i], destElements[i] );
				}
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			cloneCopyEvent( elem, clone );

			if ( deepDataAndEvents ) {
				srcElements = getAll( elem );
				destElements = getAll( clone );

				for ( i = 0; srcElements[i]; ++i ) {
					cloneCopyEvent( srcElements[i], destElements[i] );
				}
			}
		}

		srcElements = destElements = null;

		// Return the cloned set
		return clone;
	},

	clean: function( elems, context, fragment, scripts ) {
		var checkScriptType, script, j,
				ret = [];

		context = context || document;

		// !context.createElement fails in IE with an error but returns typeof 'object'
		if ( typeof context.createElement === "undefined" ) {
			context = context.ownerDocument || context[0] &amp;&amp; context[0].ownerDocument || document;
		}

		for ( var i = 0, elem; (elem = elems[i]) != null; i++ ) {
			if ( typeof elem === "number" ) {
				elem += "";
			}

			if ( !elem ) {
				continue;
			}

			// Convert html string into DOM nodes
			if ( typeof elem === "string" ) {
				if ( !rhtml.test( elem ) ) {
					elem = context.createTextNode( elem );
				} else {
					// Fix "XHTML"-style tags in all browsers
					elem = elem.replace(rxhtmlTag, "&lt;$1&gt;&lt;/$2&gt;");

					// Trim whitespace, otherwise indexOf won't work as expected
					var tag = ( rtagName.exec( elem ) || ["", ""] )[1].toLowerCase(),
						wrap = wrapMap[ tag ] || wrapMap._default,
						depth = wrap[0],
						div = context.createElement("div"),
						safeChildNodes = safeFragment.childNodes,
						remove;

					// Append wrapper element to unknown element safe doc fragment
					if ( context === document ) {
						// Use the fragment we've already created for this document
						safeFragment.appendChild( div );
					} else {
						// Use a fragment created with the owner document
						createSafeFragment( context ).appendChild( div );
					}

					// Go to html and back, then peel off extra wrappers
					div.innerHTML = wrap[1] + elem + wrap[2];

					// Move to the right depth
					while ( depth-- ) {
						div = div.lastChild;
					}

					// Remove IE's autoinserted &lt;tbody&gt; from table fragments
					if ( !jQuery.support.tbody ) {

						// String was a &lt;table&gt;, *may* have spurious &lt;tbody&gt;
						var hasBody = rtbody.test(elem),
							tbody = tag === "table" &amp;&amp; !hasBody ?
								div.firstChild &amp;&amp; div.firstChild.childNodes :

								// String was a bare &lt;thead&gt; or &lt;tfoot&gt;
								wrap[1] === "&lt;table&gt;" &amp;&amp; !hasBody ?
									div.childNodes :
									[];

						for ( j = tbody.length - 1; j &gt;= 0 ; --j ) {
							if ( jQuery.nodeName( tbody[ j ], "tbody" ) &amp;&amp; !tbody[ j ].childNodes.length ) {
								tbody[ j ].parentNode.removeChild( tbody[ j ] );
							}
						}
					}

					// IE completely kills leading whitespace when innerHTML is used
					if ( !jQuery.support.leadingWhitespace &amp;&amp; rleadingWhitespace.test( elem ) ) {
						div.insertBefore( context.createTextNode( rleadingWhitespace.exec(elem)[0] ), div.firstChild );
					}

					elem = div.childNodes;

					// Clear elements from DocumentFragment (safeFragment or otherwise)
					// to avoid hoarding elements. Fixes #11356
					if ( div ) {
						div.parentNode.removeChild( div );

						// Guard against -1 index exceptions in FF3.6
						if ( safeChildNodes.length &gt; 0 ) {
							remove = safeChildNodes[ safeChildNodes.length - 1 ];

							if ( remove &amp;&amp; remove.parentNode ) {
								remove.parentNode.removeChild( remove );
							}
						}
					}
				}
			}

			// Resets defaultChecked for any radios and checkboxes
			// about to be appended to the DOM in IE 6/7 (#8060)
			var len;
			if ( !jQuery.support.appendChecked ) {
				if ( elem[0] &amp;&amp; typeof (len = elem.length) === "number" ) {
					for ( j = 0; j &lt; len; j++ ) {
						findInputs( elem[j] );
					}
				} else {
					findInputs( elem );
				}
			}

			if ( elem.nodeType ) {
				ret.push( elem );
			} else {
				ret = jQuery.merge( ret, elem );
			}
		}

		if ( fragment ) {
			checkScriptType = function( elem ) {
				return !elem.type || rscriptType.test( elem.type );
			};
			for ( i = 0; ret[i]; i++ ) {
				script = ret[i];
				if ( scripts &amp;&amp; jQuery.nodeName( script, "script" ) &amp;&amp; (!script.type || rscriptType.test( script.type )) ) {
					scripts.push( script.parentNode ? script.parentNode.removeChild( script ) : script );

				} else {
					if ( script.nodeType === 1 ) {
						var jsTags = jQuery.grep( script.getElementsByTagName( "script" ), checkScriptType );

						ret.splice.apply( ret, [i + 1, 0].concat( jsTags ) );
					}
					fragment.appendChild( script );
				}
			}
		}

		return ret;
	},

	cleanData: function( elems ) {
		var data, id,
			cache = jQuery.cache,
			special = jQuery.event.special,
			deleteExpando = jQuery.support.deleteExpando;

		for ( var i = 0, elem; (elem = elems[i]) != null; i++ ) {
			if ( elem.nodeName &amp;&amp; jQuery.noData[elem.nodeName.toLowerCase()] ) {
				continue;
			}

			id = elem[ jQuery.expando ];

			if ( id ) {
				data = cache[ id ];

				if ( data &amp;&amp; data.events ) {
					for ( var type in data.events ) {
						if ( special[ type ] ) {
							jQuery.event.remove( elem, type );

						// This is a shortcut to avoid jQuery.event.remove's overhead
						} else {
							jQuery.removeEvent( elem, type, data.handle );
						}
					}

					// Null the DOM reference to avoid IE6/7/8 leak (#7054)
					if ( data.handle ) {
						data.handle.elem = null;
					}
				}

				if ( deleteExpando ) {
					delete elem[ jQuery.expando ];

				} else if ( elem.removeAttribute ) {
					elem.removeAttribute( jQuery.expando );
				}

				delete cache[ id ];
			}
		}
	}
});




var ralpha = /alpha\([^)]*\)/i,
	ropacity = /opacity=([^)]*)/,
	// fixed for IE9, see #8346
	rupper = /([A-Z]|^ms)/g,
	rnum = /^[\-+]?(?:\d*\.)?\d+$/i,
	rnumnonpx = /^-?(?:\d*\.)?\d+(?!px)[^\d\s]+$/i,
	rrelNum = /^([\-+])=([\-+.\de]+)/,
	rmargin = /^margin/,

	cssShow = { position: "absolute", visibility: "hidden", display: "block" },

	// order is important!
	cssExpand = [ "Top", "Right", "Bottom", "Left" ],

	curCSS,

	getComputedStyle,
	currentStyle;

jQuery.fn.css = function( name, value ) {
	return jQuery.access( this, function( elem, name, value ) {
		return value !== undefined ?
			jQuery.style( elem, name, value ) :
			jQuery.css( elem, name );
	}, name, value, arguments.length &gt; 1 );
};

jQuery.extend({
	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {
					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;

				} else {
					return elem.style.opacity;
				}
			}
		}
	},

	// Exclude the following css properties to add px
	cssNumber: {
		"fillOpacity": true,
		"fontWeight": true,
		"lineHeight": true,
		"opacity": true,
		"orphans": true,
		"widows": true,
		"zIndex": true,
		"zoom": true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {
		// normalize float css property
		"float": jQuery.support.cssFloat ? "cssFloat" : "styleFloat"
	},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {
		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, origName = jQuery.camelCase( name ),
			style = elem.style, hooks = jQuery.cssHooks[ origName ];

		name = jQuery.cssProps[ origName ] || origName;

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// convert relative number strings (+= or -=) to relative numbers. #7345
			if ( type === "string" &amp;&amp; (ret = rrelNum.exec( value )) ) {
				value = ( +( ret[1] + 1) * +ret[2] ) + parseFloat( jQuery.css( elem, name ) );
				// Fixes bug #9237
				type = "number";
			}

			// Make sure that NaN and null values aren't set. See: #7116
			if ( value == null || type === "number" &amp;&amp; isNaN( value ) ) {
				return;
			}

			// If a number was passed in, add 'px' to the (except for certain CSS properties)
			if ( type === "number" &amp;&amp; !jQuery.cssNumber[ origName ] ) {
				value += "px";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !("set" in hooks) || (value = hooks.set( elem, value )) !== undefined ) {
				// Wrapped to prevent IE from throwing errors when 'invalid' values are provided
				// Fixes bug #5509
				try {
					style[ name ] = value;
				} catch(e) {}
			}

		} else {
			// If a hook was provided get the non-computed value from there
			if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, false, extra )) !== undefined ) {
				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra ) {
		var ret, hooks;

		// Make sure that we're working with the right name
		name = jQuery.camelCase( name );
		hooks = jQuery.cssHooks[ name ];
		name = jQuery.cssProps[ name ] || name;

		// cssFloat needs a special treatment
		if ( name === "cssFloat" ) {
			name = "float";
		}

		// If a hook was provided get the computed value from there
		if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, true, extra )) !== undefined ) {
			return ret;

		// Otherwise, if a way to get the computed value exists, use that
		} else if ( curCSS ) {
			return curCSS( elem, name );
		}
	},

	// A method for quickly swapping in/out CSS properties to get correct calculations
	swap: function( elem, options, callback ) {
		var old = {},
			ret, name;

		// Remember the old values, and insert the new ones
		for ( name in options ) {
			old[ name ] = elem.style[ name ];
			elem.style[ name ] = options[ name ];
		}

		ret = callback.call( elem );

		// Revert the old values
		for ( name in options ) {
			elem.style[ name ] = old[ name ];
		}

		return ret;
	}
});

// DEPRECATED in 1.3, Use jQuery.css() instead
jQuery.curCSS = jQuery.css;

if ( document.defaultView &amp;&amp; document.defaultView.getComputedStyle ) {
	getComputedStyle = function( elem, name ) {
		var ret, defaultView, computedStyle, width,
			style = elem.style;

		name = name.replace( rupper, "-$1" ).toLowerCase();

		if ( (defaultView = elem.ownerDocument.defaultView) &amp;&amp;
				(computedStyle = defaultView.getComputedStyle( elem, null )) ) {

			ret = computedStyle.getPropertyValue( name );
			if ( ret === "" &amp;&amp; !jQuery.contains( elem.ownerDocument.documentElement, elem ) ) {
				ret = jQuery.style( elem, name );
			}
		}

		// A tribute to the "awesome hack by Dean Edwards"
		// WebKit uses "computed value (percentage if specified)" instead of "used value" for margins
		// which is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values
		if ( !jQuery.support.pixelMargin &amp;&amp; computedStyle &amp;&amp; rmargin.test( name ) &amp;&amp; rnumnonpx.test( ret ) ) {
			width = style.width;
			style.width = ret;
			ret = computedStyle.width;
			style.width = width;
		}

		return ret;
	};
}

if ( document.documentElement.currentStyle ) {
	currentStyle = function( elem, name ) {
		var left, rsLeft, uncomputed,
			ret = elem.currentStyle &amp;&amp; elem.currentStyle[ name ],
			style = elem.style;

		// Avoid setting ret to empty string here
		// so we don't default to auto
		if ( ret == null &amp;&amp; style &amp;&amp; (uncomputed = style[ name ]) ) {
			ret = uncomputed;
		}

		// From the awesome hack by Dean Edwards
		// http://erik.eae.net/archives/2007/07/27/18.54.15/#comment-102291

		// If we're not dealing with a regular pixel number
		// but a number that has a weird ending, we need to convert it to pixels
		if ( rnumnonpx.test( ret ) ) {

			// Remember the original values
			left = style.left;
			rsLeft = elem.runtimeStyle &amp;&amp; elem.runtimeStyle.left;

			// Put in the new values to get a computed value out
			if ( rsLeft ) {
				elem.runtimeStyle.left = elem.currentStyle.left;
			}
			style.left = name === "fontSize" ? "1em" : ret;
			ret = style.pixelLeft + "px";

			// Revert the changed values
			style.left = left;
			if ( rsLeft ) {
				elem.runtimeStyle.left = rsLeft;
			}
		}

		return ret === "" ? "auto" : ret;
	};
}

curCSS = getComputedStyle || currentStyle;

function getWidthOrHeight( elem, name, extra ) {

	// Start with offset property
	var val = name === "width" ? elem.offsetWidth : elem.offsetHeight,
		i = name === "width" ? 1 : 0,
		len = 4;
	if (val === undefined) {
		val = name === "width" ? elem.getBBox().width : elem.getBBox().height;
	}
	if ( val &gt; 0 ) {
		if ( extra !== "border" ) {
			for ( ; i &lt; len; i += 2 ) {
				if ( !extra ) {
					val -= parseFloat( jQuery.css( elem, "padding" + cssExpand[ i ] ) ) || 0;
				}
				if ( extra === "margin" ) {
					val += parseFloat( jQuery.css( elem, extra + cssExpand[ i ] ) ) || 0;
				} else {
					val -= parseFloat( jQuery.css( elem, "border" + cssExpand[ i ] + "Width" ) ) || 0;
				}
			}
		}

		return val + "px";
	}

	// Fall back to computed then uncomputed css if necessary
	val = curCSS( elem, name );
	if ( val &lt; 0 || val == null ) {
		val = elem.style[ name ];
	}

	// Computed unit is not pixels. Stop here and return.
	if ( rnumnonpx.test(val) ) {
		return val;
	}

	// Normalize "", auto, and prepare for extra
	val = parseFloat( val ) || 0;

	// Add padding, border, margin
	if ( extra ) {
		for ( ; i &lt; len; i += 2 ) {
			val += parseFloat( jQuery.css( elem, "padding" + cssExpand[ i ] ) ) || 0;
			if ( extra !== "padding" ) {
				val += parseFloat( jQuery.css( elem, "border" + cssExpand[ i ] + "Width" ) ) || 0;
			}
			if ( extra === "margin" ) {
				val += parseFloat( jQuery.css( elem, extra + cssExpand[ i ]) ) || 0;
			}
		}
	}

	return val + "px";
}

jQuery.each([ "height", "width" ], function( i, name ) {
	jQuery.cssHooks[ name ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {
				if ( elem.offsetWidth !== 0 ) {
					return getWidthOrHeight( elem, name, extra );
				} else {
					return jQuery.swap( elem, cssShow, function() {
						return getWidthOrHeight( elem, name, extra );
					});
				}
			}
		},

		set: function( elem, value ) {
			return rnum.test( value ) ?
				value + "px" :
				value;
		}
	};
});

if ( !jQuery.support.opacity ) {
	jQuery.cssHooks.opacity = {
		get: function( elem, computed ) {
			// IE uses filters for opacity
			return ropacity.test( (computed &amp;&amp; elem.currentStyle ? elem.currentStyle.filter : elem.style.filter) || "" ) ?
				( parseFloat( RegExp.$1 ) / 100 ) + "" :
				computed ? "1" : "";
		},

		set: function( elem, value ) {
			var style = elem.style,
				currentStyle = elem.currentStyle,
				opacity = jQuery.isNumeric( value ) ? "alpha(opacity=" + value * 100 + ")" : "",
				filter = currentStyle &amp;&amp; currentStyle.filter || style.filter || "";

			// IE has trouble with opacity if it does not have layout
			// Force it by setting the zoom level
			// style.zoom = 1;

			// if setting opacity to 1, and no other filters exist - attempt to remove filter attribute #6652
		  //    if ( value &gt;= 1 &amp;&amp; jQuery.trim( filter.replace( ralpha, "" ) ) === "" ) {

		  //    	// Setting style.filter to null, "" &amp; " " still leave "filter:" in the cssText
		  //    	// if "filter:" is present at all, clearType is disabled, we want to avoid this
		  //    	// style.removeAttribute is IE Only, but so apparently is this code path...
		  //    	style.removeAttribute( "filter" );

		  //    	// if there there is no filter style applied in a css rule, we are done
		  //    	if ( currentStyle &amp;&amp; !currentStyle.filter ) {
		  //    		return;
		  //    	}
		  //    }

			// otherwise, set new filter values
			style.filter = ralpha.test( filter ) ?
				filter.replace( ralpha, opacity ) :
				filter + " " + opacity;
		}
	};
}

jQuery(function() {
	// This hook cannot be added until DOM ready because the support test
	// for it is not run until after DOM ready
	if ( !jQuery.support.reliableMarginRight ) {
		jQuery.cssHooks.marginRight = {
			get: function( elem, computed ) {
				// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
				// Work around by temporarily setting element display to inline-block
				return jQuery.swap( elem, { "display": "inline-block" }, function() {
					if ( computed ) {
						return curCSS( elem, "margin-right" );
					} else {
						return elem.style.marginRight;
					}
				});
			}
		};
	}
});

if ( jQuery.expr &amp;&amp; jQuery.expr.filters ) {
	jQuery.expr.filters.hidden = function( elem ) {
		var width = elem.offsetWidth,
			height = elem.offsetHeight;

		return ( width === 0 &amp;&amp; height === 0 ) || (!jQuery.support.reliableHiddenOffsets &amp;&amp; ((elem.style &amp;&amp; elem.style.display) || jQuery.css( elem, "display" )) === "none");
	};

	jQuery.expr.filters.visible = function( elem ) {
		return !jQuery.expr.filters.hidden( elem );
	};
}

// These hooks are used by animate to expand properties
jQuery.each({
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {

	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i,

				// assumes a single number if not a string
				parts = typeof value === "string" ? value.split(" ") : [ value ],
				expanded = {};

			for ( i = 0; i &lt; 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};
});




var r20 = /%20/g,
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rhash = /#.*$/,
	rheaders = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg, // IE leaves an \r character at EOL
	rinput = /^(?:color|date|datetime|datetime-local|email|hidden|month|number|password|range|search|tel|text|time|url|week)$/i,
	// #7653, #8125, #8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app\-storage|.+\-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,
	rquery = /\?/,
	rscript = /&lt;script\b[^&lt;]*(?:(?!&lt;\/script&gt;)&lt;[^&lt;]*)*&lt;\/script&gt;/gi,
	rselectTextarea = /^(?:select|textarea)/i,
	rspacesAjax = /\s+/,
	rts = /([?&amp;])_=[^&amp;]*/,
	rurl = /^([\w\+\.\-]+:)(?:\/\/([^\/?#:]*)(?::(\d+))?)?/,

	// Keep a copy of the old load method
	_load = jQuery.fn.load,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Document location
	ajaxLocation,

	// Document location segments
	ajaxLocParts,

	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
	allTypes = ["*/"] + ["*"];

// #8138, IE may throw an exception when accessing
// a field from window.location if document.domain has been set
try {
	ajaxLocation = location.href;
} catch( e ) {
	// Use the href attribute of an A element
	// since IE will modify it given document.location
	ajaxLocation = document.createElement( "a" );
	ajaxLocation.href = "";
	ajaxLocation = ajaxLocation.href;
}

// Segment location into parts
ajaxLocParts = rurl.exec( ajaxLocation.toLowerCase() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		if ( jQuery.isFunction( func ) ) {
			var dataTypes = dataTypeExpression.toLowerCase().split( rspacesAjax ),
				i = 0,
				length = dataTypes.length,
				dataType,
				list,
				placeBefore;

			// For each dataType in the dataTypeExpression
			for ( ; i &lt; length; i++ ) {
				dataType = dataTypes[ i ];
				// We control if we're asked to add before
				// any existing element
				placeBefore = /^\+/.test( dataType );
				if ( placeBefore ) {
					dataType = dataType.substr( 1 ) || "*";
				}
				list = structure[ dataType ] = structure[ dataType ] || [];
				// then we add to the structure accordingly
				list[ placeBefore ? "unshift" : "push" ]( func );
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR,
		dataType /* internal */, inspected /* internal */ ) {

	dataType = dataType || options.dataTypes[ 0 ];
	inspected = inspected || {};

	inspected[ dataType ] = true;

	var list = structure[ dataType ],
		i = 0,
		length = list ? list.length : 0,
		executeOnly = ( structure === prefilters ),
		selection;

	for ( ; i &lt; length &amp;&amp; ( executeOnly || !selection ); i++ ) {
		selection = list[ i ]( options, originalOptions, jqXHR );
		// If we got redirected to another dataType
		// we try there if executing only and not done already
		if ( typeof selection === "string" ) {
			if ( !executeOnly || inspected[ selection ] ) {
				selection = undefined;
			} else {
				options.dataTypes.unshift( selection );
				selection = inspectPrefiltersOrTransports(
						structure, options, originalOptions, jqXHR, selection, inspected );
			}
		}
	}
	// If we're only executing or nothing was selected
	// we try the catchall dataType if not done already
	if ( ( executeOnly || !selection ) &amp;&amp; !inspected[ "*" ] ) {
		selection = inspectPrefiltersOrTransports(
				structure, options, originalOptions, jqXHR, "*", inspected );
	}
	// unnecessary when only executing (prefilters)
	// but it'll be ignored by the caller in that case
	return selection;
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
	var key, deep,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};
	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}
}

jQuery.fn.extend({
	load: function( url, params, callback ) {
		if ( typeof url !== "string" &amp;&amp; _load ) {
			return _load.apply( this, arguments );

		// Don't do a request if no elements are being requested
		} else if ( !this.length ) {
			return this;
		}

		var off = url.indexOf( " " );
		if ( off &gt;= 0 ) {
			var selector = url.slice( off, url.length );
			url = url.slice( 0, off );
		}

		// Default to a GET request
		var type = "GET";

		// If the second parameter was provided
		if ( params ) {
			// If it's a function
			if ( jQuery.isFunction( params ) ) {
				// We assume that it's the callback
				callback = params;
				params = undefined;

			// Otherwise, build a param string
			} else if ( typeof params === "object" ) {
				params = jQuery.param( params, jQuery.ajaxSettings.traditional );
				type = "POST";
			}
		}

		var self = this;

		// Request the remote document
		jQuery.ajax({
			url: url,
			type: type,
			dataType: "html",
			data: params,
			// Complete callback (responseText is used internally)
			complete: function( jqXHR, status, responseText ) {
				// Store the response as specified by the jqXHR object
				responseText = jqXHR.responseText;
				// If successful, inject the HTML into all the matched elements
				if ( jqXHR.isResolved() ) {
					// #4825: Get the actual response in case
					// a dataFilter is present in ajaxSettings
					jqXHR.done(function( r ) {
						responseText = r;
					});
					// See if a selector was specified
					self.html( selector ?
						// Create a dummy div to hold the results
						jQuery("&lt;div&gt;")
							// inject the contents of the document in, removing the scripts
							// to avoid any 'Permission Denied' errors in IE
							.append(responseText.replace(rscript, ""))

							// Locate the specified elements
							.find(selector) :

						// If not, just inject the full result
						responseText );
				}

				if ( callback ) {
					self.each( callback, [ responseText, status, jqXHR ] );
				}
			}
		});

		return this;
	},

	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},

	serializeArray: function() {
		return this.map(function(){
			return this.elements ? jQuery.makeArray( this.elements ) : this;
		})
		.filter(function(){
			return this.name &amp;&amp; !this.disabled &amp;&amp;
				( this.checked || rselectTextarea.test( this.nodeName ) ||
					rinput.test( this.type ) );
		})
		.map(function( i, elem ){
			var val = jQuery( this ).val();

			return val == null ?
				null :
				jQuery.isArray( val ) ?
					jQuery.map( val, function( val, i ){
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					}) :
					{ name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		}).get();
	}
});

// Attach a bunch of functions for handling common AJAX events
jQuery.each( "ajaxStart ajaxStop ajaxComplete ajaxError ajaxSuccess ajaxSend".split( " " ), function( i, o ){
	jQuery.fn[ o ] = function( f ){
		return this.on( o, f );
	};
});

jQuery.each( [ "get", "post" ], function( i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {
		// shift arguments if data argument was omitted
		if ( jQuery.isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		return jQuery.ajax({
			type: method,
			url: url,
			data: data,
			success: callback,
			dataType: type
		});
	};
});

jQuery.extend({

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		if ( settings ) {
			// Building a settings object
			ajaxExtend( target, jQuery.ajaxSettings );
		} else {
			// Extending ajaxSettings
			settings = target;
			target = jQuery.ajaxSettings;
		}
		ajaxExtend( target, settings );
		return target;
	},

	ajaxSettings: {
		url: ajaxLocation,
		isLocal: rlocalProtocol.test( ajaxLocParts[ 1 ] ),
		global: true,
		type: "GET",
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",
		processData: true,
		async: true,
		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		traditional: false,
		headers: {},
		*/

		accepts: {
			xml: "application/xml, text/xml",
			html: "text/html",
			text: "text/plain",
			json: "application/json, text/javascript",
			"*": allTypes
		},

		contents: {
			xml: /xml/,
			html: /html/,
			json: /json/
		},

		responseFields: {
			xml: "responseXML",
			text: "responseText"
		},

		// List of data converters
		// 1) key format is "source_type destination_type" (a single space in-between)
		// 2) the catchall symbol "*" can be used for source_type
		converters: {

			// Convert anything to text
			"* text": window.String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": jQuery.parseJSON,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			context: true,
			url: true
		}
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var // Create the final options object
			s = jQuery.ajaxSetup( {}, options ),
			// Callbacks context
			callbackContext = s.context || s,
			// Context for global events
			// It's the callbackContext if one was provided in the options
			// and if it's a DOM node or a jQuery collection
			globalEventContext = callbackContext !== s &amp;&amp;
				( callbackContext.nodeType || callbackContext instanceof jQuery ) ?
						jQuery( callbackContext ) : jQuery.event,
			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks( "once memory" ),
			// Status-dependent callbacks
			statusCode = s.statusCode || {},
			// ifModified key
			ifModifiedKey,
			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},
			// Response headers
			responseHeadersString,
			responseHeaders,
			// transport
			transport,
			// timeout handle
			timeoutTimer,
			// Cross-domain detection vars
			parts,
			// The jqXHR state
			state = 0,
			// To know if global events are to be dispatched
			fireGlobals,
			// Loop variable
			i,
			// Fake xhr
			jqXHR = {

				readyState: 0,

				// Caches the header
				setRequestHeader: function( name, value ) {
					if ( !state ) {
						var lname = name.toLowerCase();
						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Raw string
				getAllResponseHeaders: function() {
					return state === 2 ? responseHeadersString : null;
				},

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( state === 2 ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while( ( match = rheaders.exec( responseHeadersString ) ) ) {
								responseHeaders[ match[1].toLowerCase() ] = match[ 2 ];
							}
						}
						match = responseHeaders[ key.toLowerCase() ];
					}
					return match === undefined ? null : match;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( !state ) {
						s.mimeType = type;
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					statusText = statusText || "abort";
					if ( transport ) {
						transport.abort( statusText );
					}
					done( 0, statusText );
					return this;
				}
			};

		// Callback for when everything is done
		// It is defined here because jslint complains if it is declared
		// at the end of the function (which would be more logical and readable)
		function done( status, nativeStatusText, responses, headers ) {

			// Called once
			if ( state === 2 ) {
				return;
			}

			// State is "done" now
			state = 2;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status &gt; 0 ? 4 : 0;

			var isSuccess,
				success,
				error,
				statusText = nativeStatusText,
				response = responses ? ajaxHandleResponses( s, jqXHR, responses ) : undefined,
				lastModified,
				etag;

			// If successful, handle type chaining
			if ( status &gt;= 200 &amp;&amp; status &lt; 300 || status === 304 ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {

					if ( ( lastModified = jqXHR.getResponseHeader( "Last-Modified" ) ) ) {
						jQuery.lastModified[ ifModifiedKey ] = lastModified;
					}
					if ( ( etag = jqXHR.getResponseHeader( "Etag" ) ) ) {
						jQuery.etag[ ifModifiedKey ] = etag;
					}
				}

				// If not modified
				if ( status === 304 ) {

					statusText = "notmodified";
					isSuccess = true;

				// If we have data
				} else {

					try {
						success = ajaxConvert( s, response );
						statusText = "success";
						isSuccess = true;
					} catch(e) {
						// We have a parsererror
						statusText = "parsererror";
						error = e;
					}
				}
			} else {
				// We extract error from statusText
				// then normalize statusText and status for non-aborts
				error = statusText;
				if ( !statusText || status ) {
					statusText = "error";
					if ( status &lt; 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = "" + ( nativeStatusText || statusText );

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajax" + ( isSuccess ? "Success" : "Error" ),
						[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );
				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger( "ajaxStop" );
				}
			}
		}

		// Attach deferreds
		deferred.promise( jqXHR );
		jqXHR.success = jqXHR.done;
		jqXHR.error = jqXHR.fail;
		jqXHR.complete = completeDeferred.add;

		// Status-dependent callbacks
		jqXHR.statusCode = function( map ) {
			if ( map ) {
				var tmp;
				if ( state &lt; 2 ) {
					for ( tmp in map ) {
						statusCode[ tmp ] = [ statusCode[tmp], map[tmp] ];
					}
				} else {
					tmp = map[ jqXHR.status ];
					jqXHR.then( tmp, tmp );
				}
			}
			return this;
		};

		// Remove hash character (#7531: and string promotion)
		// Add protocol if not provided (#5866: IE7 issue with protocol-less urls)
		// We also use the url parameter if available
		s.url = ( ( url || s.url ) + "" ).replace( rhash, "" ).replace( rprotocol, ajaxLocParts[ 1 ] + "//" );

		// Extract dataTypes list
		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().split( rspacesAjax );

		// Determine if a cross-domain request is in order
		if ( s.crossDomain == null ) {
			parts = rurl.exec( s.url.toLowerCase() );
			s.crossDomain = !!( parts &amp;&amp;
				( parts[ 1 ] != ajaxLocParts[ 1 ] || parts[ 2 ] != ajaxLocParts[ 2 ] ||
					( parts[ 3 ] || ( parts[ 1 ] === "http:" ? 80 : 443 ) ) !=
						( ajaxLocParts[ 3 ] || ( ajaxLocParts[ 1 ] === "http:" ? 80 : 443 ) ) )
			);
		}

		// Convert data if not already a string
		if ( s.data &amp;&amp; s.processData &amp;&amp; typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( state === 2 ) {
			return false;
		}

		// We can fire global events as of now if asked to
		fireGlobals = s.global;

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Watch for a new set of requests
		if ( fireGlobals &amp;&amp; jQuery.active++ === 0 ) {
			jQuery.event.trigger( "ajaxStart" );
		}

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// If data is available, append data to url
			if ( s.data ) {
				s.url += ( rquery.test( s.url ) ? "&amp;" : "?" ) + s.data;
				// #9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Get ifModifiedKey before adding the anti-cache parameter
			ifModifiedKey = s.url;

			// Add anti-cache in url if needed
			if ( s.cache === false ) {

				var ts = jQuery.now(),
					// try replacing _= if it is there
					ret = s.url.replace( rts, "$1_=" + ts );

				// if nothing was replaced, add timestamp to the end
				s.url = ret + ( ( ret === s.url ) ? ( rquery.test( s.url ) ? "&amp;" : "?" ) + "_=" + ts : "" );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data &amp;&amp; s.hasContent &amp;&amp; s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			ifModifiedKey = ifModifiedKey || s.url;
			if ( jQuery.lastModified[ ifModifiedKey ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ ifModifiedKey ] );
			}
			if ( jQuery.etag[ ifModifiedKey ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ ifModifiedKey ] );
			}
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] &amp;&amp; s.accepts[ s.dataTypes[0] ] ?
				s.accepts[ s.dataTypes[0] ] + ( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend &amp;&amp; ( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {
				// Abort if not done already
				jqXHR.abort();
				return false;

		}

		// Install callbacks on deferreds
		for ( i in { success: 1, error: 1, complete: 1 } ) {
			jqXHR[ i ]( s[ i ] );
		}

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;
			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}
			// Timeout
			if ( s.async &amp;&amp; s.timeout &gt; 0 ) {
				timeoutTimer = setTimeout( function(){
					jqXHR.abort( "timeout" );
				}, s.timeout );
			}

			try {
				state = 1;
				transport.send( requestHeaders, done );
			} catch (e) {
				// Propagate exception as error if not done
				if ( state &lt; 2 ) {
					done( -1, e );
				// Simply rethrow otherwise
				} else {
					throw e;
				}
			}
		}

		return jqXHR;
	},

	// Serialize an array of form elements or a set of
	// key/values into a query string
	param: function( a, traditional ) {
		var s = [],
			add = function( key, value ) {
				// If value is a function, invoke it and return its value
				value = jQuery.isFunction( value ) ? value() : value;
				s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );
			};

		// Set traditional to true for jQuery &lt;= 1.3.2 behavior.
		if ( traditional === undefined ) {
			traditional = jQuery.ajaxSettings.traditional;
		}

		// If an array was passed in, assume that it is an array of form elements.
		if ( jQuery.isArray( a ) || ( a.jquery &amp;&amp; !jQuery.isPlainObject( a ) ) ) {
			// Serialize the form elements
			jQuery.each( a, function() {
				add( this.name, this.value );
			});

		} else {
			// If traditional, encode the "old" way (the way 1.3.2 or older
			// did it), otherwise encode params recursively.
			for ( var prefix in a ) {
				buildParams( prefix, a[ prefix ], traditional, add );
			}
		}

		// Return the resulting serialization
		return s.join( "&amp;" ).replace( r20, "+" );
	}
});

function buildParams( prefix, obj, traditional, add ) {
	if ( jQuery.isArray( obj ) ) {
		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {
				// Treat each array item as a scalar.
				add( prefix, v );

			} else {
				// If array item is non-scalar (array or object), encode its
				// numeric index to resolve deserialization ambiguity issues.
				// Note that rack (as of 1.0.0) can't currently deserialize
				// nested arrays properly, and attempting to do so may cause
				// a server error. Possible fixes are to modify rack's
				// deserialization algorithm or to provide an option or flag
				// to force array serialization to be shallow.
				buildParams( prefix + "[" + ( typeof v === "object" ? i : "" ) + "]", v, traditional, add );
			}
		});

	} else if ( !traditional &amp;&amp; jQuery.type( obj ) === "object" ) {
		// Serialize object item.
		for ( var name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {
		// Serialize scalar item.
		add( prefix, obj );
	}
}

// This is still on the jQuery object... for now
// Want to move this to jQuery.ajax some day
jQuery.extend({

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {}

});

/* Handles responses to an ajax request:
 * - sets all responseXXX fields accordingly
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

	var contents = s.contents,
		dataTypes = s.dataTypes,
		responseFields = s.responseFields,
		ct,
		type,
		finalDataType,
		firstDataType;

	// Fill responseXXX fields
	for ( type in responseFields ) {
		if ( type in responses ) {
			jqXHR[ responseFields[type] ] = responses[ type ];
		}
	}

	// Remove auto dataType and get content-type in the process
	while( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader( "content-type" );
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] &amp;&amp; contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {
		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[0] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}
		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

// Chain conversions given the request and the original response
function ajaxConvert( s, response ) {

	// Apply the dataFilter if provided
	if ( s.dataFilter ) {
		response = s.dataFilter( response, s.dataType );
	}

	var dataTypes = s.dataTypes,
		converters = {},
		i,
		key,
		length = dataTypes.length,
		tmp,
		// Current and previous dataTypes
		current = dataTypes[ 0 ],
		prev,
		// Conversion expression
		conversion,
		// Conversion function
		conv,
		// Conversion functions (transitive conversion)
		conv1,
		conv2;

	// For each dataType in the chain
	for ( i = 1; i &lt; length; i++ ) {

		// Create converters map
		// with lowercased keys
		if ( i === 1 ) {
			for ( key in s.converters ) {
				if ( typeof key === "string" ) {
					converters[ key.toLowerCase() ] = s.converters[ key ];
				}
			}
		}

		// Get the dataTypes
		prev = current;
		current = dataTypes[ i ];

		// If current is auto dataType, update it to prev
		if ( current === "*" ) {
			current = prev;
		// If no auto and dataTypes are actually different
		} else if ( prev !== "*" &amp;&amp; prev !== current ) {

			// Get the converter
			conversion = prev + " " + current;
			conv = converters[ conversion ] || converters[ "* " + current ];

			// If there is no direct converter, search transitively
			if ( !conv ) {
				conv2 = undefined;
				for ( conv1 in converters ) {
					tmp = conv1.split( " " );
					if ( tmp[ 0 ] === prev || tmp[ 0 ] === "*" ) {
						conv2 = converters[ tmp[1] + " " + current ];
						if ( conv2 ) {
							conv1 = converters[ conv1 ];
							if ( conv1 === true ) {
								conv = conv2;
							} else if ( conv2 === true ) {
								conv = conv1;
							}
							break;
						}
					}
				}
			}
			// If we found no converter, dispatch an error
			if ( !( conv || conv2 ) ) {
				jQuery.error( "No conversion from " + conversion.replace(" "," to ") );
			}
			// If found converter is not an equivalence
			if ( conv !== true ) {
				// Convert with 1 or 2 converters accordingly
				response = conv ? conv( response ) : conv2( conv1(response) );
			}
		}
	}
	return response;
}




var jsc = jQuery.now(),
	jsre = /(\=)\?(&amp;|$)|\?\?/i;

// Default jsonp settings
jQuery.ajaxSetup({
	jsonp: "callback",
	jsonpCallback: function() {
		return jQuery.expando + "_" + ( jsc++ );
	}
});

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var inspectData = ( typeof s.data === "string" ) &amp;&amp; /^application\/x\-www\-form\-urlencoded/.test( s.contentType );

	if ( s.dataTypes[ 0 ] === "jsonp" ||
		s.jsonp !== false &amp;&amp; ( jsre.test( s.url ) ||
				inspectData &amp;&amp; jsre.test( s.data ) ) ) {

		var responseContainer,
			jsonpCallback = s.jsonpCallback =
				jQuery.isFunction( s.jsonpCallback ) ? s.jsonpCallback() : s.jsonpCallback,
			previous = window[ jsonpCallback ],
			url = s.url,
			data = s.data,
			replace = "$1" + jsonpCallback + "$2";

		if ( s.jsonp !== false ) {
			url = url.replace( jsre, replace );
			if ( s.url === url ) {
				if ( inspectData ) {
					data = data.replace( jsre, replace );
				}
				if ( s.data === data ) {
					// Add callback manually
					url += (/\?/.test( url ) ? "&amp;" : "?") + s.jsonp + "=" + jsonpCallback;
				}
			}
		}

		s.url = url;
		s.data = data;

		// Install callback
		window[ jsonpCallback ] = function( response ) {
			responseContainer = [ response ];
		};

		// Clean-up function
		jqXHR.always(function() {
			// Set callback back to previous value
			window[ jsonpCallback ] = previous;
			// Call if it was a function and we have a response
			if ( responseContainer &amp;&amp; jQuery.isFunction( previous ) ) {
				window[ jsonpCallback ]( responseContainer[ 0 ] );
			}
		});

		// Use data converter to retrieve json after script execution
		s.converters["script json"] = function() {
			if ( !responseContainer ) {
				jQuery.error( jsonpCallback + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// force json dataType
		s.dataTypes[ 0 ] = "json";

		// Delegate to script
		return "script";
	}
});




// Install script dataType
jQuery.ajaxSetup({
	accepts: {
		script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /javascript|ecmascript/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
});

// Handle cache's special case and global
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
		s.global = false;
	}
});

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function(s) {

	// This transport only deals with cross domain requests
	if ( s.crossDomain ) {

		var script,
			head = document.head || document.getElementsByTagName( "head" )[0] || document.documentElement;

		return {

			send: function( _, callback ) {

				script = document.createElement( "script" );

				script.async = "async";

				if ( s.scriptCharset ) {
					script.charset = s.scriptCharset;
				}

				script.src = s.url;

				// Attach handlers for all browsers
				script.onload = script.onreadystatechange = function( _, isAbort ) {

					if ( isAbort || !script.readyState || /loaded|complete/.test( script.readyState ) ) {

						// Handle memory leak in IE
						script.onload = script.onreadystatechange = null;

						// Remove the script
						if ( head &amp;&amp; script.parentNode ) {
							head.removeChild( script );
						}

						// Dereference the script
						script = undefined;

						// Callback if not abort
						if ( !isAbort ) {
							callback( 200, "success" );
						}
					}
				};
				// Use insertBefore instead of appendChild  to circumvent an IE6 bug.
				// This arises when a base node is used (#2709 and #4378).
				head.insertBefore( script, head.firstChild );
			},

			abort: function() {
				if ( script ) {
					script.onload( 0, 1 );
				}
			}
		};
	}
});




var // #5280: Internet Explorer will keep connections alive if we don't abort on unload
	xhrOnUnloadAbort = window.ActiveXObject ? function() {
		// Abort all pending requests
		for ( var key in xhrCallbacks ) {
			xhrCallbacks[ key ]( 0, 1 );
		}
	} : false,
	xhrId = 0,
	xhrCallbacks;

// Functions to create xhrs
function createStandardXHR() {
	try {
		return new window.XMLHttpRequest();
	} catch( e ) {}
}

function createActiveXHR() {
	try {
		return new window.ActiveXObject( "Microsoft.XMLHTTP" );
	} catch( e ) {}
}

// Create the request object
// (This is still attached to ajaxSettings for backward compatibility)
jQuery.ajaxSettings.xhr = window.ActiveXObject ?
	/* Microsoft failed to properly
	 * implement the XMLHttpRequest in IE7 (can't request local files),
	 * so we use the ActiveXObject when it is available
	 * Additionally XMLHttpRequest can be disabled in IE7/IE8 so
	 * we need a fallback.
	 */
	function() {
		return !this.isLocal &amp;&amp; createStandardXHR() || createActiveXHR();
	} :
	// For all other browsers, use the standard XMLHttpRequest object
	createStandardXHR;

// Determine support properties
(function( xhr ) {
	jQuery.extend( jQuery.support, {
		ajax: !!xhr,
		cors: !!xhr &amp;&amp; ( "withCredentials" in xhr )
	});
})( jQuery.ajaxSettings.xhr() );

// Create transport if the browser can provide an xhr
if ( jQuery.support.ajax ) {

	jQuery.ajaxTransport(function( s ) {
		// Cross domain only allowed if supported through XMLHttpRequest
		if ( !s.crossDomain || jQuery.support.cors ) {

			var callback;

			return {
				send: function( headers, complete ) {

					// Get a new xhr
					var xhr = s.xhr(),
						handle,
						i;

					// Open the socket
					// Passing null username, generates a login popup on Opera (#2865)
					if ( s.username ) {
						xhr.open( s.type, s.url, s.async, s.username, s.password );
					} else {
						xhr.open( s.type, s.url, s.async );
					}

					// Apply custom fields if provided
					if ( s.xhrFields ) {
						for ( i in s.xhrFields ) {
							xhr[ i ] = s.xhrFields[ i ];
						}
					}

					// Override mime type if needed
					if ( s.mimeType &amp;&amp; xhr.overrideMimeType ) {
						xhr.overrideMimeType( s.mimeType );
					}

					// X-Requested-With header
					// For cross-domain requests, seeing as conditions for a preflight are
					// akin to a jigsaw puzzle, we simply never set it to be sure.
					// (it can always be set on a per-request basis or even using ajaxSetup)
					// For same-domain requests, won't change header if already provided.
					if ( !s.crossDomain &amp;&amp; !headers["X-Requested-With"] ) {
						headers[ "X-Requested-With" ] = "XMLHttpRequest";
					}

					// Need an extra try/catch for cross domain requests in Firefox 3
					try {
						for ( i in headers ) {
							xhr.setRequestHeader( i, headers[ i ] );
						}
					} catch( _ ) {}

					// Do send the request
					// This may raise an exception which is actually
					// handled in jQuery.ajax (so no try/catch here)
					xhr.send( ( s.hasContent &amp;&amp; s.data ) || null );

					// Listener
					callback = function( _, isAbort ) {

						var status,
							statusText,
							responseHeaders,
							responses,
							xml;

						// Firefox throws exceptions when accessing properties
						// of an xhr when a network error occured
						// http://helpful.knobs-dials.com/index.php/Component_returned_failure_code:_0x80040111_(NS_ERROR_NOT_AVAILABLE)
						try {

							// Was never called and is aborted or complete
							if ( callback &amp;&amp; ( isAbort || xhr.readyState === 4 ) ) {

								// Only called once
								callback = undefined;

								// Do not keep as active anymore
								if ( handle ) {
									xhr.onreadystatechange = jQuery.noop;
									if ( xhrOnUnloadAbort ) {
										delete xhrCallbacks[ handle ];
									}
								}

								// If it's an abort
								if ( isAbort ) {
									// Abort it manually if needed
									if ( xhr.readyState !== 4 ) {
										xhr.abort();
									}
								} else {
									status = xhr.status;
									responseHeaders = xhr.getAllResponseHeaders();
									responses = {};
									xml = xhr.responseXML;

									// Construct response list
									if ( xml &amp;&amp; xml.documentElement /* #4958 */ ) {
										responses.xml = xml;
									}

									// When requesting binary data, IE6-9 will throw an exception
									// on any attempt to access responseText (#11426)
									try {
										responses.text = xhr.responseText;
									} catch( _ ) {
									}

									// Firefox throws an exception when accessing
									// statusText for faulty cross-domain requests
									try {
										statusText = xhr.statusText;
									} catch( e ) {
										// We normalize with Webkit giving an empty statusText
										statusText = "";
									}

									// Filter status for non standard behaviors

									// If the request is local and we have data: assume a success
									// (success with no data won't get notified, that's the best we
									// can do given current implementations)
									if ( !status &amp;&amp; s.isLocal &amp;&amp; !s.crossDomain ) {
										status = responses.text ? 200 : 404;
									// IE - #1450: sometimes returns 1223 when it should be 204
									} else if ( status === 1223 ) {
										status = 204;
									}
								}
							}
						} catch( firefoxAccessException ) {
							if ( !isAbort ) {
								complete( -1, firefoxAccessException );
							}
						}

						// Call complete if needed
						if ( responses ) {
							complete( status, statusText, responses, responseHeaders );
						}
					};

					// if we're in sync mode or it's in cache
					// and has been retrieved directly (IE6 &amp; IE7)
					// we need to manually fire the callback
					if ( !s.async || xhr.readyState === 4 ) {
						callback();
					} else {
						handle = ++xhrId;
						if ( xhrOnUnloadAbort ) {
							// Create the active xhrs callbacks list if needed
							// and attach the unload handler
							if ( !xhrCallbacks ) {
								xhrCallbacks = {};
								jQuery( window ).unload( xhrOnUnloadAbort );
							}
							// Add to list of active xhrs callbacks
							xhrCallbacks[ handle ] = callback;
						}
						xhr.onreadystatechange = callback;
					}
				},

				abort: function() {
					if ( callback ) {
						callback(0,1);
					}
				}
			};
		}
	});
}




var elemdisplay = {},
	iframe, iframeDoc,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rfxnum = /^([+\-]=)?([\d+.\-]+)([a-z%]*)$/i,
	timerId,
	fxAttrs = [
		// height animations
		[ "height", "marginTop", "marginBottom", "paddingTop", "paddingBottom" ],
		// width animations
		[ "width", "marginLeft", "marginRight", "paddingLeft", "paddingRight" ],
		// opacity animations
		[ "opacity" ]
	],
	fxNow;

jQuery.fn.extend({
	show: function( speed, easing, callback ) {
		var elem, display;

		if ( speed || speed === 0 ) {
			return this.animate( genFx("show", 3), speed, easing, callback );

		} else {
			for ( var i = 0, j = this.length; i &lt; j; i++ ) {
				elem = this[ i ];

				if ( elem.style ) {
					display = elem.style.display;

					// Reset the inline display of this element to learn if it is
					// being hidden by cascaded rules or not
					if ( !jQuery._data(elem, "olddisplay") &amp;&amp; display === "none" ) {
						display = elem.style.display = "";
					}

					// Set elements which have been overridden with display: none
					// in a stylesheet to whatever the default browser style is
					// for such an element
					if ( (display === "" &amp;&amp; jQuery.css(elem, "display") === "none") ||
						!jQuery.contains( elem.ownerDocument.documentElement, elem ) ) {
						jQuery._data( elem, "olddisplay", defaultDisplay(elem.nodeName) );
					}
				}
			}

			// Set the display of most of the elements in a second loop
			// to avoid the constant reflow
			for ( i = 0; i &lt; j; i++ ) {
				elem = this[ i ];

				if ( elem.style ) {
					display = elem.style.display;

					if ( display === "" || display === "none" ) {
						elem.style.display = jQuery._data( elem, "olddisplay" ) || "";
					}
				}
			}

			return this;
		}
	},

	hide: function( speed, easing, callback ) {
		if ( speed || speed === 0 ) {
			return this.animate( genFx("hide", 3), speed, easing, callback);

		} else {
			var elem, display,
				i = 0,
				j = this.length;

			for ( ; i &lt; j; i++ ) {
				elem = this[i];
				if ( elem.style ) {
					display = jQuery.css( elem, "display" );

					if ( display !== "none" &amp;&amp; !jQuery._data( elem, "olddisplay" ) ) {
						jQuery._data( elem, "olddisplay", display );
					}
				}
			}

			// Set the display of the elements in a second loop
			// to avoid the constant reflow
			for ( i = 0; i &lt; j; i++ ) {
				if ( this[i].style ) {
					this[i].style.display = "none";
				}
			}

			return this;
		}
	},

	// Save the old toggle function
	_toggle: jQuery.fn.toggle,

	toggle: function( fn, fn2, callback ) {
		var bool = typeof fn === "boolean";

		if ( jQuery.isFunction(fn) &amp;&amp; jQuery.isFunction(fn2) ) {
			this._toggle.apply( this, arguments );

		} else if ( fn == null || bool ) {
			this.each(function() {
				var state = bool ? fn : jQuery(this).is(":hidden");
				jQuery(this)[ state ? "show" : "hide" ]();
			});

		} else {
			this.animate(genFx("toggle", 3), fn, fn2, callback);
		}

		return this;
	},

	fadeTo: function( speed, to, easing, callback ) {
		return this.filter(":hidden").css("opacity", 0).show().end()
					.animate({opacity: to}, speed, easing, callback);
	},

	animate: function( prop, speed, easing, callback ) {
		var optall = jQuery.speed( speed, easing, callback );

		if ( jQuery.isEmptyObject( prop ) ) {
			return this.each( optall.complete, [ false ] );
		}

		// Do not change referenced properties as per-property easing will be lost
		prop = jQuery.extend( {}, prop );

		function doAnimation() {
			// XXX 'this' does not always have a nodeName when running the
			// test suite

			if ( optall.queue === false ) {
				jQuery._mark( this );
			}

			var opt = jQuery.extend( {}, optall ),
				isElement = this.nodeType === 1,
				hidden = isElement &amp;&amp; jQuery(this).is(":hidden"),
				name, val, p, e, hooks, replace,
				parts, start, end, unit,
				method;

			// will store per property easing and be used to determine when an animation is complete
			opt.animatedProperties = {};

			// first pass over propertys to expand / normalize
			for ( p in prop ) {
				name = jQuery.camelCase( p );
				if ( p !== name ) {
					prop[ name ] = prop[ p ];
					delete prop[ p ];
				}

				if ( ( hooks = jQuery.cssHooks[ name ] ) &amp;&amp; "expand" in hooks ) {
					replace = hooks.expand( prop[ name ] );
					delete prop[ name ];

					// not quite $.extend, this wont overwrite keys already present.
					// also - reusing 'p' from above because we have the correct "name"
					for ( p in replace ) {
						if ( ! ( p in prop ) ) {
							prop[ p ] = replace[ p ];
						}
					}
				}
			}

			for ( name in prop ) {
				val = prop[ name ];
				// easing resolution: per property &gt; opt.specialEasing &gt; opt.easing &gt; 'swing' (default)
				if ( jQuery.isArray( val ) ) {
					opt.animatedProperties[ name ] = val[ 1 ];
					val = prop[ name ] = val[ 0 ];
				} else {
					opt.animatedProperties[ name ] = opt.specialEasing &amp;&amp; opt.specialEasing[ name ] || opt.easing || 'swing';
				}

				if ( val === "hide" &amp;&amp; hidden || val === "show" &amp;&amp; !hidden ) {
					return opt.complete.call( this );
				}

				if ( isElement &amp;&amp; ( name === "height" || name === "width" ) ) {
					// Make sure that nothing sneaks out
					// Record all 3 overflow attributes because IE does not
					// change the overflow attribute when overflowX and
					// overflowY are set to the same value
					opt.overflow = [ this.style.overflow, this.style.overflowX, this.style.overflowY ];

					// Set display property to inline-block for height/width
					// animations on inline elements that are having width/height animated
					if ( jQuery.css( this, "display" ) === "inline" &amp;&amp;
							jQuery.css( this, "float" ) === "none" ) {

						// inline-level elements accept inline-block;
						// block-level elements need to be inline with layout
						if ( !jQuery.support.inlineBlockNeedsLayout || defaultDisplay( this.nodeName ) === "inline" ) {
							this.style.display = "inline-block";

						} else {
							this.style.zoom = 1;
						}
					}
				}
			}

			if ( opt.overflow != null ) {
				this.style.overflow = "hidden";
			}

			for ( p in prop ) {
				e = new jQuery.fx( this, opt, p );
				val = prop[ p ];

				if ( rfxtypes.test( val ) ) {

					// Tracks whether to show or hide based on private
					// data attached to the element
					method = jQuery._data( this, "toggle" + p ) || ( val === "toggle" ? hidden ? "show" : "hide" : 0 );
					if ( method ) {
						jQuery._data( this, "toggle" + p, method === "show" ? "hide" : "show" );
						e[ method ]();
					} else {
						e[ val ]();
					}

				} else {
					parts = rfxnum.exec( val );
					start = e.cur();

					if ( parts ) {
						end = parseFloat( parts[2] );
						unit = parts[3] || ( jQuery.cssNumber[ p ] ? "" : "px" );

						// We need to compute starting value
						if ( unit !== "px" ) {
							jQuery.style( this, p, (end || 1) + unit);
							start = ( (end || 1) / e.cur() ) * start;
							jQuery.style( this, p, start + unit);
						}

						// If a +=/-= token was provided, we're doing a relative animation
						if ( parts[1] ) {
							end = ( (parts[ 1 ] === "-=" ? -1 : 1) * end ) + start;
						}

						e.custom( start, end, unit );

					} else {
						e.custom( start, val, "" );
					}
				}
			}

			// For JS strict compliance
			return true;
		}

		return optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},

	stop: function( type, clearQueue, gotoEnd ) {
		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue &amp;&amp; type !== false ) {
			this.queue( type || "fx", [] );
		}

		return this.each(function() {
			var index,
				hadTimers = false,
				timers = jQuery.timers,
				data = jQuery._data( this );

			// clear marker counters if we know they won't be
			if ( !gotoEnd ) {
				jQuery._unmark( true, this );
			}

			function stopQueue( elem, data, index ) {
				var hooks = data[ index ];
				jQuery.removeData( elem, index, true );
				hooks.stop( gotoEnd );
			}

			if ( type == null ) {
				for ( index in data ) {
					if ( data[ index ] &amp;&amp; data[ index ].stop &amp;&amp; index.indexOf(".run") === index.length - 4 ) {
						stopQueue( this, data, index );
					}
				}
			} else if ( data[ index = type + ".run" ] &amp;&amp; data[ index ].stop ){
				stopQueue( this, data, index );
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this &amp;&amp; (type == null || timers[ index ].queue === type) ) {
					if ( gotoEnd ) {

						// force the next step to be the last
						timers[ index ]( true );
					} else {
						timers[ index ].saveState();
					}
					hadTimers = true;
					timers.splice( index, 1 );
				}
			}

			// start the next in the queue if the last step wasn't forced
			// timers currently will call their complete callbacks, which will dequeue
			// but only if they were gotoEnd
			if ( !( gotoEnd &amp;&amp; hadTimers ) ) {
				jQuery.dequeue( this, type );
			}
		});
	}

});

// Animations created synchronously will run synchronously
function createFxNow() {
	setTimeout( clearFxNow, 0 );
	return ( fxNow = jQuery.now() );
}

function clearFxNow() {
	fxNow = undefined;
}

// Generate parameters to create a standard animation
function genFx( type, num ) {
	var obj = {};

	jQuery.each( fxAttrs.concat.apply([], fxAttrs.slice( 0, num )), function() {
		obj[ this ] = type;
	});

	return obj;
}

// Generate shortcuts for custom animations
jQuery.each({
	slideDown: genFx( "show", 1 ),
	slideUp: genFx( "hide", 1 ),
	slideToggle: genFx( "toggle", 1 ),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
});

jQuery.extend({
	speed: function( speed, easing, fn ) {
		var opt = speed &amp;&amp; typeof speed === "object" ? jQuery.extend( {}, speed ) : {
			complete: fn || !fn &amp;&amp; easing ||
				jQuery.isFunction( speed ) &amp;&amp; speed,
			duration: speed,
			easing: fn &amp;&amp; easing || easing &amp;&amp; !jQuery.isFunction( easing ) &amp;&amp; easing
		};

		opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ? opt.duration :
			opt.duration in jQuery.fx.speeds ? jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;

		// normalize opt.queue - true/undefined/null -&gt; "fx"
		if ( opt.queue == null || opt.queue === true ) {
			opt.queue = "fx";
		}

		// Queueing
		opt.old = opt.complete;

		opt.complete = function( noUnmark ) {
			if ( jQuery.isFunction( opt.old ) ) {
				opt.old.call( this );
			}

			if ( opt.queue ) {
				jQuery.dequeue( this, opt.queue );
			} else if ( noUnmark !== false ) {
				jQuery._unmark( this );
			}
		};

		return opt;
	},

	easing: {
		linear: function( p ) {
			return p;
		},
		swing: function( p ) {
			return ( -Math.cos( p*Math.PI ) / 2 ) + 0.5;
		}
	},

	timers: [],

	fx: function( elem, options, prop ) {
		this.options = options;
		this.elem = elem;
		this.prop = prop;

		options.orig = options.orig || {};
	}

});

jQuery.fx.prototype = {
	// Simple function for setting a style value
	update: function() {
		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		( jQuery.fx.step[ this.prop ] || jQuery.fx.step._default )( this );
	},

	// Get the current size
	cur: function() {
		if ( this.elem[ this.prop ] != null &amp;&amp; (!this.elem.style || this.elem.style[ this.prop ] == null) ) {
              if (this.elem[ this.prop ].baseVal) {
			return this.elem[ this.prop ].baseVal.value;
              }
			return this.elem[ this.prop ];
		}

		var parsed,
			r = jQuery.css( this.elem, this.prop );
		// Empty strings, null, undefined and "auto" are converted to 0,
		// complex values such as "rotate(1rad)" are returned as is,
		// simple values such as "10px" are parsed to Float.
		return isNaN( parsed = parseFloat( r ) ) ? !r || r === "auto" ? 0 : r : parsed;
	},

	// Start an animation from one number to another
	custom: function( from, to, unit ) {
		var self = this,
			fx = jQuery.fx;

		this.startTime = fxNow || createFxNow();
		this.end = to;
		this.now = this.start = from;
		this.pos = this.state = 0;
		this.unit = unit || this.unit || ( jQuery.cssNumber[ this.prop ] ? "" : "px" );

		function t( gotoEnd ) {
			return self.step( gotoEnd );
		}

		t.queue = this.options.queue;
		t.elem = this.elem;
		t.saveState = function() {
			if ( jQuery._data( self.elem, "fxshow" + self.prop ) === undefined ) {
				if ( self.options.hide ) {
					jQuery._data( self.elem, "fxshow" + self.prop, self.start );
				} else if ( self.options.show ) {
					jQuery._data( self.elem, "fxshow" + self.prop, self.end );
				}
			}
		};

		if ( t() &amp;&amp; jQuery.timers.push(t) &amp;&amp; !timerId ) {
			timerId = setInterval( fx.tick, fx.interval );
		}
	},

	// Simple 'show' function
	show: function() {
		var dataShow = jQuery._data( this.elem, "fxshow" + this.prop );

		// Remember where we started, so that we can go back to it later
		this.options.orig[ this.prop ] = dataShow || jQuery.style( this.elem, this.prop );
		this.options.show = true;

		// Begin the animation
		// Make sure that we start at a small width/height to avoid any flash of content
		if ( dataShow !== undefined ) {
			// This show is picking up where a previous hide or show left off
			this.custom( this.cur(), dataShow );
		} else {
			this.custom( this.prop === "width" || this.prop === "height" ? 1 : 0, this.cur() );
		}

		// Start by showing the element
		jQuery( this.elem ).show();
	},

	// Simple 'hide' function
	hide: function() {
		// Remember where we started, so that we can go back to it later
		this.options.orig[ this.prop ] = jQuery._data( this.elem, "fxshow" + this.prop ) || jQuery.style( this.elem, this.prop );
		this.options.hide = true;

		// Begin the animation
		this.custom( this.cur(), 0 );
	},

	// Each step of an animation
	step: function( gotoEnd ) {
		var p, n, complete,
			t = fxNow || createFxNow(),
			done = true,
			elem = this.elem,
			options = this.options;

		if ( gotoEnd || t &gt;= options.duration + this.startTime ) {
			this.now = this.end;
			this.pos = this.state = 1;
			this.update();

			options.animatedProperties[ this.prop ] = true;

			for ( p in options.animatedProperties ) {
				if ( options.animatedProperties[ p ] !== true ) {
					done = false;
				}
			}

			if ( done ) {
				// Reset the overflow
				if ( options.overflow != null &amp;&amp; !jQuery.support.shrinkWrapBlocks ) {

					jQuery.each( [ "", "X", "Y" ], function( index, value ) {
						elem.style[ "overflow" + value ] = options.overflow[ index ];
					});
				}

				// Hide the element if the "hide" operation was done
				if ( options.hide ) {
					jQuery( elem ).hide();
				}

				// Reset the properties, if the item has been hidden or shown
				if ( options.hide || options.show ) {
					for ( p in options.animatedProperties ) {
						jQuery.style( elem, p, options.orig[ p ] );
						jQuery.removeData( elem, "fxshow" + p, true );
						// Toggle data is no longer needed
						jQuery.removeData( elem, "toggle" + p, true );
					}
				}

				// Execute the complete function
				// in the event that the complete function throws an exception
				// we must ensure it won't be called twice. #5684

				complete = options.complete;
				if ( complete ) {

					options.complete = false;
					complete.call( elem );
				}
			}

			return false;

		} else {
			// classical easing cannot be used with an Infinity duration
			if ( options.duration == Infinity ) {
				this.now = t;
			} else {
				n = t - this.startTime;
				this.state = n / options.duration;

				// Perform the easing function, defaults to swing
				this.pos = jQuery.easing[ options.animatedProperties[this.prop] ]( this.state, n, 0, 1, options.duration );
				this.now = this.start + ( (this.end - this.start) * this.pos );
			}
			// Perform the next step of the animation
			this.update();
		}

		return true;
	}
};

jQuery.extend( jQuery.fx, {
	tick: function() {
		var timer,
			timers = jQuery.timers,
			i = 0;

		for ( ; i &lt; timers.length; i++ ) {
			timer = timers[ i ];
			// Checks the timer has not already been removed
			if ( !timer() &amp;&amp; timers[ i ] === timer ) {
				timers.splice( i--, 1 );
			}
		}

		if ( !timers.length ) {
			jQuery.fx.stop();
		}
	},

	interval: 13,

	stop: function() {
		clearInterval( timerId );
		timerId = null;
	},

	speeds: {
		slow: 600,
		fast: 200,
		// Default speed
		_default: 400
	},

	step: {
		opacity: function( fx ) {
			jQuery.style( fx.elem, "opacity", fx.now );
		},

		_default: function( fx ) {
			if ( fx.elem.style &amp;&amp; fx.elem.style[ fx.prop ] != null ) {
				fx.elem.style[ fx.prop ] = fx.now + fx.unit;
			} else {
				fx.elem[ fx.prop ] = fx.now;
			}
		}
	}
});

// Ensure props that can't be negative don't go there on undershoot easing
jQuery.each( fxAttrs.concat.apply( [], fxAttrs ), function( i, prop ) {
	// exclude marginTop, marginLeft, marginBottom and marginRight from this list
	if ( prop.indexOf( "margin" ) ) {
		jQuery.fx.step[ prop ] = function( fx ) {
			jQuery.style( fx.elem, prop, Math.max(0, fx.now) + fx.unit );
		};
	}
});

if ( jQuery.expr &amp;&amp; jQuery.expr.filters ) {
	jQuery.expr.filters.animated = function( elem ) {
		return jQuery.grep(jQuery.timers, function( fn ) {
			return elem === fn.elem;
		}).length;
	};
}

// Try to restore the default display value of an element
function defaultDisplay( nodeName ) {

	if ( !elemdisplay[ nodeName ] ) {

		var body = document.body,
			elem = jQuery( "&lt;" + nodeName + "&gt;" ).appendTo( body ),
			display = elem.css( "display" );
		elem.remove();

		// If the simple way fails,
		// get element's real default display by attaching it to a temp iframe
		if ( display === "none" || display === "" ) {
			// No iframe to use yet, so create it
			if ( !iframe ) {
				iframe = document.createElement( "iframe" );
				iframe.frameBorder = iframe.width = iframe.height = 0;
			}

			body.appendChild( iframe );

			// Create a cacheable copy of the iframe document on first call.
			// IE and Opera will allow us to reuse the iframeDoc without re-writing the fake HTML
			// document to it; WebKit &amp; Firefox won't allow reusing the iframe document.
			if ( !iframeDoc || !iframe.createElement ) {
				iframeDoc = ( iframe.contentWindow || iframe.contentDocument ).document;
				iframeDoc.write( ( jQuery.support.boxModel ? "&lt;!doctype html&gt;" : "" ) + "&lt;html&gt;&lt;body&gt;" );
				iframeDoc.close();
			}

			elem = iframeDoc.createElement( nodeName );

			iframeDoc.body.appendChild( elem );

			display = jQuery.css( elem, "display" );
			body.removeChild( iframe );
		}

		// Store the correct default display
		elemdisplay[ nodeName ] = display;
	}

	return elemdisplay[ nodeName ];
}




var getOffset,
	rtable = /^t(?:able|d|h)$/i,
	rroot = /^(?:body|html)$/i;

if ( "getBoundingClientRect" in document.documentElement ) {
	getOffset = function( elem, doc, docElem, box ) {
		try {
			box = elem.getBoundingClientRect();
		} catch(e) {}

		// Make sure we're not dealing with a disconnected DOM node
		if ( !box || !jQuery.contains( docElem, elem ) ) {
			return box ? { top: box.top, left: box.left } : { top: 0, left: 0 };
		}

		var body = doc.body,
			win = getWindow( doc ),
			clientTop  = docElem.clientTop  || body.clientTop  || 0,
			clientLeft = docElem.clientLeft || body.clientLeft || 0,
			scrollTop  = win.pageYOffset || jQuery.support.boxModel &amp;&amp; docElem.scrollTop  || body.scrollTop,
			scrollLeft = win.pageXOffset || jQuery.support.boxModel &amp;&amp; docElem.scrollLeft || body.scrollLeft,
			top  = box.top  + scrollTop  - clientTop,
			left = box.left + scrollLeft - clientLeft;

		return { top: top, left: left };
	};

} else {
	getOffset = function( elem, doc, docElem ) {
		var computedStyle,
			offsetParent = elem.offsetParent,
			prevOffsetParent = elem,
			body = doc.body,
			defaultView = doc.defaultView,
			prevComputedStyle = defaultView ? defaultView.getComputedStyle( elem, null ) : elem.currentStyle,
			top = elem.offsetTop,
			left = elem.offsetLeft;

		while ( (elem = elem.parentNode) &amp;&amp; elem !== body &amp;&amp; elem !== docElem ) {
			if ( jQuery.support.fixedPosition &amp;&amp; prevComputedStyle.position === "fixed" ) {
				break;
			}

			computedStyle = defaultView ? defaultView.getComputedStyle(elem, null) : elem.currentStyle;
			top  -= elem.scrollTop;
			left -= elem.scrollLeft;

			if ( elem === offsetParent ) {
				top  += elem.offsetTop;
				left += elem.offsetLeft;

				if ( jQuery.support.doesNotAddBorder &amp;&amp; !(jQuery.support.doesAddBorderForTableAndCells &amp;&amp; rtable.test(elem.nodeName)) ) {
					top  += parseFloat( computedStyle.borderTopWidth  ) || 0;
					left += parseFloat( computedStyle.borderLeftWidth ) || 0;
				}

				prevOffsetParent = offsetParent;
				offsetParent = elem.offsetParent;
			}

			if ( jQuery.support.subtractsBorderForOverflowNotVisible &amp;&amp; computedStyle.overflow !== "visible" ) {
				top  += parseFloat( computedStyle.borderTopWidth  ) || 0;
				left += parseFloat( computedStyle.borderLeftWidth ) || 0;
			}

			prevComputedStyle = computedStyle;
		}

		if ( prevComputedStyle.position === "relative" || prevComputedStyle.position === "static" ) {
			top  += body.offsetTop;
			left += body.offsetLeft;
		}

		if ( jQuery.support.fixedPosition &amp;&amp; prevComputedStyle.position === "fixed" ) {
			top  += Math.max( docElem.scrollTop, body.scrollTop );
			left += Math.max( docElem.scrollLeft, body.scrollLeft );
		}

		return { top: top, left: left };
	};
}

jQuery.fn.offset = function( options ) {
	if ( arguments.length ) {
		return options === undefined ?
			this :
			this.each(function( i ) {
				jQuery.offset.setOffset( this, options, i );
			});
	}

	var elem = this[0],
		doc = elem &amp;&amp; elem.ownerDocument;

	if ( !doc ) {
		return null;
	}

	if ( elem === doc.body ) {
		return jQuery.offset.bodyOffset( elem );
	}

	return getOffset( elem, doc, doc.documentElement );
};

jQuery.offset = {

	bodyOffset: function( body ) {
		var top = body.offsetTop,
			left = body.offsetLeft;

		if ( jQuery.support.doesNotIncludeMarginInBodyOffset ) {
			top  += parseFloat( jQuery.css(body, "marginTop") ) || 0;
			left += parseFloat( jQuery.css(body, "marginLeft") ) || 0;
		}

		return { top: top, left: left };
	},

	setOffset: function( elem, options, i ) {
		var position = jQuery.css( elem, "position" );

		// set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		var curElem = jQuery( elem ),
			curOffset = curElem.offset(),
			curCSSTop = jQuery.css( elem, "top" ),
			curCSSLeft = jQuery.css( elem, "left" ),
			calculatePosition = ( position === "absolute" || position === "fixed" ) &amp;&amp; jQuery.inArray("auto", [curCSSTop, curCSSLeft]) &gt; -1,
			props = {}, curPosition = {}, curTop, curLeft;

		// need to be able to calculate position if either top or left is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;
		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( jQuery.isFunction( options ) ) {
			options = options.call( elem, i, curOffset );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );
		} else {
			curElem.css( props );
		}
	}
};


jQuery.fn.extend({

	position: function() {
		if ( !this[0] ) {
			return null;
		}

		var elem = this[0],

		// Get *real* offsetParent
		offsetParent = this.offsetParent(),

		// Get correct offsets
		offset       = this.offset(),
		parentOffset = rroot.test(offsetParent[0].nodeName) ? { top: 0, left: 0 } : offsetParent.offset();

		// Subtract element margins
		// note: when an element has margin: auto the offsetLeft and marginLeft
		// are the same in Safari causing offset.left to incorrectly be 0
		offset.top  -= parseFloat( jQuery.css(elem, "marginTop") ) || 0;
		offset.left -= parseFloat( jQuery.css(elem, "marginLeft") ) || 0;

		// Add offsetParent borders
		parentOffset.top  += parseFloat( jQuery.css(offsetParent[0], "borderTopWidth") ) || 0;
		parentOffset.left += parseFloat( jQuery.css(offsetParent[0], "borderLeftWidth") ) || 0;

		// Subtract the two offsets
		return {
			top:  offset.top  - parentOffset.top,
			left: offset.left - parentOffset.left
		};
	},

	offsetParent: function() {
		return this.map(function() {
			var offsetParent = this.offsetParent || document.body;
			while ( offsetParent &amp;&amp; (!rroot.test(offsetParent.nodeName) &amp;&amp; jQuery.css(offsetParent, "position") === "static") ) {
				offsetParent = offsetParent.offsetParent;
			}
			return offsetParent;
		});
	}
});


// Create scrollLeft and scrollTop methods
jQuery.each( {scrollLeft: "pageXOffset", scrollTop: "pageYOffset"}, function( method, prop ) {
	var top = /Y/.test( prop );

	jQuery.fn[ method ] = function( val ) {
		return jQuery.access( this, function( elem, method, val ) {
			var win = getWindow( elem );

			if ( val === undefined ) {
				return win ? (prop in win) ? win[ prop ] :
					jQuery.support.boxModel &amp;&amp; win.document.documentElement[ method ] ||
						win.document.body[ method ] :
					elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : jQuery( win ).scrollLeft(),
					 top ? val : jQuery( win ).scrollTop()
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length, null );
	};
});

function getWindow( elem ) {
	return jQuery.isWindow( elem ) ?
		elem :
		elem.nodeType === 9 ?
			elem.defaultView || elem.parentWindow :
			false;
}




// Create width, height, innerHeight, innerWidth, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	var clientProp = "client" + name,
		scrollProp = "scroll" + name,
		offsetProp = "offset" + name;

	// innerHeight and innerWidth
	jQuery.fn[ "inner" + name ] = function() {
		var elem = this[0];
		return elem ?
			elem.style ?
			parseFloat( jQuery.css( elem, type, "padding" ) ) :
			this[ type ]() :
			null;
	};

	// outerHeight and outerWidth
	jQuery.fn[ "outer" + name ] = function( margin ) {
		var elem = this[0];
		return elem ?
			elem.style ?
			parseFloat( jQuery.css( elem, type, margin ? "margin" : "border" ) ) :
			this[ type ]() :
			null;
	};

	jQuery.fn[ type ] = function( value ) {
		return jQuery.access( this, function( elem, type, value ) {
			var doc, docElemProp, orig, ret;

			if ( jQuery.isWindow( elem ) ) {
				// 3rd condition allows Nokia support, as it supports the docElem prop but not CSS1Compat
				doc = elem.document;
				docElemProp = doc.documentElement[ clientProp ];
				return jQuery.support.boxModel &amp;&amp; docElemProp ||
					doc.body &amp;&amp; doc.body[ clientProp ] || docElemProp;
			}

			// Get document width or height
			if ( elem.nodeType === 9 ) {
				// Either scroll[Width/Height] or offset[Width/Height], whichever is greater
				doc = elem.documentElement;

				// when a window &gt; document, IE6 reports a offset[Width/Height] &gt; client[Width/Height]
				// so we can't use max, as it'll choose the incorrect offset[Width/Height]
				// instead we use the correct client[Width/Height]
				// support:IE6
				if ( doc[ clientProp ] &gt;= doc[ scrollProp ] ) {
					return doc[ clientProp ];
				}

				return Math.max(
					elem.body[ scrollProp ], doc[ scrollProp ],
					elem.body[ offsetProp ], doc[ offsetProp ]
				);
			}

			// Get width or height on the element
			if ( value === undefined ) {
				orig = jQuery.css( elem, type );
				ret = parseFloat( orig );
				return jQuery.isNumeric( ret ) ? ret : orig;
			}

			// Set the width or height on the element
			jQuery( elem ).css( type, value );
		}, type, value, arguments.length, null );
	};
});




// Expose jQuery to the global object
window.jQuerySvg = window.$vg = jQuery;

// Expose jQuery as an AMD module, but only for AMD loaders that
// understand the issues with loading multiple versions of jQuery
// in a page that all might call define(). The loader will indicate
// they have special allowances for multiple jQuery versions by
// specifying define.amd.jQuery = true. Register as a named module,
// since jQuery can be concatenated with other files that may use define,
// but not use a proper concatenation script that understands anonymous
// AMD modules. A named AMD is safest and most robust way to register.
// Lowercase jquery is used because AMD module names are derived from
// file names, and jQuery is normally delivered in a lowercase file name.
// Do this after creating the global so that if an AMD module wants to call
// noConflict to hide this version of jQuery, it will work.
if ( typeof define === "function" &amp;&amp; define.amd &amp;&amp; define.amd.jQuery ) {
	define( "jquery", [], function () { return jQuery; } );
}



})( window );
</script><script type="text/javascript">// Generated by CoffeeScript 1.6.3
(function() {
  var get_translation, init, padding, r_translation, tooltip_timeout;

  padding = 5;

  tooltip_timeout = 0;

  r_translation = /translate\((\d+)[ ,]+(\d+)\)/;

  get_translation = function($elt) {
    return (r_translation.exec($elt.attr('transform')) || []).slice(1);
  };

  init = function(ctx) {
    var tooltip, untooltip;
    $vg('.text-overlay .series', ctx).hide();
    $vg('.reactive', ctx).hover((function() {
      return $vg(this).addClass('active');
    }), (function() {
      return $vg(this).removeClass('active');
    }));
    $vg('.activate-serie', ctx).hover((function() {
      var num;
      num = this.id.replace('activate-serie-', '');
      $vg('.text-overlay .serie-' + num, ctx).show();
      return $vg('.serie-' + num + ' .reactive', ctx).addClass('active');
    }), function() {
      var num;
      num = this.id.replace('activate-serie-', '');
      $vg('.text-overlay .serie-' + num, ctx).hide();
      return $vg('.serie-' + num + ' .reactive', ctx).removeClass('active');
    });
    $vg('.tooltip-trigger', ctx).hover((function() {
      return tooltip($vg(this));
    }), (function() {
      return untooltip();
    }));
    tooltip = function($elt) {
      var $label, $rect, $text, $tooltip, $value, current_x, current_y, h, target, w, x, x_elt, xlink, y, y_elt, _ref;
      clearTimeout(tooltip_timeout);
      $tooltip = $vg('#tooltip', ctx).css({
        opacity: 1
      });
      $text = $tooltip.find('text');
      $label = $tooltip.find('tspan.label');
      $value = $tooltip.find('tspan.value');
      $rect = $tooltip.find('rect');
      if ($elt.siblings('.tooltip').size()) {
        $label.text($elt.siblings('.tooltip').text());
        $value.text('');
      } else {
        $label.text($elt.siblings('.label').text());
        $value.text($elt.siblings('.value').text());
      }
      xlink = $elt.siblings('.xlink').text() || null;
      target = $elt.parent().attr('target');
      if (xlink) {
        $tooltip.find('a').attr('href', xlink).attr('target', target);
      }
      $text.attr('x', padding);
      $text.attr('y', padding + this.config.tooltip_font_size);
      $value.attr('x', padding);
      $value.attr('dy', $label.text() ? this.config.tooltip_font_size + padding : 0);
      w = $text.width() + 2 * padding;
      h = $text.height() + 2 * padding;
      $rect.attr('width', w);
      $rect.attr('height', h);
      x_elt = $elt.siblings('.x');
      y_elt = $elt.siblings('.y');
      x = parseInt(x_elt.text());
      if (x_elt.hasClass('centered')) {
        x -= w / 2;
      } else if (x_elt.hasClass('left')) {
        x -= w;
      }
      y = parseInt(y_elt.text());
      if (y_elt.hasClass('centered')) {
        y -= h / 2;
      } else if (y_elt.hasClass('top')) {
        y -= h;
      }
      _ref = get_translation($tooltip), current_x = _ref[0], current_y = _ref[1];
      if (current_x === x &amp;&amp; current_y === y) {
        return;
      }
      return $tooltip.attr('transform', "translate(" + x + " " + y + ")");
    };
    return untooltip = function() {
      return tooltip_timeout = setTimeout((function() {
        return $vg('#tooltip', ctx).css({
          opacity: 0
        });
      }), 1000);
    };
  };

  this.init_svg = function(ctx) {
    return init($vg(ctx));
  };

  $vg(function() {
    var $charts;
    $charts = $vg('.pygal-chart');
    if ($charts.size()) {
      return $charts.each(function() {
        return init_svg(this);
      });
    } else {
      return init();
    }
  });

}).call(this);
</script></defs><title>CPU0</title><g class="graph line-graph vertical"><rect class="background" height="600" width="1000" x="0" y="0"/><g class="plot" transform="translate(130, 46)"><rect class="background" height="499.316955788" width="840.583893384" x="0" y="0"/><g class="axis x"><g class="guides"><path class="major line" d="M16.165075 0.000000 v499.316956"/><text class="major" x="16.1650748728" y="513.316955788" transform="rotate(40 16.165075 513.316956)">00:10:01</text></g><g class="guides"><path class="major guide line" d="M50.316642 0.000000 v499.316956"/><text class="major" x="50.3166415054" y="513.316955788" transform="rotate(40 50.316642 513.316956)">01:10:01</text></g><g class="guides"><path class="major guide line" d="M84.468208 0.000000 v499.316956"/><text class="major" x="84.468208138" y="513.316955788" transform="rotate(40 84.468208 513.316956)">02:10:01</text></g><g class="guides"><path class="major guide line" d="M118.619775 0.000000 v499.316956"/><text class="major" x="118.619774771" y="513.316955788" transform="rotate(40 118.619775 513.316956)">03:10:01</text></g><g class="guides"><path class="major guide line" d="M152.771341 0.000000 v499.316956"/><text class="major" x="152.771341403" y="513.316955788" transform="rotate(40 152.771341 513.316956)">04:10:01</text></g><g class="guides"><path class="major guide line" d="M186.922908 0.000000 v499.316956"/><text class="major" x="186.922908036" y="513.316955788" transform="rotate(40 186.922908 513.316956)">05:10:01</text></g><g class="guides"><path class="major guide line" d="M221.074475 0.000000 v499.316956"/><text class="major" x="221.074474668" y="513.316955788" transform="rotate(40 221.074475 513.316956)">06:10:01</text></g><g class="guides"><path class="major guide line" d="M255.226041 0.000000 v499.316956"/><text class="major" x="255.226041301" y="513.316955788" transform="rotate(40 255.226041 513.316956)">07:10:01</text></g><g class="guides"><path class="major guide line" d="M289.377608 0.000000 v499.316956"/><text class="major" x="289.377607934" y="513.316955788" transform="rotate(40 289.377608 513.316956)">08:10:01</text></g><g class="guides"><path class="major guide line" d="M323.529175 0.000000 v499.316956"/><text class="major" x="323.529174566" y="513.316955788" transform="rotate(40 323.529175 513.316956)">09:10:01</text></g><g class="guides"><path class="major guide line" d="M357.680741 0.000000 v499.316956"/><text class="major" x="357.680741199" y="513.316955788" transform="rotate(40 357.680741 513.316956)">10:10:01</text></g><g class="guides"><path class="major guide line" d="M391.832308 0.000000 v499.316956"/><text class="major" x="391.832307832" y="513.316955788" transform="rotate(40 391.832308 513.316956)">11:10:01</text></g><g class="guides"><path class="major guide line" d="M425.983874 0.000000 v499.316956"/><text class="major" x="425.983874464" y="513.316955788" transform="rotate(40 425.983874 513.316956)">12:10:01</text></g><g class="guides"><path class="major guide line" d="M460.135441 0.000000 v499.316956"/><text class="major" x="460.135441097" y="513.316955788" transform="rotate(40 460.135441 513.316956)">13:10:01</text></g><g class="guides"><path class="major guide line" d="M494.287008 0.000000 v499.316956"/><text class="major" x="494.287007729" y="513.316955788" transform="rotate(40 494.287008 513.316956)">14:10:01</text></g><g class="guides"><path class="major guide line" d="M528.438574 0.000000 v499.316956"/><text class="major" x="528.438574362" y="513.316955788" transform="rotate(40 528.438574 513.316956)">15:10:01</text></g><g class="guides"><path class="major guide line" d="M562.590141 0.000000 v499.316956"/><text class="major" x="562.590140995" y="513.316955788" transform="rotate(40 562.590141 513.316956)">16:10:01</text></g><g class="guides"><path class="major guide line" d="M596.741708 0.000000 v499.316956"/><text class="major" x="596.741707627" y="513.316955788" transform="rotate(40 596.741708 513.316956)">17:10:02</text></g><g class="guides"><path class="major guide line" d="M630.893274 0.000000 v499.316956"/><text class="major" x="630.89327426" y="513.316955788" transform="rotate(40 630.893274 513.316956)">18:10:01</text></g><g class="guides"><path class="major guide line" d="M665.044841 0.000000 v499.316956"/><text class="major" x="665.044840893" y="513.316955788" transform="rotate(40 665.044841 513.316956)">19:10:01</text></g><g class="guides"><path class="major guide line" d="M699.196408 0.000000 v499.316956"/><text class="major" x="699.196407525" y="513.316955788" transform="rotate(40 699.196408 513.316956)">20:10:01</text></g><g class="guides"><path class="major guide line" d="M733.347974 0.000000 v499.316956"/><text class="major" x="733.347974158" y="513.316955788" transform="rotate(40 733.347974 513.316956)">21:10:01</text></g><g class="guides"><path class="major guide line" d="M767.499541 0.000000 v499.316956"/><text class="major" x="767.49954079" y="513.316955788" transform="rotate(40 767.499541 513.316956)">22:10:01</text></g><g class="guides"><path class="major guide line" d="M801.651107 0.000000 v499.316956"/><text class="major" x="801.651107423" y="513.316955788" transform="rotate(40 801.651107 513.316956)">23:10:01</text></g></g><g class="axis y"><path class="line" d="M0.000000 499.316956 h840.583893"/><g class="logarithmic guides"><path class="guide line" d="M0.000000 491.574409 h840.583893"/><text class="" x="-5" y="494.374409272">0.3</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 459.290033 h840.583893"/><text class="" x="-5" y="462.090032931">0.4</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 434.248325 h840.583893"/><text class="" x="-5" y="437.048325163">0.5</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 413.787760 h840.583893"/><text class="" x="-5" y="416.587760122">0.6</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 396.488599 h840.583893"/><text class="" x="-5" y="399.28859887">0.7</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 381.503384 h840.583893"/><text class="" x="-5" y="384.303383782">0.8</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 368.285487 h840.583893"/><text class="" x="-5" y="371.085487313">0.9</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 356.461676 h840.583893"/><text class="major" x="-5" y="359.261676014">1.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 278.675027 h840.583893"/><text class="" x="-5" y="281.475026864">2.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 233.172754 h840.583893"/><text class="" x="-5" y="235.972754055">3.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 200.888378 h840.583893"/><text class="" x="-5" y="203.688377715">4.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 175.846670 h840.583893"/><text class="" x="-5" y="178.646669947">5.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 155.386105 h840.583893"/><text class="" x="-5" y="158.186104906">6.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 138.086944 h840.583893"/><text class="" x="-5" y="140.886943654">7.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 123.101729 h840.583893"/><text class="" x="-5" y="125.901728566">8.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 109.883832 h840.583893"/><text class="" x="-5" y="112.683832097">9.0</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 98.060021 h840.583893"/><text class="major" x="-5" y="100.860020797">10.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 20.273372 h840.583893"/><text class="" x="-5" y="23.073371648">20.0</text></g></g><g class="series serie-0 color-0"><path class="line reactive" d="M16.165075 499.316956 L16.165075 123.101729 21.857003 128.122099 27.548930 130.644057 33.240858 133.223996 38.932786 132.611594 44.624714 121.154823 50.316642 136.337161 56.008569 139.214817 61.700497 130.194867 67.392425 135.237703 73.084353 131.396725 78.776280 132.002515 84.468208 132.459014 90.160136 134.303807 95.852064 132.002515 101.543991 128.857986 107.235919 127.245356 112.927847 131.850761 118.619775 132.002515 124.311703 136.811675 130.003630 127.682871 135.695558 138.247376 141.387486 129.153697 147.079414 135.864645 152.771341 132.917377 158.463269 128.415875 164.155197 122.402521 169.847125 127.682871 175.539052 121.154823 181.230980 126.086921 186.922908 129.896403 192.614836 129.005744 198.306764 120.604712 203.998691 125.942954 209.690619 118.027890 215.382547 127.682871 221.074475 123.242094 226.766402 133.377620 232.458330 123.805320 238.150258 125.942954 243.842186 127.536843 249.534114 126.954625 255.226041 130.794187 260.917969 122.961538 266.609897 125.512157 272.301825 123.805320 277.993752 127.536843 283.685680 132.611594 289.377608 121.292773 295.069536 134.458918 300.761463 123.805320 306.453391 132.154475 312.145319 197.299163 317.837247 332.321358 323.529175 131.850761 329.221102 141.010414 334.913030 136.970294 340.604958 104.171225 346.296886 80.825005 351.988813 101.362593 357.680741 142.835208 363.372669 129.005744 369.064597 189.937678 374.756525 31.848076 380.448452 30.303565 386.140380 43.644243 391.832308 79.390528 397.524236 70.356787 403.216163 61.996383 408.908091 116.827639 414.600019 76.482806 420.291947 83.552767 425.983874 128.563052 431.675802 220.454736 437.367730 162.529065 443.059658 170.585281 448.751586 174.730020 454.443513 206.349701 460.135441 278.675027 465.827369 253.185325 471.519297 228.771311 477.211224 268.490353 482.903152 238.535938 488.595080 122.402521 494.287008 57.999436 499.978936 191.992150 505.670863 159.578261 511.362791 99.870099 517.054719 177.885075 522.746647 102.758123 528.438574 131.245787 534.130502 108.520539 539.822430 77.319250 545.514358 102.992408 551.206285 121.985078 556.898213 155.386105 562.590141 153.715269 568.282069 108.028874 573.973997 85.241848 579.665924 96.168274 585.357852 72.391621 591.049780 36.030762 596.741708 48.950420 602.433635 0.000000 608.125563 119.105665 613.817491 162.728571 619.509419 162.928433 625.201346 170.157766 630.893274 176.748060 636.585202 29.813778 642.277130 53.685621 647.969058 184.474446 653.660985 187.920142 659.352913 253.633319 665.044841 237.364911 670.736769 214.915785 676.428696 237.364911 682.120624 236.590960 687.812552 236.977268 693.504480 230.219362 699.196408 242.530039 704.888335 199.217542 710.580263 259.153567 716.272191 258.214462 721.964119 279.237546 727.656046 226.987133 733.347974 256.820377 739.039902 230.950455 744.731830 227.341707 750.423757 257.747840 756.115685 265.457130 761.807613 276.452728 767.499541 235.822310 773.191469 235.058889 778.883396 280.371116 784.575324 268.490353 790.267252 272.135949 795.959180 336.001111 801.651107 256.359504 807.343035 220.121236 813.034963 234.679118 818.726891 293.020796 824.418819 244.581695 824.418819 499.316956"/></g><g class="series serie-1 color-1"><path class="line reactive" d="M16.165075 499.316956 L16.165075 279.237546 21.857003 287.424059 27.548930 272.135949 33.240858 280.371116 38.932786 277.558377 44.624714 276.452728 50.316642 278.675027 56.008569 272.666553 61.700497 275.357867 67.392425 275.357867 73.084353 274.814416 78.776280 283.256170 84.468208 283.842189 90.160136 282.673195 95.852064 277.004191 101.543991 272.135949 107.235919 274.273584 112.927847 273.735346 118.619775 277.558377 124.311703 278.115313 130.003630 277.004191 135.695558 260.100597 141.387486 282.093233 147.079414 284.431284 152.771341 280.942223 158.463269 274.273584 164.155197 272.135949 169.847125 277.558377 175.539052 274.273584 181.230980 273.735346 186.922908 273.199677 192.614836 272.666553 198.306764 280.371116 203.998691 274.814416 209.690619 274.814416 215.382547 277.558377 221.074475 270.559028 226.766402 277.558377 232.458330 271.082210 238.150258 270.038273 243.842186 277.558377 249.534114 271.607843 255.226041 270.038273 260.917969 276.452728 266.609897 274.273584 272.301825 274.814416 277.993752 269.003958 283.685680 273.735346 289.377608 278.115313 295.069536 279.237546 300.761463 270.559028 306.453391 273.199677 312.145319 281.516252 317.837247 343.743657 323.529175 298.911371 329.221102 275.357867 334.913030 265.457130 340.604958 247.944415 346.296886 205.469520 351.988813 248.801079 357.680741 303.017527 363.372669 237.753897 369.064597 305.840882 374.756525 151.887502 380.448452 172.747629 386.140380 197.571218 391.832308 209.032436 397.524236 219.788725 403.216163 198.117313 408.908091 232.056104 414.600019 216.516703 420.291947 206.055540 425.983874 153.531147 431.675802 321.132855 437.367730 317.903210 443.059658 314.763917 448.751586 332.321358 454.443513 363.405483 460.135441 347.824922 465.827369 354.239377 471.519297 372.090001 477.211224 372.090001 482.903152 320.316689 488.595080 238.929011 494.287008 175.398676 499.978936 334.146153 505.670863 318.701950 511.362791 250.534278 517.054719 319.506415 522.746647 270.559028 528.438574 288.032312 534.130502 232.427087 539.822430 226.633677 545.514358 271.082210 551.206285 302.322649 556.898213 279.802900 562.590141 303.017527 568.282069 277.558377 573.973997 228.412199 579.665924 242.937381 585.357852 211.472150 591.049780 217.814086 596.741708 212.712189 602.433635 150.625550 608.125563 308.006011 613.817491 354.239377 619.509419 334.146153 625.201346 337.887246 630.893274 342.746118 636.585202 179.033728 642.277130 210.551030 647.969058 369.539382 653.660985 370.807445 659.352913 359.879882 665.044841 373.387385 670.736769 381.503384 676.428696 356.461676 682.120624 377.372031 687.812552 376.028034 693.504480 368.285487 699.196408 374.699942 704.888335 380.109299 710.580263 380.109299 716.272191 376.028034 721.964119 372.090001 727.656046 380.109299 733.347974 373.387385 739.039902 378.732319 744.731830 382.915005 750.423757 370.807445 756.115685 374.699942 761.807613 368.285487 767.499541 372.090001 773.191469 376.028034 778.883396 370.807445 784.575324 373.387385 790.267252 365.818961 795.959180 364.605734 801.651107 369.539382 807.343035 374.699942 813.034963 372.090001 818.726891 368.285487 824.418819 372.090001 824.418819 499.316956"/></g><g class="series serie-2 color-2"><path class="line reactive" d="M16.165075 499.316956 L16.165075 459.290033 21.857003 468.039065 27.548930 456.518968 33.240858 474.275248 38.932786 477.528299 44.624714 480.878471 50.316642 468.039065 56.008569 484.331741 61.700497 477.528299 67.392425 487.894656 73.084353 477.528299 78.776280 477.528299 84.468208 487.894656 90.160136 480.878471 95.852064 487.894656 101.543991 468.039065 107.235919 487.894656 112.927847 495.378923 118.619775 491.574409 124.311703 499.316956 130.003630 471.113844 135.695558 212.090457 141.387486 480.878471 147.079414 491.574409 152.771341 487.894656 158.463269 487.894656 164.155197 456.518968 169.847125 471.113844 175.539052 474.275248 181.230980 495.378923 186.922908 471.113844 192.614836 487.894656 198.306764 484.331741 203.998691 474.275248 209.690619 468.039065 215.382547 471.113844 221.074475 468.039065 226.766402 468.039065 232.458330 451.174034 238.150258 480.878471 243.842186 448.594095 249.534114 480.878471 255.226041 477.528299 260.917969 484.331741 266.609897 456.518968 272.301825 477.528299 277.993752 484.331741 283.685680 487.894656 289.377608 474.275248 295.069536 487.894656 300.761463 468.039065 306.453391 484.331741 312.145319 474.275248 317.837247 432.026027 323.529175 377.372031 329.221102 273.199677 334.913030 377.372031 340.604958 394.896765 346.296886 312.465767 351.988813 330.525761 357.680741 382.915005 363.372669 403.091822 369.064597 465.046290 374.756525 305.128356 380.448452 269.519924 386.140380 161.339417 391.832308 311.710058 397.524236 295.600735 403.216163 298.241385 408.908091 350.986326 414.600019 291.124034 420.291947 245.412976 425.983874 175.846670 431.675802 385.792661 437.367730 390.252416 443.059658 391.779275 448.751586 378.732319 454.443513 432.026027 460.135441 285.618834 465.827369 446.072136 471.519297 448.594095 477.211224 448.594095 482.903152 429.846882 488.595080 359.879882 494.287008 278.675027 499.978936 417.592274 505.670863 382.915005 511.362791 344.750143 517.054719 434.248325 522.746647 373.387385 528.438574 378.732319 534.130502 328.758442 539.822430 292.384974 545.514358 326.158579 551.206285 337.887246 556.898213 403.091822 562.590141 380.109299 568.282069 336.940216 573.973997 298.241385 579.665924 346.790607 585.357852 317.110116 591.049780 309.472992 596.741708 135.081513 602.433635 78.821832 608.125563 235.439951 613.817491 398.103338 619.509419 411.932802 625.201346 399.741650 630.893274 429.846882 636.585202 131.850761 642.277130 176.522033 647.969058 432.026027 653.660985 451.174034 659.352913 462.131258 665.044841 434.248325 670.736769 465.046290 676.428696 174.952463 682.120624 462.131258 687.812552 462.131258 693.504480 468.039065 699.196408 468.039065 704.888335 477.528299 710.580263 448.594095 716.272191 465.046290 721.964119 459.290033 727.656046 456.518968 733.347974 453.814683 739.039902 459.290033 744.731830 456.518968 750.423757 453.814683 756.115685 462.131258 761.807613 446.072136 767.499541 446.072136 773.191469 451.174034 778.883396 448.594095 784.575324 451.174034 790.267252 446.072136 795.959180 446.072136 801.651107 419.544017 807.343035 381.503384 813.034963 421.530307 818.726891 456.518968 824.418819 468.039065 824.418819 499.316956"/></g></g><g class="titles"><text class="title" x="500.0" y="26">CPU0</text></g><g class="plot overlay" transform="translate(130, 46)"><g class="series serie-0 color-0"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="16.1650748728" cy="123.101728566" r="1.5"/><desc class="value">8.0</desc><desc class="x ">16.1650748728</desc><desc class="y ">123.101728566</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.8570026449" cy="128.122098519" r="1.5"/><desc class="value">7.65</desc><desc class="x ">21.8570026449</desc><desc class="y ">128.122098519</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.548930417" cy="130.644056629" r="1.5"/><desc class="value">7.48</desc><desc class="x ">27.548930417</desc><desc class="y ">130.644056629</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="33.2408581891" cy="133.223995838" r="1.5"/><desc class="value">7.31</desc><desc class="x ">33.2408581891</desc><desc class="y ">133.223995838</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.9327859612" cy="132.611593701" r="1.5"/><desc class="value">7.35</desc><desc class="x ">38.9327859612</desc><desc class="y ">132.611593701</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="44.6247137333" cy="121.15482251" r="1.5"/><desc class="value">8.14</desc><desc class="x ">44.6247137333</desc><desc class="y ">121.15482251</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="50.3166415054" cy="136.337161157" r="1.5"/><desc class="value">7.11</desc><desc class="x ">50.3166415054</desc><desc class="y ">136.337161157</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="56.0085692775" cy="139.214816595" r="1.5"/><desc class="value">6.93</desc><desc class="x ">56.0085692775</desc><desc class="y ">139.214816595</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="61.7004970496" cy="130.194866918" r="1.5"/><desc class="value">7.51</desc><desc class="x ">61.7004970496</desc><desc class="y ">130.194866918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="67.3924248217" cy="135.23770255" r="1.5"/><desc class="value">7.18</desc><desc class="x ">67.3924248217</desc><desc class="y ">135.23770255</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="73.0843525938" cy="131.396724863" r="1.5"/><desc class="value">7.43</desc><desc class="x ">73.0843525938</desc><desc class="y ">131.396724863</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="78.7762803659" cy="132.002515338" r="1.5"/><desc class="value">7.39</desc><desc class="x ">78.7762803659</desc><desc class="y ">132.002515338</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="84.468208138" cy="132.459013919" r="1.5"/><desc class="value">7.36</desc><desc class="x ">84.468208138</desc><desc class="y ">132.459013919</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="90.1601359101" cy="134.303807455" r="1.5"/><desc class="value">7.24</desc><desc class="x ">90.1601359101</desc><desc class="y ">134.303807455</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="95.8520636822" cy="132.002515338" r="1.5"/><desc class="value">7.39</desc><desc class="x ">95.8520636822</desc><desc class="y ">132.002515338</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="101.543991454" cy="128.857985831" r="1.5"/><desc class="value">7.6</desc><desc class="x ">101.543991454</desc><desc class="y ">128.857985831</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="107.235919226" cy="127.245356458" r="1.5"/><desc class="value">7.71</desc><desc class="x ">107.235919226</desc><desc class="y ">127.245356458</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="112.927846999" cy="131.850760869" r="1.5"/><desc class="value">7.4</desc><desc class="x ">112.927846999</desc><desc class="y ">131.850760869</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="118.619774771" cy="132.002515338" r="1.5"/><desc class="value">7.39</desc><desc class="x ">118.619774771</desc><desc class="y ">132.002515338</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="124.311702543" cy="136.811675238" r="1.5"/><desc class="value">7.08</desc><desc class="x ">124.311702543</desc><desc class="y ">136.811675238</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="130.003630315" cy="127.682871293" r="1.5"/><desc class="value">7.68</desc><desc class="x ">130.003630315</desc><desc class="y ">127.682871293</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="135.695558087" cy="138.247376009" r="1.5"/><desc class="value">6.99</desc><desc class="x ">135.695558087</desc><desc class="y ">138.247376009</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="141.387485859" cy="129.153697236" r="1.5"/><desc class="value">7.58</desc><desc class="x ">141.387485859</desc><desc class="y ">129.153697236</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="147.079413631" cy="135.864645036" r="1.5"/><desc class="value">7.14</desc><desc class="x ">147.079413631</desc><desc class="y ">135.864645036</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="152.771341403" cy="132.917377032" r="1.5"/><desc class="value">7.33</desc><desc class="x ">152.771341403</desc><desc class="y ">132.917377032</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="158.463269175" cy="128.415874637" r="1.5"/><desc class="value">7.63</desc><desc class="x ">158.463269175</desc><desc class="y ">128.415874637</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="164.155196947" cy="122.402521238" r="1.5"/><desc class="value">8.05</desc><desc class="x ">164.155196947</desc><desc class="y ">122.402521238</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="169.84712472" cy="127.682871293" r="1.5"/><desc class="value">7.68</desc><desc class="x ">169.84712472</desc><desc class="y ">127.682871293</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="175.539052492" cy="121.15482251" r="1.5"/><desc class="value">8.14</desc><desc class="x ">175.539052492</desc><desc class="y ">121.15482251</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="181.230980264" cy="126.086921264" r="1.5"/><desc class="value">7.79</desc><desc class="x ">181.230980264</desc><desc class="y ">126.086921264</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="186.922908036" cy="129.896402878" r="1.5"/><desc class="value">7.53</desc><desc class="x ">186.922908036</desc><desc class="y ">129.896402878</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="192.614835808" cy="129.005744132" r="1.5"/><desc class="value">7.59</desc><desc class="x ">192.614835808</desc><desc class="y ">129.005744132</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="198.30676358" cy="120.604711541" r="1.5"/><desc class="value">8.18</desc><desc class="x ">198.30676358</desc><desc class="y ">120.604711541</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="203.998691352" cy="125.942954069" r="1.5"/><desc class="value">7.8</desc><desc class="x ">203.998691352</desc><desc class="y ">125.942954069</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="209.690619124" cy="118.027890358" r="1.5"/><desc class="value">8.37</desc><desc class="x ">209.690619124</desc><desc class="y ">118.027890358</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="215.382546896" cy="127.682871293" r="1.5"/><desc class="value">7.68</desc><desc class="x ">215.382546896</desc><desc class="y ">127.682871293</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="221.074474668" cy="123.242094329" r="1.5"/><desc class="value">7.99</desc><desc class="x ">221.074474668</desc><desc class="y ">123.242094329</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="226.766402441" cy="133.377619972" r="1.5"/><desc class="value">7.3</desc><desc class="x ">226.766402441</desc><desc class="y ">133.377619972</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="232.458330213" cy="123.805319666" r="1.5"/><desc class="value">7.95</desc><desc class="x ">232.458330213</desc><desc class="y ">123.805319666</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="238.150257985" cy="125.942954069" r="1.5"/><desc class="value">7.8</desc><desc class="x ">238.150257985</desc><desc class="y ">125.942954069</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="243.842185757" cy="127.536843409" r="1.5"/><desc class="value">7.69</desc><desc class="x ">243.842185757</desc><desc class="y ">127.536843409</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="249.534113529" cy="126.954624656" r="1.5"/><desc class="value">7.73</desc><desc class="x ">249.534113529</desc><desc class="y ">126.954624656</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="255.226041301" cy="130.79418697" r="1.5"/><desc class="value">7.47</desc><desc class="x ">255.226041301</desc><desc class="y ">130.79418697</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="260.917969073" cy="122.96153815" r="1.5"/><desc class="value">8.01</desc><desc class="x ">260.917969073</desc><desc class="y ">122.96153815</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="266.609896845" cy="125.512157329" r="1.5"/><desc class="value">7.83</desc><desc class="x ">266.609896845</desc><desc class="y ">125.512157329</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="272.301824617" cy="123.805319666" r="1.5"/><desc class="value">7.95</desc><desc class="x ">272.301824617</desc><desc class="y ">123.805319666</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="277.99375239" cy="127.536843409" r="1.5"/><desc class="value">7.69</desc><desc class="x ">277.99375239</desc><desc class="y ">127.536843409</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="283.685680162" cy="132.611593701" r="1.5"/><desc class="value">7.35</desc><desc class="x ">283.685680162</desc><desc class="y ">132.611593701</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="289.377607934" cy="121.292772635" r="1.5"/><desc class="value">8.13</desc><desc class="x ">289.377607934</desc><desc class="y ">121.292772635</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="295.069535706" cy="134.458917933" r="1.5"/><desc class="value">7.23</desc><desc class="x ">295.069535706</desc><desc class="y ">134.458917933</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="300.761463478" cy="123.805319666" r="1.5"/><desc class="value">7.95</desc><desc class="x ">300.761463478</desc><desc class="y ">123.805319666</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="306.45339125" cy="132.154475298" r="1.5"/><desc class="value">7.38</desc><desc class="x ">306.45339125</desc><desc class="y ">132.154475298</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.145319022" cy="197.299163135" r="1.5"/><desc class="value">4.13</desc><desc class="x ">312.145319022</desc><desc class="y ">197.299163135</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="317.837246794" cy="332.321357935" r="1.5"/><desc class="value">1.24</desc><desc class="x top">317.837246794</desc><desc class="y top">332.321357935</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="323.529174566" cy="131.850760869" r="1.5"/><desc class="value">7.4</desc><desc class="x ">323.529174566</desc><desc class="y ">131.850760869</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.221102338" cy="141.010413509" r="1.5"/><desc class="value">6.82</desc><desc class="x ">329.221102338</desc><desc class="y ">141.010413509</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="334.913030111" cy="136.970293516" r="1.5"/><desc class="value">7.07</desc><desc class="x ">334.913030111</desc><desc class="y ">136.970293516</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.604957883" cy="104.171225369" r="1.5"/><desc class="value">9.47</desc><desc class="x ">340.604957883</desc><desc class="y ">104.171225369</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.296885655" cy="80.8250049675" r="1.5"/><desc class="value">11.66</desc><desc class="x ">346.296885655</desc><desc class="y ">80.8250049675</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="351.988813427" cy="101.362592944" r="1.5"/><desc class="value">9.71</desc><desc class="x ">351.988813427</desc><desc class="y ">101.362592944</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.680741199" cy="142.835208397" r="1.5"/><desc class="value">6.71</desc><desc class="x ">357.680741199</desc><desc class="y ">142.835208397</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.372668971" cy="129.005744132" r="1.5"/><desc class="value">7.59</desc><desc class="x ">363.372668971</desc><desc class="y ">129.005744132</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="369.064596743" cy="189.93767781" r="1.5"/><desc class="value">4.41</desc><desc class="x ">369.064596743</desc><desc class="y ">189.93767781</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.756524515" cy="31.84807649" r="1.5"/><desc class="value">18.04</desc><desc class="x ">374.756524515</desc><desc class="y ">31.84807649</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.448452287" cy="30.3035652818" r="1.5"/><desc class="value">18.29</desc><desc class="x ">380.448452287</desc><desc class="y ">30.3035652818</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="386.140380059" cy="43.6442433964" r="1.5"/><desc class="value">16.24</desc><desc class="x ">386.140380059</desc><desc class="y ">43.6442433964</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.832307832" cy="79.3905276648" r="1.5"/><desc class="value">11.81</desc><desc class="x ">391.832307832</desc><desc class="y ">79.3905276648</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="397.524235604" cy="70.3567871846" r="1.5"/><desc class="value">12.8</desc><desc class="x ">397.524235604</desc><desc class="y ">70.3567871846</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="403.216163376" cy="61.9963834084" r="1.5"/><desc class="value">13.79</desc><desc class="x ">403.216163376</desc><desc class="y ">61.9963834084</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.908091148" cy="116.827639206" r="1.5"/><desc class="value">8.46</desc><desc class="x ">408.908091148</desc><desc class="y ">116.827639206</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="414.60001892" cy="76.4828056183" r="1.5"/><desc class="value">12.12</desc><desc class="x ">414.60001892</desc><desc class="y ">76.4828056183</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="420.291946692" cy="83.5527673537" r="1.5"/><desc class="value">11.38</desc><desc class="x ">420.291946692</desc><desc class="y ">83.5527673537</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="425.983874464" cy="128.563051593" r="1.5"/><desc class="value">7.62</desc><desc class="x left">425.983874464</desc><desc class="y left">128.563051593</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="431.675802236" cy="220.454735531" r="1.5"/><desc class="value">3.36</desc><desc class="x left">431.675802236</desc><desc class="y left">220.454735531</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="437.367730008" cy="162.52906453" r="1.5"/><desc class="value">5.63</desc><desc class="x left">437.367730008</desc><desc class="y left">162.52906453</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="443.059657781" cy="170.585280808" r="1.5"/><desc class="value">5.24</desc><desc class="x left">443.059657781</desc><desc class="y left">170.585280808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="448.751585553" cy="174.730019808" r="1.5"/><desc class="value">5.05</desc><desc class="x left">448.751585553</desc><desc class="y left">174.730019808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="454.443513325" cy="206.349700743" r="1.5"/><desc class="value">3.81</desc><desc class="x left">454.443513325</desc><desc class="y left">206.349700743</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="460.135441097" cy="278.675026864" r="1.5"/><desc class="value">2.0</desc><desc class="x left top">460.135441097</desc><desc class="y left top">278.675026864</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="465.827368869" cy="253.185324837" r="1.5"/><desc class="value">2.51</desc><desc class="x left top">465.827368869</desc><desc class="y left top">253.185324837</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="471.519296641" cy="228.771310987" r="1.5"/><desc class="value">3.12</desc><desc class="x left">471.519296641</desc><desc class="y left">228.771310987</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="477.211224413" cy="268.49035323" r="1.5"/><desc class="value">2.19</desc><desc class="x left top">477.211224413</desc><desc class="y left top">268.49035323</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="482.903152185" cy="238.535937669" r="1.5"/><desc class="value">2.86</desc><desc class="x left">482.903152185</desc><desc class="y left">238.535937669</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="488.595079957" cy="122.402521238" r="1.5"/><desc class="value">8.05</desc><desc class="x left">488.595079957</desc><desc class="y left">122.402521238</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="494.287007729" cy="57.9994362659" r="1.5"/><desc class="value">14.29</desc><desc class="x left">494.287007729</desc><desc class="y left">57.9994362659</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="499.978935502" cy="191.992150071" r="1.5"/><desc class="value">4.33</desc><desc class="x left">499.978935502</desc><desc class="y left">191.992150071</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="505.670863274" cy="159.57826141" r="1.5"/><desc class="value">5.78</desc><desc class="x left">505.670863274</desc><desc class="y left">159.57826141</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="511.362791046" cy="99.8700989573" r="1.5"/><desc class="value">9.84</desc><desc class="x left">511.362791046</desc><desc class="y left">99.8700989573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="517.054718818" cy="177.88507456" r="1.5"/><desc class="value">4.91</desc><desc class="x left">517.054718818</desc><desc class="y left">177.88507456</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="522.74664659" cy="102.758122799" r="1.5"/><desc class="value">9.59</desc><desc class="x left">522.74664659</desc><desc class="y left">102.758122799</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="528.438574362" cy="131.245786827" r="1.5"/><desc class="value">7.44</desc><desc class="x left">528.438574362</desc><desc class="y left">131.245786827</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="534.130502134" cy="108.520539193" r="1.5"/><desc class="value">9.11</desc><desc class="x left">534.130502134</desc><desc class="y left">108.520539193</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="539.822429906" cy="77.3192498357" r="1.5"/><desc class="value">12.03</desc><desc class="x left">539.822429906</desc><desc class="y left">77.3192498357</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="545.514357678" cy="102.992407671" r="1.5"/><desc class="value">9.57</desc><desc class="x left">545.514357678</desc><desc class="y left">102.992407671</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="551.20628545" cy="121.985078427" r="1.5"/><desc class="value">8.08</desc><desc class="x left">551.20628545</desc><desc class="y left">121.985078427</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="556.898213223" cy="155.386104906" r="1.5"/><desc class="value">6.0</desc><desc class="x left">556.898213223</desc><desc class="y left">155.386104906</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="562.590140995" cy="153.715268886" r="1.5"/><desc class="value">6.09</desc><desc class="x left">562.590140995</desc><desc class="y left">153.715268886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="568.282068767" cy="108.028873947" r="1.5"/><desc class="value">9.15</desc><desc class="x left">568.282068767</desc><desc class="y left">108.028873947</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="573.973996539" cy="85.2418483948" r="1.5"/><desc class="value">11.21</desc><desc class="x left">573.973996539</desc><desc class="y left">85.2418483948</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="579.665924311" cy="96.1682744443" r="1.5"/><desc class="value">10.17</desc><desc class="x left">579.665924311</desc><desc class="y left">96.1682744443</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="585.357852083" cy="72.3916206219" r="1.5"/><desc class="value">12.57</desc><desc class="x left">585.357852083</desc><desc class="y left">72.3916206219</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="591.049779855" cy="36.0307623491" r="1.5"/><desc class="value">17.38</desc><desc class="x left">591.049779855</desc><desc class="y left">36.0307623491</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="596.741707627" cy="48.9504198721" r="1.5"/><desc class="value">15.49</desc><desc class="x left">596.741707627</desc><desc class="y left">48.9504198721</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="602.433635399" cy="0.0" r="1.5"/><desc class="value">23.96</desc><desc class="x left">602.433635399</desc><desc class="y left">0.0</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="608.125563172" cy="119.105664913" r="1.5"/><desc class="value">8.29</desc><desc class="x left">608.125563172</desc><desc class="y left">119.105664913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="613.817490944" cy="162.728571095" r="1.5"/><desc class="value">5.62</desc><desc class="x left">613.817490944</desc><desc class="y left">162.728571095</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="619.509418716" cy="162.92843297" r="1.5"/><desc class="value">5.61</desc><desc class="x left">619.509418716</desc><desc class="y left">162.92843297</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="625.201346488" cy="170.157766337" r="1.5"/><desc class="value">5.26</desc><desc class="x left">625.201346488</desc><desc class="y left">170.157766337</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="630.89327426" cy="176.748059636" r="1.5"/><desc class="value">4.96</desc><desc class="x left">630.89327426</desc><desc class="y left">176.748059636</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="636.585202032" cy="29.8137776501" r="1.5"/><desc class="value">18.37</desc><desc class="x left">636.585202032</desc><desc class="y left">29.8137776501</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="642.277129804" cy="53.6856209289" r="1.5"/><desc class="value">14.85</desc><desc class="x left">642.277129804</desc><desc class="y left">53.6856209289</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="647.969057576" cy="184.474446254" r="1.5"/><desc class="value">4.63</desc><desc class="x left">647.969057576</desc><desc class="y left">184.474446254</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="653.660985348" cy="187.92014189" r="1.5"/><desc class="value">4.49</desc><desc class="x left">653.660985348</desc><desc class="y left">187.92014189</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="659.35291312" cy="253.633319096" r="1.5"/><desc class="value">2.5</desc><desc class="x left top">659.35291312</desc><desc class="y left top">253.633319096</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.044840893" cy="237.36491056" r="1.5"/><desc class="value">2.89</desc><desc class="x left">665.044840893</desc><desc class="y left">237.36491056</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="670.736768665" cy="214.915785456" r="1.5"/><desc class="value">3.53</desc><desc class="x left">670.736768665</desc><desc class="y left">214.915785456</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="676.428696437" cy="237.36491056" r="1.5"/><desc class="value">2.89</desc><desc class="x left">676.428696437</desc><desc class="y left">237.36491056</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.120624209" cy="236.590959817" r="1.5"/><desc class="value">2.91</desc><desc class="x left">682.120624209</desc><desc class="y left">236.590959817</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="687.812551981" cy="236.977267988" r="1.5"/><desc class="value">2.9</desc><desc class="x left">687.812551981</desc><desc class="y left">236.977267988</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="693.504479753" cy="230.219362213" r="1.5"/><desc class="value">3.08</desc><desc class="x left">693.504479753</desc><desc class="y left">230.219362213</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.196407525" cy="242.530039408" r="1.5"/><desc class="value">2.76</desc><desc class="x left">699.196407525</desc><desc class="y left">242.530039408</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="704.888335297" cy="199.217541695" r="1.5"/><desc class="value">4.06</desc><desc class="x left">704.888335297</desc><desc class="y left">199.217541695</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.580263069" cy="259.153566994" r="1.5"/><desc class="value">2.38</desc><desc class="x left top">710.580263069</desc><desc class="y left top">259.153566994</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="716.272190841" cy="258.214461824" r="1.5"/><desc class="value">2.4</desc><desc class="x left top">716.272190841</desc><desc class="y left top">258.214461824</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.964118614" cy="279.237546403" r="1.5"/><desc class="value">1.99</desc><desc class="x left top">721.964118614</desc><desc class="y left top">279.237546403</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.656046386" cy="226.987133295" r="1.5"/><desc class="value">3.17</desc><desc class="x left">727.656046386</desc><desc class="y left">226.987133295</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.347974158" cy="256.820376654" r="1.5"/><desc class="value">2.43</desc><desc class="x left top">733.347974158</desc><desc class="y left top">256.820376654</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="739.03990193" cy="230.950455437" r="1.5"/><desc class="value">3.06</desc><desc class="x left">739.03990193</desc><desc class="y left">230.950455437</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.731829702" cy="227.341706775" r="1.5"/><desc class="value">3.16</desc><desc class="x left">744.731829702</desc><desc class="y left">227.341706775</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="750.423757474" cy="257.747839892" r="1.5"/><desc class="value">2.41</desc><desc class="x left top">750.423757474</desc><desc class="y left top">257.747839892</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="756.115685246" cy="265.457130396" r="1.5"/><desc class="value">2.25</desc><desc class="x left top">756.115685246</desc><desc class="y left top">265.457130396</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="761.807613018" cy="276.452728246" r="1.5"/><desc class="value">2.04</desc><desc class="x left top">761.807613018</desc><desc class="y left top">276.452728246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="767.49954079" cy="235.822310147" r="1.5"/><desc class="value">2.93</desc><desc class="x left">767.49954079</desc><desc class="y left">235.822310147</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="773.191468563" cy="235.058889428" r="1.5"/><desc class="value">2.95</desc><desc class="x left">773.191468563</desc><desc class="y left">235.058889428</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="778.883396335" cy="280.371115768" r="1.5"/><desc class="value">1.97</desc><desc class="x left top">778.883396335</desc><desc class="y left top">280.371115768</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="784.575324107" cy="268.49035323" r="1.5"/><desc class="value">2.19</desc><desc class="x left top">784.575324107</desc><desc class="y left top">268.49035323</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="790.267251879" cy="272.135949393" r="1.5"/><desc class="value">2.12</desc><desc class="x left top">790.267251879</desc><desc class="y left top">272.135949393</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="795.959179651" cy="336.001110973" r="1.5"/><desc class="value">1.2</desc><desc class="x left top">795.959179651</desc><desc class="y left top">336.001110973</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="801.651107423" cy="256.359503674" r="1.5"/><desc class="value">2.44</desc><desc class="x left top">801.651107423</desc><desc class="y left top">256.359503674</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="807.343035195" cy="220.121236287" r="1.5"/><desc class="value">3.37</desc><desc class="x left">807.343035195</desc><desc class="y left">220.121236287</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="813.034962967" cy="234.679117786" r="1.5"/><desc class="value">2.96</desc><desc class="x left">813.034962967</desc><desc class="y left">234.679117786</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="818.726890739" cy="293.020796274" r="1.5"/><desc class="value">1.76</desc><desc class="x left top">818.726890739</desc><desc class="y left top">293.020796274</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="824.418818511" cy="244.581694594" r="1.5"/><desc class="value">2.71</desc><desc class="x left">824.418818511</desc><desc class="y left">244.581694594</desc></g></g><g class="series serie-1 color-1"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="16.1650748728" cy="279.237546403" r="1.5"/><desc class="value">1.99</desc><desc class="x top">16.1650748728</desc><desc class="y top">279.237546403</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.8570026449" cy="287.424059167" r="1.5"/><desc class="value">1.85</desc><desc class="x top">21.8570026449</desc><desc class="y top">287.424059167</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.548930417" cy="272.135949393" r="1.5"/><desc class="value">2.12</desc><desc class="x top">27.548930417</desc><desc class="y top">272.135949393</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="33.2408581891" cy="280.371115768" r="1.5"/><desc class="value">1.97</desc><desc class="x top">33.2408581891</desc><desc class="y top">280.371115768</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.9327859612" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">38.9327859612</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="44.6247137333" cy="276.452728246" r="1.5"/><desc class="value">2.04</desc><desc class="x top">44.6247137333</desc><desc class="y top">276.452728246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="50.3166415054" cy="278.675026864" r="1.5"/><desc class="value">2.0</desc><desc class="x top">50.3166415054</desc><desc class="y top">278.675026864</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="56.0085692775" cy="272.666552807" r="1.5"/><desc class="value">2.11</desc><desc class="x top">56.0085692775</desc><desc class="y top">272.666552807</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="61.7004970496" cy="275.357866752" r="1.5"/><desc class="value">2.06</desc><desc class="x top">61.7004970496</desc><desc class="y top">275.357866752</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="67.3924248217" cy="275.357866752" r="1.5"/><desc class="value">2.06</desc><desc class="x top">67.3924248217</desc><desc class="y top">275.357866752</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="73.0843525938" cy="274.814415748" r="1.5"/><desc class="value">2.07</desc><desc class="x top">73.0843525938</desc><desc class="y top">274.814415748</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="78.7762803659" cy="283.256169592" r="1.5"/><desc class="value">1.92</desc><desc class="x top">78.7762803659</desc><desc class="y top">283.256169592</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="84.468208138" cy="283.842188746" r="1.5"/><desc class="value">1.91</desc><desc class="x top">84.468208138</desc><desc class="y top">283.842188746</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="90.1601359101" cy="282.673194707" r="1.5"/><desc class="value">1.93</desc><desc class="x top">90.1601359101</desc><desc class="y top">282.673194707</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="95.8520636822" cy="277.004190844" r="1.5"/><desc class="value">2.03</desc><desc class="x top">95.8520636822</desc><desc class="y top">277.004190844</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="101.543991454" cy="272.135949393" r="1.5"/><desc class="value">2.12</desc><desc class="x top">101.543991454</desc><desc class="y top">272.135949393</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="107.235919226" cy="274.273583796" r="1.5"/><desc class="value">2.08</desc><desc class="x top">107.235919226</desc><desc class="y top">274.273583796</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="112.927846999" cy="273.735345771" r="1.5"/><desc class="value">2.09</desc><desc class="x top">112.927846999</desc><desc class="y top">273.735345771</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="118.619774771" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">118.619774771</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="124.311702543" cy="278.115312921" r="1.5"/><desc class="value">2.01</desc><desc class="x top">124.311702543</desc><desc class="y top">278.115312921</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="130.003630315" cy="277.004190844" r="1.5"/><desc class="value">2.03</desc><desc class="x top">130.003630315</desc><desc class="y top">277.004190844</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="135.695558087" cy="260.100597196" r="1.5"/><desc class="value">2.36</desc><desc class="x top">135.695558087</desc><desc class="y top">260.100597196</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="141.387485859" cy="282.093232626" r="1.5"/><desc class="value">1.94</desc><desc class="x top">141.387485859</desc><desc class="y top">282.093232626</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="147.079413631" cy="284.43128413" r="1.5"/><desc class="value">1.9</desc><desc class="x top">147.079413631</desc><desc class="y top">284.43128413</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="152.771341403" cy="280.942223428" r="1.5"/><desc class="value">1.96</desc><desc class="x top">152.771341403</desc><desc class="y top">280.942223428</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="158.463269175" cy="274.273583796" r="1.5"/><desc class="value">2.08</desc><desc class="x top">158.463269175</desc><desc class="y top">274.273583796</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="164.155196947" cy="272.135949393" r="1.5"/><desc class="value">2.12</desc><desc class="x top">164.155196947</desc><desc class="y top">272.135949393</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="169.84712472" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">169.84712472</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="175.539052492" cy="274.273583796" r="1.5"/><desc class="value">2.08</desc><desc class="x top">175.539052492</desc><desc class="y top">274.273583796</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="181.230980264" cy="273.735345771" r="1.5"/><desc class="value">2.09</desc><desc class="x top">181.230980264</desc><desc class="y top">273.735345771</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="186.922908036" cy="273.199676912" r="1.5"/><desc class="value">2.1</desc><desc class="x top">186.922908036</desc><desc class="y top">273.199676912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="192.614835808" cy="272.666552807" r="1.5"/><desc class="value">2.11</desc><desc class="x top">192.614835808</desc><desc class="y top">272.666552807</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="198.30676358" cy="280.371115768" r="1.5"/><desc class="value">1.97</desc><desc class="x top">198.30676358</desc><desc class="y top">280.371115768</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="203.998691352" cy="274.814415748" r="1.5"/><desc class="value">2.07</desc><desc class="x top">203.998691352</desc><desc class="y top">274.814415748</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="209.690619124" cy="274.814415748" r="1.5"/><desc class="value">2.07</desc><desc class="x top">209.690619124</desc><desc class="y top">274.814415748</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="215.382546896" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">215.382546896</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="221.074474668" cy="270.559027714" r="1.5"/><desc class="value">2.15</desc><desc class="x top">221.074474668</desc><desc class="y top">270.559027714</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="226.766402441" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">226.766402441</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="232.458330213" cy="271.082210074" r="1.5"/><desc class="value">2.14</desc><desc class="x top">232.458330213</desc><desc class="y top">271.082210074</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="238.150257985" cy="270.038273123" r="1.5"/><desc class="value">2.16</desc><desc class="x top">238.150257985</desc><desc class="y top">270.038273123</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="243.842185757" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x top">243.842185757</desc><desc class="y top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="249.534113529" cy="271.607842946" r="1.5"/><desc class="value">2.13</desc><desc class="x top">249.534113529</desc><desc class="y top">271.607842946</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="255.226041301" cy="270.038273123" r="1.5"/><desc class="value">2.16</desc><desc class="x top">255.226041301</desc><desc class="y top">270.038273123</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="260.917969073" cy="276.452728246" r="1.5"/><desc class="value">2.04</desc><desc class="x top">260.917969073</desc><desc class="y top">276.452728246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="266.609896845" cy="274.273583796" r="1.5"/><desc class="value">2.08</desc><desc class="x top">266.609896845</desc><desc class="y top">274.273583796</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="272.301824617" cy="274.814415748" r="1.5"/><desc class="value">2.07</desc><desc class="x top">272.301824617</desc><desc class="y top">274.814415748</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="277.99375239" cy="269.003957848" r="1.5"/><desc class="value">2.18</desc><desc class="x top">277.99375239</desc><desc class="y top">269.003957848</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="283.685680162" cy="273.735345771" r="1.5"/><desc class="value">2.09</desc><desc class="x top">283.685680162</desc><desc class="y top">273.735345771</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="289.377607934" cy="278.115312921" r="1.5"/><desc class="value">2.01</desc><desc class="x top">289.377607934</desc><desc class="y top">278.115312921</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="295.069535706" cy="279.237546403" r="1.5"/><desc class="value">1.99</desc><desc class="x top">295.069535706</desc><desc class="y top">279.237546403</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="300.761463478" cy="270.559027714" r="1.5"/><desc class="value">2.15</desc><desc class="x top">300.761463478</desc><desc class="y top">270.559027714</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="306.45339125" cy="273.199676912" r="1.5"/><desc class="value">2.1</desc><desc class="x top">306.45339125</desc><desc class="y top">273.199676912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.145319022" cy="281.516252368" r="1.5"/><desc class="value">1.95</desc><desc class="x top">312.145319022</desc><desc class="y top">281.516252368</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="317.837246794" cy="343.743657489" r="1.5"/><desc class="value">1.12</desc><desc class="x top">317.837246794</desc><desc class="y top">343.743657489</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="323.529174566" cy="298.911371225" r="1.5"/><desc class="value">1.67</desc><desc class="x top">323.529174566</desc><desc class="y top">298.911371225</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.221102338" cy="275.357866752" r="1.5"/><desc class="value">2.06</desc><desc class="x top">329.221102338</desc><desc class="y top">275.357866752</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="334.913030111" cy="265.457130396" r="1.5"/><desc class="value">2.25</desc><desc class="x top">334.913030111</desc><desc class="y top">265.457130396</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.604957883" cy="247.944415486" r="1.5"/><desc class="value">2.63</desc><desc class="x ">340.604957883</desc><desc class="y ">247.944415486</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.296885655" cy="205.469520443" r="1.5"/><desc class="value">3.84</desc><desc class="x ">346.296885655</desc><desc class="y ">205.469520443</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="351.988813427" cy="248.801079287" r="1.5"/><desc class="value">2.61</desc><desc class="x ">351.988813427</desc><desc class="y ">248.801079287</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.680741199" cy="303.017527306" r="1.5"/><desc class="value">1.61</desc><desc class="x top">357.680741199</desc><desc class="y top">303.017527306</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.372668971" cy="237.753896783" r="1.5"/><desc class="value">2.88</desc><desc class="x ">363.372668971</desc><desc class="y ">237.753896783</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="369.064596743" cy="305.840881575" r="1.5"/><desc class="value">1.57</desc><desc class="x top">369.064596743</desc><desc class="y top">305.840881575</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.756524515" cy="151.887501888" r="1.5"/><desc class="value">6.19</desc><desc class="x ">374.756524515</desc><desc class="y ">151.887501888</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.448452287" cy="172.747629268" r="1.5"/><desc class="value">5.14</desc><desc class="x ">380.448452287</desc><desc class="y ">172.747629268</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="386.140380059" cy="197.571217603" r="1.5"/><desc class="value">4.12</desc><desc class="x ">386.140380059</desc><desc class="y ">197.571217603</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.832307832" cy="209.032435976" r="1.5"/><desc class="value">3.72</desc><desc class="x ">391.832307832</desc><desc class="y ">209.032435976</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="397.524235604" cy="219.78872519" r="1.5"/><desc class="value">3.38</desc><desc class="x ">397.524235604</desc><desc class="y ">219.78872519</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="403.216163376" cy="198.117313147" r="1.5"/><desc class="value">4.1</desc><desc class="x ">403.216163376</desc><desc class="y ">198.117313147</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.908091148" cy="232.056103917" r="1.5"/><desc class="value">3.03</desc><desc class="x ">408.908091148</desc><desc class="y ">232.056103917</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="414.60001892" cy="216.516702947" r="1.5"/><desc class="value">3.48</desc><desc class="x ">414.60001892</desc><desc class="y ">216.516702947</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="420.291946692" cy="206.055539597" r="1.5"/><desc class="value">3.82</desc><desc class="x ">420.291946692</desc><desc class="y ">206.055539597</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="425.983874464" cy="153.531146756" r="1.5"/><desc class="value">6.1</desc><desc class="x left">425.983874464</desc><desc class="y left">153.531146756</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="431.675802236" cy="321.132855158" r="1.5"/><desc class="value">1.37</desc><desc class="x left top">431.675802236</desc><desc class="y left top">321.132855158</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="437.367730008" cy="317.903210314" r="1.5"/><desc class="value">1.41</desc><desc class="x left top">437.367730008</desc><desc class="y left top">317.903210314</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="443.059657781" cy="314.763917137" r="1.5"/><desc class="value">1.45</desc><desc class="x left top">443.059657781</desc><desc class="y left top">314.763917137</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="448.751585553" cy="332.321357935" r="1.5"/><desc class="value">1.24</desc><desc class="x left top">448.751585553</desc><desc class="y left top">332.321357935</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="454.443513325" cy="363.405483123" r="1.5"/><desc class="value">0.94</desc><desc class="x left top">454.443513325</desc><desc class="y left top">363.405483123</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="460.135441097" cy="347.824922272" r="1.5"/><desc class="value">1.08</desc><desc class="x left top">460.135441097</desc><desc class="y left top">347.824922272</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="465.827368869" cy="354.239377395" r="1.5"/><desc class="value">1.02</desc><desc class="x left top">465.827368869</desc><desc class="y left top">354.239377395</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="471.519296641" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">471.519296641</desc><desc class="y left top">372.090001246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="477.211224413" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">477.211224413</desc><desc class="y left top">372.090001246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="482.903152185" cy="320.316688558" r="1.5"/><desc class="value">1.38</desc><desc class="x left top">482.903152185</desc><desc class="y left top">320.316688558</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="488.595079957" cy="238.929011321" r="1.5"/><desc class="value">2.85</desc><desc class="x left">488.595079957</desc><desc class="y left">238.929011321</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="494.287007729" cy="175.398675687" r="1.5"/><desc class="value">5.02</desc><desc class="x left">494.287007729</desc><desc class="y left">175.398675687</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="499.978935502" cy="334.146152823" r="1.5"/><desc class="value">1.22</desc><desc class="x left top">499.978935502</desc><desc class="y left top">334.146152823</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="505.670863274" cy="318.701949721" r="1.5"/><desc class="value">1.4</desc><desc class="x left top">505.670863274</desc><desc class="y left top">318.701949721</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="511.362791046" cy="250.534278417" r="1.5"/><desc class="value">2.57</desc><desc class="x left top">511.362791046</desc><desc class="y left top">250.534278417</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="517.054718818" cy="319.506414908" r="1.5"/><desc class="value">1.39</desc><desc class="x left top">517.054718818</desc><desc class="y left top">319.506414908</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="522.74664659" cy="270.559027714" r="1.5"/><desc class="value">2.15</desc><desc class="x left top">522.74664659</desc><desc class="y left top">270.559027714</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="528.438574362" cy="288.032312217" r="1.5"/><desc class="value">1.84</desc><desc class="x left top">528.438574362</desc><desc class="y left top">288.032312217</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="534.130502134" cy="232.427087438" r="1.5"/><desc class="value">3.02</desc><desc class="x left">534.130502134</desc><desc class="y left">232.427087438</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="539.822429906" cy="226.633676584" r="1.5"/><desc class="value">3.18</desc><desc class="x left">539.822429906</desc><desc class="y left">226.633676584</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="545.514357678" cy="271.082210074" r="1.5"/><desc class="value">2.14</desc><desc class="x left top">545.514357678</desc><desc class="y left top">271.082210074</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="551.20628545" cy="302.322649463" r="1.5"/><desc class="value">1.62</desc><desc class="x left top">551.20628545</desc><desc class="y left top">302.322649463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="556.898213223" cy="279.802899805" r="1.5"/><desc class="value">1.98</desc><desc class="x left top">556.898213223</desc><desc class="y left top">279.802899805</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="562.590140995" cy="303.017527306" r="1.5"/><desc class="value">1.61</desc><desc class="x left top">562.590140995</desc><desc class="y left top">303.017527306</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="568.282068767" cy="277.558376726" r="1.5"/><desc class="value">2.02</desc><desc class="x left top">568.282068767</desc><desc class="y left top">277.558376726</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="573.973996539" cy="228.412198959" r="1.5"/><desc class="value">3.13</desc><desc class="x left">573.973996539</desc><desc class="y left">228.412198959</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="579.665924311" cy="242.937380737" r="1.5"/><desc class="value">2.75</desc><desc class="x left">579.665924311</desc><desc class="y left">242.937380737</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="585.357852083" cy="211.472149735" r="1.5"/><desc class="value">3.64</desc><desc class="x left">585.357852083</desc><desc class="y left">211.472149735</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="591.049779855" cy="217.814086333" r="1.5"/><desc class="value">3.44</desc><desc class="x left">591.049779855</desc><desc class="y left">217.814086333</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="596.741707627" cy="212.712189014" r="1.5"/><desc class="value">3.6</desc><desc class="x left">596.741707627</desc><desc class="y left">212.712189014</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="602.433635399" cy="150.625549809" r="1.5"/><desc class="value">6.26</desc><desc class="x left">602.433635399</desc><desc class="y left">150.625549809</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="608.125563172" cy="308.006011362" r="1.5"/><desc class="value">1.54</desc><desc class="x left top">608.125563172</desc><desc class="y left top">308.006011362</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="613.817490944" cy="354.239377395" r="1.5"/><desc class="value">1.02</desc><desc class="x left top">613.817490944</desc><desc class="y left top">354.239377395</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="619.509418716" cy="334.146152823" r="1.5"/><desc class="value">1.22</desc><desc class="x left top">619.509418716</desc><desc class="y left top">334.146152823</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="625.201346488" cy="337.887246346" r="1.5"/><desc class="value">1.18</desc><desc class="x left top">625.201346488</desc><desc class="y left top">337.887246346</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="630.89327426" cy="342.746118361" r="1.5"/><desc class="value">1.13</desc><desc class="x left top">630.89327426</desc><desc class="y left top">342.746118361</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="636.585202032" cy="179.033727505" r="1.5"/><desc class="value">4.86</desc><desc class="x left">636.585202032</desc><desc class="y left">179.033727505</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="642.277129804" cy="210.551030366" r="1.5"/><desc class="value">3.67</desc><desc class="x left">642.277129804</desc><desc class="y left">210.551030366</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="647.969057576" cy="369.539382067" r="1.5"/><desc class="value">0.89</desc><desc class="x left top">647.969057576</desc><desc class="y left top">369.539382067</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="653.660985348" cy="370.807445423" r="1.5"/><desc class="value">0.88</desc><desc class="x left top">653.660985348</desc><desc class="y left top">370.807445423</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="659.35291312" cy="359.879881775" r="1.5"/><desc class="value">0.97</desc><desc class="x left top">659.35291312</desc><desc class="y left top">359.879881775</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.044840893" cy="373.387384632" r="1.5"/><desc class="value">0.86</desc><desc class="x left top">665.044840893</desc><desc class="y left top">373.387384632</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="670.736768665" cy="381.503383782" r="1.5"/><desc class="value">0.8</desc><desc class="x left top">670.736768665</desc><desc class="y left top">381.503383782</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="676.428696437" cy="356.461676014" r="1.5"/><desc class="value">1.0</desc><desc class="x left top">676.428696437</desc><desc class="y left top">356.461676014</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.120624209" cy="377.372030887" r="1.5"/><desc class="value">0.83</desc><desc class="x left top">682.120624209</desc><desc class="y left top">377.372030887</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="687.812551981" cy="376.02803383" r="1.5"/><desc class="value">0.84</desc><desc class="x left top">687.812551981</desc><desc class="y left top">376.02803383</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="693.504479753" cy="368.285487313" r="1.5"/><desc class="value">0.9</desc><desc class="x left top">693.504479753</desc><desc class="y left top">368.285487313</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.196407525" cy="374.699942436" r="1.5"/><desc class="value">0.85</desc><desc class="x left top">699.196407525</desc><desc class="y left top">374.699942436</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="704.888335297" cy="380.109298613" r="1.5"/><desc class="value">0.81</desc><desc class="x left top">704.888335297</desc><desc class="y left top">380.109298613</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.580263069" cy="380.109298613" r="1.5"/><desc class="value">0.81</desc><desc class="x left top">710.580263069</desc><desc class="y left top">380.109298613</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="716.272190841" cy="376.02803383" r="1.5"/><desc class="value">0.84</desc><desc class="x left top">716.272190841</desc><desc class="y left top">376.02803383</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.964118614" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">721.964118614</desc><desc class="y left top">372.090001246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.656046386" cy="380.109298613" r="1.5"/><desc class="value">0.81</desc><desc class="x left top">727.656046386</desc><desc class="y left top">380.109298613</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.347974158" cy="373.387384632" r="1.5"/><desc class="value">0.86</desc><desc class="x left top">733.347974158</desc><desc class="y left top">373.387384632</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="739.03990193" cy="378.732319215" r="1.5"/><desc class="value">0.82</desc><desc class="x left top">739.03990193</desc><desc class="y left top">378.732319215</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.731829702" cy="382.915005074" r="1.5"/><desc class="value">0.79</desc><desc class="x left top">744.731829702</desc><desc class="y left top">382.915005074</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="750.423757474" cy="370.807445423" r="1.5"/><desc class="value">0.88</desc><desc class="x left top">750.423757474</desc><desc class="y left top">370.807445423</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="756.115685246" cy="374.699942436" r="1.5"/><desc class="value">0.85</desc><desc class="x left top">756.115685246</desc><desc class="y left top">374.699942436</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="761.807613018" cy="368.285487313" r="1.5"/><desc class="value">0.9</desc><desc class="x left top">761.807613018</desc><desc class="y left top">368.285487313</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="767.49954079" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">767.49954079</desc><desc class="y left top">372.090001246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="773.191468563" cy="376.02803383" r="1.5"/><desc class="value">0.84</desc><desc class="x left top">773.191468563</desc><desc class="y left top">376.02803383</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="778.883396335" cy="370.807445423" r="1.5"/><desc class="value">0.88</desc><desc class="x left top">778.883396335</desc><desc class="y left top">370.807445423</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="784.575324107" cy="373.387384632" r="1.5"/><desc class="value">0.86</desc><desc class="x left top">784.575324107</desc><desc class="y left top">373.387384632</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="790.267251879" cy="365.818961367" r="1.5"/><desc class="value">0.92</desc><desc class="x left top">790.267251879</desc><desc class="y left top">365.818961367</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="795.959179651" cy="364.605734275" r="1.5"/><desc class="value">0.93</desc><desc class="x left top">795.959179651</desc><desc class="y left top">364.605734275</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="801.651107423" cy="369.539382067" r="1.5"/><desc class="value">0.89</desc><desc class="x left top">801.651107423</desc><desc class="y left top">369.539382067</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="807.343035195" cy="374.699942436" r="1.5"/><desc class="value">0.85</desc><desc class="x left top">807.343035195</desc><desc class="y left top">374.699942436</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="813.034962967" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">813.034962967</desc><desc class="y left top">372.090001246</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="818.726890739" cy="368.285487313" r="1.5"/><desc class="value">0.9</desc><desc class="x left top">818.726890739</desc><desc class="y left top">368.285487313</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="824.418818511" cy="372.090001246" r="1.5"/><desc class="value">0.87</desc><desc class="x left top">824.418818511</desc><desc class="y left top">372.090001246</desc></g></g><g class="series serie-2 color-2"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="16.1650748728" cy="459.290032931" r="1.5"/><desc class="value">0.4</desc><desc class="x top">16.1650748728</desc><desc class="y top">459.290032931</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.8570026449" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">21.8570026449</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.548930417" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x top">27.548930417</desc><desc class="y top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="33.2408581891" cy="474.27524802" r="1.5"/><desc class="value">0.35</desc><desc class="x top">33.2408581891</desc><desc class="y top">474.27524802</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.9327859612" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">38.9327859612</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="44.6247137333" cy="480.878470913" r="1.5"/><desc class="value">0.33</desc><desc class="x top">44.6247137333</desc><desc class="y top">480.878470913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="50.3166415054" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">50.3166415054</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="56.0085692775" cy="484.3317407" r="1.5"/><desc class="value">0.32</desc><desc class="x top">56.0085692775</desc><desc class="y top">484.3317407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="61.7004970496" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">61.7004970496</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="67.3924248217" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">67.3924248217</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="73.0843525938" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">73.0843525938</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="78.7762803659" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">78.7762803659</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="84.468208138" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">84.468208138</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="90.1601359101" cy="480.878470913" r="1.5"/><desc class="value">0.33</desc><desc class="x top">90.1601359101</desc><desc class="y top">480.878470913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="95.8520636822" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">95.8520636822</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="101.543991454" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">101.543991454</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="107.235919226" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">107.235919226</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="112.927846999" cy="495.378923204" r="1.5"/><desc class="value">0.29</desc><desc class="x top">112.927846999</desc><desc class="y top">495.378923204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="118.619774771" cy="491.574409272" r="1.5"/><desc class="value">0.3</desc><desc class="x top">118.619774771</desc><desc class="y top">491.574409272</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="124.311702543" cy="499.316955788" r="1.5"/><desc class="value">0.28</desc><desc class="x top">124.311702543</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="130.003630315" cy="471.113844231" r="1.5"/><desc class="value">0.36</desc><desc class="x top">130.003630315</desc><desc class="y top">471.113844231</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="135.695558087" cy="212.090456604" r="1.5"/><desc class="value">3.62</desc><desc class="x ">135.695558087</desc><desc class="y ">212.090456604</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="141.387485859" cy="480.878470913" r="1.5"/><desc class="value">0.33</desc><desc class="x top">141.387485859</desc><desc class="y top">480.878470913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="147.079413631" cy="491.574409272" r="1.5"/><desc class="value">0.3</desc><desc class="x top">147.079413631</desc><desc class="y top">491.574409272</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="152.771341403" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">152.771341403</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="158.463269175" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">158.463269175</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="164.155196947" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x top">164.155196947</desc><desc class="y top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="169.84712472" cy="471.113844231" r="1.5"/><desc class="value">0.36</desc><desc class="x top">169.84712472</desc><desc class="y top">471.113844231</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="175.539052492" cy="474.27524802" r="1.5"/><desc class="value">0.35</desc><desc class="x top">175.539052492</desc><desc class="y top">474.27524802</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="181.230980264" cy="495.378923204" r="1.5"/><desc class="value">0.29</desc><desc class="x top">181.230980264</desc><desc class="y top">495.378923204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="186.922908036" cy="471.113844231" r="1.5"/><desc class="value">0.36</desc><desc class="x top">186.922908036</desc><desc class="y top">471.113844231</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="192.614835808" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">192.614835808</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="198.30676358" cy="484.3317407" r="1.5"/><desc class="value">0.32</desc><desc class="x top">198.30676358</desc><desc class="y top">484.3317407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="203.998691352" cy="474.27524802" r="1.5"/><desc class="value">0.35</desc><desc class="x top">203.998691352</desc><desc class="y top">474.27524802</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="209.690619124" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">209.690619124</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="215.382546896" cy="471.113844231" r="1.5"/><desc class="value">0.36</desc><desc class="x top">215.382546896</desc><desc class="y top">471.113844231</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="221.074474668" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">221.074474668</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="226.766402441" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">226.766402441</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="232.458330213" cy="451.174033781" r="1.5"/><desc class="value">0.43</desc><desc class="x top">232.458330213</desc><desc class="y top">451.174033781</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="238.150257985" cy="480.878470913" r="1.5"/><desc class="value">0.33</desc><desc class="x top">238.150257985</desc><desc class="y top">480.878470913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="243.842185757" cy="448.594094573" r="1.5"/><desc class="value">0.44</desc><desc class="x top">243.842185757</desc><desc class="y top">448.594094573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="249.534113529" cy="480.878470913" r="1.5"/><desc class="value">0.33</desc><desc class="x top">249.534113529</desc><desc class="y top">480.878470913</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="255.226041301" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">255.226041301</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="260.917969073" cy="484.3317407" r="1.5"/><desc class="value">0.32</desc><desc class="x top">260.917969073</desc><desc class="y top">484.3317407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="266.609896845" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x top">266.609896845</desc><desc class="y top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="272.301824617" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x top">272.301824617</desc><desc class="y top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="277.99375239" cy="484.3317407" r="1.5"/><desc class="value">0.32</desc><desc class="x top">277.99375239</desc><desc class="y top">484.3317407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="283.685680162" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">283.685680162</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="289.377607934" cy="474.27524802" r="1.5"/><desc class="value">0.35</desc><desc class="x top">289.377607934</desc><desc class="y top">474.27524802</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="295.069535706" cy="487.894656233" r="1.5"/><desc class="value">0.31</desc><desc class="x top">295.069535706</desc><desc class="y top">487.894656233</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="300.761463478" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x top">300.761463478</desc><desc class="y top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="306.45339125" cy="484.3317407" r="1.5"/><desc class="value">0.32</desc><desc class="x top">306.45339125</desc><desc class="y top">484.3317407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.145319022" cy="474.27524802" r="1.5"/><desc class="value">0.35</desc><desc class="x top">312.145319022</desc><desc class="y top">474.27524802</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="317.837246794" cy="432.026026545" r="1.5"/><desc class="value">0.51</desc><desc class="x top">317.837246794</desc><desc class="y top">432.026026545</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="323.529174566" cy="377.372030887" r="1.5"/><desc class="value">0.83</desc><desc class="x top">323.529174566</desc><desc class="y top">377.372030887</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.221102338" cy="273.199676912" r="1.5"/><desc class="value">2.1</desc><desc class="x top">329.221102338</desc><desc class="y top">273.199676912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="334.913030111" cy="377.372030887" r="1.5"/><desc class="value">0.83</desc><desc class="x top">334.913030111</desc><desc class="y top">377.372030887</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.604957883" cy="394.896764904" r="1.5"/><desc class="value">0.71</desc><desc class="x top">340.604957883</desc><desc class="y top">394.896764904</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.296885655" cy="312.465766936" r="1.5"/><desc class="value">1.48</desc><desc class="x top">346.296885655</desc><desc class="y top">312.465766936</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="351.988813427" cy="330.52576102" r="1.5"/><desc class="value">1.26</desc><desc class="x top">351.988813427</desc><desc class="y top">330.52576102</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.680741199" cy="382.915005074" r="1.5"/><desc class="value">0.79</desc><desc class="x top">357.680741199</desc><desc class="y top">382.915005074</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.372668971" cy="403.091821764" r="1.5"/><desc class="value">0.66</desc><desc class="x top">363.372668971</desc><desc class="y top">403.091821764</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="369.064596743" cy="465.046290197" r="1.5"/><desc class="value">0.38</desc><desc class="x top">369.064596743</desc><desc class="y top">465.046290197</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.756524515" cy="305.128355924" r="1.5"/><desc class="value">1.58</desc><desc class="x top">374.756524515</desc><desc class="y top">305.128355924</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.448452287" cy="269.519923874" r="1.5"/><desc class="value">2.17</desc><desc class="x top">380.448452287</desc><desc class="y top">269.519923874</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="386.140380059" cy="161.339416503" r="1.5"/><desc class="value">5.69</desc><desc class="x ">386.140380059</desc><desc class="y ">161.339416503</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.832307832" cy="311.710057595" r="1.5"/><desc class="value">1.49</desc><desc class="x top">391.832307832</desc><desc class="y top">311.710057595</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="397.524235604" cy="295.600735482" r="1.5"/><desc class="value">1.72</desc><desc class="x top">397.524235604</desc><desc class="y top">295.600735482</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="403.216163376" cy="298.24138468" r="1.5"/><desc class="value">1.68</desc><desc class="x top">403.216163376</desc><desc class="y top">298.24138468</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.908091148" cy="350.986326061" r="1.5"/><desc class="value">1.05</desc><desc class="x top">408.908091148</desc><desc class="y top">350.986326061</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="414.60001892" cy="291.124034282" r="1.5"/><desc class="value">1.79</desc><desc class="x top">414.60001892</desc><desc class="y top">291.124034282</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="420.291946692" cy="245.412975528" r="1.5"/><desc class="value">2.69</desc><desc class="x ">420.291946692</desc><desc class="y ">245.412975528</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="425.983874464" cy="175.846669947" r="1.5"/><desc class="value">5.0</desc><desc class="x left">425.983874464</desc><desc class="y left">175.846669947</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="431.675802236" cy="385.792660512" r="1.5"/><desc class="value">0.77</desc><desc class="x left top">431.675802236</desc><desc class="y left top">385.792660512</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="437.367730008" cy="390.252416085" r="1.5"/><desc class="value">0.74</desc><desc class="x left top">437.367730008</desc><desc class="y left top">390.252416085</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="443.059657781" cy="391.779275189" r="1.5"/><desc class="value">0.73</desc><desc class="x left top">443.059657781</desc><desc class="y left top">391.779275189</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="448.751585553" cy="378.732319215" r="1.5"/><desc class="value">0.82</desc><desc class="x left top">448.751585553</desc><desc class="y left top">378.732319215</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="454.443513325" cy="432.026026545" r="1.5"/><desc class="value">0.51</desc><desc class="x left top">454.443513325</desc><desc class="y left top">432.026026545</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="460.135441097" cy="285.618833973" r="1.5"/><desc class="value">1.88</desc><desc class="x left top">460.135441097</desc><desc class="y left top">285.618833973</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="465.827368869" cy="446.072136463" r="1.5"/><desc class="value">0.45</desc><desc class="x left top">465.827368869</desc><desc class="y left top">446.072136463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="471.519296641" cy="448.594094573" r="1.5"/><desc class="value">0.44</desc><desc class="x left top">471.519296641</desc><desc class="y left top">448.594094573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="477.211224413" cy="448.594094573" r="1.5"/><desc class="value">0.44</desc><desc class="x left top">477.211224413</desc><desc class="y left top">448.594094573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="482.903152185" cy="429.846882094" r="1.5"/><desc class="value">0.52</desc><desc class="x left top">482.903152185</desc><desc class="y left top">429.846882094</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="488.595079957" cy="359.879881775" r="1.5"/><desc class="value">0.97</desc><desc class="x left top">488.595079957</desc><desc class="y left top">359.879881775</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="494.287007729" cy="278.675026864" r="1.5"/><desc class="value">2.0</desc><desc class="x left top">494.287007729</desc><desc class="y left top">278.675026864</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="499.978935502" cy="417.592274055" r="1.5"/><desc class="value">0.58</desc><desc class="x left top">499.978935502</desc><desc class="y left top">417.592274055</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="505.670863274" cy="382.915005074" r="1.5"/><desc class="value">0.79</desc><desc class="x left top">505.670863274</desc><desc class="y left top">382.915005074</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="511.362791046" cy="344.750143276" r="1.5"/><desc class="value">1.11</desc><desc class="x left top">511.362791046</desc><desc class="y left top">344.750143276</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="517.054718818" cy="434.248325163" r="1.5"/><desc class="value">0.5</desc><desc class="x left top">517.054718818</desc><desc class="y left top">434.248325163</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="522.74664659" cy="373.387384632" r="1.5"/><desc class="value">0.86</desc><desc class="x left top">522.74664659</desc><desc class="y left top">373.387384632</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="528.438574362" cy="378.732319215" r="1.5"/><desc class="value">0.82</desc><desc class="x left top">528.438574362</desc><desc class="y left top">378.732319215</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="534.130502134" cy="328.758442401" r="1.5"/><desc class="value">1.28</desc><desc class="x left top">534.130502134</desc><desc class="y left top">328.758442401</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="539.822429906" cy="292.384973536" r="1.5"/><desc class="value">1.77</desc><desc class="x left top">539.822429906</desc><desc class="y left top">292.384973536</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="545.514357678" cy="326.158579107" r="1.5"/><desc class="value">1.31</desc><desc class="x left top">545.514357678</desc><desc class="y left top">326.158579107</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="551.20628545" cy="337.887246346" r="1.5"/><desc class="value">1.18</desc><desc class="x left top">551.20628545</desc><desc class="y left top">337.887246346</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="556.898213223" cy="403.091821764" r="1.5"/><desc class="value">0.66</desc><desc class="x left top">556.898213223</desc><desc class="y left top">403.091821764</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="562.590140995" cy="380.109298613" r="1.5"/><desc class="value">0.81</desc><desc class="x left top">562.590140995</desc><desc class="y left top">380.109298613</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="568.282068767" cy="336.940216143" r="1.5"/><desc class="value">1.19</desc><desc class="x left top">568.282068767</desc><desc class="y left top">336.940216143</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="573.973996539" cy="298.24138468" r="1.5"/><desc class="value">1.68</desc><desc class="x left top">573.973996539</desc><desc class="y left top">298.24138468</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="579.665924311" cy="346.790606997" r="1.5"/><desc class="value">1.09</desc><desc class="x left top">579.665924311</desc><desc class="y left top">346.790606997</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="585.357852083" cy="317.110115755" r="1.5"/><desc class="value">1.42</desc><desc class="x left top">585.357852083</desc><desc class="y left top">317.110115755</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="591.049779855" cy="309.472991898" r="1.5"/><desc class="value">1.52</desc><desc class="x left top">591.049779855</desc><desc class="y left top">309.472991898</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="596.741707627" cy="135.081512667" r="1.5"/><desc class="value">7.19</desc><desc class="x left">596.741707627</desc><desc class="y left">135.081512667</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="602.433635399" cy="78.8218317874" r="1.5"/><desc class="value">11.87</desc><desc class="x left">602.433635399</desc><desc class="y left">78.8218317874</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="608.125563172" cy="235.439950619" r="1.5"/><desc class="value">2.94</desc><desc class="x left">608.125563172</desc><desc class="y left">235.439950619</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="613.817490944" cy="398.103337707" r="1.5"/><desc class="value">0.69</desc><desc class="x left top">613.817490944</desc><desc class="y left top">398.103337707</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="619.509418716" cy="411.932801973" r="1.5"/><desc class="value">0.61</desc><desc class="x left top">619.509418716</desc><desc class="y left top">411.932801973</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="625.201346488" cy="399.741650204" r="1.5"/><desc class="value">0.68</desc><desc class="x left top">625.201346488</desc><desc class="y left top">399.741650204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="630.89327426" cy="429.846882094" r="1.5"/><desc class="value">0.52</desc><desc class="x left top">630.89327426</desc><desc class="y left top">429.846882094</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="636.585202032" cy="131.850760869" r="1.5"/><desc class="value">7.4</desc><desc class="x left">636.585202032</desc><desc class="y left">131.850760869</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="642.277129804" cy="176.522032545" r="1.5"/><desc class="value">4.97</desc><desc class="x left">642.277129804</desc><desc class="y left">176.522032545</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="647.969057576" cy="432.026026545" r="1.5"/><desc class="value">0.51</desc><desc class="x left top">647.969057576</desc><desc class="y left top">432.026026545</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="653.660985348" cy="451.174033781" r="1.5"/><desc class="value">0.43</desc><desc class="x left top">653.660985348</desc><desc class="y left top">451.174033781</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="659.35291312" cy="462.131258435" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">659.35291312</desc><desc class="y left top">462.131258435</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.044840893" cy="434.248325163" r="1.5"/><desc class="value">0.5</desc><desc class="x left top">665.044840893</desc><desc class="y left top">434.248325163</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="670.736768665" cy="465.046290197" r="1.5"/><desc class="value">0.38</desc><desc class="x left top">670.736768665</desc><desc class="y left top">465.046290197</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="676.428696437" cy="174.952462722" r="1.5"/><desc class="value">5.04</desc><desc class="x left">676.428696437</desc><desc class="y left">174.952462722</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.120624209" cy="462.131258435" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">682.120624209</desc><desc class="y left top">462.131258435</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="687.812551981" cy="462.131258435" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">687.812551981</desc><desc class="y left top">462.131258435</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="693.504479753" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x left top">693.504479753</desc><desc class="y left top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.196407525" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x left top">699.196407525</desc><desc class="y left top">468.039065234</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="704.888335297" cy="477.528299354" r="1.5"/><desc class="value">0.34</desc><desc class="x left top">704.888335297</desc><desc class="y left top">477.528299354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.580263069" cy="448.594094573" r="1.5"/><desc class="value">0.44</desc><desc class="x left top">710.580263069</desc><desc class="y left top">448.594094573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="716.272190841" cy="465.046290197" r="1.5"/><desc class="value">0.38</desc><desc class="x left top">716.272190841</desc><desc class="y left top">465.046290197</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.964118614" cy="459.290032931" r="1.5"/><desc class="value">0.4</desc><desc class="x left top">721.964118614</desc><desc class="y left top">459.290032931</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.656046386" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x left top">727.656046386</desc><desc class="y left top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.347974158" cy="453.814682979" r="1.5"/><desc class="value">0.42</desc><desc class="x left top">733.347974158</desc><desc class="y left top">453.814682979</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="739.03990193" cy="459.290032931" r="1.5"/><desc class="value">0.4</desc><desc class="x left top">739.03990193</desc><desc class="y left top">459.290032931</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.731829702" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x left top">744.731829702</desc><desc class="y left top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="750.423757474" cy="453.814682979" r="1.5"/><desc class="value">0.42</desc><desc class="x left top">750.423757474</desc><desc class="y left top">453.814682979</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="756.115685246" cy="462.131258435" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">756.115685246</desc><desc class="y left top">462.131258435</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="761.807613018" cy="446.072136463" r="1.5"/><desc class="value">0.45</desc><desc class="x left top">761.807613018</desc><desc class="y left top">446.072136463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="767.49954079" cy="446.072136463" r="1.5"/><desc class="value">0.45</desc><desc class="x left top">767.49954079</desc><desc class="y left top">446.072136463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="773.191468563" cy="451.174033781" r="1.5"/><desc class="value">0.43</desc><desc class="x left top">773.191468563</desc><desc class="y left top">451.174033781</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="778.883396335" cy="448.594094573" r="1.5"/><desc class="value">0.44</desc><desc class="x left top">778.883396335</desc><desc class="y left top">448.594094573</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="784.575324107" cy="451.174033781" r="1.5"/><desc class="value">0.43</desc><desc class="x left top">784.575324107</desc><desc class="y left top">451.174033781</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="790.267251879" cy="446.072136463" r="1.5"/><desc class="value">0.45</desc><desc class="x left top">790.267251879</desc><desc class="y left top">446.072136463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="795.959179651" cy="446.072136463" r="1.5"/><desc class="value">0.45</desc><desc class="x left top">795.959179651</desc><desc class="y left top">446.072136463</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="801.651107423" cy="419.544017388" r="1.5"/><desc class="value">0.57</desc><desc class="x left top">801.651107423</desc><desc class="y left top">419.544017388</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="807.343035195" cy="381.503383782" r="1.5"/><desc class="value">0.8</desc><desc class="x left top">807.343035195</desc><desc class="y left top">381.503383782</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="813.034962967" cy="421.530306639" r="1.5"/><desc class="value">0.56</desc><desc class="x left top">813.034962967</desc><desc class="y left top">421.530306639</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="818.726890739" cy="456.518968364" r="1.5"/><desc class="value">0.41</desc><desc class="x left top">818.726890739</desc><desc class="y left top">456.518968364</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="824.418818511" cy="468.039065234" r="1.5"/><desc class="value">0.37</desc><desc class="x left top">824.418818511</desc><desc class="y left top">468.039065234</desc></g></g></g><g class="plot text-overlay" transform="translate(130, 46)"><g class="series serie-0 color-0"><text class="centered" x="24.1650748728" y="133.768395232">8.0</text><text class="centered" x="29.8570026449" y="138.788765186">7.65</text><text class="centered" x="35.548930417" y="141.310723296">7.48</text><text class="centered" x="41.2408581891" y="143.890662504">7.31</text><text class="centered" x="46.9327859612" y="143.278260368">7.35</text><text class="centered" x="52.6247137333" y="131.821489177">8.14</text><text class="centered" x="58.3166415054" y="147.003827823">7.11</text><text class="centered" x="64.0085692775" y="149.881483261">6.93</text><text class="centered" x="69.7004970496" y="140.861533585">7.51</text><text class="centered" x="75.3924248217" y="145.904369217">7.18</text><text class="centered" x="81.0843525938" y="142.06339153">7.43</text><text class="centered" x="86.7762803659" y="142.669182005">7.39</text><text class="centered" x="92.468208138" y="143.125680585">7.36</text><text class="centered" x="98.1601359101" y="144.970474122">7.24</text><text class="centered" x="103.852063682" y="142.669182005">7.39</text><text class="centered" x="109.543991454" y="139.524652498">7.6</text><text class="centered" x="115.235919226" y="137.912023125">7.71</text><text class="centered" x="120.927846999" y="142.517427535">7.4</text><text class="centered" x="126.619774771" y="142.669182005">7.39</text><text class="centered" x="132.311702543" y="147.478341904">7.08</text><text class="centered" x="138.003630315" y="138.34953796">7.68</text><text class="centered" x="143.695558087" y="148.914042675">6.99</text><text class="centered" x="149.387485859" y="139.820363902">7.58</text><text class="centered" x="155.079413631" y="146.531311702">7.14</text><text class="centered" x="160.771341403" y="143.584043699">7.33</text><text class="centered" x="166.463269175" y="139.082541304">7.63</text><text class="centered" x="172.155196947" y="133.069187905">8.05</text><text class="centered" x="177.84712472" y="138.34953796">7.68</text><text class="centered" x="183.539052492" y="131.821489177">8.14</text><text class="centered" x="189.230980264" y="136.75358793">7.79</text><text class="centered" x="194.922908036" y="140.563069545">7.53</text><text class="centered" x="200.614835808" y="139.672410798">7.59</text><text class="centered" x="206.30676358" y="131.271378208">8.18</text><text class="centered" x="211.998691352" y="136.609620736">7.8</text><text class="centered" x="217.690619124" y="128.694557025">8.37</text><text class="centered" x="223.382546896" y="138.34953796">7.68</text><text class="centered" x="229.074474668" y="133.908760995">7.99</text><text class="centered" x="234.766402441" y="144.044286639">7.3</text><text class="centered" x="240.458330213" y="134.471986333">7.95</text><text class="centered" x="246.150257985" y="136.609620736">7.8</text><text class="centered" x="251.842185757" y="138.203510076">7.69</text><text class="centered" x="257.534113529" y="137.621291323">7.73</text><text class="centered" x="263.226041301" y="141.460853637">7.47</text><text class="centered" x="268.917969073" y="133.628204817">8.01</text><text class="centered" x="274.609896845" y="136.178823996">7.83</text><text class="centered" x="280.301824617" y="134.471986333">7.95</text><text class="centered" x="285.99375239" y="138.203510076">7.69</text><text class="centered" x="291.685680162" y="143.278260368">7.35</text><text class="centered" x="297.377607934" y="131.959439302">8.13</text><text class="centered" x="303.069535706" y="145.1255846">7.23</text><text class="centered" x="308.761463478" y="134.471986333">7.95</text><text class="centered" x="314.45339125" y="142.821141964">7.38</text><text class="centered" x="320.145319022" y="207.965829802">4.13</text><text class="centered" x="325.837246794" y="342.988024601">1.24</text><text class="centered" x="331.529174566" y="142.517427535">7.4</text><text class="centered" x="337.221102338" y="151.677080176">6.82</text><text class="centered" x="342.913030111" y="147.636960182">7.07</text><text class="centered" x="348.604957883" y="114.837892035">9.47</text><text class="centered" x="354.296885655" y="91.4916716342">11.66</text><text class="centered" x="359.988813427" y="112.029259611">9.71</text><text class="centered" x="365.680741199" y="153.501875064">6.71</text><text class="centered" x="371.372668971" y="139.672410798">7.59</text><text class="centered" x="377.064596743" y="200.604344477">4.41</text><text class="centered" x="382.756524515" y="42.5147431567">18.04</text><text class="centered" x="388.448452287" y="40.9702319485">18.29</text><text class="centered" x="394.140380059" y="54.3109100631">16.24</text><text class="centered" x="399.832307832" y="90.0571943315">11.81</text><text class="centered" x="405.524235604" y="81.0234538513">12.8</text><text class="centered" x="411.216163376" y="72.6630500751">13.79</text><text class="centered" x="416.908091148" y="127.494305872">8.46</text><text class="centered" x="422.60001892" y="87.149472285">12.12</text><text class="centered" x="428.291946692" y="94.2194340204">11.38</text><text class="centered" x="433.983874464" y="139.22971826">7.62</text><text class="centered" x="439.675802236" y="231.121402197">3.36</text><text class="centered" x="445.367730008" y="173.195731197">5.63</text><text class="centered" x="451.059657781" y="181.251947475">5.24</text><text class="centered" x="456.751585553" y="185.396686475">5.05</text><text class="centered" x="462.443513325" y="217.016367409">3.81</text><text class="centered" x="468.135441097" y="289.341693531">2.0</text><text class="centered" x="473.827368869" y="263.851991503">2.51</text><text class="centered" x="479.519296641" y="239.437977653">3.12</text><text class="centered" x="485.211224413" y="279.157019897">2.19</text><text class="centered" x="490.903152185" y="249.202604335">2.86</text><text class="centered" x="496.595079957" y="133.069187905">8.05</text><text class="centered" x="502.287007729" y="68.6661029325">14.29</text><text class="centered" x="507.978935502" y="202.658816738">4.33</text><text class="centered" x="513.670863274" y="170.244928077">5.78</text><text class="centered" x="519.362791046" y="110.536765624">9.84</text><text class="centered" x="525.054718818" y="188.551741226">4.91</text><text class="centered" x="530.74664659" y="113.424789465">9.59</text><text class="centered" x="536.438574362" y="141.912453494">7.44</text><text class="centered" x="542.130502134" y="119.18720586">9.11</text><text class="centered" x="547.822429906" y="87.9859165024">12.03</text><text class="centered" x="553.514357678" y="113.659074337">9.57</text><text class="centered" x="559.20628545" y="132.651745094">8.08</text><text class="centered" x="564.898213223" y="166.052771573">6.0</text><text class="centered" x="570.590140995" y="164.381935553">6.09</text><text class="centered" x="576.282068767" y="118.695540614">9.15</text><text class="centered" x="581.973996539" y="95.9085150614">11.21</text><text class="centered" x="587.665924311" y="106.834941111">10.17</text><text class="centered" x="593.357852083" y="83.0582872886">12.57</text><text class="centered" x="599.049779855" y="46.6974290158">17.38</text><text class="centered" x="604.741707627" y="59.6170865388">15.49</text><text class="centered" x="610.433635399" y="10.6666666667">23.96</text><text class="centered" x="616.125563172" y="129.77233158">8.29</text><text class="centered" x="621.817490944" y="173.395237762">5.62</text><text class="centered" x="627.509418716" y="173.595099636">5.61</text><text class="centered" x="633.201346488" y="180.824433004">5.26</text><text class="centered" x="638.89327426" y="187.414726303">4.96</text><text class="centered" x="644.585202032" y="40.4804443168">18.37</text><text class="centered" x="650.277129804" y="64.3522875956">14.85</text><text class="centered" x="655.969057576" y="195.141112921">4.63</text><text class="centered" x="661.660985348" y="198.586808556">4.49</text><text class="centered" x="667.35291312" y="264.299985763">2.5</text><text class="centered" x="673.044840893" y="248.031577226">2.89</text><text class="centered" x="678.736768665" y="225.582452123">3.53</text><text class="centered" x="684.428696437" y="248.031577226">2.89</text><text class="centered" x="690.120624209" y="247.257626483">2.91</text><text class="centered" x="695.812551981" y="247.643934655">2.9</text><text class="centered" x="701.504479753" y="240.88602888">3.08</text><text class="centered" x="707.196407525" y="253.196706075">2.76</text><text class="centered" x="712.888335297" y="209.884208362">4.06</text><text class="centered" x="718.580263069" y="269.820233661">2.38</text><text class="centered" x="724.272190841" y="268.88112849">2.4</text><text class="centered" x="729.964118614" y="289.90421307">1.99</text><text class="centered" x="735.656046386" y="237.653799962">3.17</text><text class="centered" x="741.347974158" y="267.487043321">2.43</text><text class="centered" x="747.03990193" y="241.617122104">3.06</text><text class="centered" x="752.731829702" y="238.008373442">3.16</text><text class="centered" x="758.423757474" y="268.414506558">2.41</text><text class="centered" x="764.115685246" y="276.123797062">2.25</text><text class="centered" x="769.807613018" y="287.119394913">2.04</text><text class="centered" x="775.49954079" y="246.488976814">2.93</text><text class="centered" x="781.191468563" y="245.725556095">2.95</text><text class="centered" x="786.883396335" y="291.037782435">1.97</text><text class="centered" x="792.575324107" y="279.157019897">2.19</text><text class="centered" x="798.267251879" y="282.80261606">2.12</text><text class="centered" x="803.959179651" y="346.66777764">1.2</text><text class="centered" x="809.651107423" y="267.026170341">2.44</text><text class="centered" x="815.343035195" y="230.787902954">3.37</text><text class="centered" x="821.034962967" y="245.345784453">2.96</text><text class="centered" x="826.726890739" y="303.687462941">1.76</text><text class="centered" x="832.418818511" y="255.248361261">2.71</text></g><g class="series serie-1 color-1"><text class="centered" x="24.1650748728" y="289.90421307">1.99</text><text class="centered" x="29.8570026449" y="298.090725834">1.85</text><text class="centered" x="35.548930417" y="282.80261606">2.12</text><text class="centered" x="41.2408581891" y="291.037782435">1.97</text><text class="centered" x="46.9327859612" y="288.225043393">2.02</text><text class="centered" x="52.6247137333" y="287.119394913">2.04</text><text class="centered" x="58.3166415054" y="289.341693531">2.0</text><text class="centered" x="64.0085692775" y="283.333219474">2.11</text><text class="centered" x="69.7004970496" y="286.024533419">2.06</text><text class="centered" x="75.3924248217" y="286.024533419">2.06</text><text class="centered" x="81.0843525938" y="285.481082415">2.07</text><text class="centered" x="86.7762803659" y="293.922836259">1.92</text><text class="centered" x="92.468208138" y="294.508855413">1.91</text><text class="centered" x="98.1601359101" y="293.339861374">1.93</text><text class="centered" x="103.852063682" y="287.670857511">2.03</text><text class="centered" x="109.543991454" y="282.80261606">2.12</text><text class="centered" x="115.235919226" y="284.940250462">2.08</text><text class="centered" x="120.927846999" y="284.402012438">2.09</text><text class="centered" x="126.619774771" y="288.225043393">2.02</text><text class="centered" x="132.311702543" y="288.781979588">2.01</text><text class="centered" x="138.003630315" y="287.670857511">2.03</text><text class="centered" x="143.695558087" y="270.767263863">2.36</text><text class="centered" x="149.387485859" y="292.759899292">1.94</text><text class="centered" x="155.079413631" y="295.097950797">1.9</text><text class="centered" x="160.771341403" y="291.608890095">1.96</text><text class="centered" x="166.463269175" y="284.940250462">2.08</text><text class="centered" x="172.155196947" y="282.80261606">2.12</text><text class="centered" x="177.84712472" y="288.225043393">2.02</text><text class="centered" x="183.539052492" y="284.940250462">2.08</text><text class="centered" x="189.230980264" y="284.402012438">2.09</text><text class="centered" x="194.922908036" y="283.866343579">2.1</text><text class="centered" x="200.614835808" y="283.333219474">2.11</text><text class="centered" x="206.30676358" y="291.037782435">1.97</text><text class="centered" x="211.998691352" y="285.481082415">2.07</text><text class="centered" x="217.690619124" y="285.481082415">2.07</text><text class="centered" x="223.382546896" y="288.225043393">2.02</text><text class="centered" x="229.074474668" y="281.225694381">2.15</text><text class="centered" x="234.766402441" y="288.225043393">2.02</text><text class="centered" x="240.458330213" y="281.748876741">2.14</text><text class="centered" x="246.150257985" y="280.70493979">2.16</text><text class="centered" x="251.842185757" y="288.225043393">2.02</text><text class="centered" x="257.534113529" y="282.274509613">2.13</text><text class="centered" x="263.226041301" y="280.70493979">2.16</text><text class="centered" x="268.917969073" y="287.119394913">2.04</text><text class="centered" x="274.609896845" y="284.940250462">2.08</text><text class="centered" x="280.301824617" y="285.481082415">2.07</text><text class="centered" x="285.99375239" y="279.670624514">2.18</text><text class="centered" x="291.685680162" y="284.402012438">2.09</text><text class="centered" x="297.377607934" y="288.781979588">2.01</text><text class="centered" x="303.069535706" y="289.90421307">1.99</text><text class="centered" x="308.761463478" y="281.225694381">2.15</text><text class="centered" x="314.45339125" y="283.866343579">2.1</text><text class="centered" x="320.145319022" y="292.182919034">1.95</text><text class="centered" x="325.837246794" y="354.410324156">1.12</text><text class="centered" x="331.529174566" y="309.578037892">1.67</text><text class="centered" x="337.221102338" y="286.024533419">2.06</text><text class="centered" x="342.913030111" y="276.123797062">2.25</text><text class="centered" x="348.604957883" y="258.611082153">2.63</text><text class="centered" x="354.296885655" y="216.136187109">3.84</text><text class="centered" x="359.988813427" y="259.467745954">2.61</text><text class="centered" x="365.680741199" y="313.684193972">1.61</text><text class="centered" x="371.372668971" y="248.420563449">2.88</text><text class="centered" x="377.064596743" y="316.507548242">1.57</text><text class="centered" x="382.756524515" y="162.554168554">6.19</text><text class="centered" x="388.448452287" y="183.414295934">5.14</text><text class="centered" x="394.140380059" y="208.23788427">4.12</text><text class="centered" x="399.832307832" y="219.699102643">3.72</text><text class="centered" x="405.524235604" y="230.455391857">3.38</text><text class="centered" x="411.216163376" y="208.783979814">4.1</text><text class="centered" x="416.908091148" y="242.722770584">3.03</text><text class="centered" x="422.60001892" y="227.183369614">3.48</text><text class="centered" x="428.291946692" y="216.722206263">3.82</text><text class="centered" x="433.983874464" y="164.197813423">6.1</text><text class="centered" x="439.675802236" y="331.799521825">1.37</text><text class="centered" x="445.367730008" y="328.56987698">1.41</text><text class="centered" x="451.059657781" y="325.430583804">1.45</text><text class="centered" x="456.751585553" y="342.988024601">1.24</text><text class="centered" x="462.443513325" y="374.072149789">0.94</text><text class="centered" x="468.135441097" y="358.491588939">1.08</text><text class="centered" x="473.827368869" y="364.906044062">1.02</text><text class="centered" x="479.519296641" y="382.756667913">0.87</text><text class="centered" x="485.211224413" y="382.756667913">0.87</text><text class="centered" x="490.903152185" y="330.983355224">1.38</text><text class="centered" x="496.595079957" y="249.595677988">2.85</text><text class="centered" x="502.287007729" y="186.065342354">5.02</text><text class="centered" x="507.978935502" y="344.81281949">1.22</text><text class="centered" x="513.670863274" y="329.368616388">1.4</text><text class="centered" x="519.362791046" y="261.200945084">2.57</text><text class="centered" x="525.054718818" y="330.173081574">1.39</text><text class="centered" x="530.74664659" y="281.225694381">2.15</text><text class="centered" x="536.438574362" y="298.698978884">1.84</text><text class="centered" x="542.130502134" y="243.093754105">3.02</text><text class="centered" x="547.822429906" y="237.300343251">3.18</text><text class="centered" x="553.514357678" y="281.748876741">2.14</text><text class="centered" x="559.20628545" y="312.98931613">1.62</text><text class="centered" x="564.898213223" y="290.469566472">1.98</text><text class="centered" x="570.590140995" y="313.684193972">1.61</text><text class="centered" x="576.282068767" y="288.225043393">2.02</text><text class="centered" x="581.973996539" y="239.078865625">3.13</text><text class="centered" x="587.665924311" y="253.604047404">2.75</text><text class="centered" x="593.357852083" y="222.138816401">3.64</text><text class="centered" x="599.049779855" y="228.480753">3.44</text><text class="centered" x="604.741707627" y="223.378855681">3.6</text><text class="centered" x="610.433635399" y="161.292216476">6.26</text><text class="centered" x="616.125563172" y="318.672678029">1.54</text><text class="centered" x="621.817490944" y="364.906044062">1.02</text><text class="centered" x="627.509418716" y="344.81281949">1.22</text><text class="centered" x="633.201346488" y="348.553913012">1.18</text><text class="centered" x="638.89327426" y="353.412785028">1.13</text><text class="centered" x="644.585202032" y="189.700394171">4.86</text><text class="centered" x="650.277129804" y="221.217697033">3.67</text><text class="centered" x="655.969057576" y="380.206048734">0.89</text><text class="centered" x="661.660985348" y="381.47411209">0.88</text><text class="centered" x="667.35291312" y="370.546548442">0.97</text><text class="centered" x="673.044840893" y="384.054051298">0.86</text><text class="centered" x="678.736768665" y="392.170050449">0.8</text><text class="centered" x="684.428696437" y="367.12834268">1.0</text><text class="centered" x="690.120624209" y="388.038697554">0.83</text><text class="centered" x="695.812551981" y="386.694700496">0.84</text><text class="centered" x="701.504479753" y="378.95215398">0.9</text><text class="centered" x="707.196407525" y="385.366609103">0.85</text><text class="centered" x="712.888335297" y="390.775965279">0.81</text><text class="centered" x="718.580263069" y="390.775965279">0.81</text><text class="centered" x="724.272190841" y="386.694700496">0.84</text><text class="centered" x="729.964118614" y="382.756667913">0.87</text><text class="centered" x="735.656046386" y="390.775965279">0.81</text><text class="centered" x="741.347974158" y="384.054051298">0.86</text><text class="centered" x="747.03990193" y="389.398985881">0.82</text><text class="centered" x="752.731829702" y="393.58167174">0.79</text><text class="centered" x="758.423757474" y="381.47411209">0.88</text><text class="centered" x="764.115685246" y="385.366609103">0.85</text><text class="centered" x="769.807613018" y="378.95215398">0.9</text><text class="centered" x="775.49954079" y="382.756667913">0.87</text><text class="centered" x="781.191468563" y="386.694700496">0.84</text><text class="centered" x="786.883396335" y="381.47411209">0.88</text><text class="centered" x="792.575324107" y="384.054051298">0.86</text><text class="centered" x="798.267251879" y="376.485628033">0.92</text><text class="centered" x="803.959179651" y="375.272400942">0.93</text><text class="centered" x="809.651107423" y="380.206048734">0.89</text><text class="centered" x="815.343035195" y="385.366609103">0.85</text><text class="centered" x="821.034962967" y="382.756667913">0.87</text><text class="centered" x="826.726890739" y="378.95215398">0.9</text><text class="centered" x="832.418818511" y="382.756667913">0.87</text></g><g class="series serie-2 color-2"><text class="centered" x="24.1650748728" y="469.956699598">0.4</text><text class="centered" x="29.8570026449" y="478.705731901">0.37</text><text class="centered" x="35.548930417" y="467.185635031">0.41</text><text class="centered" x="41.2408581891" y="484.941914686">0.35</text><text class="centered" x="46.9327859612" y="488.194966021">0.34</text><text class="centered" x="52.6247137333" y="491.54513758">0.33</text><text class="centered" x="58.3166415054" y="478.705731901">0.37</text><text class="centered" x="64.0085692775" y="494.998407366">0.32</text><text class="centered" x="69.7004970496" y="488.194966021">0.34</text><text class="centered" x="75.3924248217" y="498.5613229">0.31</text><text class="centered" x="81.0843525938" y="488.194966021">0.34</text><text class="centered" x="86.7762803659" y="488.194966021">0.34</text><text class="centered" x="92.468208138" y="498.5613229">0.31</text><text class="centered" x="98.1601359101" y="491.54513758">0.33</text><text class="centered" x="103.852063682" y="498.5613229">0.31</text><text class="centered" x="109.543991454" y="478.705731901">0.37</text><text class="centered" x="115.235919226" y="498.5613229">0.31</text><text class="centered" x="120.927846999" y="506.045589871">0.29</text><text class="centered" x="126.619774771" y="502.241075938">0.3</text><text class="centered" x="132.311702543" y="509.983622455">0.28</text><text class="centered" x="138.003630315" y="481.780510898">0.36</text><text class="centered" x="143.695558087" y="222.757123271">3.62</text><text class="centered" x="149.387485859" y="491.54513758">0.33</text><text class="centered" x="155.079413631" y="502.241075938">0.3</text><text class="centered" x="160.771341403" y="498.5613229">0.31</text><text class="centered" x="166.463269175" y="498.5613229">0.31</text><text class="centered" x="172.155196947" y="467.185635031">0.41</text><text class="centered" x="177.84712472" y="481.780510898">0.36</text><text class="centered" x="183.539052492" y="484.941914686">0.35</text><text class="centered" x="189.230980264" y="506.045589871">0.29</text><text class="centered" x="194.922908036" y="481.780510898">0.36</text><text class="centered" x="200.614835808" y="498.5613229">0.31</text><text class="centered" x="206.30676358" y="494.998407366">0.32</text><text class="centered" x="211.998691352" y="484.941914686">0.35</text><text class="centered" x="217.690619124" y="478.705731901">0.37</text><text class="centered" x="223.382546896" y="481.780510898">0.36</text><text class="centered" x="229.074474668" y="478.705731901">0.37</text><text class="centered" x="234.766402441" y="478.705731901">0.37</text><text class="centered" x="240.458330213" y="461.840700448">0.43</text><text class="centered" x="246.150257985" y="491.54513758">0.33</text><text class="centered" x="251.842185757" y="459.260761239">0.44</text><text class="centered" x="257.534113529" y="491.54513758">0.33</text><text class="centered" x="263.226041301" y="488.194966021">0.34</text><text class="centered" x="268.917969073" y="494.998407366">0.32</text><text class="centered" x="274.609896845" y="467.185635031">0.41</text><text class="centered" x="280.301824617" y="488.194966021">0.34</text><text class="centered" x="285.99375239" y="494.998407366">0.32</text><text class="centered" x="291.685680162" y="498.5613229">0.31</text><text class="centered" x="297.377607934" y="484.941914686">0.35</text><text class="centered" x="303.069535706" y="498.5613229">0.31</text><text class="centered" x="308.761463478" y="478.705731901">0.37</text><text class="centered" x="314.45339125" y="494.998407366">0.32</text><text class="centered" x="320.145319022" y="484.941914686">0.35</text><text class="centered" x="325.837246794" y="442.692693211">0.51</text><text class="centered" x="331.529174566" y="388.038697554">0.83</text><text class="centered" x="337.221102338" y="283.866343579">2.1</text><text class="centered" x="342.913030111" y="388.038697554">0.83</text><text class="centered" x="348.604957883" y="405.563431571">0.71</text><text class="centered" x="354.296885655" y="323.132433602">1.48</text><text class="centered" x="359.988813427" y="341.192427687">1.26</text><text class="centered" x="365.680741199" y="393.58167174">0.79</text><text class="centered" x="371.372668971" y="413.75848843">0.66</text><text class="centered" x="377.064596743" y="475.712956864">0.38</text><text class="centered" x="382.756524515" y="315.795022591">1.58</text><text class="centered" x="388.448452287" y="280.18659054">2.17</text><text class="centered" x="394.140380059" y="172.00608317">5.69</text><text class="centered" x="399.832307832" y="322.376724262">1.49</text><text class="centered" x="405.524235604" y="306.267402149">1.72</text><text class="centered" x="411.216163376" y="308.908051347">1.68</text><text class="centered" x="416.908091148" y="361.652992728">1.05</text><text class="centered" x="422.60001892" y="301.790700949">1.79</text><text class="centered" x="428.291946692" y="256.079642195">2.69</text><text class="centered" x="433.983874464" y="186.513336613">5.0</text><text class="centered" x="439.675802236" y="396.459327178">0.77</text><text class="centered" x="445.367730008" y="400.919082752">0.74</text><text class="centered" x="451.059657781" y="402.445941856">0.73</text><text class="centered" x="456.751585553" y="389.398985881">0.82</text><text class="centered" x="462.443513325" y="442.692693211">0.51</text><text class="centered" x="468.135441097" y="296.28550064">1.88</text><text class="centered" x="473.827368869" y="456.738803129">0.45</text><text class="centered" x="479.519296641" y="459.260761239">0.44</text><text class="centered" x="485.211224413" y="459.260761239">0.44</text><text class="centered" x="490.903152185" y="440.513548761">0.52</text><text class="centered" x="496.595079957" y="370.546548442">0.97</text><text class="centered" x="502.287007729" y="289.341693531">2.0</text><text class="centered" x="507.978935502" y="428.258940722">0.58</text><text class="centered" x="513.670863274" y="393.58167174">0.79</text><text class="centered" x="519.362791046" y="355.416809943">1.11</text><text class="centered" x="525.054718818" y="444.91499183">0.5</text><text class="centered" x="530.74664659" y="384.054051298">0.86</text><text class="centered" x="536.438574362" y="389.398985881">0.82</text><text class="centered" x="542.130502134" y="339.425109068">1.28</text><text class="centered" x="547.822429906" y="303.051640203">1.77</text><text class="centered" x="553.514357678" y="336.825245774">1.31</text><text class="centered" x="559.20628545" y="348.553913012">1.18</text><text class="centered" x="564.898213223" y="413.75848843">0.66</text><text class="centered" x="570.590140995" y="390.775965279">0.81</text><text class="centered" x="576.282068767" y="347.60688281">1.19</text><text class="centered" x="581.973996539" y="308.908051347">1.68</text><text class="centered" x="587.665924311" y="357.457273664">1.09</text><text class="centered" x="593.357852083" y="327.776782422">1.42</text><text class="centered" x="599.049779855" y="320.139658565">1.52</text><text class="centered" x="604.741707627" y="145.748179334">7.19</text><text class="centered" x="610.433635399" y="89.4884984541">11.87</text><text class="centered" x="616.125563172" y="246.106617286">2.94</text><text class="centered" x="621.817490944" y="408.770004374">0.69</text><text class="centered" x="627.509418716" y="422.599468639">0.61</text><text class="centered" x="633.201346488" y="410.408316871">0.68</text><text class="centered" x="638.89327426" y="440.513548761">0.52</text><text class="centered" x="644.585202032" y="142.517427535">7.4</text><text class="centered" x="650.277129804" y="187.188699211">4.97</text><text class="centered" x="655.969057576" y="442.692693211">0.51</text><text class="centered" x="661.660985348" y="461.840700448">0.43</text><text class="centered" x="667.35291312" y="472.797925101">0.39</text><text class="centered" x="673.044840893" y="444.91499183">0.5</text><text class="centered" x="678.736768665" y="475.712956864">0.38</text><text class="centered" x="684.428696437" y="185.619129388">5.04</text><text class="centered" x="690.120624209" y="472.797925101">0.39</text><text class="centered" x="695.812551981" y="472.797925101">0.39</text><text class="centered" x="701.504479753" y="478.705731901">0.37</text><text class="centered" x="707.196407525" y="478.705731901">0.37</text><text class="centered" x="712.888335297" y="488.194966021">0.34</text><text class="centered" x="718.580263069" y="459.260761239">0.44</text><text class="centered" x="724.272190841" y="475.712956864">0.38</text><text class="centered" x="729.964118614" y="469.956699598">0.4</text><text class="centered" x="735.656046386" y="467.185635031">0.41</text><text class="centered" x="741.347974158" y="464.481349646">0.42</text><text class="centered" x="747.03990193" y="469.956699598">0.4</text><text class="centered" x="752.731829702" y="467.185635031">0.41</text><text class="centered" x="758.423757474" y="464.481349646">0.42</text><text class="centered" x="764.115685246" y="472.797925101">0.39</text><text class="centered" x="769.807613018" y="456.738803129">0.45</text><text class="centered" x="775.49954079" y="456.738803129">0.45</text><text class="centered" x="781.191468563" y="461.840700448">0.43</text><text class="centered" x="786.883396335" y="459.260761239">0.44</text><text class="centered" x="792.575324107" y="461.840700448">0.43</text><text class="centered" x="798.267251879" y="456.738803129">0.45</text><text class="centered" x="803.959179651" y="456.738803129">0.45</text><text class="centered" x="809.651107423" y="430.210684055">0.57</text><text class="centered" x="815.343035195" y="392.170050449">0.8</text><text class="centered" x="821.034962967" y="432.196973305">0.56</text><text class="centered" x="826.726890739" y="467.185635031">0.41</text><text class="centered" x="832.418818511" y="478.705731901">0.37</text></g></g><g class="plot tooltip-overlay" transform="translate(130, 46)"><g id="tooltip" style="opacity: 0" transform="translate(0 0)"><a><rect height="0" id="tooltip-box" rx="0" ry="0" width="0"/><text class="text"><tspan class="label"/><tspan class="value"/></text></a></g></g><g class="legends" transform="translate(10, 56)"><g class="legend reactive activate-serie" id="activate-serie-0"><rect class="color-0 reactive" height="12" width="12" x="0.0" y="1.0"/><text x="17.0" y="11.2">%usr</text></g><g class="legend reactive activate-serie" id="activate-serie-1"><rect class="color-1 reactive" height="12" width="12" x="0.0" y="22.0"/><text x="17.0" y="32.2">%sys</text></g><g class="legend reactive activate-serie" id="activate-serie-2"><rect class="color-2 reactive" height="12" width="12" x="0.0" y="43.0"/><text x="17.0" y="53.2">%iowait</text></g></g><g class="legends" transform="translate(980, 56)"/></g></svg>