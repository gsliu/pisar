<?xml version='1.0' encoding='utf-8'?>
<svg xmlns:xlink="http://www.w3.org/1999/xlink" xmlns="http://www.w3.org/2000/svg" id="chart-52756699-a52a-439b-a043-e49165d62f2a" class="pygal-chart" viewBox="0 0 1000 600"><!--Generated with pygal 1.1.0 ©Kozea 2011-2013 on 2013-08-25--><!--http://pygal.org--><!--http://github.com/Kozea/pygal--><defs><style type="text/css">#chart-52756699-a52a-439b-a043-e49165d62f2a .title{font-family:monospace;font-size:16px}#chart-52756699-a52a-439b-a043-e49165d62f2a .legends .legend text{font-family:monospace;font-size:14px}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis text{font-family:monospace;font-size:8px}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis text.major{font-family:monospace;font-size:10px}#chart-52756699-a52a-439b-a043-e49165d62f2a .series text{font-family:monospace;font-size:8px}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip text{font-family:monospace;font-size:16px}#chart-52756699-a52a-439b-a043-e49165d62f2a text.no_data{font-size:64px}
#chart-52756699-a52a-439b-a043-e49165d62f2a{background-color:black}#chart-52756699-a52a-439b-a043-e49165d62f2a path,#chart-52756699-a52a-439b-a043-e49165d62f2a line,#chart-52756699-a52a-439b-a043-e49165d62f2a rect,#chart-52756699-a52a-439b-a043-e49165d62f2a circle{-webkit-transition:1s ease-out;-moz-transition:1s ease-out;transition:1s ease-out}#chart-52756699-a52a-439b-a043-e49165d62f2a .graph &gt; .background{fill:black}#chart-52756699-a52a-439b-a043-e49165d62f2a .plot &gt; .background{fill:#111}#chart-52756699-a52a-439b-a043-e49165d62f2a .graph{fill:#999}#chart-52756699-a52a-439b-a043-e49165d62f2a text.no_data{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .title{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .legends .legend text{fill:#999}#chart-52756699-a52a-439b-a043-e49165d62f2a .legends .legend:hover text{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .line{stroke:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .guide.line{stroke:#555}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .major.line{stroke:#999}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis text.major{stroke:#eee;fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .line-graph .axis.x .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .stackedline-graph .axis.x .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .xy-graph .axis.x .guides:hover .guide.line{stroke:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .guides:hover text{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .reactive{fill-opacity:.1}#chart-52756699-a52a-439b-a043-e49165d62f2a .reactive.active,#chart-52756699-a52a-439b-a043-e49165d62f2a .active .reactive{fill-opacity:.75}#chart-52756699-a52a-439b-a043-e49165d62f2a .series text{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip rect{fill:#111;stroke:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip text{fill:#eee}#chart-52756699-a52a-439b-a043-e49165d62f2a .country{fill:#999;stroke:#111 !important;opacity:.9;stroke-width:3;-webkit-transition:250ms;-moz-transition:250ms;-o-transition:250ms;transition:250ms}#chart-52756699-a52a-439b-a043-e49165d62f2a .country:hover{opacity:1;stroke-width:10}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-0{stroke:#ff5995;fill:#ff5995}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-1{stroke:#b6e354;fill:#b6e354}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-2{stroke:#feed6c;fill:#feed6c}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-3{stroke:#8cedff;fill:#8cedff}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-4{stroke:#9e6ffe;fill:#9e6ffe}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-5{stroke:#899ca1;fill:#899ca1}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-6{stroke:#f8f8f2;fill:#f8f8f2}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-7{stroke:#bf4646;fill:#bf4646}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-8{stroke:#516083;fill:#516083}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-9{stroke:#f92672;fill:#f92672}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-10{stroke:#82b414;fill:#82b414}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-11{stroke:#fd971f;fill:#fd971f}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-12{stroke:#56c2d6;fill:#56c2d6}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-13{stroke:#808384;fill:#808384}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-14{stroke:#8c54fe;fill:#8c54fe}#chart-52756699-a52a-439b-a043-e49165d62f2a .color-15{stroke:#465457;fill:#465457}
#chart-52756699-a52a-439b-a043-e49165d62f2a text.no_data{text-anchor:middle}#chart-52756699-a52a-439b-a043-e49165d62f2a .guide.line{fill-opacity:0}#chart-52756699-a52a-439b-a043-e49165d62f2a .centered{text-anchor:middle}#chart-52756699-a52a-439b-a043-e49165d62f2a .title{text-anchor:middle}#chart-52756699-a52a-439b-a043-e49165d62f2a .legends .legend text{fill-opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.x text{text-anchor:middle}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.x:not(.web) text[transform]{text-anchor:start}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y text{text-anchor:end}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y2 text{text-anchor:start}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y .logarithmic text:not(.major),#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y2 .logarithmic text:not(.major){font-size:50%}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .guide.line{stroke-dasharray:4,4}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .major.guide.line{stroke-dasharray:6,6}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis text.major{stroke-width:0.5px}#chart-52756699-a52a-439b-a043-e49165d62f2a .horizontal .axis.y .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .horizontal .axis.y2 .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .vertical .axis.x .guide.line{opacity:0}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .axis.y2 .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .line-graph .axis.x .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .gauge-graph .axis.x .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .stackedline-graph .axis.x .guides:hover .guide.line,#chart-52756699-a52a-439b-a043-e49165d62f2a .xy-graph .axis.x .guides:hover .guide.line{opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a .axis .guides:hover text{opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a .nofill{fill:none}#chart-52756699-a52a-439b-a043-e49165d62f2a .dot{stroke-width:1px;fill-opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a .dot.active{stroke-width:5px}#chart-52756699-a52a-439b-a043-e49165d62f2a .series text{stroke:none}#chart-52756699-a52a-439b-a043-e49165d62f2a .series text.active{opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip rect{fill-opacity:0.8}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip text{fill-opacity:1}#chart-52756699-a52a-439b-a043-e49165d62f2a #tooltip text tspan.label{fill-opacity:.8}#chart-52756699-a52a-439b-a043-e49165d62f2a a:visited{fill:none}</style><script type="text/javascript">window.config = {"truncate_legend": null, "print_values": true, "tooltip_font_size": 16, "y_title": null, "height": 600, "legend_at_bottom": false, "show_legend": true, "show_dots": true, "explicit_size": false, "legend_font_size": 14, "x_labels_major_count": null, "major_label_font_size": 10, "fill": true, "style": {"opacity": ".1", "foreground": "#999", "plot_background": "#111", "foreground_light": "#eee", "transition": "1s ease-out", "foreground_dark": "#555", "colors": ["#ff5995", "#b6e354", "#feed6c", "#8cedff", "#9e6ffe", "#899ca1", "#f8f8f2", "#bf4646", "#516083", "#f92672", "#82b414", "#fd971f", "#56c2d6", "#808384", "#8c54fe", "#465457"], "background": "black", "opacity_hover": ".75"}, "x_title": null, "zero": 0, "show_minor_x_labels": false, "interpolation_parameters": {}, "x_labels_major": ["00:10:01", "01:10:01", "02:10:01", "03:10:01", "04:10:01", "05:10:01", "06:10:01", "07:10:01", "08:10:01", "09:10:01", "10:10:01", "11:10:01", "12:10:01", "13:10:01", "14:10:01", "15:10:01", "16:10:01", "17:10:02", "18:10:01", "19:10:01", "20:10:01", "21:10:01", "22:10:01", "23:10:01"], "rounded_bars": null, "label_font_size": 8, "show_y_labels": true, "dots_size": 1.5, "x_label_rotation": 40, "x_labels": ["00:10:01", "00:20:02", "00:30:01", "00:40:01", "00:50:01", "01:00:01", "01:10:01", "01:20:01", "01:30:01", "01:40:01", "01:50:01", "02:00:01", "02:10:01", "02:20:01", "02:30:01", "02:40:01", "02:50:01", "03:00:01", "03:10:01", "03:20:01", "03:30:01", "03:40:01", "03:50:01", "04:00:01", "04:10:01", "04:20:01", "04:30:01", "04:40:01", "04:50:01", "05:00:01", "05:10:01", "05:20:02", "05:30:01", "05:40:01", "05:50:01", "06:00:01", "06:10:01", "06:20:01", "06:30:01", "06:40:01", "06:50:01", "07:00:01", "07:10:01", "07:20:01", "07:30:01", "07:40:02", "07:50:01", "08:00:01", "08:10:01", "08:20:01", "08:30:01", "08:40:01", "08:50:01", "09:00:01", "09:10:01", "09:20:01", "09:30:01", "09:40:01", "09:50:01", "10:00:01", "10:10:01", "10:20:01", "10:30:01", "10:40:01", "10:50:01", "11:00:01", "11:10:01", "11:20:02", "11:30:01", "11:40:01", "11:50:01", "12:00:01", "12:10:01", "12:20:01", "12:30:01", "12:40:01", "12:50:01", "13:00:01", "13:10:01", "13:20:01", "13:30:01", "13:40:01", "13:50:01", "14:00:01", "14:10:01", "14:20:01", "14:30:01", "14:40:01", "14:50:01", "15:00:02", "15:10:01", "15:20:01", "15:30:01", "15:40:01", "15:50:01", "16:00:01", "16:10:01", "16:20:01", "16:30:01", "16:40:01", "16:50:01", "17:00:01", "17:10:02", "17:20:03", "17:30:01", "17:40:01", "17:50:01", "18:00:01", "18:10:01", "18:20:01", "18:30:01", "18:40:01", "18:50:01", "19:00:01", "19:10:01", "19:20:01", "19:30:01", "19:40:01", "19:50:01", "20:00:01", "20:10:01", "20:20:01", "20:30:01", "20:40:01", "20:50:01", "21:00:01", "21:10:01", "21:20:01", "21:30:01", "21:40:01", "21:50:01", "22:00:01", "22:10:01", "22:20:01", "22:30:01", "22:40:01", "22:50:01", "23:00:01", "23:10:01", "23:20:01", "23:30:01", "23:40:01", "23:50:01"], "value_font_size": 8, "disable_xml_declaration": false, "strict": false, "css": ["style.css", "graph.css"], "human_readable": false, "spacing": 10, "y_label_rotation": 0, "pretty_print": false, "tooltip_border_radius": 0, "interpolate": null, "js": ["./svg.jquery.js", "./pygal-tooltips.js"], "y_labels": null, "interpolation_precision": 250, "value_formatter": null, "no_data_font_size": 64, "logarithmic": true, "print_zeroes": false, "no_data_text": "No data", "x_labels_major_every": null, "title": "Load", "legend_box_size": 12, "no_prefix": false, "stroke": true, "include_x_axis": false, "range": null, "truncate_label": null, "order_min": null, "title_font_size": 16, "margin": 20, "width": 1000}</script><script type="text/javascript">/*!
 * jQuery JavaScript Library v1.7.2 modified by Florian Mounier to work with SVG
 * http://jquery.com/
 *
 * Copyright 2011, John Resig
 * Dual licensed under the MIT or GPL Version 2 licenses.
 * http://jquery.org/license
 *
 * Includes Sizzle.js
 * http://sizzlejs.com/
 * Copyright 2011, The Dojo Foundation
 * Released under the MIT, BSD, and GPL Licenses.
 *
 * Date: Wed Mar 21 12:46:34 2012 -0700
 */
(function( window, undefined ) {

// Use the correct document accordingly with window argument (sandbox)
var document = window.document,
	navigator = window.navigator,
	location = window.location;
var jQuery = (function() {

// Define a local copy of jQuery
var jQuery = function( selector, context ) {
		// The jQuery object is actually just the init constructor 'enhanced'
		return new jQuery.fn.init( selector, context, rootjQuery );
	},

	// Map over jQuery in case of overwrite
	_jQuery = window.jQuery,

	// Map over the $ in case of overwrite
	_$ = window.$,

	// A central reference to the root jQuery(document)
	rootjQuery,

	// A simple way to check for HTML strings or ID strings
	// Prioritize #id over &lt;tag&gt; to avoid XSS via location.hash (#9521)
	quickExpr = /^(?:[^#&lt;]*(&lt;[\w\W]+&gt;)[^&gt;]*$|#([\w\-]*)$)/,

	// Check if a string has a non-whitespace character in it
	rnotwhite = /\S/,

	// Used for trimming whitespace
	trimLeft = /^\s+/,
	trimRight = /\s+$/,

	// Match a standalone tag
	rsingleTag = /^&lt;(\w+)\s*\/?&gt;(?:&lt;\/\1&gt;)?$/,

	// JSON RegExp
	rvalidchars = /^[\],:{}\s]*$/,
	rvalidescape = /\\(?:["\\\/bfnrt]|u[0-9a-fA-F]{4})/g,
	rvalidtokens = /"[^"\\\n\r]*"|true|false|null|-?\d+(?:\.\d*)?(?:[eE][+\-]?\d+)?/g,
	rvalidbraces = /(?:^|:|,)(?:\s*\[)+/g,

	// Useragent RegExp
	rwebkit = /(webkit)[ \/]([\w.]+)/,
	ropera = /(opera)(?:.*version)?[ \/]([\w.]+)/,
	rmsie = /(msie) ([\w.]+)/,
	rmozilla = /(mozilla)(?:.*? rv:([\w.]+))?/,

	// Matches dashed string for camelizing
	rdashAlpha = /-([a-z]|[0-9])/ig,
	rmsPrefix = /^-ms-/,

	// Used by jQuery.camelCase as callback to replace()
	fcamelCase = function( all, letter ) {
		return ( letter + "" ).toUpperCase();
	},

	// Keep a UserAgent string for use with jQuery.browser
	userAgent = navigator.userAgent,

	// For matching the engine and version of the browser
	browserMatch,

	// The deferred used on DOM ready
	readyList,

	// The ready event handler
	DOMContentLoaded,

	// Save a reference to some core methods
	toString = Object.prototype.toString,
	hasOwn = Object.prototype.hasOwnProperty,
	push = Array.prototype.push,
	slice = Array.prototype.slice,
	trim = String.prototype.trim,
	indexOf = Array.prototype.indexOf,

	// [[Class]] -&gt; type pairs
	class2type = {};

jQuery.fn = jQuery.prototype = {
	constructor: jQuery,
	init: function( selector, context, rootjQuery ) {
		var match, elem, ret, doc;

		// Handle $(""), $(null), or $(undefined)
		if ( !selector ) {
			return this;
		}

		// Handle $(DOMElement)
		if ( selector.nodeType ) {
			this.context = this[0] = selector;
			this.length = 1;
			return this;
		}

		// The body element only exists once, optimize finding it
		if ( selector === "body" &amp;&amp; !context &amp;&amp; document.body ) {
			this.context = document;
			this[0] = document.body;
			this.selector = selector;
			this.length = 1;
			return this;
		}

		// Handle HTML strings
		if ( typeof selector === "string" ) {
			// Are we dealing with HTML string or an ID?
			if ( selector.charAt(0) === "&lt;" &amp;&amp; selector.charAt( selector.length - 1 ) === "&gt;" &amp;&amp; selector.length &gt;= 3 ) {
				// Assume that strings that start and end with &lt;&gt; are HTML and skip the regex check
				match = [ null, selector, null ];

			} else {
				match = quickExpr.exec( selector );
			}

			// Verify a match, and that no context was specified for #id
			if ( match &amp;&amp; (match[1] || !context) ) {

				// HANDLE: $(html) -&gt; $(array)
				if ( match[1] ) {
					context = context instanceof jQuery ? context[0] : context;
					doc = ( context ? context.ownerDocument || context : document );

					// If a single string is passed in and it's a single tag
					// just do a createElement and skip the rest
					ret = rsingleTag.exec( selector );

					if ( ret ) {
						if ( jQuery.isPlainObject( context ) ) {
							selector = [ document.createElementNS( 'http://www.w3.org/2000/svg', ret[1] ) ];
							jQuery.fn.attr.call( selector, context, true );

						} else {
							selector = [ doc.createElementNS( 'http://www.w3.org/2000/svg', ret[1] ) ];
						}

					} else {
						ret = jQuery.buildFragment( [ match[1] ], [ doc ] );
						selector = ( ret.cacheable ? jQuery.clone(ret.fragment) : ret.fragment ).childNodes;
					}

					return jQuery.merge( this, selector );

				// HANDLE: $("#id")
				} else {
					elem = document.getElementById( match[2] );

					// Check parentNode to catch when Blackberry 4.6 returns
					// nodes that are no longer in the document #6963
					if ( elem &amp;&amp; elem.parentNode ) {
						// Handle the case where IE and Opera return items
						// by name instead of ID
						if ( elem.id !== match[2] ) {
							return rootjQuery.find( selector );
						}

						// Otherwise, we inject the element directly into the jQuery object
						this.length = 1;
						this[0] = elem;
					}

					this.context = document;
					this.selector = selector;
					return this;
				}

			// HANDLE: $(expr, $(...))
			} else if ( !context || context.jquery ) {
				return ( context || rootjQuery ).find( selector );

			// HANDLE: $(expr, context)
			// (which is just equivalent to: $(context).find(expr)
			} else {
				return this.constructor( context ).find( selector );
			}

		// HANDLE: $(function)
		// Shortcut for document ready
		} else if ( jQuery.isFunction( selector ) ) {
			return rootjQuery.ready( selector );
		}

		if ( selector.selector !== undefined ) {
			this.selector = selector.selector;
			this.context = selector.context;
		}

		return jQuery.makeArray( selector, this );
	},

	// Start with an empty selector
	selector: "",

	// The current version of jQuery being used
	jquery: "1.7.2",

	// The default length of a jQuery object is 0
	length: 0,

	// The number of elements contained in the matched element set
	size: function() {
		return this.length;
	},

	toArray: function() {
		return slice.call( this, 0 );
	},

	// Get the Nth element in the matched element set OR
	// Get the whole matched element set as a clean array
	get: function( num ) {
		return num == null ?

			// Return a 'clean' array
			this.toArray() :

			// Return just the object
			( num &lt; 0 ? this[ this.length + num ] : this[ num ] );
	},

	// Take an array of elements and push it onto the stack
	// (returning the new matched element set)
	pushStack: function( elems, name, selector ) {
		// Build a new jQuery matched element set
		var ret = this.constructor();

		if ( jQuery.isArray( elems ) ) {
			push.apply( ret, elems );

		} else {
			jQuery.merge( ret, elems );
		}

		// Add the old object onto the stack (as a reference)
		ret.prevObject = this;

		ret.context = this.context;

		if ( name === "find" ) {
			ret.selector = this.selector + ( this.selector ? " " : "" ) + selector;
		} else if ( name ) {
			ret.selector = this.selector + "." + name + "(" + selector + ")";
		}

		// Return the newly-formed element set
		return ret;
	},

	// Execute a callback for every element in the matched set.
	// (You can seed the arguments with an array of args, but this is
	// only used internally.)
	each: function( callback, args ) {
		return jQuery.each( this, callback, args );
	},

	ready: function( fn ) {
		// Attach the listeners
		jQuery.bindReady();

		// Add the callback
		readyList.add( fn );

		return this;
	},

	eq: function( i ) {
		i = +i;
		return i === -1 ?
			this.slice( i ) :
			this.slice( i, i + 1 );
	},

	first: function() {
		return this.eq( 0 );
	},

	last: function() {
		return this.eq( -1 );
	},

	slice: function() {
		return this.pushStack( slice.apply( this, arguments ),
			"slice", slice.call(arguments).join(",") );
	},

	map: function( callback ) {
		return this.pushStack( jQuery.map(this, function( elem, i ) {
			return callback.call( elem, i, elem );
		}));
	},

	end: function() {
		return this.prevObject || this.constructor(null);
	},

	// For internal use only.
	// Behaves like an Array's method, not like a jQuery method.
	push: push,
	sort: [].sort,
	splice: [].splice
};

// Give the init function the jQuery prototype for later instantiation
jQuery.fn.init.prototype = jQuery.fn;

jQuery.extend = jQuery.fn.extend = function() {
	var options, name, src, copy, copyIsArray, clone,
		target = arguments[0] || {},
		i = 1,
		length = arguments.length,
		deep = false;

	// Handle a deep copy situation
	if ( typeof target === "boolean" ) {
		deep = target;
		target = arguments[1] || {};
		// skip the boolean and the target
		i = 2;
	}

	// Handle case when target is a string or something (possible in deep copy)
	if ( typeof target !== "object" &amp;&amp; !jQuery.isFunction(target) ) {
		target = {};
	}

	// extend jQuery itself if only one argument is passed
	if ( length === i ) {
		target = this;
		--i;
	}

	for ( ; i &lt; length; i++ ) {
		// Only deal with non-null/undefined values
		if ( (options = arguments[ i ]) != null ) {
			// Extend the base object
			for ( name in options ) {
				src = target[ name ];
				copy = options[ name ];

				// Prevent never-ending loop
				if ( target === copy ) {
					continue;
				}

				// Recurse if we're merging plain objects or arrays
				if ( deep &amp;&amp; copy &amp;&amp; ( jQuery.isPlainObject(copy) || (copyIsArray = jQuery.isArray(copy)) ) ) {
					if ( copyIsArray ) {
						copyIsArray = false;
						clone = src &amp;&amp; jQuery.isArray(src) ? src : [];

					} else {
						clone = src &amp;&amp; jQuery.isPlainObject(src) ? src : {};
					}

					// Never move original objects, clone them
					target[ name ] = jQuery.extend( deep, clone, copy );

				// Don't bring in undefined values
				} else if ( copy !== undefined ) {
					target[ name ] = copy;
				}
			}
		}
	}

	// Return the modified object
	return target;
};

jQuery.extend({
	noConflict: function( deep ) {
		if ( window.$ === jQuery ) {
			window.$ = _$;
		}

		if ( deep &amp;&amp; window.jQuery === jQuery ) {
			window.jQuery = _jQuery;
		}

		return jQuery;
	},

	// Is the DOM ready to be used? Set to true once it occurs.
	isReady: false,

	// A counter to track how many items to wait for before
	// the ready event fires. See #6781
	readyWait: 1,

	// Hold (or release) the ready event
	holdReady: function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	},

	// Handle when the DOM is ready
	ready: function( wait ) {
		// Either a released hold or an DOMready/load event and not yet ready
		if ( (wait === true &amp;&amp; !--jQuery.readyWait) || (wait !== true &amp;&amp; !jQuery.isReady) ) {
			// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).
		  //    if ( !document.body ) {
		  //    	return setTimeout( jQuery.ready, 1 );
		  //    }

			// Remember that the DOM is ready
			jQuery.isReady = true;

			// If a normal DOM Ready event fired, decrement, and wait if need be
			if ( wait !== true &amp;&amp; --jQuery.readyWait &gt; 0 ) {
				return;
			}

			// If there are functions bound, to execute
			readyList.fireWith( document, [ jQuery ] );

			// Trigger any bound ready events
			if ( jQuery.fn.trigger ) {
				jQuery( document ).trigger( "ready" ).off( "ready" );
			}
		}
	},

	bindReady: function() {
		if ( readyList ) {
			return;
		}

		readyList = jQuery.Callbacks( "once memory" );

		// Catch cases where $(document).ready() is called after the
		// browser event has already occurred.
		if ( document.readyState === "complete" ) {
			// Handle it asynchronously to allow scripts the opportunity to delay ready
			return setTimeout( jQuery.ready, 1 );
		}

		// Mozilla, Opera and webkit nightlies currently support this event
		if ( document.addEventListener ) {
			// Use the handy event callback
			document.addEventListener( "DOMContentLoaded", DOMContentLoaded, false );

			// A fallback to window.onload, that will always work
			window.addEventListener( "load", jQuery.ready, false );

		// If IE event model is used
		} else if ( document.attachEvent ) {
			// ensure firing before onload,
			// maybe late but safe also for iframes
			document.attachEvent( "onreadystatechange", DOMContentLoaded );

			// A fallback to window.onload, that will always work
			window.attachEvent( "onload", jQuery.ready );

			// If IE and not a frame
			// continually check to see if the document is ready
			var toplevel = false;

			try {
				toplevel = window.frameElement == null;
			} catch(e) {}

			if ( document.documentElement.doScroll &amp;&amp; toplevel ) {
				doScrollCheck();
			}
		}
	},

	// See test/unit/core.js for details concerning isFunction.
	// Since version 1.3, DOM methods and functions like alert
	// aren't supported. They return false on IE (#2968).
	isFunction: function( obj ) {
		return jQuery.type(obj) === "function";
	},

	isArray: Array.isArray || function( obj ) {
		return jQuery.type(obj) === "array";
	},

	isWindow: function( obj ) {
		return obj != null &amp;&amp; obj == obj.window;
	},

	isNumeric: function( obj ) {
		return !isNaN( parseFloat(obj) ) &amp;&amp; isFinite( obj );
	},

	type: function( obj ) {
		return obj == null ?
			String( obj ) :
			class2type[ toString.call(obj) ] || "object";
	},

	isPlainObject: function( obj ) {
		// Must be an Object.
		// Because of IE, we also have to check the presence of the constructor property.
		// Make sure that DOM nodes and window objects don't pass through, as well
		if ( !obj || jQuery.type(obj) !== "object" || obj.nodeType || jQuery.isWindow( obj ) ) {
			return false;
		}

		try {
			// Not own constructor property must be Object
			if ( obj.constructor &amp;&amp;
				!hasOwn.call(obj, "constructor") &amp;&amp;
				!hasOwn.call(obj.constructor.prototype, "isPrototypeOf") ) {
				return false;
			}
		} catch ( e ) {
			// IE8,9 Will throw exceptions on certain host objects #9897
			return false;
		}

		// Own properties are enumerated firstly, so to speed up,
		// if last one is own, then all properties are own.

		var key;
		for ( key in obj ) {}

		return key === undefined || hasOwn.call( obj, key );
	},

	isEmptyObject: function( obj ) {
		for ( var name in obj ) {
			return false;
		}
		return true;
	},

	error: function( msg ) {
		throw new Error( msg );
	},

	parseJSON: function( data ) {
		if ( typeof data !== "string" || !data ) {
			return null;
		}

		// Make sure leading/trailing whitespace is removed (IE can't handle it)
		data = jQuery.trim( data );

		// Attempt to parse using the native JSON parser first
		if ( window.JSON &amp;&amp; window.JSON.parse ) {
			return window.JSON.parse( data );
		}

		// Make sure the incoming data is actual JSON
		// Logic borrowed from http://json.org/json2.js
		if ( rvalidchars.test( data.replace( rvalidescape, "@" )
			.replace( rvalidtokens, "]" )
			.replace( rvalidbraces, "")) ) {

			return ( new Function( "return " + data ) )();

		}
		jQuery.error( "Invalid JSON: " + data );
	},

	// Cross-browser xml parsing
	parseXML: function( data ) {
		if ( typeof data !== "string" || !data ) {
			return null;
		}
		var xml, tmp;
		try {
			if ( window.DOMParser ) { // Standard
				tmp = new DOMParser();
				xml = tmp.parseFromString( data , "text/xml" );
			} else { // IE
				xml = new ActiveXObject( "Microsoft.XMLDOM" );
				xml.async = "false";
				xml.loadXML( data );
			}
		} catch( e ) {
			xml = undefined;
		}
		if ( !xml || !xml.documentElement || xml.getElementsByTagName( "parsererror" ).length ) {
			jQuery.error( "Invalid XML: " + data );
		}
		return xml;
	},

	noop: function() {},

	// Evaluates a script in a global context
	// Workarounds based on findings by Jim Driscoll
	// http://weblogs.java.net/blog/driscoll/archive/2009/09/08/eval-javascript-global-context
	globalEval: function( data ) {
		if ( data &amp;&amp; rnotwhite.test( data ) ) {
			// We use execScript on Internet Explorer
			// We use an anonymous function so that context is window
			// rather than jQuery in Firefox
			( window.execScript || function( data ) {
				window[ "eval" ].call( window, data );
			} )( data );
		}
	},

	// Convert dashed to camelCase; used by the css and data modules
	// Microsoft forgot to hump their vendor prefix (#9572)
	camelCase: function( string ) {
		return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
	},

	nodeName: function( elem, name ) {
		return elem.nodeName &amp;&amp; elem.nodeName.toUpperCase() === name.toUpperCase();
	},

	// args is for internal usage only
	each: function( object, callback, args ) {
		var name, i = 0,
			length = object.length,
			isObj = length === undefined || jQuery.isFunction( object );

		if ( args ) {
			if ( isObj ) {
				for ( name in object ) {
					if ( callback.apply( object[ name ], args ) === false ) {
						break;
					}
				}
			} else {
				for ( ; i &lt; length; ) {
					if ( callback.apply( object[ i++ ], args ) === false ) {
						break;
					}
				}
			}

		// A special, fast, case for the most common use of each
		} else {
			if ( isObj ) {
				for ( name in object ) {
					if ( callback.call( object[ name ], name, object[ name ] ) === false ) {
						break;
					}
				}
			} else {
				for ( ; i &lt; length; ) {
					if ( callback.call( object[ i ], i, object[ i++ ] ) === false ) {
						break;
					}
				}
			}
		}

		return object;
	},

	// Use native String.trim function wherever possible
	trim: trim ?
		function( text ) {
			return text == null ?
				"" :
				trim.call( text );
		} :

		// Otherwise use our own trimming functionality
		function( text ) {
			return text == null ?
				"" :
				text.toString().replace( trimLeft, "" ).replace( trimRight, "" );
		},

	// results is for internal usage only
	makeArray: function( array, results ) {
		var ret = results || [];

		if ( array != null ) {
			// The window, strings (and functions) also have 'length'
			// Tweaked logic slightly to handle Blackberry 4.7 RegExp issues #6930
			var type = jQuery.type( array );

			if ( array.length == null || type === "string" || type === "function" || type === "regexp" || jQuery.isWindow( array ) ) {
				push.call( ret, array );
			} else {
				jQuery.merge( ret, array );
			}
		}

		return ret;
	},

	inArray: function( elem, array, i ) {
		var len;

		if ( array ) {
			if ( indexOf ) {
				return indexOf.call( array, elem, i );
			}

			len = array.length;
			i = i ? i &lt; 0 ? Math.max( 0, len + i ) : i : 0;

			for ( ; i &lt; len; i++ ) {
				// Skip accessing in sparse arrays
				if ( i in array &amp;&amp; array[ i ] === elem ) {
					return i;
				}
			}
		}

		return -1;
	},

	merge: function( first, second ) {
		var i = first.length,
			j = 0;

		if ( typeof second.length === "number" ) {
			for ( var l = second.length; j &lt; l; j++ ) {
				first[ i++ ] = second[ j ];
			}

		} else {
			while ( second[j] !== undefined ) {
				first[ i++ ] = second[ j++ ];
			}
		}

		first.length = i;

		return first;
	},

	grep: function( elems, callback, inv ) {
		var ret = [], retVal;
		inv = !!inv;

		// Go through the array, only saving the items
		// that pass the validator function
		for ( var i = 0, length = elems.length; i &lt; length; i++ ) {
			retVal = !!callback( elems[ i ], i );
			if ( inv !== retVal ) {
				ret.push( elems[ i ] );
			}
		}

		return ret;
	},

	// arg is for internal usage only
	map: function( elems, callback, arg ) {
		var value, key, ret = [],
			i = 0,
			length = elems.length,
			// jquery objects are treated as arrays
			isArray = elems instanceof jQuery || length !== undefined &amp;&amp; typeof length === "number" &amp;&amp; ( ( length &gt; 0 &amp;&amp; elems[ 0 ] &amp;&amp; elems[ length -1 ] ) || length === 0 || jQuery.isArray( elems ) ) ;

		// Go through the array, translating each of the items to their
		if ( isArray ) {
			for ( ; i &lt; length; i++ ) {
				value = callback( elems[ i ], i, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}

		// Go through every key on the object,
		} else {
			for ( key in elems ) {
				value = callback( elems[ key ], key, arg );

				if ( value != null ) {
					ret[ ret.length ] = value;
				}
			}
		}

		// Flatten any nested arrays
		return ret.concat.apply( [], ret );
	},

	// A global GUID counter for objects
	guid: 1,

	// Bind a function to a context, optionally partially applying any
	// arguments.
	proxy: function( fn, context ) {
		if ( typeof context === "string" ) {
			var tmp = fn[ context ];
			context = fn;
			fn = tmp;
		}

		// Quick check to determine if target is callable, in the spec
		// this throws a TypeError, but we will just return undefined.
		if ( !jQuery.isFunction( fn ) ) {
			return undefined;
		}

		// Simulated bind
		var args = slice.call( arguments, 2 ),
			proxy = function() {
				return fn.apply( context, args.concat( slice.call( arguments ) ) );
			};

		// Set the guid of unique handler to the same of original handler, so it can be removed
		proxy.guid = fn.guid = fn.guid || proxy.guid || jQuery.guid++;

		return proxy;
	},

	// Mutifunctional method to get and set values to a collection
	// The value/s can optionally be executed if it's a function
	access: function( elems, fn, key, value, chainable, emptyGet, pass ) {
		var exec,
			bulk = key == null,
			i = 0,
			length = elems.length;

		// Sets many values
		if ( key &amp;&amp; typeof key === "object" ) {
			for ( i in key ) {
				jQuery.access( elems, fn, i, key[i], 1, emptyGet, value );
			}
			chainable = 1;

		// Sets one value
		} else if ( value !== undefined ) {
			// Optionally, function values get executed if exec is true
			exec = pass === undefined &amp;&amp; jQuery.isFunction( value );

			if ( bulk ) {
				// Bulk operations only iterate when executing function values
				if ( exec ) {
					exec = fn;
					fn = function( elem, key, value ) {
						return exec.call( jQuery( elem ), value );
					};

				// Otherwise they run against the entire set
				} else {
					fn.call( elems, value );
					fn = null;
				}
			}

			if ( fn ) {
				for (; i &lt; length; i++ ) {
					fn( elems[i], key, exec ? value.call( elems[i], i, fn( elems[i], key ) ) : value, pass );
				}
			}

			chainable = 1;
		}

		return chainable ?
			elems :

			// Gets
			bulk ?
				fn.call( elems ) :
				length ? fn( elems[0], key ) : emptyGet;
	},

	now: function() {
		return ( new Date() ).getTime();
	},

	// Use of jQuery.browser is frowned upon.
	// More details: http://docs.jquery.com/Utilities/jQuery.browser
	uaMatch: function( ua ) {
		ua = ua.toLowerCase();

		var match = rwebkit.exec( ua ) ||
			ropera.exec( ua ) ||
			rmsie.exec( ua ) ||
			ua.indexOf("compatible") &lt; 0 &amp;&amp; rmozilla.exec( ua ) ||
			[];

		return { browser: match[1] || "", version: match[2] || "0" };
	},

	sub: function() {
		function jQuerySub( selector, context ) {
			return new jQuerySub.fn.init( selector, context );
		}
		jQuery.extend( true, jQuerySub, this );
		jQuerySub.superclass = this;
		jQuerySub.fn = jQuerySub.prototype = this();
		jQuerySub.fn.constructor = jQuerySub;
		jQuerySub.sub = this.sub;
		jQuerySub.fn.init = function init( selector, context ) {
			if ( context &amp;&amp; context instanceof jQuery &amp;&amp; !(context instanceof jQuerySub) ) {
				context = jQuerySub( context );
			}

			return jQuery.fn.init.call( this, selector, context, rootjQuerySub );
		};
		jQuerySub.fn.init.prototype = jQuerySub.fn;
		var rootjQuerySub = jQuerySub(document);
		return jQuerySub;
	},

	browser: {}
});

// Populate the class2type map
jQuery.each("Boolean Number String Function Array Date RegExp Object".split(" "), function(i, name) {
	class2type[ "[object " + name + "]" ] = name.toLowerCase();
});

browserMatch = jQuery.uaMatch( userAgent );
if ( browserMatch.browser ) {
	jQuery.browser[ browserMatch.browser ] = true;
	jQuery.browser.version = browserMatch.version;
}

// Deprecated, use jQuery.browser.webkit instead
if ( jQuery.browser.webkit ) {
	jQuery.browser.safari = true;
}

// IE doesn't match non-breaking spaces with \s
if ( rnotwhite.test( "\xA0" ) ) {
	trimLeft = /^[\s\xA0]+/;
	trimRight = /[\s\xA0]+$/;
}

// All jQuery objects should point back to these
rootjQuery = jQuery(document);

// Cleanup functions for the document ready method
if ( document.addEventListener ) {
	DOMContentLoaded = function() {
		document.removeEventListener( "DOMContentLoaded", DOMContentLoaded, false );
		jQuery.ready();
	};

} else if ( document.attachEvent ) {
	DOMContentLoaded = function() {
		// Make sure body exists, at least, in case IE gets a little overzealous (ticket #5443).
		if ( document.readyState === "complete" ) {
			document.detachEvent( "onreadystatechange", DOMContentLoaded );
			jQuery.ready();
		}
	};
}

// The DOM ready check for Internet Explorer
function doScrollCheck() {
	if ( jQuery.isReady ) {
		return;
	}

	try {
		// If IE is used, use the trick by Diego Perini
		// http://javascript.nwbox.com/IEContentLoaded/
		document.documentElement.doScroll("left");
	} catch(e) {
		setTimeout( doScrollCheck, 1 );
		return;
	}

	// and execute any waiting functions
	jQuery.ready();
}

return jQuery;

})();


// String to Object flags format cache
var flagsCache = {};

// Convert String-formatted flags into Object-formatted ones and store in cache
function createFlags( flags ) {
	var object = flagsCache[ flags ] = {},
		i, length;
	flags = flags.split( /\s+/ );
	for ( i = 0, length = flags.length; i &lt; length; i++ ) {
		object[ flags[i] ] = true;
	}
	return object;
}

/*
 * Create a callback list using the following parameters:
 *
 *	flags:	an optional list of space-separated flags that will change how
 *			the callback list behaves
 *
 * By default a callback list will act like an event callback list and can be
 * "fired" multiple times.
 *
 * Possible flags:
 *
 *	once:			will ensure the callback list can only be fired once (like a Deferred)
 *
 *	memory:			will keep track of previous values and will call any callback added
 *					after the list has been fired right away with the latest "memorized"
 *					values (like a Deferred)
 *
 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
 *
 *	stopOnFalse:	interrupt callings when a callback returns false
 *
 */
jQuery.Callbacks = function( flags ) {

	// Convert flags from String-formatted to Object-formatted
	// (we check in cache first)
	flags = flags ? ( flagsCache[ flags ] || createFlags( flags ) ) : {};

	var // Actual callback list
		list = [],
		// Stack of fire calls for repeatable lists
		stack = [],
		// Last fire value (for non-forgettable lists)
		memory,
		// Flag to know if list was already fired
		fired,
		// Flag to know if list is currently firing
		firing,
		// First callback to fire (used internally by add and fireWith)
		firingStart,
		// End of the loop when firing
		firingLength,
		// Index of currently firing callback (modified by remove if needed)
		firingIndex,
		// Add one or several callbacks to the list
		add = function( args ) {
			var i,
				length,
				elem,
				type,
				actual;
			for ( i = 0, length = args.length; i &lt; length; i++ ) {
				elem = args[ i ];
				type = jQuery.type( elem );
				if ( type === "array" ) {
					// Inspect recursively
					add( elem );
				} else if ( type === "function" ) {
					// Add if not in unique mode and callback is not in
					if ( !flags.unique || !self.has( elem ) ) {
						list.push( elem );
					}
				}
			}
		},
		// Fire callbacks
		fire = function( context, args ) {
			args = args || [];
			memory = !flags.memory || [ context, args ];
			fired = true;
			firing = true;
			firingIndex = firingStart || 0;
			firingStart = 0;
			firingLength = list.length;
			for ( ; list &amp;&amp; firingIndex &lt; firingLength; firingIndex++ ) {
				if ( list[ firingIndex ].apply( context, args ) === false &amp;&amp; flags.stopOnFalse ) {
					memory = true; // Mark as halted
					break;
				}
			}
			firing = false;
			if ( list ) {
				if ( !flags.once ) {
					if ( stack &amp;&amp; stack.length ) {
						memory = stack.shift();
						self.fireWith( memory[ 0 ], memory[ 1 ] );
					}
				} else if ( memory === true ) {
					self.disable();
				} else {
					list = [];
				}
			}
		},
		// Actual Callbacks object
		self = {
			// Add a callback or a collection of callbacks to the list
			add: function() {
				if ( list ) {
					var length = list.length;
					add( arguments );
					// Do we need to add the callbacks to the
					// current firing batch?
					if ( firing ) {
						firingLength = list.length;
					// With memory, if we're not firing then
					// we should call right away, unless previous
					// firing was halted (stopOnFalse)
					} else if ( memory &amp;&amp; memory !== true ) {
						firingStart = length;
						fire( memory[ 0 ], memory[ 1 ] );
					}
				}
				return this;
			},
			// Remove a callback from the list
			remove: function() {
				if ( list ) {
					var args = arguments,
						argIndex = 0,
						argLength = args.length;
					for ( ; argIndex &lt; argLength ; argIndex++ ) {
						for ( var i = 0; i &lt; list.length; i++ ) {
							if ( args[ argIndex ] === list[ i ] ) {
								// Handle firingIndex and firingLength
								if ( firing ) {
									if ( i &lt;= firingLength ) {
										firingLength--;
										if ( i &lt;= firingIndex ) {
											firingIndex--;
										}
									}
								}
								// Remove the element
								list.splice( i--, 1 );
								// If we have some unicity property then
								// we only need to do this once
								if ( flags.unique ) {
									break;
								}
							}
						}
					}
				}
				return this;
			},
			// Control if a given callback is in the list
			has: function( fn ) {
				if ( list ) {
					var i = 0,
						length = list.length;
					for ( ; i &lt; length; i++ ) {
						if ( fn === list[ i ] ) {
							return true;
						}
					}
				}
				return false;
			},
			// Remove all callbacks from the list
			empty: function() {
				list = [];
				return this;
			},
			// Have the list do nothing anymore
			disable: function() {
				list = stack = memory = undefined;
				return this;
			},
			// Is it disabled?
			disabled: function() {
				return !list;
			},
			// Lock the list in its current state
			lock: function() {
				stack = undefined;
				if ( !memory || memory === true ) {
					self.disable();
				}
				return this;
			},
			// Is it locked?
			locked: function() {
				return !stack;
			},
			// Call all callbacks with the given context and arguments
			fireWith: function( context, args ) {
				if ( stack ) {
					if ( firing ) {
						if ( !flags.once ) {
							stack.push( [ context, args ] );
						}
					} else if ( !( flags.once &amp;&amp; memory ) ) {
						fire( context, args );
					}
				}
				return this;
			},
			// Call all the callbacks with the given arguments
			fire: function() {
				self.fireWith( this, arguments );
				return this;
			},
			// To know if the callbacks have already been called at least once
			fired: function() {
				return !!fired;
			}
		};

	return self;
};




var // Static reference to slice
	sliceDeferred = [].slice;

jQuery.extend({

	Deferred: function( func ) {
		var doneList = jQuery.Callbacks( "once memory" ),
			failList = jQuery.Callbacks( "once memory" ),
			progressList = jQuery.Callbacks( "memory" ),
			state = "pending",
			lists = {
				resolve: doneList,
				reject: failList,
				notify: progressList
			},
			promise = {
				done: doneList.add,
				fail: failList.add,
				progress: progressList.add,

				state: function() {
					return state;
				},

				// Deprecated
				isResolved: doneList.fired,
				isRejected: failList.fired,

				then: function( doneCallbacks, failCallbacks, progressCallbacks ) {
					deferred.done( doneCallbacks ).fail( failCallbacks ).progress( progressCallbacks );
					return this;
				},
				always: function() {
					deferred.done.apply( deferred, arguments ).fail.apply( deferred, arguments );
					return this;
				},
				pipe: function( fnDone, fnFail, fnProgress ) {
					return jQuery.Deferred(function( newDefer ) {
						jQuery.each( {
							done: [ fnDone, "resolve" ],
							fail: [ fnFail, "reject" ],
							progress: [ fnProgress, "notify" ]
						}, function( handler, data ) {
							var fn = data[ 0 ],
								action = data[ 1 ],
								returned;
							if ( jQuery.isFunction( fn ) ) {
								deferred[ handler ](function() {
									returned = fn.apply( this, arguments );
									if ( returned &amp;&amp; jQuery.isFunction( returned.promise ) ) {
										returned.promise().then( newDefer.resolve, newDefer.reject, newDefer.notify );
									} else {
										newDefer[ action + "With" ]( this === deferred ? newDefer : this, [ returned ] );
									}
								});
							} else {
								deferred[ handler ]( newDefer[ action ] );
							}
						});
					}).promise();
				},
				// Get a promise for this deferred
				// If obj is provided, the promise aspect is added to the object
				promise: function( obj ) {
					if ( obj == null ) {
						obj = promise;
					} else {
						for ( var key in promise ) {
							obj[ key ] = promise[ key ];
						}
					}
					return obj;
				}
			},
			deferred = promise.promise({}),
			key;

		for ( key in lists ) {
			deferred[ key ] = lists[ key ].fire;
			deferred[ key + "With" ] = lists[ key ].fireWith;
		}

		// Handle state
		deferred.done( function() {
			state = "resolved";
		}, failList.disable, progressList.lock ).fail( function() {
			state = "rejected";
		}, doneList.disable, progressList.lock );

		// Call given func if any
		if ( func ) {
			func.call( deferred, deferred );
		}

		// All done!
		return deferred;
	},

	// Deferred helper
	when: function( firstParam ) {
		var args = sliceDeferred.call( arguments, 0 ),
			i = 0,
			length = args.length,
			pValues = new Array( length ),
			count = length,
			pCount = length,
			deferred = length &lt;= 1 &amp;&amp; firstParam &amp;&amp; jQuery.isFunction( firstParam.promise ) ?
				firstParam :
				jQuery.Deferred(),
			promise = deferred.promise();
		function resolveFunc( i ) {
			return function( value ) {
				args[ i ] = arguments.length &gt; 1 ? sliceDeferred.call( arguments, 0 ) : value;
				if ( !( --count ) ) {
					deferred.resolveWith( deferred, args );
				}
			};
		}
		function progressFunc( i ) {
			return function( value ) {
				pValues[ i ] = arguments.length &gt; 1 ? sliceDeferred.call( arguments, 0 ) : value;
				deferred.notifyWith( promise, pValues );
			};
		}
		if ( length &gt; 1 ) {
			for ( ; i &lt; length; i++ ) {
				if ( args[ i ] &amp;&amp; args[ i ].promise &amp;&amp; jQuery.isFunction( args[ i ].promise ) ) {
					args[ i ].promise().then( resolveFunc(i), deferred.reject, progressFunc(i) );
				} else {
					--count;
				}
			}
			if ( !count ) {
				deferred.resolveWith( deferred, args );
			}
		} else if ( deferred !== firstParam ) {
			deferred.resolveWith( deferred, length ? [ firstParam ] : [] );
		}
		return promise;
	}
});




jQuery.support = {
		// IE strips leading whitespace when .innerHTML is used
		leadingWhitespace: true,

		// Make sure that tbody elements aren't automatically inserted
		// IE will insert them into empty tables
		tbody: false,

		// Make sure that link elements get serialized correctly by innerHTML
		// This requires a wrapper element in IE
		htmlSerialize: true,

		// Get the style information from getAttribute
		// (IE uses .cssText instead)
		style: true,

		// Make sure that URLs aren't manipulated
		// (IE normalizes it by default)
		hrefNormalized: true,

		// Make sure that element opacity exists
		// (IE uses filter instead)
		// Use a regex to work around a WebKit issue. See #5145
		opacity: true,

		// Verify style float existence
		// (IE uses styleFloat instead of cssFloat)
		cssFloat: true,

		// Make sure that if no value is specified for a checkbox
		// that it defaults to "on".
		// (WebKit defaults to "" instead)
		checkOn: true,

		// Make sure that a selected-by-default option has a working selected property.
		// (WebKit defaults to false instead of true, IE too, if it's in an optgroup)
		optSelected: true,

		// Test setAttribute on camelCase class. If it works, we need attrFixes when doing get/setAttribute (ie6/7)
		getSetAttribute: true,

		// Tests for enctype support on a form(#6743)
		enctype: true,

		// Makes sure cloning an html5 element does not cause problems
		// Where outerHTML is undefined, this still works
		html5Clone: true,

		// Will be defined later
		submitBubbles: true,
		changeBubbles: true,
		focusinBubbles: false,
		deleteExpando: true,
		noCloneEvent: true,
		inlineBlockNeedsLayout: false,
		shrinkWrapBlocks: false,
		reliableMarginRight: true,
		pixelMargin: true
	};



var rbrace = /^(?:\{.*\}|\[.*\])$/,
	rmultiDash = /([A-Z])/g;

jQuery.extend({
	cache: {},

	// Please use with caution
	uuid: 0,

	// Unique for each copy of jQuery on the page
	// Non-digits removed to match rinlinejQuery
	expando: "jQuery" + ( jQuery.fn.jquery + Math.random() ).replace( /\D/g, "" ),

	// The following elements throw uncatchable exceptions if you
	// attempt to add expando properties to them.
	noData: {
		"embed": true,
		// Ban all objects except for Flash (which handle expandos)
		"object": "clsid:D27CDB6E-AE6D-11cf-96B8-444553540000",
		"applet": true
	},

	hasData: function( elem ) {
		elem = elem.nodeType ? jQuery.cache[ elem[jQuery.expando] ] : elem[ jQuery.expando ];
		return !!elem &amp;&amp; !isEmptyDataObject( elem );
	},

	data: function( elem, name, data, pvt /* Internal Use Only */ ) {
		if ( !jQuery.acceptData( elem ) ) {
			return;
		}

		var privateCache, thisCache, ret,
			internalKey = jQuery.expando,
			getByName = typeof name === "string",

			// We have to handle DOM nodes and JS objects differently because IE6-7
			// can't GC object references properly across the DOM-JS boundary
			isNode = elem.nodeType,

			// Only DOM nodes need the global jQuery cache; JS object data is
			// attached directly to the object so GC can occur automatically
			cache = isNode ? jQuery.cache : elem,

			// Only defining an ID for JS objects if its cache already exists allows
			// the code to shortcut on the same path as a DOM node with no cache
			id = isNode ? elem[ internalKey ] : elem[ internalKey ] &amp;&amp; internalKey,
			isEvents = name === "events";

		// Avoid doing any more work than we need to when trying to get data on an
		// object that has no data at all
		if ( (!id || !cache[id] || (!isEvents &amp;&amp; !pvt &amp;&amp; !cache[id].data)) &amp;&amp; getByName &amp;&amp; data === undefined ) {
			return;
		}

		if ( !id ) {
			// Only DOM nodes need a new unique ID for each element since their data
			// ends up in the global cache
			if ( isNode ) {
				elem[ internalKey ] = id = ++jQuery.uuid;
			} else {
				id = internalKey;
			}
		}

		if ( !cache[ id ] ) {
			cache[ id ] = {};

			// Avoids exposing jQuery metadata on plain JS objects when the object
			// is serialized using JSON.stringify
			if ( !isNode ) {
				cache[ id ].toJSON = jQuery.noop;
			}
		}

		// An object can be passed to jQuery.data instead of a key/value pair; this gets
		// shallow copied over onto the existing cache
		if ( typeof name === "object" || typeof name === "function" ) {
			if ( pvt ) {
				cache[ id ] = jQuery.extend( cache[ id ], name );
			} else {
				cache[ id ].data = jQuery.extend( cache[ id ].data, name );
			}
		}

		privateCache = thisCache = cache[ id ];

		// jQuery data() is stored in a separate object inside the object's internal data
		// cache in order to avoid key collisions between internal data and user-defined
		// data.
		if ( !pvt ) {
			if ( !thisCache.data ) {
				thisCache.data = {};
			}

			thisCache = thisCache.data;
		}

		if ( data !== undefined ) {
			thisCache[ jQuery.camelCase( name ) ] = data;
		}

		// Users should not attempt to inspect the internal events object using jQuery.data,
		// it is undocumented and subject to change. But does anyone listen? No.
		if ( isEvents &amp;&amp; !thisCache[ name ] ) {
			return privateCache.events;
		}

		// Check for both converted-to-camel and non-converted data property names
		// If a data property was specified
		if ( getByName ) {

			// First Try to find as-is property data
			ret = thisCache[ name ];

			// Test for null|undefined property data
			if ( ret == null ) {

				// Try to find the camelCased property
				ret = thisCache[ jQuery.camelCase( name ) ];
			}
		} else {
			ret = thisCache;
		}

		return ret;
	},

	removeData: function( elem, name, pvt /* Internal Use Only */ ) {
		if ( !jQuery.acceptData( elem ) ) {
			return;
		}

		var thisCache, i, l,

			// Reference to internal data cache key
			internalKey = jQuery.expando,

			isNode = elem.nodeType,

			// See jQuery.data for more information
			cache = isNode ? jQuery.cache : elem,

			// See jQuery.data for more information
			id = isNode ? elem[ internalKey ] : internalKey;

		// If there is already no cache entry for this object, there is no
		// purpose in continuing
		if ( !cache[ id ] ) {
			return;
		}

		if ( name ) {

			thisCache = pvt ? cache[ id ] : cache[ id ].data;

			if ( thisCache ) {

				// Support array or space separated string names for data keys
				if ( !jQuery.isArray( name ) ) {

					// try the string as a key before any manipulation
					if ( name in thisCache ) {
						name = [ name ];
					} else {

						// split the camel cased version by spaces unless a key with the spaces exists
						name = jQuery.camelCase( name );
						if ( name in thisCache ) {
							name = [ name ];
						} else {
							name = name.split( " " );
						}
					}
				}

				for ( i = 0, l = name.length; i &lt; l; i++ ) {
					delete thisCache[ name[i] ];
				}

				// If there is no data left in the cache, we want to continue
				// and let the cache object itself get destroyed
				if ( !( pvt ? isEmptyDataObject : jQuery.isEmptyObject )( thisCache ) ) {
					return;
				}
			}
		}

		// See jQuery.data for more information
		if ( !pvt ) {
			delete cache[ id ].data;

			// Don't destroy the parent cache unless the internal data object
			// had been the only thing left in it
			if ( !isEmptyDataObject(cache[ id ]) ) {
				return;
			}
		}

		// Browsers that fail expando deletion also refuse to delete expandos on
		// the window, but it will allow it on all other JS objects; other browsers
		// don't care
		// Ensure that `cache` is not a window object #10080
		if ( jQuery.support.deleteExpando || !cache.setInterval ) {
			delete cache[ id ];
		} else {
			cache[ id ] = null;
		}

		// We destroyed the cache and need to eliminate the expando on the node to avoid
		// false lookups in the cache for entries that no longer exist
		if ( isNode ) {
			// IE does not allow us to delete expando properties from nodes,
			// nor does it have a removeAttribute function on Document nodes;
			// we must handle all of these cases
			if ( jQuery.support.deleteExpando ) {
				delete elem[ internalKey ];
			} else if ( elem.removeAttribute ) {
				elem.removeAttribute( internalKey );
			} else {
				elem[ internalKey ] = null;
			}
		}
	},

	// For internal use only.
	_data: function( elem, name, data ) {
		return jQuery.data( elem, name, data, true );
	},

	// A method for determining if a DOM node can handle the data expando
	acceptData: function( elem ) {
		if ( elem.nodeName ) {
			var match = jQuery.noData[ elem.nodeName.toLowerCase() ];

			if ( match ) {
				return !(match === true || elem.getAttribute("classid") !== match);
			}
		}

		return true;
	}
});

jQuery.fn.extend({
	data: function( key, value ) {
		var parts, part, attr, name, l,
			elem = this[0],
			i = 0,
			data = null;

		// Gets all values
		if ( key === undefined ) {
			if ( this.length ) {
				data = jQuery.data( elem );

				if ( elem.nodeType === 1 &amp;&amp; !jQuery._data( elem, "parsedAttrs" ) ) {
					attr = elem.attributes;
					for ( l = attr.length; i &lt; l; i++ ) {
						name = attr[i].name;

						if ( name.indexOf( "data-" ) === 0 ) {
							name = jQuery.camelCase( name.substring(5) );

							dataAttr( elem, name, data[ name ] );
						}
					}
					jQuery._data( elem, "parsedAttrs", true );
				}
			}

			return data;
		}

		// Sets multiple values
		if ( typeof key === "object" ) {
			return this.each(function() {
				jQuery.data( this, key );
			});
		}

		parts = key.split( ".", 2 );
		parts[1] = parts[1] ? "." + parts[1] : "";
		part = parts[1] + "!";

		return jQuery.access( this, function( value ) {

			if ( value === undefined ) {
				data = this.triggerHandler( "getData" + part, [ parts[0] ] );

				// Try to fetch any internally stored data first
				if ( data === undefined &amp;&amp; elem ) {
					data = jQuery.data( elem, key );
					data = dataAttr( elem, key, data );
				}

				return data === undefined &amp;&amp; parts[1] ?
					this.data( parts[0] ) :
					data;
			}

			parts[1] = value;
			this.each(function() {
				var self = jQuery( this );

				self.triggerHandler( "setData" + part, parts );
				jQuery.data( this, key, value );
				self.triggerHandler( "changeData" + part, parts );
			});
		}, null, value, arguments.length &gt; 1, null, false );
	},

	removeData: function( key ) {
		return this.each(function() {
			jQuery.removeData( this, key );
		});
	}
});

function dataAttr( elem, key, data ) {
	// If nothing was found internally, try to fetch any
	// data from the HTML5 data-* attribute
	if ( data === undefined &amp;&amp; elem.nodeType === 1 ) {

		var name = "data-" + key.replace( rmultiDash, "-$1" ).toLowerCase();

		data = elem.getAttribute( name );

		if ( typeof data === "string" ) {
			try {
				data = data === "true" ? true :
				data === "false" ? false :
				data === "null" ? null :
				jQuery.isNumeric( data ) ? +data :
					rbrace.test( data ) ? jQuery.parseJSON( data ) :
					data;
			} catch( e ) {}

			// Make sure we set the data so it isn't changed later
			jQuery.data( elem, key, data );

		} else {
			data = undefined;
		}
	}

	return data;
}

// checks a cache object for emptiness
function isEmptyDataObject( obj ) {
	for ( var name in obj ) {

		// if the public data object is empty, the private is still empty
		if ( name === "data" &amp;&amp; jQuery.isEmptyObject( obj[name] ) ) {
			continue;
		}
		if ( name !== "toJSON" ) {
			return false;
		}
	}

	return true;
}




function handleQueueMarkDefer( elem, type, src ) {
	var deferDataKey = type + "defer",
		queueDataKey = type + "queue",
		markDataKey = type + "mark",
		defer = jQuery._data( elem, deferDataKey );
	if ( defer &amp;&amp;
		( src === "queue" || !jQuery._data(elem, queueDataKey) ) &amp;&amp;
		( src === "mark" || !jQuery._data(elem, markDataKey) ) ) {
		// Give room for hard-coded callbacks to fire first
		// and eventually mark/queue something else on the element
		setTimeout( function() {
			if ( !jQuery._data( elem, queueDataKey ) &amp;&amp;
				!jQuery._data( elem, markDataKey ) ) {
				jQuery.removeData( elem, deferDataKey, true );
				defer.fire();
			}
		}, 0 );
	}
}

jQuery.extend({

	_mark: function( elem, type ) {
		if ( elem ) {
			type = ( type || "fx" ) + "mark";
			jQuery._data( elem, type, (jQuery._data( elem, type ) || 0) + 1 );
		}
	},

	_unmark: function( force, elem, type ) {
		if ( force !== true ) {
			type = elem;
			elem = force;
			force = false;
		}
		if ( elem ) {
			type = type || "fx";
			var key = type + "mark",
				count = force ? 0 : ( (jQuery._data( elem, key ) || 1) - 1 );
			if ( count ) {
				jQuery._data( elem, key, count );
			} else {
				jQuery.removeData( elem, key, true );
				handleQueueMarkDefer( elem, type, "mark" );
			}
		}
	},

	queue: function( elem, type, data ) {
		var q;
		if ( elem ) {
			type = ( type || "fx" ) + "queue";
			q = jQuery._data( elem, type );

			// Speed up dequeue by getting out quickly if this is just a lookup
			if ( data ) {
				if ( !q || jQuery.isArray(data) ) {
					q = jQuery._data( elem, type, jQuery.makeArray(data) );
				} else {
					q.push( data );
				}
			}
			return q || [];
		}
	},

	dequeue: function( elem, type ) {
		type = type || "fx";

		var queue = jQuery.queue( elem, type ),
			fn = queue.shift(),
			hooks = {};

		// If the fx queue is dequeued, always remove the progress sentinel
		if ( fn === "inprogress" ) {
			fn = queue.shift();
		}

		if ( fn ) {
			// Add a progress sentinel to prevent the fx queue from being
			// automatically dequeued
			if ( type === "fx" ) {
				queue.unshift( "inprogress" );
			}

			jQuery._data( elem, type + ".run", hooks );
			fn.call( elem, function() {
				jQuery.dequeue( elem, type );
			}, hooks );
		}

		if ( !queue.length ) {
			jQuery.removeData( elem, type + "queue " + type + ".run", true );
			handleQueueMarkDefer( elem, type, "queue" );
		}
	}
});

jQuery.fn.extend({
	queue: function( type, data ) {
		var setter = 2;

		if ( typeof type !== "string" ) {
			data = type;
			type = "fx";
			setter--;
		}

		if ( arguments.length &lt; setter ) {
			return jQuery.queue( this[0], type );
		}

		return data === undefined ?
			this :
			this.each(function() {
				var queue = jQuery.queue( this, type, data );

				if ( type === "fx" &amp;&amp; queue[0] !== "inprogress" ) {
					jQuery.dequeue( this, type );
				}
			});
	},
	dequeue: function( type ) {
		return this.each(function() {
			jQuery.dequeue( this, type );
		});
	},
	// Based off of the plugin by Clint Helfers, with permission.
	// http://blindsignals.com/index.php/2009/07/jquery-delay/
	delay: function( time, type ) {
		time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
		type = type || "fx";

		return this.queue( type, function( next, hooks ) {
			var timeout = setTimeout( next, time );
			hooks.stop = function() {
				clearTimeout( timeout );
			};
		});
	},
	clearQueue: function( type ) {
		return this.queue( type || "fx", [] );
	},
	// Get a promise resolved when queues of a certain type
	// are emptied (fx is the type by default)
	promise: function( type, object ) {
		if ( typeof type !== "string" ) {
			object = type;
			type = undefined;
		}
		type = type || "fx";
		var defer = jQuery.Deferred(),
			elements = this,
			i = elements.length,
			count = 1,
			deferDataKey = type + "defer",
			queueDataKey = type + "queue",
			markDataKey = type + "mark",
			tmp;
		function resolve() {
			if ( !( --count ) ) {
				defer.resolveWith( elements, [ elements ] );
			}
		}
		while( i-- ) {
			if (( tmp = jQuery.data( elements[ i ], deferDataKey, undefined, true ) ||
					( jQuery.data( elements[ i ], queueDataKey, undefined, true ) ||
						jQuery.data( elements[ i ], markDataKey, undefined, true ) ) &amp;&amp;
					jQuery.data( elements[ i ], deferDataKey, jQuery.Callbacks( "once memory" ), true ) )) {
				count++;
				tmp.add( resolve );
			}
		}
		resolve();
		return defer.promise( object );
	}
});




var rclass = /[\n\t\r]/g,
	rspace = /\s+/,
	rreturn = /\r/g,
	rtype = /^(?:button|input)$/i,
	rfocusable = /^(?:button|input|object|select|textarea)$/i,
	rclickable = /^a(?:rea)?$/i,
	rboolean = /^(?:autofocus|autoplay|async|checked|controls|defer|disabled|hidden|loop|multiple|open|readonly|required|scoped|selected)$/i,
	getSetAttribute = jQuery.support.getSetAttribute,
	nodeHook, boolHook, fixSpecified;

jQuery.fn.extend({
	attr: function( name, value ) {
		return jQuery.access( this, jQuery.attr, name, value, arguments.length &gt; 1 );
	},

	removeAttr: function( name ) {
		return this.each(function() {
			jQuery.removeAttr( this, name );
		});
	},

	prop: function( name, value ) {
		return jQuery.access( this, jQuery.prop, name, value, arguments.length &gt; 1 );
	},

	removeProp: function( name ) {
		name = jQuery.propFix[ name ] || name;
		return this.each(function() {
			// try/catch handles cases where IE balks (such as removing a property on window)
			try {
				this[ name ] = undefined;
				delete this[ name ];
			} catch( e ) {}
		});
	},

	addClass: function( value ) {
		var classNames, i, l, elem,
			setClass, c, cl;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).addClass( value.call(this, j, this.className.baseVal) );
			});
		}

		if ( value &amp;&amp; typeof value === "string" ) {
			classNames = value.split( rspace );

			for ( i = 0, l = this.length; i &lt; l; i++ ) {
				elem = this[ i ];

				if ( elem.nodeType === 1 ) {
					if ( !elem.className.baseVal &amp;&amp; classNames.length === 1 ) {
						elem.className.baseVal = value;

					} else {
						setClass = " " + elem.className.baseVal + " ";

						for ( c = 0, cl = classNames.length; c &lt; cl; c++ ) {
							if ( !~setClass.indexOf( " " + classNames[ c ] + " " ) ) {
								setClass += classNames[ c ] + " ";
							}
						}
						elem.className.baseVal = jQuery.trim( setClass );
					}
				}
			}
		}

		return this;
	},

	removeClass: function( value ) {
		var classNames, i, l, elem, className, c, cl;

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( j ) {
				jQuery( this ).removeClass( value.call(this, j, this.className.baseVal) );
			});
		}

		if ( (value &amp;&amp; typeof value === "string") || value === undefined ) {
			classNames = ( value || "" ).split( rspace );

			for ( i = 0, l = this.length; i &lt; l; i++ ) {
				elem = this[ i ];

				if ( elem.nodeType === 1 &amp;&amp; elem.className.baseVal ) {
					if ( value ) {
						className = (" " + elem.className.baseVal + " ").replace( rclass, " " );
						for ( c = 0, cl = classNames.length; c &lt; cl; c++ ) {
							className = className.replace(" " + classNames[ c ] + " ", " ");
						}
						elem.className.baseVal = jQuery.trim( className );

					} else {
						elem.className.baseVal = "";
					}
				}
			}
		}

		return this;
	},

	toggleClass: function( value, stateVal ) {
		var type = typeof value,
			isBool = typeof stateVal === "boolean";

		if ( jQuery.isFunction( value ) ) {
			return this.each(function( i ) {
				jQuery( this ).toggleClass( value.call(this, i, this.className.baseVal, stateVal), stateVal );
			});
		}

		return this.each(function() {
			if ( type === "string" ) {
				// toggle individual class names
				var className,
					i = 0,
					self = jQuery( this ),
					state = stateVal,
					classNames = value.split( rspace );

				while ( (className = classNames[ i++ ]) ) {
					// check each className given, space seperated list
					state = isBool ? state : !self.hasClass( className );
					self[ state ? "addClass" : "removeClass" ]( className );
				}

			} else if ( type === "undefined" || type === "boolean" ) {
				if ( this.className.baseVal ) {
					// store className if set
					jQuery._data( this, "__className__", this.className.baseVal );
				}

				// toggle whole className
				this.className.baseVal = this.className.baseVal || value === false ? "" : jQuery._data( this, "__className__" ) || "";
			}
		});
	},

	hasClass: function( selector ) {
		var className = " " + selector + " ",
			i = 0,
			l = this.length;
		for ( ; i &lt; l; i++ ) {
			if ( this[i].nodeType === 1 &amp;&amp; (" " + this[i].className.baseVal + " ").replace(rclass, " ").indexOf( className ) &gt; -1 ) {
				return true;
			}
		}

		return false;
	},

	val: function( value ) {
		var hooks, ret, isFunction,
			elem = this[0];

		if ( !arguments.length ) {
			if ( elem ) {
				hooks = jQuery.valHooks[ elem.type ] || jQuery.valHooks[ elem.nodeName.toLowerCase() ];

				if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, "value" )) !== undefined ) {
					return ret;
				}

				ret = elem.value;

				return typeof ret === "string" ?
					// handle most common string cases
					ret.replace(rreturn, "") :
					// handle cases where value is null/undef or number
					ret == null ? "" : ret;
			}

			return;
		}

		isFunction = jQuery.isFunction( value );

		return this.each(function( i ) {
			var self = jQuery(this), val;

			if ( this.nodeType !== 1 ) {
				return;
			}

			if ( isFunction ) {
				val = value.call( this, i, self.val() );
			} else {
				val = value;
			}

			// Treat null/undefined as ""; convert numbers to string
			if ( val == null ) {
				val = "";
			} else if ( typeof val === "number" ) {
				val += "";
			} else if ( jQuery.isArray( val ) ) {
				val = jQuery.map(val, function ( value ) {
					return value == null ? "" : value + "";
				});
			}

			hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

			// If set returns undefined, fall back to normal setting
			if ( !hooks || !("set" in hooks) || hooks.set( this, val, "value" ) === undefined ) {
				this.value = val;
			}
		});
	}
});

jQuery.extend({
	valHooks: {
		option: {
			get: function( elem ) {
				// attributes.value is undefined in Blackberry 4.7 but
				// uses .value. See #6932
				var val = elem.attributes.value;
				return !val || val.specified ? elem.value : elem.text;
			}
		},
		select: {
			get: function( elem ) {
				var value, i, max, option,
					index = elem.selectedIndex,
					values = [],
					options = elem.options,
					one = elem.type === "select-one";

				// Nothing was selected
				if ( index &lt; 0 ) {
					return null;
				}

				// Loop through all the selected options
				i = one ? index : 0;
				max = one ? index + 1 : options.length;
				for ( ; i &lt; max; i++ ) {
					option = options[ i ];

					// Don't return options that are disabled or in a disabled optgroup
					if ( option.selected &amp;&amp; (jQuery.support.optDisabled ? !option.disabled : option.getAttribute("disabled") === null) &amp;&amp;
							(!option.parentNode.disabled || !jQuery.nodeName( option.parentNode, "optgroup" )) ) {

						// Get the specific value for the option
						value = jQuery( option ).val();

						// We don't need an array for one selects
						if ( one ) {
							return value;
						}

						// Multi-Selects return an array
						values.push( value );
					}
				}

				// Fixes Bug #2551 -- select.val() broken in IE after form.reset()
				if ( one &amp;&amp; !values.length &amp;&amp; options.length ) {
					return jQuery( options[ index ] ).val();
				}

				return values;
			},

			set: function( elem, value ) {
				var values = jQuery.makeArray( value );

				jQuery(elem).find("option").each(function() {
					this.selected = jQuery.inArray( jQuery(this).val(), values ) &gt;= 0;
				});

				if ( !values.length ) {
					elem.selectedIndex = -1;
				}
				return values;
			}
		}
	},

	attrFn: {
		val: true,
		css: true,
		html: true,
		text: true,
		data: true,
		width: true,
		height: true,
		offset: true
	},

	attr: function( elem, name, value, pass ) {
		var ret, hooks, notxml,
			nType = elem.nodeType;

		// don't get/set attributes on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		if ( pass &amp;&amp; name in jQuery.attrFn ) {
			return jQuery( elem )[ name ]( value );
		}

		// Fallback to prop when attributes are not supported
		if ( typeof elem.getAttribute === "undefined" ) {
			return jQuery.prop( elem, name, value );
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		// All attributes are lowercase
		// Grab necessary hook if one is defined
		if ( notxml ) {
			name = name.toLowerCase();
			hooks = jQuery.attrHooks[ name ] || ( rboolean.test( name ) ? boolHook : nodeHook );
		}

		if ( value !== undefined ) {

			if ( value === null ) {
				jQuery.removeAttr( elem, name );
				return;

			} else if ( hooks &amp;&amp; "set" in hooks &amp;&amp; notxml &amp;&amp; (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				if (name == 'href') {
					elem.setAttributeNS( 'http://www.w3.org/1999/xlink', name, "" + value );
				} else {
					elem.setAttribute( name, "" + value );
				}
				return value;
			}

		} else if ( hooks &amp;&amp; "get" in hooks &amp;&amp; notxml &amp;&amp; (ret = hooks.get( elem, name )) !== null ) {
			return ret;

		} else {

			ret = elem.getAttribute( name );

			// Non-existent attributes return null, we normalize to undefined
			return ret === null ?
				undefined :
				ret;
		}
	},

	removeAttr: function( elem, value ) {
		var propName, attrNames, name, l, isBool,
			i = 0;

		if ( value &amp;&amp; elem.nodeType === 1 ) {
			attrNames = value.toLowerCase().split( rspace );
			l = attrNames.length;

			for ( ; i &lt; l; i++ ) {
				name = attrNames[ i ];

				if ( name ) {
					propName = jQuery.propFix[ name ] || name;
					isBool = rboolean.test( name );

					// See #9699 for explanation of this approach (setting first, then removal)
					// Do not do this for boolean attributes (see #10870)
					if ( !isBool ) {
						jQuery.attr( elem, name, "" );
					}
					elem.removeAttribute( getSetAttribute ? name : propName );

					// Set corresponding property to false for boolean attributes
					if ( isBool &amp;&amp; propName in elem ) {
						elem[ propName ] = false;
					}
				}
			}
		}
	},

	attrHooks: {
		type: {
			set: function( elem, value ) {
				// We can't allow the type property to be changed (since it causes problems in IE)
				if ( rtype.test( elem.nodeName ) &amp;&amp; elem.parentNode ) {
					jQuery.error( "type property can't be changed" );
				} else if ( !jQuery.support.radioValue &amp;&amp; value === "radio" &amp;&amp; jQuery.nodeName(elem, "input") ) {
					// Setting the type on a radio button after the value resets the value in IE6-9
					// Reset value to it's default in case type is set after value
					// This is for element creation
					var val = elem.value;
					elem.setAttribute( "type", value );
					if ( val ) {
						elem.value = val;
					}
					return value;
				}
			}
		},
		// Use the value property for back compat
		// Use the nodeHook for button elements in IE6/7 (#1954)
		value: {
			get: function( elem, name ) {
				if ( nodeHook &amp;&amp; jQuery.nodeName( elem, "button" ) ) {
					return nodeHook.get( elem, name );
				}
				return name in elem ?
					elem.value :
					null;
			},
			set: function( elem, value, name ) {
				if ( nodeHook &amp;&amp; jQuery.nodeName( elem, "button" ) ) {
					return nodeHook.set( elem, value, name );
				}
				// Does not return so that setAttribute is also used
				elem.value = value;
			}
		}
	},

	propFix: {
		tabindex: "tabIndex",
		readonly: "readOnly",
		"for": "htmlFor",
		"class": "className",
		maxlength: "maxLength",
		cellspacing: "cellSpacing",
		cellpadding: "cellPadding",
		rowspan: "rowSpan",
		colspan: "colSpan",
		usemap: "useMap",
		frameborder: "frameBorder",
		contenteditable: "contentEditable"
	},

	prop: function( elem, name, value ) {
		var ret, hooks, notxml,
			nType = elem.nodeType;

		// don't get/set properties on text, comment and attribute nodes
		if ( !elem || nType === 3 || nType === 8 || nType === 2 ) {
			return;
		}

		notxml = nType !== 1 || !jQuery.isXMLDoc( elem );

		if ( notxml ) {
			// Fix name and attach hooks
			name = jQuery.propFix[ name ] || name;
			hooks = jQuery.propHooks[ name ];
		}

		if ( value !== undefined ) {
			if ( hooks &amp;&amp; "set" in hooks &amp;&amp; (ret = hooks.set( elem, value, name )) !== undefined ) {
				return ret;

			} else {
				return ( elem[ name ] = value );
			}

		} else {
			if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, name )) !== null ) {
				return ret;

			} else {
				return elem[ name ];
			}
		}
	},

	propHooks: {
		tabIndex: {
			get: function( elem ) {
				// elem.tabIndex doesn't always return the correct value when it hasn't been explicitly set
				// http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
				var attributeNode = elem.getAttributeNode("tabindex");

				return attributeNode &amp;&amp; attributeNode.specified ?
					parseInt( attributeNode.value, 10 ) :
					rfocusable.test( elem.nodeName ) || rclickable.test( elem.nodeName ) &amp;&amp; elem.href ?
						0 :
						undefined;
			}
		}
	}
});

// Add the tabIndex propHook to attrHooks for back-compat (different case is intentional)
jQuery.attrHooks.tabindex = jQuery.propHooks.tabIndex;

// Hook for boolean attributes
boolHook = {
	get: function( elem, name ) {
		// Align boolean attributes with corresponding properties
		// Fall back to attribute presence where some booleans are not supported
		var attrNode,
			property = jQuery.prop( elem, name );
		return property === true || typeof property !== "boolean" &amp;&amp; ( attrNode = elem.getAttributeNode(name) ) &amp;&amp; attrNode.nodeValue !== false ?
			name.toLowerCase() :
			undefined;
	},
	set: function( elem, value, name ) {
		var propName;
		if ( value === false ) {
			// Remove boolean attributes when set to false
			jQuery.removeAttr( elem, name );
		} else {
			// value is true since we know at this point it's type boolean and not false
			// Set boolean attributes to the same name and set the DOM property
			propName = jQuery.propFix[ name ] || name;
			if ( propName in elem ) {
				// Only set the IDL specifically if it already exists on the element
				elem[ propName ] = true;
			}

			elem.setAttribute( name, name.toLowerCase() );
		}
		return name;
	}
};

// IE6/7 do not support getting/setting some attributes with get/setAttribute
if ( !getSetAttribute ) {

	fixSpecified = {
		name: true,
		id: true,
		coords: true
	};

	// Use this for any attribute in IE6/7
	// This fixes almost every IE6/7 issue
	nodeHook = jQuery.valHooks.button = {
		get: function( elem, name ) {
			var ret;
			ret = elem.getAttributeNode( name );
			return ret &amp;&amp; ( fixSpecified[ name ] ? ret.nodeValue !== "" : ret.specified ) ?
				ret.nodeValue :
				undefined;
		},
		set: function( elem, value, name ) {
			// Set the existing or create a new attribute node
			var ret = elem.getAttributeNode( name );
			if ( !ret ) {
				ret = document.createAttribute( name );
				elem.setAttributeNode( ret );
			}
			return ( ret.nodeValue = value + "" );
		}
	};

	// Apply the nodeHook to tabindex
	jQuery.attrHooks.tabindex.set = nodeHook.set;

	// Set width and height to auto instead of 0 on empty string( Bug #8150 )
	// This is for removals
	jQuery.each([ "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			set: function( elem, value ) {
				if ( value === "" ) {
					elem.setAttribute( name, "auto" );
					return value;
				}
			}
		});
	});

	// Set contenteditable to false on removals(#10429)
	// Setting to empty string throws an error as an invalid value
	jQuery.attrHooks.contenteditable = {
		get: nodeHook.get,
		set: function( elem, value, name ) {
			if ( value === "" ) {
				value = "false";
			}
			nodeHook.set( elem, value, name );
		}
	};
}


// Some attributes require a special call on IE
if ( !jQuery.support.hrefNormalized ) {
	jQuery.each([ "href", "src", "width", "height" ], function( i, name ) {
		jQuery.attrHooks[ name ] = jQuery.extend( jQuery.attrHooks[ name ], {
			get: function( elem ) {
				var ret = elem.getAttribute( name, 2 );
				return ret === null ? undefined : ret;
			}
		});
	});
}

if ( !jQuery.support.style ) {
	jQuery.attrHooks.style = {
		get: function( elem ) {
			// Return undefined in the case of empty string
			// Normalize to lowercase since IE uppercases css property names
			return elem.style.cssText.toLowerCase() || undefined;
		},
		set: function( elem, value ) {
			return ( elem.style.cssText = "" + value );
		}
	};
}

// Safari mis-reports the default selected property of an option
// Accessing the parent's selectedIndex property fixes it
if ( !jQuery.support.optSelected ) {
	jQuery.propHooks.selected = jQuery.extend( jQuery.propHooks.selected, {
		get: function( elem ) {
			var parent = elem.parentNode;

			if ( parent ) {
				parent.selectedIndex;

				// Make sure that it also works with optgroups, see #5701
				if ( parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
			}
			return null;
		}
	});
}

// IE6/7 call enctype encoding
if ( !jQuery.support.enctype ) {
	jQuery.propFix.enctype = "encoding";
}

// Radios and checkboxes getter/setter
if ( !jQuery.support.checkOn ) {
	jQuery.each([ "radio", "checkbox" ], function() {
		jQuery.valHooks[ this ] = {
			get: function( elem ) {
				// Handle the case where in Webkit "" is returned instead of "on" if a value isn't specified
				return elem.getAttribute("value") === null ? "on" : elem.value;
			}
		};
	});
}
jQuery.each([ "radio", "checkbox" ], function() {
	jQuery.valHooks[ this ] = jQuery.extend( jQuery.valHooks[ this ], {
		set: function( elem, value ) {
			if ( jQuery.isArray( value ) ) {
				return ( elem.checked = jQuery.inArray( jQuery(elem).val(), value ) &gt;= 0 );
			}
		}
	});
});




var rformElems = /^(?:textarea|input|select)$/i,
	rtypenamespace = /^([^\.]*)?(?:\.(.+))?$/,
	rhoverHack = /(?:^|\s)hover(\.\S+)?\b/,
	rkeyEvent = /^key/,
	rmouseEvent = /^(?:mouse|contextmenu)|click/,
	rfocusMorph = /^(?:focusinfocus|focusoutblur)$/,
	rquickIs = /^(\w*)(?:#([\w\-]+))?(?:\.([\w\-]+))?$/,
	quickParse = function( selector ) {
		var quick = rquickIs.exec( selector );
		if ( quick ) {
			//   0  1    2   3
			// [ _, tag, id, class ]
			quick[1] = ( quick[1] || "" ).toLowerCase();
			quick[3] = quick[3] &amp;&amp; new RegExp( "(?:^|\\s)" + quick[3] + "(?:\\s|$)" );
		}
		return quick;
	},
	quickIs = function( elem, m ) {
		var attrs = elem.attributes || {};
		return (
			(!m[1] || elem.nodeName.toLowerCase() === m[1]) &amp;&amp;
			(!m[2] || (attrs.id || {}).value === m[2]) &amp;&amp;
			(!m[3] || m[3].test( (attrs[ "class" ] || {}).value ))
		);
	},
	hoverHack = function( events ) {
		return jQuery.event.special.hover ? events : events.replace( rhoverHack, "mouseenter$1 mouseleave$1" );
	};

/*
 * Helper functions for managing events -- not part of the public interface.
 * Props to Dean Edwards' addEvent library for many of the ideas.
 */
jQuery.event = {

	add: function( elem, types, handler, data, selector ) {

		var elemData, eventHandle, events,
			t, tns, type, namespaces, handleObj,
			handleObjIn, quick, handlers, special;

		// Don't attach events to noData or text/comment nodes (allow plain objects tho)
		if ( elem.nodeType === 3 || elem.nodeType === 8 || !types || !handler || !(elemData = jQuery._data( elem )) ) {
			return;
		}

		// Caller can pass in an object of custom data in lieu of the handler
		if ( handler.handler ) {
			handleObjIn = handler;
			handler = handleObjIn.handler;
			selector = handleObjIn.selector;
		}

		// Make sure that the handler has a unique ID, used to find/remove it later
		if ( !handler.guid ) {
			handler.guid = jQuery.guid++;
		}

		// Init the element's event structure and main handler, if this is the first
		events = elemData.events;
		if ( !events ) {
			elemData.events = events = {};
		}
		eventHandle = elemData.handle;
		if ( !eventHandle ) {
			elemData.handle = eventHandle = function( e ) {
				// Discard the second event of a jQuery.event.trigger() and
				// when an event is called after a page has unloaded
				return typeof jQuery !== "undefined" &amp;&amp; (!e || jQuery.event.triggered !== e.type) ?
					jQuery.event.dispatch.apply( eventHandle.elem, arguments ) :
					undefined;
			};
			// Add elem as a property of the handle fn to prevent a memory leak with IE non-native events
			eventHandle.elem = elem;
		}

		// Handle multiple events separated by a space
		// jQuery(...).bind("mouseover mouseout", fn);
		types = jQuery.trim( hoverHack(types) ).split( " " );
		for ( t = 0; t &lt; types.length; t++ ) {

			tns = rtypenamespace.exec( types[t] ) || [];
			type = tns[1];
			namespaces = ( tns[2] || "" ).split( "." ).sort();

			// If event changes its type, use the special event handlers for the changed type
			special = jQuery.event.special[ type ] || {};

			// If selector defined, determine special event api type, otherwise given type
			type = ( selector ? special.delegateType : special.bindType ) || type;

			// Update special based on newly reset type
			special = jQuery.event.special[ type ] || {};

			// handleObj is passed to all event handlers
			handleObj = jQuery.extend({
				type: type,
				origType: tns[1],
				data: data,
				handler: handler,
				guid: handler.guid,
				selector: selector,
				quick: selector &amp;&amp; quickParse( selector ),
				namespace: namespaces.join(".")
			}, handleObjIn );

			// Init the event handler queue if we're the first
			handlers = events[ type ];
			if ( !handlers ) {
				handlers = events[ type ] = [];
				handlers.delegateCount = 0;

				// Only use addEventListener/attachEvent if the special events handler returns false
				if ( !special.setup || special.setup.call( elem, data, namespaces, eventHandle ) === false ) {
					// Bind the global event handler to the element
					if ( elem.addEventListener ) {
						elem.addEventListener( type, eventHandle, false );

					} else if ( elem.attachEvent ) {
						elem.attachEvent( "on" + type, eventHandle );
					}
				}
			}

			if ( special.add ) {
				special.add.call( elem, handleObj );

				if ( !handleObj.handler.guid ) {
					handleObj.handler.guid = handler.guid;
				}
			}

			// Add to the element's handler list, delegates in front
			if ( selector ) {
				handlers.splice( handlers.delegateCount++, 0, handleObj );
			} else {
				handlers.push( handleObj );
			}

			// Keep track of which events have ever been used, for event optimization
			jQuery.event.global[ type ] = true;
		}

		// Nullify elem to prevent memory leaks in IE
		elem = null;
	},

	global: {},

	// Detach an event or set of events from an element
	remove: function( elem, types, handler, selector, mappedTypes ) {

		var elemData = jQuery.hasData( elem ) &amp;&amp; jQuery._data( elem ),
			t, tns, type, origType, namespaces, origCount,
			j, events, special, handle, eventType, handleObj;

		if ( !elemData || !(events = elemData.events) ) {
			return;
		}

		// Once for each type.namespace in types; type may be omitted
		types = jQuery.trim( hoverHack( types || "" ) ).split(" ");
		for ( t = 0; t &lt; types.length; t++ ) {
			tns = rtypenamespace.exec( types[t] ) || [];
			type = origType = tns[1];
			namespaces = tns[2];

			// Unbind all events (on this namespace, if provided) for the element
			if ( !type ) {
				for ( type in events ) {
					jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
				}
				continue;
			}

			special = jQuery.event.special[ type ] || {};
			type = ( selector? special.delegateType : special.bindType ) || type;
			eventType = events[ type ] || [];
			origCount = eventType.length;
			namespaces = namespaces ? new RegExp("(^|\\.)" + namespaces.split(".").sort().join("\\.(?:.*\\.)?") + "(\\.|$)") : null;

			// Remove matching events
			for ( j = 0; j &lt; eventType.length; j++ ) {
				handleObj = eventType[ j ];

				if ( ( mappedTypes || origType === handleObj.origType ) &amp;&amp;
					 ( !handler || handler.guid === handleObj.guid ) &amp;&amp;
					 ( !namespaces || namespaces.test( handleObj.namespace ) ) &amp;&amp;
					 ( !selector || selector === handleObj.selector || selector === "**" &amp;&amp; handleObj.selector ) ) {
					eventType.splice( j--, 1 );

					if ( handleObj.selector ) {
						eventType.delegateCount--;
					}
					if ( special.remove ) {
						special.remove.call( elem, handleObj );
					}
				}
			}

			// Remove generic event handler if we removed something and no more handlers exist
			// (avoids potential for endless recursion during removal of special event handlers)
			if ( eventType.length === 0 &amp;&amp; origCount !== eventType.length ) {
				if ( !special.teardown || special.teardown.call( elem, namespaces ) === false ) {
					jQuery.removeEvent( elem, type, elemData.handle );
				}

				delete events[ type ];
			}
		}

		// Remove the expando if it's no longer used
		if ( jQuery.isEmptyObject( events ) ) {
			handle = elemData.handle;
			if ( handle ) {
				handle.elem = null;
			}

			// removeData also checks for emptiness and clears the expando if empty
			// so use it instead of delete
			jQuery.removeData( elem, [ "events", "handle" ], true );
		}
	},

	// Events that are safe to short-circuit if no handlers are attached.
	// Native DOM events should not be added, they may have inline handlers.
	customEvent: {
		"getData": true,
		"setData": true,
		"changeData": true
	},

	trigger: function( event, data, elem, onlyHandlers ) {
		// Don't do events on text and comment nodes
		if ( elem &amp;&amp; (elem.nodeType === 3 || elem.nodeType === 8) ) {
			return;
		}

		// Event object or event type
		var type = event.type || event,
			namespaces = [],
			cache, exclusive, i, cur, old, ontype, special, handle, eventPath, bubbleType;

		// focus/blur morphs to focusin/out; ensure we're not firing them right now
		if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
			return;
		}

		if ( type.indexOf( "!" ) &gt;= 0 ) {
			// Exclusive events trigger only for the exact event (no namespaces)
			type = type.slice(0, -1);
			exclusive = true;
		}

		if ( type.indexOf( "." ) &gt;= 0 ) {
			// Namespaced trigger; create a regexp to match event type in handle()
			namespaces = type.split(".");
			type = namespaces.shift();
			namespaces.sort();
		}

		if ( (!elem || jQuery.event.customEvent[ type ]) &amp;&amp; !jQuery.event.global[ type ] ) {
			// No jQuery handlers for this event type, and it can't have inline handlers
			return;
		}

		// Caller can pass in an Event, Object, or just an event type string
		event = typeof event === "object" ?
			// jQuery.Event object
			event[ jQuery.expando ] ? event :
			// Object literal
			new jQuery.Event( type, event ) :
			// Just the event type (string)
			new jQuery.Event( type );

		event.type = type;
		event.isTrigger = true;
		event.exclusive = exclusive;
		event.namespace = namespaces.join( "." );
		event.namespace_re = event.namespace? new RegExp("(^|\\.)" + namespaces.join("\\.(?:.*\\.)?") + "(\\.|$)") : null;
		ontype = type.indexOf( ":" ) &lt; 0 ? "on" + type : "";

		// Handle a global trigger
		if ( !elem ) {

			// TODO: Stop taunting the data cache; remove global events and always attach to document
			cache = jQuery.cache;
			for ( i in cache ) {
				if ( cache[ i ].events &amp;&amp; cache[ i ].events[ type ] ) {
					jQuery.event.trigger( event, data, cache[ i ].handle.elem, true );
				}
			}
			return;
		}

		// Clean up the event in case it is being reused
		event.result = undefined;
		if ( !event.target ) {
			event.target = elem;
		}

		// Clone any incoming data and prepend the event, creating the handler arg list
		data = data != null ? jQuery.makeArray( data ) : [];
		data.unshift( event );

		// Allow special events to draw outside the lines
		special = jQuery.event.special[ type ] || {};
		if ( special.trigger &amp;&amp; special.trigger.apply( elem, data ) === false ) {
			return;
		}

		// Determine event propagation path in advance, per W3C events spec (#9951)
		// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
		eventPath = [[ elem, special.bindType || type ]];
		if ( !onlyHandlers &amp;&amp; !special.noBubble &amp;&amp; !jQuery.isWindow( elem ) ) {

			bubbleType = special.delegateType || type;
			cur = rfocusMorph.test( bubbleType + type ) ? elem : elem.parentNode;
			old = null;
			for ( ; cur; cur = cur.parentNode ) {
				eventPath.push([ cur, bubbleType ]);
				old = cur;
			}

			// Only add window if we got to document (e.g., not plain obj or detached DOM)
			if ( old &amp;&amp; old === elem.ownerDocument ) {
				eventPath.push([ old.defaultView || old.parentWindow || window, bubbleType ]);
			}
		}

		// Fire handlers on the event path
		for ( i = 0; i &lt; eventPath.length &amp;&amp; !event.isPropagationStopped(); i++ ) {

			cur = eventPath[i][0];
			event.type = eventPath[i][1];

			handle = ( jQuery._data( cur, "events" ) || {} )[ event.type ] &amp;&amp; jQuery._data( cur, "handle" );
			if ( handle ) {
				handle.apply( cur, data );
			}
			// Note that this is a bare JS function and not a jQuery handler
			handle = ontype &amp;&amp; cur[ ontype ];
			if ( handle &amp;&amp; jQuery.acceptData( cur ) &amp;&amp; handle.apply( cur, data ) === false ) {
				event.preventDefault();
			}
		}
		event.type = type;

		// If nobody prevented the default action, do it now
		if ( !onlyHandlers &amp;&amp; !event.isDefaultPrevented() ) {

			if ( (!special._default || special._default.apply( elem.ownerDocument, data ) === false) &amp;&amp;
				!(type === "click" &amp;&amp; jQuery.nodeName( elem, "a" )) &amp;&amp; jQuery.acceptData( elem ) ) {

				// Call a native DOM method on the target with the same name name as the event.
				// Can't use an .isFunction() check here because IE6/7 fails that test.
				// Don't do default actions on window, that's where global variables be (#6170)
				// IE&lt;9 dies on focus/blur to hidden element (#1486)
				if ( ontype &amp;&amp; elem[ type ] &amp;&amp; ((type !== "focus" &amp;&amp; type !== "blur") || event.target.offsetWidth !== 0) &amp;&amp; !jQuery.isWindow( elem ) ) {

					// Don't re-trigger an onFOO event when we call its FOO() method
					old = elem[ ontype ];

					if ( old ) {
						elem[ ontype ] = null;
					}

					// Prevent re-triggering of the same event, since we already bubbled it above
					jQuery.event.triggered = type;
					elem[ type ]();
					jQuery.event.triggered = undefined;

					if ( old ) {
						elem[ ontype ] = old;
					}
				}
			}
		}

		return event.result;
	},

	dispatch: function( event ) {

		// Make a writable jQuery.Event from the native event object
		event = jQuery.event.fix( event || window.event );

		var handlers = ( (jQuery._data( this, "events" ) || {} )[ event.type ] || []),
			delegateCount = handlers.delegateCount,
			args = [].slice.call( arguments, 0 ),
			run_all = !event.exclusive &amp;&amp; !event.namespace,
			special = jQuery.event.special[ event.type ] || {},
			handlerQueue = [],
			i, j, cur, jqcur, ret, selMatch, matched, matches, handleObj, sel, related;

		// Use the fix-ed jQuery.Event rather than the (read-only) native event
		args[0] = event;
		event.delegateTarget = this;

		// Call the preDispatch hook for the mapped type, and let it bail if desired
		if ( special.preDispatch &amp;&amp; special.preDispatch.call( this, event ) === false ) {
			return;
		}

		// Determine handlers that should run if there are delegated events
		// Avoid non-left-click bubbling in Firefox (#3861)
		if ( delegateCount &amp;&amp; !(event.button &amp;&amp; event.type === "click") ) {

			// Pregenerate a single jQuery object for reuse with .is()
			jqcur = jQuery(this);
			jqcur.context = this.ownerDocument || this;

			for ( cur = event.target; cur != this; cur = cur.parentNode || this ) {

				// Don't process events on disabled elements (#6911, #8165)
				if ( cur.disabled !== true ) {
					selMatch = {};
					matches = [];
					jqcur[0] = cur;
					for ( i = 0; i &lt; delegateCount; i++ ) {
						handleObj = handlers[ i ];
						sel = handleObj.selector;

						if ( selMatch[ sel ] === undefined ) {
							selMatch[ sel ] = (
								handleObj.quick ? quickIs( cur, handleObj.quick ) : jqcur.is( sel )
							);
						}
						if ( selMatch[ sel ] ) {
							matches.push( handleObj );
						}
					}
					if ( matches.length ) {
						handlerQueue.push({ elem: cur, matches: matches });
					}
				}
			}
		}

		// Add the remaining (directly-bound) handlers
		if ( handlers.length &gt; delegateCount ) {
			handlerQueue.push({ elem: this, matches: handlers.slice( delegateCount ) });
		}

		// Run delegates first; they may want to stop propagation beneath us
		for ( i = 0; i &lt; handlerQueue.length &amp;&amp; !event.isPropagationStopped(); i++ ) {
			matched = handlerQueue[ i ];
			event.currentTarget = matched.elem;

			for ( j = 0; j &lt; matched.matches.length &amp;&amp; !event.isImmediatePropagationStopped(); j++ ) {
				handleObj = matched.matches[ j ];

				// Triggered event must either 1) be non-exclusive and have no namespace, or
				// 2) have namespace(s) a subset or equal to those in the bound event (both can have no namespace).
				if ( run_all || (!event.namespace &amp;&amp; !handleObj.namespace) || event.namespace_re &amp;&amp; event.namespace_re.test( handleObj.namespace ) ) {

					event.data = handleObj.data;
					event.handleObj = handleObj;

					ret = ( (jQuery.event.special[ handleObj.origType ] || {}).handle || handleObj.handler )
							.apply( matched.elem, args );

					if ( ret !== undefined ) {
						event.result = ret;
						if ( ret === false ) {
							event.preventDefault();
							event.stopPropagation();
						}
					}
				}
			}
		}

		// Call the postDispatch hook for the mapped type
		if ( special.postDispatch ) {
			special.postDispatch.call( this, event );
		}

		return event.result;
	},

	// Includes some event props shared by KeyEvent and MouseEvent
	// *** attrChange attrName relatedNode srcElement  are not normalized, non-W3C, deprecated, will be removed in 1.8 ***
	props: "attrChange attrName relatedNode srcElement altKey bubbles cancelable ctrlKey currentTarget eventPhase metaKey relatedTarget shiftKey target timeStamp view which".split(" "),

	fixHooks: {},

	keyHooks: {
		props: "char charCode key keyCode".split(" "),
		filter: function( event, original ) {

			// Add which for key events
			if ( event.which == null ) {
				event.which = original.charCode != null ? original.charCode : original.keyCode;
			}

			return event;
		}
	},

	mouseHooks: {
		props: "button buttons clientX clientY fromElement offsetX offsetY pageX pageY screenX screenY toElement".split(" "),
		filter: function( event, original ) {
			var eventDoc, doc, body,
				button = original.button,
				fromElement = original.fromElement;

			// Calculate pageX/Y if missing and clientX/Y available
			if ( event.pageX == null &amp;&amp; original.clientX != null ) {
				eventDoc = event.target.ownerDocument || document;
				doc = eventDoc.documentElement;
				body = eventDoc.body;

				event.pageX = original.clientX + ( doc &amp;&amp; doc.scrollLeft || body &amp;&amp; body.scrollLeft || 0 ) - ( doc &amp;&amp; doc.clientLeft || body &amp;&amp; body.clientLeft || 0 );
				event.pageY = original.clientY + ( doc &amp;&amp; doc.scrollTop  || body &amp;&amp; body.scrollTop  || 0 ) - ( doc &amp;&amp; doc.clientTop  || body &amp;&amp; body.clientTop  || 0 );
			}

			// Add relatedTarget, if necessary
			if ( !event.relatedTarget &amp;&amp; fromElement ) {
				event.relatedTarget = fromElement === event.target ? original.toElement : fromElement;
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			// Note: button is not normalized, so don't use it
			if ( !event.which &amp;&amp; button !== undefined ) {
				event.which = ( button &amp; 1 ? 1 : ( button &amp; 2 ? 3 : ( button &amp; 4 ? 2 : 0 ) ) );
			}

			return event;
		}
	},

	fix: function( event ) {
		if ( event[ jQuery.expando ] ) {
			return event;
		}

		// Create a writable copy of the event object and normalize some properties
		var i, prop,
			originalEvent = event,
			fixHook = jQuery.event.fixHooks[ event.type ] || {},
			copy = fixHook.props ? this.props.concat( fixHook.props ) : this.props;

		event = jQuery.Event( originalEvent );

		for ( i = copy.length; i; ) {
			prop = copy[ --i ];
			event[ prop ] = originalEvent[ prop ];
		}

		// Fix target property, if necessary (#1925, IE 6/7/8 &amp; Safari2)
		if ( !event.target ) {
			event.target = originalEvent.srcElement || document;
		}

		// Target should not be a text node (#504, Safari)
		if ( event.target.nodeType === 3 ) {
			event.target = event.target.parentNode;
		}

		// For mouse/key events; add metaKey if it's not there (#3368, IE6/7/8)
		if ( event.metaKey === undefined ) {
			event.metaKey = event.ctrlKey;
		}

		return fixHook.filter? fixHook.filter( event, originalEvent ) : event;
	},

	special: {
		ready: {
			// Make sure the ready event is setup
			setup: jQuery.bindReady
		},

		load: {
			// Prevent triggered image.load events from bubbling to window.load
			noBubble: true
		},

		focus: {
			delegateType: "focusin"
		},
		blur: {
			delegateType: "focusout"
		},

		beforeunload: {
			setup: function( data, namespaces, eventHandle ) {
				// We only want to do this special case on windows
				if ( jQuery.isWindow( this ) ) {
					this.onbeforeunload = eventHandle;
				}
			},

			teardown: function( namespaces, eventHandle ) {
				if ( this.onbeforeunload === eventHandle ) {
					this.onbeforeunload = null;
				}
			}
		}
	},

	simulate: function( type, elem, event, bubble ) {
		// Piggyback on a donor event to simulate a different one.
		// Fake originalEvent to avoid donor's stopPropagation, but if the
		// simulated event prevents default then we do the same on the donor.
		var e = jQuery.extend(
			new jQuery.Event(),
			event,
			{ type: type,
				isSimulated: true,
				originalEvent: {}
			}
		);
		if ( bubble ) {
			jQuery.event.trigger( e, null, elem );
		} else {
			jQuery.event.dispatch.call( elem, e );
		}
		if ( e.isDefaultPrevented() ) {
			event.preventDefault();
		}
	}
};

// Some plugins are using, but it's undocumented/deprecated and will be removed.
// The 1.7 special event interface should provide all the hooks needed now.
jQuery.event.handle = jQuery.event.dispatch;

jQuery.removeEvent = document.removeEventListener ?
	function( elem, type, handle ) {
		if ( elem.removeEventListener ) {
			elem.removeEventListener( type, handle, false );
		}
	} :
	function( elem, type, handle ) {
		if ( elem.detachEvent ) {
			elem.detachEvent( "on" + type, handle );
		}
	};

jQuery.Event = function( src, props ) {
	// Allow instantiation without the 'new' keyword
	if ( !(this instanceof jQuery.Event) ) {
		return new jQuery.Event( src, props );
	}

	// Event object
	if ( src &amp;&amp; src.type ) {
		this.originalEvent = src;
		this.type = src.type;

		// Events bubbling up the document may have been marked as prevented
		// by a handler lower down the tree; reflect the correct value.
		this.isDefaultPrevented = ( src.defaultPrevented || src.returnValue === false ||
			src.getPreventDefault &amp;&amp; src.getPreventDefault() ) ? returnTrue : returnFalse;

	// Event type
	} else {
		this.type = src;
	}

	// Put explicitly provided properties onto the event object
	if ( props ) {
		jQuery.extend( this, props );
	}

	// Create a timestamp if incoming event doesn't have one
	this.timeStamp = src &amp;&amp; src.timeStamp || jQuery.now();

	// Mark it as fixed
	this[ jQuery.expando ] = true;
};

function returnFalse() {
	return false;
}
function returnTrue() {
	return true;
}

// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
// http://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
jQuery.Event.prototype = {
	preventDefault: function() {
		this.isDefaultPrevented = returnTrue;

		var e = this.originalEvent;
		if ( !e ) {
			return;
		}

		// if preventDefault exists run it on the original event
		if ( e.preventDefault ) {
			e.preventDefault();

		// otherwise set the returnValue property of the original event to false (IE)
		} else {
			e.returnValue = false;
		}
	},
	stopPropagation: function() {
		this.isPropagationStopped = returnTrue;

		var e = this.originalEvent;
		if ( !e ) {
			return;
		}
		// if stopPropagation exists run it on the original event
		if ( e.stopPropagation ) {
			e.stopPropagation();
		}
		// otherwise set the cancelBubble property of the original event to true (IE)
		e.cancelBubble = true;
	},
	stopImmediatePropagation: function() {
		this.isImmediatePropagationStopped = returnTrue;
		this.stopPropagation();
	},
	isDefaultPrevented: returnFalse,
	isPropagationStopped: returnFalse,
	isImmediatePropagationStopped: returnFalse
};

// Create mouseenter/leave events using mouseover/out and event-time checks
jQuery.each({
	mouseenter: "mouseover",
	mouseleave: "mouseout"
}, function( orig, fix ) {
	jQuery.event.special[ orig ] = {
		delegateType: fix,
		bindType: fix,

		handle: function( event ) {
			var target = this,
				related = event.relatedTarget,
				handleObj = event.handleObj,
				selector = handleObj.selector,
				ret;

			// For mousenter/leave call the handler if related is outside the target.
			// NB: No relatedTarget if the mouse left/entered the browser window
			if ( !related || (related !== target &amp;&amp; !jQuery.contains( target, related )) ) {
				event.type = handleObj.origType;
				ret = handleObj.handler.apply( this, arguments );
				event.type = fix;
			}
			return ret;
		}
	};
});

// IE submit delegation
if ( !jQuery.support.submitBubbles ) {

	jQuery.event.special.submit = {
		setup: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Lazy-add a submit handler when a descendant form may potentially be submitted
			jQuery.event.add( this, "click._submit keypress._submit", function( e ) {
				// Node name check avoids a VML-related crash in IE (#9807)
				var elem = e.target,
					form = jQuery.nodeName( elem, "input" ) || jQuery.nodeName( elem, "button" ) ? elem.form : undefined;
				if ( form &amp;&amp; !form._submit_attached ) {
					jQuery.event.add( form, "submit._submit", function( event ) {
						event._submit_bubble = true;
					});
					form._submit_attached = true;
				}
			});
			// return undefined since we don't need an event listener
		},
		
		postDispatch: function( event ) {
			// If form was submitted by the user, bubble the event up the tree
			if ( event._submit_bubble ) {
				delete event._submit_bubble;
				if ( this.parentNode &amp;&amp; !event.isTrigger ) {
					jQuery.event.simulate( "submit", this.parentNode, event, true );
				}
			}
		},

		teardown: function() {
			// Only need this for delegated form submit events
			if ( jQuery.nodeName( this, "form" ) ) {
				return false;
			}

			// Remove delegated handlers; cleanData eventually reaps submit handlers attached above
			jQuery.event.remove( this, "._submit" );
		}
	};
}

// IE change delegation and checkbox/radio fix
if ( !jQuery.support.changeBubbles ) {

	jQuery.event.special.change = {

		setup: function() {

			if ( rformElems.test( this.nodeName ) ) {
				// IE doesn't fire change on a check/radio until blur; trigger it on click
				// after a propertychange. Eat the blur-change in special.change.handle.
				// This still fires onchange a second time for check/radio after blur.
				if ( this.type === "checkbox" || this.type === "radio" ) {
					jQuery.event.add( this, "propertychange._change", function( event ) {
						if ( event.originalEvent.propertyName === "checked" ) {
							this._just_changed = true;
						}
					});
					jQuery.event.add( this, "click._change", function( event ) {
						if ( this._just_changed &amp;&amp; !event.isTrigger ) {
							this._just_changed = false;
							jQuery.event.simulate( "change", this, event, true );
						}
					});
				}
				return false;
			}
			// Delegated event; lazy-add a change handler on descendant inputs
			jQuery.event.add( this, "beforeactivate._change", function( e ) {
				var elem = e.target;

				if ( rformElems.test( elem.nodeName ) &amp;&amp; !elem._change_attached ) {
					jQuery.event.add( elem, "change._change", function( event ) {
						if ( this.parentNode &amp;&amp; !event.isSimulated &amp;&amp; !event.isTrigger ) {
							jQuery.event.simulate( "change", this.parentNode, event, true );
						}
					});
					elem._change_attached = true;
				}
			});
		},

		handle: function( event ) {
			var elem = event.target;

			// Swallow native change events from checkbox/radio, we already triggered them above
			if ( this !== elem || event.isSimulated || event.isTrigger || (elem.type !== "radio" &amp;&amp; elem.type !== "checkbox") ) {
				return event.handleObj.handler.apply( this, arguments );
			}
		},

		teardown: function() {
			jQuery.event.remove( this, "._change" );

			return rformElems.test( this.nodeName );
		}
	};
}

// Create "bubbling" focus and blur events
if ( !jQuery.support.focusinBubbles ) {
	jQuery.each({ focus: "focusin", blur: "focusout" }, function( orig, fix ) {

		// Attach a single capturing handler while someone wants focusin/focusout
		var attaches = 0,
			handler = function( event ) {
				jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ), true );
			};

		jQuery.event.special[ fix ] = {
			setup: function() {
				if ( attaches++ === 0 ) {
					document.addEventListener( orig, handler, true );
				}
			},
			teardown: function() {
				if ( --attaches === 0 ) {
					document.removeEventListener( orig, handler, true );
				}
			}
		};
	});
}

jQuery.fn.extend({

	on: function( types, selector, data, fn, /*INTERNAL*/ one ) {
		var origFn, type;

		// Types can be a map of types/handlers
		if ( typeof types === "object" ) {
			// ( types-Object, selector, data )
			if ( typeof selector !== "string" ) { // &amp;&amp; selector != null
				// ( types-Object, data )
				data = data || selector;
				selector = undefined;
			}
			for ( type in types ) {
				this.on( type, selector, data, types[ type ], one );
			}
			return this;
		}

		if ( data == null &amp;&amp; fn == null ) {
			// ( types, fn )
			fn = selector;
			data = selector = undefined;
		} else if ( fn == null ) {
			if ( typeof selector === "string" ) {
				// ( types, selector, fn )
				fn = data;
				data = undefined;
			} else {
				// ( types, data, fn )
				fn = data;
				data = selector;
				selector = undefined;
			}
		}
		if ( fn === false ) {
			fn = returnFalse;
		} else if ( !fn ) {
			return this;
		}

		if ( one === 1 ) {
			origFn = fn;
			fn = function( event ) {
				// Can use an empty set, since event contains the info
				jQuery().off( event );
				return origFn.apply( this, arguments );
			};
			// Use same guid so caller can remove using origFn
			fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
		}
		return this.each( function() {
			jQuery.event.add( this, types, fn, data, selector );
		});
	},
	one: function( types, selector, data, fn ) {
		return this.on( types, selector, data, fn, 1 );
	},
	off: function( types, selector, fn ) {
		if ( types &amp;&amp; types.preventDefault &amp;&amp; types.handleObj ) {
			// ( event )  dispatched jQuery.Event
			var handleObj = types.handleObj;
			jQuery( types.delegateTarget ).off(
				handleObj.namespace ? handleObj.origType + "." + handleObj.namespace : handleObj.origType,
				handleObj.selector,
				handleObj.handler
			);
			return this;
		}
		if ( typeof types === "object" ) {
			// ( types-object [, selector] )
			for ( var type in types ) {
				this.off( type, selector, types[ type ] );
			}
			return this;
		}
		if ( selector === false || typeof selector === "function" ) {
			// ( types [, fn] )
			fn = selector;
			selector = undefined;
		}
		if ( fn === false ) {
			fn = returnFalse;
		}
		return this.each(function() {
			jQuery.event.remove( this, types, fn, selector );
		});
	},

	bind: function( types, data, fn ) {
		return this.on( types, null, data, fn );
	},
	unbind: function( types, fn ) {
		return this.off( types, null, fn );
	},

	live: function( types, data, fn ) {
		jQuery( this.context ).on( types, this.selector, data, fn );
		return this;
	},
	die: function( types, fn ) {
		jQuery( this.context ).off( types, this.selector || "**", fn );
		return this;
	},

	delegate: function( selector, types, data, fn ) {
		return this.on( types, selector, data, fn );
	},
	undelegate: function( selector, types, fn ) {
		// ( namespace ) or ( selector, types [, fn] )
		return arguments.length == 1? this.off( selector, "**" ) : this.off( types, selector, fn );
	},

	trigger: function( type, data ) {
		return this.each(function() {
			jQuery.event.trigger( type, data, this );
		});
	},
	triggerHandler: function( type, data ) {
		if ( this[0] ) {
			return jQuery.event.trigger( type, data, this[0], true );
		}
	},

	toggle: function( fn ) {
		// Save reference to arguments for access in closure
		var args = arguments,
			guid = fn.guid || jQuery.guid++,
			i = 0,
			toggler = function( event ) {
				// Figure out which function to execute
				var lastToggle = ( jQuery._data( this, "lastToggle" + fn.guid ) || 0 ) % i;
				jQuery._data( this, "lastToggle" + fn.guid, lastToggle + 1 );

				// Make sure that clicks stop
				event.preventDefault();

				// and execute the function
				return args[ lastToggle ].apply( this, arguments ) || false;
			};

		// link all the functions, so any of them can unbind this click handler
		toggler.guid = guid;
		while ( i &lt; args.length ) {
			args[ i++ ].guid = guid;
		}

		return this.click( toggler );
	},

	hover: function( fnOver, fnOut ) {
		return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
	}
});

jQuery.each( ("blur focus focusin focusout load resize scroll unload click dblclick " +
	"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
	"change select submit keydown keypress keyup error contextmenu").split(" "), function( i, name ) {

	// Handle event binding
	jQuery.fn[ name ] = function( data, fn ) {
		if ( fn == null ) {
			fn = data;
			data = null;
		}

		return arguments.length &gt; 0 ?
			this.on( name, null, data, fn ) :
			this.trigger( name );
	};

	if ( jQuery.attrFn ) {
		jQuery.attrFn[ name ] = true;
	}

	if ( rkeyEvent.test( name ) ) {
		jQuery.event.fixHooks[ name ] = jQuery.event.keyHooks;
	}

	if ( rmouseEvent.test( name ) ) {
		jQuery.event.fixHooks[ name ] = jQuery.event.mouseHooks;
	}
});



/*!
 * Sizzle CSS Selector Engine
 *  Copyright 2011, The Dojo Foundation
 *  Released under the MIT, BSD, and GPL Licenses.
 *  More information: http://sizzlejs.com/
 */
(function(){

var chunker = /((?:\((?:\([^()]+\)|[^()]+)+\)|\[(?:\[[^\[\]]*\]|['"][^'"]*['"]|[^\[\]'"]+)+\]|\\.|[^ &gt;+~,(\[\\]+)+|[&gt;+~])(\s*,\s*)?((?:.|\r|\n)*)/g,
	expando = "sizcache" + (Math.random() + '').replace('.', ''),
	done = 0,
	toString = Object.prototype.toString,
	hasDuplicate = false,
	baseHasDuplicate = true,
	rBackslash = /\\/g,
	rReturn = /\r\n/g,
	rNonWord = /\W/;

// Here we check if the JavaScript engine is using some sort of
// optimization where it does not always call our comparision
// function. If that is the case, discard the hasDuplicate value.
//   Thus far that includes Google Chrome.
[0, 0].sort(function() {
	baseHasDuplicate = false;
	return 0;
});

var Sizzle = function( selector, context, results, seed ) {
	results = results || [];
	context = context || document;

	var origContext = context;

	if ( context.nodeType !== 1 &amp;&amp; context.nodeType !== 9 ) {
		return [];
	}

	if ( !selector || typeof selector !== "string" ) {
		return results;
	}

	var m, set, checkSet, extra, ret, cur, pop, i,
		prune = true,
		contextXML = Sizzle.isXML( context ),
		parts = [],
		soFar = selector;

	// Reset the position of the chunker regexp (start from head)
	do {
		chunker.exec( "" );
		m = chunker.exec( soFar );

		if ( m ) {
			soFar = m[3];

			parts.push( m[1] );

			if ( m[2] ) {
				extra = m[3];
				break;
			}
		}
	} while ( m );

	if ( parts.length &gt; 1 &amp;&amp; origPOS.exec( selector ) ) {

		if ( parts.length === 2 &amp;&amp; Expr.relative[ parts[0] ] ) {
			set = posProcess( parts[0] + parts[1], context, seed );

		} else {
			set = Expr.relative[ parts[0] ] ?
				[ context ] :
				Sizzle( parts.shift(), context );

			while ( parts.length ) {
				selector = parts.shift();

				if ( Expr.relative[ selector ] ) {
					selector += parts.shift();
				}

				set = posProcess( selector, set, seed );
			}
		}

	} else {
		// Take a shortcut and set the context if the root selector is an ID
		// (but not if it'll be faster if the inner selector is an ID)
		if ( !seed &amp;&amp; parts.length &gt; 1 &amp;&amp; context.nodeType === 9 &amp;&amp; !contextXML &amp;&amp;
				Expr.match.ID.test(parts[0]) &amp;&amp; !Expr.match.ID.test(parts[parts.length - 1]) ) {

			ret = Sizzle.find( parts.shift(), context, contextXML );
			context = ret.expr ?
				Sizzle.filter( ret.expr, ret.set )[0] :
				ret.set[0];
		}

		if ( context ) {
			ret = seed ?
				{ expr: parts.pop(), set: makeArray(seed) } :
				Sizzle.find( parts.pop(), parts.length === 1 &amp;&amp; (parts[0] === "~" || parts[0] === "+") &amp;&amp; context.parentNode ? context.parentNode : context, contextXML );

			set = ret.expr ?
				Sizzle.filter( ret.expr, ret.set ) :
				ret.set;

			if ( parts.length &gt; 0 ) {
				checkSet = makeArray( set );

			} else {
				prune = false;
			}

			while ( parts.length ) {
				cur = parts.pop();
				pop = cur;

				if ( !Expr.relative[ cur ] ) {
					cur = "";
				} else {
					pop = parts.pop();
				}

				if ( pop == null ) {
					pop = context;
				}

				Expr.relative[ cur ]( checkSet, pop, contextXML );
			}

		} else {
			checkSet = parts = [];
		}
	}

	if ( !checkSet ) {
		checkSet = set;
	}

	if ( !checkSet ) {
		Sizzle.error( cur || selector );
	}

	if ( toString.call(checkSet) === "[object Array]" ) {
		if ( !prune ) {
			results.push.apply( results, checkSet );

		} else if ( context &amp;&amp; context.nodeType === 1 ) {
			for ( i = 0; checkSet[i] != null; i++ ) {
				if ( checkSet[i] &amp;&amp; (checkSet[i] === true || checkSet[i].nodeType === 1 &amp;&amp; Sizzle.contains(context, checkSet[i])) ) {
					results.push( set[i] );
				}
			}

		} else {
			for ( i = 0; checkSet[i] != null; i++ ) {
				if ( checkSet[i] &amp;&amp; checkSet[i].nodeType === 1 ) {
					results.push( set[i] );
				}
			}
		}

	} else {
		makeArray( checkSet, results );
	}

	if ( extra ) {
		Sizzle( extra, origContext, results, seed );
		Sizzle.uniqueSort( results );
	}

	return results;
};

Sizzle.uniqueSort = function( results ) {
	if ( sortOrder ) {
		hasDuplicate = baseHasDuplicate;
		results.sort( sortOrder );

		if ( hasDuplicate ) {
			for ( var i = 1; i &lt; results.length; i++ ) {
				if ( results[i] === results[ i - 1 ] ) {
					results.splice( i--, 1 );
				}
			}
		}
	}

	return results;
};

Sizzle.matches = function( expr, set ) {
	return Sizzle( expr, null, null, set );
};

Sizzle.matchesSelector = function( node, expr ) {
	return Sizzle( expr, null, null, [node] ).length &gt; 0;
};

Sizzle.find = function( expr, context, isXML ) {
	var set, i, len, match, type, left;

	if ( !expr ) {
		return [];
	}

	for ( i = 0, len = Expr.order.length; i &lt; len; i++ ) {
		type = Expr.order[i];

		if ( (match = Expr.leftMatch[ type ].exec( expr )) ) {
			left = match[1];
			match.splice( 1, 1 );

			if ( left.substr( left.length - 1 ) !== "\\" ) {
				match[1] = (match[1] || "").replace( rBackslash, "" );
				set = Expr.find[ type ]( match, context, isXML );

				if ( set != null ) {
					expr = expr.replace( Expr.match[ type ], "" );
					break;
				}
			}
		}
	}

	if ( !set ) {
		set = typeof context.getElementsByTagName !== "undefined" ?
			context.getElementsByTagName( "*" ) :
			[];
	}

	return { set: set, expr: expr };
};

Sizzle.filter = function( expr, set, inplace, not ) {
	var match, anyFound,
		type, found, item, filter, left,
		i, pass,
		old = expr,
		result = [],
		curLoop = set,
		isXMLFilter = set &amp;&amp; set[0] &amp;&amp; Sizzle.isXML( set[0] );

	while ( expr &amp;&amp; set.length ) {
		for ( type in Expr.filter ) {
			if ( (match = Expr.leftMatch[ type ].exec( expr )) != null &amp;&amp; match[2] ) {
				filter = Expr.filter[ type ];
				left = match[1];

				anyFound = false;

				match.splice(1,1);

				if ( left.substr( left.length - 1 ) === "\\" ) {
					continue;
				}

				if ( curLoop === result ) {
					result = [];
				}

				if ( Expr.preFilter[ type ] ) {
					match = Expr.preFilter[ type ]( match, curLoop, inplace, result, not, isXMLFilter );

					if ( !match ) {
						anyFound = found = true;

					} else if ( match === true ) {
						continue;
					}
				}

				if ( match ) {
					for ( i = 0; (item = curLoop[i]) != null; i++ ) {
						if ( item ) {
							found = filter( item, match, i, curLoop );
							pass = not ^ found;

							if ( inplace &amp;&amp; found != null ) {
								if ( pass ) {
									anyFound = true;

								} else {
									curLoop[i] = false;
								}

							} else if ( pass ) {
								result.push( item );
								anyFound = true;
							}
						}
					}
				}

				if ( found !== undefined ) {
					if ( !inplace ) {
						curLoop = result;
					}

					expr = expr.replace( Expr.match[ type ], "" );

					if ( !anyFound ) {
						return [];
					}

					break;
				}
			}
		}

		// Improper expression
		if ( expr === old ) {
			if ( anyFound == null ) {
				Sizzle.error( expr );

			} else {
				break;
			}
		}

		old = expr;
	}

	return curLoop;
};

Sizzle.error = function( msg ) {
	throw new Error( "Syntax error, unrecognized expression: " + msg );
};

/**
 * Utility function for retreiving the text value of an array of DOM nodes
 * @param {Array|Element} elem
 */
var getText = Sizzle.getText = function( elem ) {
    var i, node,
		nodeType = elem.nodeType,
		ret = "";

	if ( nodeType ) {
		if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
			// Use textContent || innerText for elements
			if ( typeof elem.textContent === 'string' ) {
				return elem.textContent;
			} else if ( typeof elem.innerText === 'string' ) {
				// Replace IE's carriage returns
				return elem.innerText.replace( rReturn, '' );
			} else {
				// Traverse it's children
				for ( elem = elem.firstChild; elem; elem = elem.nextSibling) {
					ret += getText( elem );
				}
			}
		} else if ( nodeType === 3 || nodeType === 4 ) {
			return elem.nodeValue;
		}
	} else {

		// If no nodeType, this is expected to be an array
		for ( i = 0; (node = elem[i]); i++ ) {
			// Do not traverse comment nodes
			if ( node.nodeType !== 8 ) {
				ret += getText( node );
			}
		}
	}
	return ret;
};

var Expr = Sizzle.selectors = {
	order: [ "ID", "NAME", "TAG" ],

	match: {
		ID: /#((?:[\w\u00c0-\uFFFF\-]|\\.)+)/,
		CLASS: /\.((?:[\w\u00c0-\uFFFF\-]|\\.)+)/,
		NAME: /\[name=['"]*((?:[\w\u00c0-\uFFFF\-]|\\.)+)['"]*\]/,
		ATTR: /\[\s*((?:[\w\u00c0-\uFFFF\-]|\\.)+)\s*(?:(\S?=)\s*(?:(['"])(.*?)\3|(#?(?:[\w\u00c0-\uFFFF\-]|\\.)*)|)|)\s*\]/,
		TAG: /^((?:[\w\u00c0-\uFFFF\*\-]|\\.)+)/,
		CHILD: /:(only|nth|last|first)-child(?:\(\s*(even|odd|(?:[+\-]?\d+|(?:[+\-]?\d*)?n\s*(?:[+\-]\s*\d+)?))\s*\))?/,
		POS: /:(nth|eq|gt|lt|first|last|even|odd)(?:\((\d*)\))?(?=[^\-]|$)/,
		PSEUDO: /:((?:[\w\u00c0-\uFFFF\-]|\\.)+)(?:\((['"]?)((?:\([^\)]+\)|[^\(\)]*)+)\2\))?/
	},

	leftMatch: {},

	attrMap: {
		"class": "className",
		"for": "htmlFor"
	},

	attrHandle: {
		href: function( elem ) {
			return elem.getAttribute( "href" );
		},
		type: function( elem ) {
			return elem.getAttribute( "type" );
		}
	},

	relative: {
		"+": function(checkSet, part){
			var isPartStr = typeof part === "string",
				isTag = isPartStr &amp;&amp; !rNonWord.test( part ),
				isPartStrNotTag = isPartStr &amp;&amp; !isTag;

			if ( isTag ) {
				part = part.toLowerCase();
			}

			for ( var i = 0, l = checkSet.length, elem; i &lt; l; i++ ) {
				if ( (elem = checkSet[i]) ) {
					while ( (elem = elem.previousSibling) &amp;&amp; elem.nodeType !== 1 ) {}

					checkSet[i] = isPartStrNotTag || elem &amp;&amp; elem.nodeName.toLowerCase() === part ?
						elem || false :
						elem === part;
				}
			}

			if ( isPartStrNotTag ) {
				Sizzle.filter( part, checkSet, true );
			}
		},

		"&gt;": function( checkSet, part ) {
			var elem,
				isPartStr = typeof part === "string",
				i = 0,
				l = checkSet.length;

			if ( isPartStr &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();

				for ( ; i &lt; l; i++ ) {
					elem = checkSet[i];

					if ( elem ) {
						var parent = elem.parentNode;
						checkSet[i] = parent.nodeName.toLowerCase() === part ? parent : false;
					}
				}

			} else {
				for ( ; i &lt; l; i++ ) {
					elem = checkSet[i];

					if ( elem ) {
						checkSet[i] = isPartStr ?
							elem.parentNode :
							elem.parentNode === part;
					}
				}

				if ( isPartStr ) {
					Sizzle.filter( part, checkSet, true );
				}
			}
		},

		"": function(checkSet, part, isXML){
			var nodeCheck,
				doneName = done++,
				checkFn = dirCheck;

			if ( typeof part === "string" &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();
				nodeCheck = part;
				checkFn = dirNodeCheck;
			}

			checkFn( "parentNode", part, doneName, checkSet, nodeCheck, isXML );
		},

		"~": function( checkSet, part, isXML ) {
			var nodeCheck,
				doneName = done++,
				checkFn = dirCheck;

			if ( typeof part === "string" &amp;&amp; !rNonWord.test( part ) ) {
				part = part.toLowerCase();
				nodeCheck = part;
				checkFn = dirNodeCheck;
			}

			checkFn( "previousSibling", part, doneName, checkSet, nodeCheck, isXML );
		}
	},

	find: {
		ID: function( match, context, isXML ) {
			if ( typeof context.getElementById !== "undefined" &amp;&amp; !isXML ) {
				var m = context.getElementById(match[1]);
				// Check parentNode to catch when Blackberry 4.6 returns
				// nodes that are no longer in the document #6963
				return m &amp;&amp; m.parentNode ? [m] : [];
			}
		},

		NAME: function( match, context ) {
			if ( typeof context.getElementsByName !== "undefined" ) {
				var ret = [],
					results = context.getElementsByName( match[1] );

				for ( var i = 0, l = results.length; i &lt; l; i++ ) {
					if ( results[i].getAttribute("name") === match[1] ) {
						ret.push( results[i] );
					}
				}

				return ret.length === 0 ? null : ret;
			}
		},

		TAG: function( match, context ) {
			if ( typeof context.getElementsByTagName !== "undefined" ) {
				return context.getElementsByTagName( match[1] );
			}
		}
	},
	preFilter: {
		CLASS: function( match, curLoop, inplace, result, not, isXML ) {
			match = " " + match[1].replace( rBackslash, "" ) + " ";

			if ( isXML ) {
				return match;
			}

			for ( var i = 0, elem; (elem = curLoop[i]) != null; i++ ) {
				if ( elem ) {
					if ( not ^ (elem.className &amp;&amp; elem.className.baseVal &amp;&amp; (" " + elem.className.baseVal + " ").replace(/[\t\n\r]/g, " ").indexOf(match) &gt;= 0) ) {
						if ( !inplace ) {
							result.push( elem );
						}

					} else if ( inplace ) {
						curLoop[i] = false;
					}
				}
			}

			return false;
		},

		ID: function( match ) {
			return match[1].replace( rBackslash, "" );
		},

		TAG: function( match, curLoop ) {
			return match[1].replace( rBackslash, "" ).toLowerCase();
		},

		CHILD: function( match ) {
			if ( match[1] === "nth" ) {
				if ( !match[2] ) {
					Sizzle.error( match[0] );
				}

				match[2] = match[2].replace(/^\+|\s*/g, '');

				// parse equations like 'even', 'odd', '5', '2n', '3n+2', '4n-1', '-n+6'
				var test = /(-?)(\d*)(?:n([+\-]?\d*))?/.exec(
					match[2] === "even" &amp;&amp; "2n" || match[2] === "odd" &amp;&amp; "2n+1" ||
					!/\D/.test( match[2] ) &amp;&amp; "0n+" + match[2] || match[2]);

				// calculate the numbers (first)n+(last) including if they are negative
				match[2] = (test[1] + (test[2] || 1)) - 0;
				match[3] = test[3] - 0;
			}
			else if ( match[2] ) {
				Sizzle.error( match[0] );
			}

			// TODO: Move to normal caching system
			match[0] = done++;

			return match;
		},

		ATTR: function( match, curLoop, inplace, result, not, isXML ) {
			var name = match[1] = match[1].replace( rBackslash, "" );

			if ( !isXML &amp;&amp; Expr.attrMap[name] ) {
				match[1] = Expr.attrMap[name];
			}

			// Handle if an un-quoted value was used
			match[4] = ( match[4] || match[5] || "" ).replace( rBackslash, "" );

			if ( match[2] === "~=" ) {
				match[4] = " " + match[4] + " ";
			}

			return match;
		},

		PSEUDO: function( match, curLoop, inplace, result, not ) {
			if ( match[1] === "not" ) {
				// If we're dealing with a complex expression, or a simple one
				if ( ( chunker.exec(match[3]) || "" ).length &gt; 1 || /^\w/.test(match[3]) ) {
					match[3] = Sizzle(match[3], null, null, curLoop);

				} else {
					var ret = Sizzle.filter(match[3], curLoop, inplace, true ^ not);

					if ( !inplace ) {
						result.push.apply( result, ret );
					}

					return false;
				}

			} else if ( Expr.match.POS.test( match[0] ) || Expr.match.CHILD.test( match[0] ) ) {
				return true;
			}

			return match;
		},

		POS: function( match ) {
			match.unshift( true );

			return match;
		}
	},

	filters: {
		enabled: function( elem ) {
			return elem.disabled === false &amp;&amp; elem.type !== "hidden";
		},

		disabled: function( elem ) {
			return elem.disabled === true;
		},

		checked: function( elem ) {
			return elem.checked === true;
		},

		selected: function( elem ) {
			// Accessing this property makes selected-by-default
			// options in Safari work properly
			if ( elem.parentNode ) {
				elem.parentNode.selectedIndex;
			}

			return elem.selected === true;
		},

		parent: function( elem ) {
			return !!elem.firstChild;
		},

		empty: function( elem ) {
			return !elem.firstChild;
		},

		has: function( elem, i, match ) {
			return !!Sizzle( match[3], elem ).length;
		},

		header: function( elem ) {
			return (/h\d/i).test( elem.nodeName );
		},

		text: function( elem ) {
			var attr = elem.getAttribute( "type" ), type = elem.type;
			// IE6 and 7 will map elem.type to 'text' for new HTML5 types (search, etc)
			// use getAttribute instead to test this case
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "text" === type &amp;&amp; ( attr === type || attr === null );
		},

		radio: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "radio" === elem.type;
		},

		checkbox: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "checkbox" === elem.type;
		},

		file: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "file" === elem.type;
		},

		password: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "password" === elem.type;
		},

		submit: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return (name === "input" || name === "button") &amp;&amp; "submit" === elem.type;
		},

		image: function( elem ) {
			return elem.nodeName.toLowerCase() === "input" &amp;&amp; "image" === elem.type;
		},

		reset: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return (name === "input" || name === "button") &amp;&amp; "reset" === elem.type;
		},

		button: function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" &amp;&amp; "button" === elem.type || name === "button";
		},

		input: function( elem ) {
			return (/input|select|textarea|button/i).test( elem.nodeName );
		},

		focus: function( elem ) {
			return elem === elem.ownerDocument.activeElement;
		}
	},
	setFilters: {
		first: function( elem, i ) {
			return i === 0;
		},

		last: function( elem, i, match, array ) {
			return i === array.length - 1;
		},

		even: function( elem, i ) {
			return i % 2 === 0;
		},

		odd: function( elem, i ) {
			return i % 2 === 1;
		},

		lt: function( elem, i, match ) {
			return i &lt; match[3] - 0;
		},

		gt: function( elem, i, match ) {
			return i &gt; match[3] - 0;
		},

		nth: function( elem, i, match ) {
			return match[3] - 0 === i;
		},

		eq: function( elem, i, match ) {
			return match[3] - 0 === i;
		}
	},
	filter: {
		PSEUDO: function( elem, match, i, array ) {
			var name = match[1],
				filter = Expr.filters[ name ];

			if ( filter ) {
				return filter( elem, i, match, array );

			} else if ( name === "contains" ) {
				return (elem.textContent || elem.innerText || getText([ elem ]) || "").indexOf(match[3]) &gt;= 0;

			} else if ( name === "not" ) {
				var not = match[3];

				for ( var j = 0, l = not.length; j &lt; l; j++ ) {
					if ( not[j] === elem ) {
						return false;
					}
				}

				return true;

			} else {
				Sizzle.error( name );
			}
		},

		CHILD: function( elem, match ) {
			var first, last,
				doneName, parent, cache,
				count, diff,
				type = match[1],
				node = elem;

			switch ( type ) {
				case "only":
				case "first":
					while ( (node = node.previousSibling) ) {
						if ( node.nodeType === 1 ) {
							return false;
						}
					}

					if ( type === "first" ) {
						return true;
					}

					node = elem;

					/* falls through */
				case "last":
					while ( (node = node.nextSibling) ) {
						if ( node.nodeType === 1 ) {
							return false;
						}
					}

					return true;

				case "nth":
					first = match[2];
					last = match[3];

					if ( first === 1 &amp;&amp; last === 0 ) {
						return true;
					}

					doneName = match[0];
					parent = elem.parentNode;

					if ( parent &amp;&amp; (parent[ expando ] !== doneName || !elem.nodeIndex) ) {
						count = 0;

						for ( node = parent.firstChild; node; node = node.nextSibling ) {
							if ( node.nodeType === 1 ) {
								node.nodeIndex = ++count;
							}
						}

						parent[ expando ] = doneName;
					}

					diff = elem.nodeIndex - last;

					if ( first === 0 ) {
						return diff === 0;

					} else {
						return ( diff % first === 0 &amp;&amp; diff / first &gt;= 0 );
					}
			}
		},

		ID: function( elem, match ) {
			return elem.nodeType === 1 &amp;&amp; elem.getAttribute("id") === match;
		},

		TAG: function( elem, match ) {
			return (match === "*" &amp;&amp; elem.nodeType === 1) || !!elem.nodeName &amp;&amp; elem.nodeName.toLowerCase() === match;
		},

		CLASS: function( elem, match ) {
			return (" " + ((elem.className &amp;&amp; elem.className.baseVal) || elem.getAttribute("class")) + " ")
				.indexOf( match ) &gt; -1;
		},

		ATTR: function( elem, match ) {
			var name = match[1],
				result = Sizzle.attr ?
					Sizzle.attr( elem, name ) :
					Expr.attrHandle[ name ] ?
					Expr.attrHandle[ name ]( elem ) :
					elem[ name ] != null ?
						elem[ name ] :
						elem.getAttribute( name ),
				value = result + "",
				type = match[2],
				check = match[4];

			return result == null ?
				type === "!=" :
				!type &amp;&amp; Sizzle.attr ?
				result != null :
				type === "=" ?
				value === check :
				type === "*=" ?
				value.indexOf(check) &gt;= 0 :
				type === "~=" ?
				(" " + value + " ").indexOf(check) &gt;= 0 :
				!check ?
				value &amp;&amp; result !== false :
				type === "!=" ?
				value !== check :
				type === "^=" ?
				value.indexOf(check) === 0 :
				type === "$=" ?
				value.substr(value.length - check.length) === check :
				type === "|=" ?
				value === check || value.substr(0, check.length + 1) === check + "-" :
				false;
		},

		POS: function( elem, match, i, array ) {
			var name = match[2],
				filter = Expr.setFilters[ name ];

			if ( filter ) {
				return filter( elem, i, match, array );
			}
		}
	}
};

var origPOS = Expr.match.POS,
	fescape = function(all, num){
		return "\\" + (num - 0 + 1);
	};

for ( var type in Expr.match ) {
	Expr.match[ type ] = new RegExp( Expr.match[ type ].source + (/(?![^\[]*\])(?![^\(]*\))/.source) );
	Expr.leftMatch[ type ] = new RegExp( /(^(?:.|\r|\n)*?)/.source + Expr.match[ type ].source.replace(/\\(\d+)/g, fescape) );
}
// Expose origPOS
// "global" as in regardless of relation to brackets/parens
Expr.match.globalPOS = origPOS;

var makeArray = function( array, results ) {
	array = Array.prototype.slice.call( array, 0 );

	if ( results ) {
		results.push.apply( results, array );
		return results;
	}

	return array;
};

// Perform a simple check to determine if the browser is capable of
// converting a NodeList to an array using builtin methods.
// Also verifies that the returned array holds DOM nodes
// (which is not the case in the Blackberry browser)
try {
	Array.prototype.slice.call( document.documentElement.childNodes, 0 )[0].nodeType;

// Provide a fallback method if it does not work
} catch( e ) {
	makeArray = function( array, results ) {
		var i = 0,
			ret = results || [];

		if ( toString.call(array) === "[object Array]" ) {
			Array.prototype.push.apply( ret, array );

		} else {
			if ( typeof array.length === "number" ) {
				for ( var l = array.length; i &lt; l; i++ ) {
					ret.push( array[i] );
				}

			} else {
				for ( ; array[i]; i++ ) {
					ret.push( array[i] );
				}
			}
		}

		return ret;
	};
}

var sortOrder, siblingCheck;

if ( document.documentElement.compareDocumentPosition ) {
	sortOrder = function( a, b ) {
		if ( a === b ) {
			hasDuplicate = true;
			return 0;
		}

		if ( !a.compareDocumentPosition || !b.compareDocumentPosition ) {
			return a.compareDocumentPosition ? -1 : 1;
		}

		return a.compareDocumentPosition(b) &amp; 4 ? -1 : 1;
	};

} else {
	sortOrder = function( a, b ) {
		// The nodes are identical, we can exit early
		if ( a === b ) {
			hasDuplicate = true;
			return 0;

		// Fallback to using sourceIndex (in IE) if it's available on both nodes
		} else if ( a.sourceIndex &amp;&amp; b.sourceIndex ) {
			return a.sourceIndex - b.sourceIndex;
		}

		var al, bl,
			ap = [],
			bp = [],
			aup = a.parentNode,
			bup = b.parentNode,
			cur = aup;

		// If the nodes are siblings (or identical) we can do a quick check
		if ( aup === bup ) {
			return siblingCheck( a, b );

		// If no parents were found then the nodes are disconnected
		} else if ( !aup ) {
			return -1;

		} else if ( !bup ) {
			return 1;
		}

		// Otherwise they're somewhere else in the tree so we need
		// to build up a full list of the parentNodes for comparison
		while ( cur ) {
			ap.unshift( cur );
			cur = cur.parentNode;
		}

		cur = bup;

		while ( cur ) {
			bp.unshift( cur );
			cur = cur.parentNode;
		}

		al = ap.length;
		bl = bp.length;

		// Start walking down the tree looking for a discrepancy
		for ( var i = 0; i &lt; al &amp;&amp; i &lt; bl; i++ ) {
			if ( ap[i] !== bp[i] ) {
				return siblingCheck( ap[i], bp[i] );
			}
		}

		// We ended someplace up the tree so do a sibling check
		return i === al ?
			siblingCheck( a, bp[i], -1 ) :
			siblingCheck( ap[i], b, 1 );
	};

	siblingCheck = function( a, b, ret ) {
		if ( a === b ) {
			return ret;
		}

		var cur = a.nextSibling;

		while ( cur ) {
			if ( cur === b ) {
				return -1;
			}

			cur = cur.nextSibling;
		}

		return 1;
	};
}

// Check to see if the browser returns elements by name when
// querying by getElementById (and provide a workaround)
(function(){
	// We're going to inject a fake input element with a specified name
	var form = document.createElement("div"),
		id = "script" + (new Date()).getTime(),
		root = document.documentElement;

	form.innerHTML = "&lt;a name='" + id + "'/&gt;";

	// Inject it into the root element, check its status, and remove it quickly
	root.insertBefore( form, root.firstChild );

	// The workaround has to do additional checks after a getElementById
	// Which slows things down for other browsers (hence the branching)
	if ( document.getElementById( id ) ) {
		Expr.find.ID = function( match, context, isXML ) {
			if ( typeof context.getElementById !== "undefined" &amp;&amp; !isXML ) {
				var m = context.getElementById(match[1]);

				return m ?
					m.id === match[1] || typeof m.getAttributeNode !== "undefined" &amp;&amp; m.getAttributeNode("id").nodeValue === match[1] ?
						[m] :
						undefined :
					[];
			}
		};

		Expr.filter.ID = function( elem, match ) {
			var node = typeof elem.getAttributeNode !== "undefined" &amp;&amp; elem.getAttributeNode("id");

			return elem.nodeType === 1 &amp;&amp; node &amp;&amp; node.nodeValue === match;
		};
	}

	root.removeChild( form );

	// release memory in IE
	root = form = null;
})();

(function(){
	// Check to see if the browser returns only elements
	// when doing getElementsByTagName("*")

	// Create a fake element
	var div = document.createElement("div");
	div.appendChild( document.createComment("") );

	// Make sure no comments are found
	if ( div.getElementsByTagName("*").length &gt; 0 ) {
		Expr.find.TAG = function( match, context ) {
			var results = context.getElementsByTagName( match[1] );

			// Filter out possible comments
			if ( match[1] === "*" ) {
				var tmp = [];

				for ( var i = 0; results[i]; i++ ) {
					if ( results[i].nodeType === 1 ) {
						tmp.push( results[i] );
					}
				}

				results = tmp;
			}

			return results;
		};
	}

	// Check to see if an attribute returns normalized href attributes
	div.innerHTML = "&lt;a href='#'&gt;&lt;/a&gt;";

	if ( div.firstChild &amp;&amp; typeof div.firstChild.getAttribute !== "undefined" &amp;&amp;
			div.firstChild.getAttribute("href") !== "#" ) {

		Expr.attrHandle.href = function( elem ) {
			return elem.getAttribute( "href", 2 );
		};
	}

	// release memory in IE
	div = null;
})();

if ( document.querySelectorAll ) {
	(function(){
		var oldSizzle = Sizzle,
			div = document.createElement("div"),
			id = "__sizzle__";

		div.innerHTML = "&lt;p class='TEST'&gt;&lt;/p&gt;";

		// Safari can't handle uppercase or unicode characters when
		// in quirks mode.
		if ( div.querySelectorAll &amp;&amp; div.querySelectorAll(".TEST").length === 0 ) {
			return;
		}

		Sizzle = function( query, context, extra, seed ) {
			context = context || document;

			// Only use querySelectorAll on non-XML documents
			// (ID selectors don't work in non-HTML documents)
			if ( !seed &amp;&amp; !Sizzle.isXML(context) ) {
				// See if we find a selector to speed up
				var match = /^(\w+$)|^\.([\w\-]+$)|^#([\w\-]+$)/.exec( query );

				if ( match &amp;&amp; (context.nodeType === 1 || context.nodeType === 9) ) {
					// Speed-up: Sizzle("TAG")
					if ( match[1] ) {
						return makeArray( context.getElementsByTagName( query ), extra );

					// Speed-up: Sizzle(".CLASS")
					} else if ( match[2] &amp;&amp; Expr.find.CLASS &amp;&amp; context.getElementsByClassName ) {
						return makeArray( context.getElementsByClassName( match[2] ), extra );
					}
				}

				if ( context.nodeType === 9 ) {
					// Speed-up: Sizzle("body")
					// The body element only exists once, optimize finding it
					if ( query === "body" &amp;&amp; context.body ) {
						return makeArray( [ context.body ], extra );

					// Speed-up: Sizzle("#ID")
					} else if ( match &amp;&amp; match[3] ) {
						var elem = context.getElementById( match[3] );

						// Check parentNode to catch when Blackberry 4.6 returns
						// nodes that are no longer in the document #6963
						if ( elem &amp;&amp; elem.parentNode ) {
							// Handle the case where IE and Opera return items
							// by name instead of ID
							if ( elem.id === match[3] ) {
								return makeArray( [ elem ], extra );
							}

						} else {
							return makeArray( [], extra );
						}
					}

					try {
						return makeArray( context.querySelectorAll(query), extra );
					} catch(qsaError) {}

				// qSA works strangely on Element-rooted queries
				// We can work around this by specifying an extra ID on the root
				// and working up from there (Thanks to Andrew Dupont for the technique)
				// IE 8 doesn't work on object elements
				} else if ( context.nodeType === 1 &amp;&amp; context.nodeName.toLowerCase() !== "object" ) {
					var oldContext = context,
						old = context.getAttribute( "id" ),
						nid = old || id,
						hasParent = context.parentNode,
						relativeHierarchySelector = /^\s*[+~]/.test( query );

					if ( !old ) {
						context.setAttribute( "id", nid );
					} else {
						nid = nid.replace( /'/g, "\\$&amp;" );
					}
					if ( relativeHierarchySelector &amp;&amp; hasParent ) {
						context = context.parentNode;
					}

					try {
						if ( !relativeHierarchySelector || hasParent ) {
							return makeArray( context.querySelectorAll( "[id='" + nid + "'] " + query ), extra );
						}

					} catch(pseudoError) {
					} finally {
						if ( !old ) {
							oldContext.removeAttribute( "id" );
						}
					}
				}
			}

			return oldSizzle(query, context, extra, seed);
		};

		for ( var prop in oldSizzle ) {
			Sizzle[ prop ] = oldSizzle[ prop ];
		}

		// release memory in IE
		div = null;
	})();
}

(function(){
	var html = document.documentElement,
		matches = html.matchesSelector || html.mozMatchesSelector || html.webkitMatchesSelector || html.msMatchesSelector;

	if ( matches ) {
		// Check to see if it's possible to do matchesSelector
		// on a disconnected node (IE 9 fails this)
		var disconnectedMatch = !matches.call( document.createElement( "div" ), "div" ),
			pseudoWorks = false;

		try {
			// This should fail with an exception
			// Gecko does not error, returns false instead
			matches.call( document.documentElement, "[test!='']:sizzle" );

		} catch( pseudoError ) {
			pseudoWorks = true;
		}

		Sizzle.matchesSelector = function( node, expr ) {
			// Make sure that attribute selectors are quoted
			expr = expr.replace(/\=\s*([^'"\]]*)\s*\]/g, "='$1']");

			if ( !Sizzle.isXML( node ) ) {
				try {
					if ( pseudoWorks || !Expr.match.PSEUDO.test( expr ) &amp;&amp; !/!=/.test( expr ) ) {
						var ret = matches.call( node, expr );

						// IE 9's matchesSelector returns false on disconnected nodes
						if ( ret || !disconnectedMatch ||
								// As well, disconnected nodes are said to be in a document
								// fragment in IE 9, so check for that
								node.document &amp;&amp; node.document.nodeType !== 11 ) {
							return ret;
						}
					}
				} catch(e) {}
			}

			return Sizzle(expr, null, null, [node]).length &gt; 0;
		};
	}
})();

(function(){
	var div = document.createElement("div");

	div.innerHTML = "&lt;div class='test e'&gt;&lt;/div&gt;&lt;div class='test'&gt;&lt;/div&gt;";

	// Opera can't find a second classname (in 9.6)
	// Also, make sure that getElementsByClassName actually exists
	if ( !div.getElementsByClassName || div.getElementsByClassName("e").length === 0 ) {
		return;
	}

	// Safari caches class attributes, doesn't catch changes (in 3.2)
	div.lastChild.className.baseVal = "e";

	if ( div.getElementsByClassName("e").length === 1 ) {
		return;
	}

	Expr.order.splice(1, 0, "CLASS");
	Expr.find.CLASS = function( match, context, isXML ) {
		if ( typeof context.getElementsByClassName !== "undefined" &amp;&amp; !isXML ) {
			return context.getElementsByClassName(match[1]);
		}
	};

	// release memory in IE
	div = null;
})();

function dirNodeCheck( dir, cur, doneName, checkSet, nodeCheck, isXML ) {
	for ( var i = 0, l = checkSet.length; i &lt; l; i++ ) {
		var elem = checkSet[i];

		if ( elem ) {
			var match = false;

			elem = elem[dir];

			while ( elem ) {
				if ( elem[ expando ] === doneName ) {
					match = checkSet[elem.sizset];
					break;
				}

				if ( elem.nodeType === 1 &amp;&amp; !isXML ){
					elem[ expando ] = doneName;
					elem.sizset = i;
				}

				if ( elem.nodeName.toLowerCase() === cur ) {
					match = elem;
					break;
				}

				elem = elem[dir];
			}

			checkSet[i] = match;
		}
	}
}

function dirCheck( dir, cur, doneName, checkSet, nodeCheck, isXML ) {
	for ( var i = 0, l = checkSet.length; i &lt; l; i++ ) {
		var elem = checkSet[i];

		if ( elem ) {
			var match = false;

			elem = elem[dir];

			while ( elem ) {
				if ( elem[ expando ] === doneName ) {
					match = checkSet[elem.sizset];
					break;
				}

				if ( elem.nodeType === 1 ) {
					if ( !isXML ) {
						elem[ expando ] = doneName;
						elem.sizset = i;
					}

					if ( typeof cur !== "string" ) {
						if ( elem === cur ) {
							match = true;
							break;
						}

					} else if ( Sizzle.filter( cur, [elem] ).length &gt; 0 ) {
						match = elem;
						break;
					}
				}

				elem = elem[dir];
			}

			checkSet[i] = match;
		}
	}
}

if ( document.documentElement.contains ) {
	Sizzle.contains = function( a, b ) {
		return a !== b &amp;&amp; (a.contains ? a.contains(b) : true);
	};

} else if ( document.documentElement.compareDocumentPosition ) {
	Sizzle.contains = function( a, b ) {
		return !!(a.compareDocumentPosition(b) &amp; 16);
	};

} else {
	Sizzle.contains = function() {
		return false;
	};
}

Sizzle.isXML = function( elem ) {
	// documentElement is verified for cases where it doesn't yet exist
	// (such as loading iframes in IE - #4833)
	var documentElement = (elem ? elem.ownerDocument || elem : 0).documentElement;

	return documentElement ? documentElement.nodeName !== "HTML" : false;
};

var posProcess = function( selector, context, seed ) {
	var match,
		tmpSet = [],
		later = "",
		root = context.nodeType ? [context] : context;

	// Position selectors must be done after the filter
	// And so must :not(positional) so we move all PSEUDOs to the end
	while ( (match = Expr.match.PSEUDO.exec( selector )) ) {
		later += match[0];
		selector = selector.replace( Expr.match.PSEUDO, "" );
	}

	selector = Expr.relative[selector] ? selector + "*" : selector;

	for ( var i = 0, l = root.length; i &lt; l; i++ ) {
		Sizzle( selector, root[i], tmpSet, seed );
	}

	return Sizzle.filter( later, tmpSet );
};

// EXPOSE
// Override sizzle attribute retrieval
Sizzle.attr = jQuery.attr;
Sizzle.selectors.attrMap = {};
jQuery.find = Sizzle;
jQuery.expr = Sizzle.selectors;
jQuery.expr[":"] = jQuery.expr.filters;
jQuery.unique = Sizzle.uniqueSort;
jQuery.text = Sizzle.getText;
jQuery.isXMLDoc = Sizzle.isXML;
jQuery.contains = Sizzle.contains;


})();


var runtil = /Until$/,
	rparentsprev = /^(?:parents|prevUntil|prevAll)/,
	// Note: This RegExp should be improved, or likely pulled from Sizzle
	rmultiselector = /,/,
	isSimple = /^.[^:#\[\.,]*$/,
	slice = Array.prototype.slice,
	POS = jQuery.expr.match.globalPOS,
	// methods guaranteed to produce a unique set when starting from a unique set
	guaranteedUnique = {
		children: true,
		contents: true,
		next: true,
		prev: true
	};

jQuery.fn.extend({
	find: function( selector ) {
		var self = this,
			i, l;

		if ( typeof selector !== "string" ) {
			return jQuery( selector ).filter(function() {
				for ( i = 0, l = self.length; i &lt; l; i++ ) {
					if ( jQuery.contains( self[ i ], this ) ) {
						return true;
					}
				}
			});
		}

		var ret = this.pushStack( "", "find", selector ),
			length, n, r;

		for ( i = 0, l = this.length; i &lt; l; i++ ) {
			length = ret.length;
			jQuery.find( selector, this[i], ret );

			if ( i &gt; 0 ) {
				// Make sure that the results are unique
				for ( n = length; n &lt; ret.length; n++ ) {
					for ( r = 0; r &lt; length; r++ ) {
						if ( ret[r] === ret[n] ) {
							ret.splice(n--, 1);
							break;
						}
					}
				}
			}
		}

		return ret;
	},

	has: function( target ) {
		var targets = jQuery( target );
		return this.filter(function() {
			for ( var i = 0, l = targets.length; i &lt; l; i++ ) {
				if ( jQuery.contains( this, targets[i] ) ) {
					return true;
				}
			}
		});
	},

	not: function( selector ) {
		return this.pushStack( winnow(this, selector, false), "not", selector);
	},

	filter: function( selector ) {
		return this.pushStack( winnow(this, selector, true), "filter", selector );
	},

	is: function( selector ) {
		return !!selector &amp;&amp; (
			typeof selector === "string" ?
				// If this is a positional selector, check membership in the returned set
				// so $("p:first").is("p:last") won't return true for a doc with two "p".
				POS.test( selector ) ?
					jQuery( selector, this.context ).index( this[0] ) &gt;= 0 :
					jQuery.filter( selector, this ).length &gt; 0 :
				this.filter( selector ).length &gt; 0 );
	},

	closest: function( selectors, context ) {
		var ret = [], i, l, cur = this[0];

		// Array (deprecated as of jQuery 1.7)
		if ( jQuery.isArray( selectors ) ) {
			var level = 1;

			while ( cur &amp;&amp; cur.ownerDocument &amp;&amp; cur !== context ) {
				for ( i = 0; i &lt; selectors.length; i++ ) {

					if ( jQuery( cur ).is( selectors[ i ] ) ) {
						ret.push({ selector: selectors[ i ], elem: cur, level: level });
					}
				}

				cur = cur.parentNode;
				level++;
			}

			return ret;
		}

		// String
		var pos = POS.test( selectors ) || typeof selectors !== "string" ?
				jQuery( selectors, context || this.context ) :
				0;

		for ( i = 0, l = this.length; i &lt; l; i++ ) {
			cur = this[i];

			while ( cur ) {
				if ( pos ? pos.index(cur) &gt; -1 : jQuery.find.matchesSelector(cur, selectors) ) {
					ret.push( cur );
					break;

				} else {
					cur = cur.parentNode;
					if ( !cur || !cur.ownerDocument || cur === context || cur.nodeType === 11 ) {
						break;
					}
				}
			}
		}

		ret = ret.length &gt; 1 ? jQuery.unique( ret ) : ret;

		return this.pushStack( ret, "closest", selectors );
	},

	// Determine the position of an element within
	// the matched set of elements
	index: function( elem ) {

		// No argument, return index in parent
		if ( !elem ) {
			return ( this[0] &amp;&amp; this[0].parentNode ) ? this.prevAll().length : -1;
		}

		// index in selector
		if ( typeof elem === "string" ) {
			return jQuery.inArray( this[0], jQuery( elem ) );
		}

		// Locate the position of the desired element
		return jQuery.inArray(
			// If it receives a jQuery object, the first element is used
			elem.jquery ? elem[0] : elem, this );
	},

	add: function( selector, context ) {
		var set = typeof selector === "string" ?
				jQuery( selector, context ) :
				jQuery.makeArray( selector &amp;&amp; selector.nodeType ? [ selector ] : selector ),
			all = jQuery.merge( this.get(), set );

		return this.pushStack( isDisconnected( set[0] ) || isDisconnected( all[0] ) ?
			all :
			jQuery.unique( all ) );
	},

	andSelf: function() {
		return this.add( this.prevObject );
	}
});

// A painfully simple check to see if an element is disconnected
// from a document (should be improved, where feasible).
function isDisconnected( node ) {
	return !node || !node.parentNode || node.parentNode.nodeType === 11;
}

jQuery.each({
	parent: function( elem ) {
		var parent = elem.parentNode;
		return parent &amp;&amp; parent.nodeType !== 11 ? parent : null;
	},
	parents: function( elem ) {
		return jQuery.dir( elem, "parentNode" );
	},
	parentsUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "parentNode", until );
	},
	next: function( elem ) {
		return jQuery.nth( elem, 2, "nextSibling" );
	},
	prev: function( elem ) {
		return jQuery.nth( elem, 2, "previousSibling" );
	},
	nextAll: function( elem ) {
		return jQuery.dir( elem, "nextSibling" );
	},
	prevAll: function( elem ) {
		return jQuery.dir( elem, "previousSibling" );
	},
	nextUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "nextSibling", until );
	},
	prevUntil: function( elem, i, until ) {
		return jQuery.dir( elem, "previousSibling", until );
	},
	siblings: function( elem ) {
		return jQuery.sibling( ( elem.parentNode || {} ).firstChild, elem );
	},
	children: function( elem ) {
		return jQuery.sibling( elem.firstChild );
	},
	contents: function( elem ) {
		return jQuery.nodeName( elem, "iframe" ) ?
			elem.contentDocument || elem.contentWindow.document :
			jQuery.makeArray( elem.childNodes );
	}
}, function( name, fn ) {
	jQuery.fn[ name ] = function( until, selector ) {
		var ret = jQuery.map( this, fn, until );

		if ( !runtil.test( name ) ) {
			selector = until;
		}

		if ( selector &amp;&amp; typeof selector === "string" ) {
			ret = jQuery.filter( selector, ret );
		}

		ret = this.length &gt; 1 &amp;&amp; !guaranteedUnique[ name ] ? jQuery.unique( ret ) : ret;

		if ( (this.length &gt; 1 || rmultiselector.test( selector )) &amp;&amp; rparentsprev.test( name ) ) {
			ret = ret.reverse();
		}

		return this.pushStack( ret, name, slice.call( arguments ).join(",") );
	};
});

jQuery.extend({
	filter: function( expr, elems, not ) {
		if ( not ) {
			expr = ":not(" + expr + ")";
		}

		return elems.length === 1 ?
			jQuery.find.matchesSelector(elems[0], expr) ? [ elems[0] ] : [] :
			jQuery.find.matches(expr, elems);
	},

	dir: function( elem, dir, until ) {
		var matched = [],
			cur = elem[ dir ];

		while ( cur &amp;&amp; cur.nodeType !== 9 &amp;&amp; (until === undefined || cur.nodeType !== 1 || !jQuery( cur ).is( until )) ) {
			if ( cur.nodeType === 1 ) {
				matched.push( cur );
			}
			cur = cur[dir];
		}
		return matched;
	},

	nth: function( cur, result, dir, elem ) {
		result = result || 1;
		var num = 0;

		for ( ; cur; cur = cur[dir] ) {
			if ( cur.nodeType === 1 &amp;&amp; ++num === result ) {
				break;
			}
		}

		return cur;
	},

	sibling: function( n, elem ) {
		var r = [];

		for ( ; n; n = n.nextSibling ) {
			if ( n.nodeType === 1 &amp;&amp; n !== elem ) {
				r.push( n );
			}
		}

		return r;
	}
});

// Implement the identical functionality for filter and not
function winnow( elements, qualifier, keep ) {

	// Can't pass null or undefined to indexOf in Firefox 4
	// Set to 0 to skip string check
	qualifier = qualifier || 0;

	if ( jQuery.isFunction( qualifier ) ) {
		return jQuery.grep(elements, function( elem, i ) {
			var retVal = !!qualifier.call( elem, i, elem );
			return retVal === keep;
		});

	} else if ( qualifier.nodeType ) {
		return jQuery.grep(elements, function( elem, i ) {
			return ( elem === qualifier ) === keep;
		});

	} else if ( typeof qualifier === "string" ) {
		var filtered = jQuery.grep(elements, function( elem ) {
			return elem.nodeType === 1;
		});

		if ( isSimple.test( qualifier ) ) {
			return jQuery.filter(qualifier, filtered, !keep);
		} else {
			qualifier = jQuery.filter( qualifier, filtered );
		}
	}

	return jQuery.grep(elements, function( elem, i ) {
		return ( jQuery.inArray( elem, qualifier ) &gt;= 0 ) === keep;
	});
}




function createSafeFragment( document ) {
	var list = nodeNames.split( "|" ),
	safeFrag = document.createDocumentFragment();

	if ( safeFrag.createElement ) {
		while ( list.length ) {
			safeFrag.createElement(
				list.pop()
			);
		}
	}
	return safeFrag;
}

var nodeNames = "abbr|article|aside|audio|bdi|canvas|data|datalist|details|figcaption|figure|footer|" +
		"header|hgroup|mark|meter|nav|output|progress|section|summary|time|video",
	rinlinejQuery = / jQuery\d+="(?:\d+|null)"/g,
	rleadingWhitespace = /^\s+/,
	rxhtmlTag = /&lt;(?!area|br|col|embed|hr|img|input|link|meta|param)(([\w:]+)[^&gt;]*)\/&gt;/ig,
	rtagName = /&lt;([\w:]+)/,
	rtbody = /&lt;tbody/i,
	rhtml = /&lt;|&amp;#?\w+;/,
	rnoInnerhtml = /&lt;(?:script|style)/i,
	rnocache = /&lt;(?:script|object|embed|option|style)/i,
	rnoshimcache = new RegExp("&lt;(?:" + nodeNames + ")[\\s/&gt;]", "i"),
	// checked="checked" or checked
	rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
	rscriptType = /\/(java|ecma)script/i,
	rcleanScript = /^\s*&lt;!(?:\[CDATA\[|\-\-)/,
	wrapMap = {
		option: [ 1, "&lt;select multiple='multiple'&gt;", "&lt;/select&gt;" ],
		legend: [ 1, "&lt;fieldset&gt;", "&lt;/fieldset&gt;" ],
		thead: [ 1, "&lt;table&gt;", "&lt;/table&gt;" ],
		tr: [ 2, "&lt;table&gt;&lt;tbody&gt;", "&lt;/tbody&gt;&lt;/table&gt;" ],
		td: [ 3, "&lt;table&gt;&lt;tbody&gt;&lt;tr&gt;", "&lt;/tr&gt;&lt;/tbody&gt;&lt;/table&gt;" ],
		col: [ 2, "&lt;table&gt;&lt;tbody&gt;&lt;/tbody&gt;&lt;colgroup&gt;", "&lt;/colgroup&gt;&lt;/table&gt;" ],
		area: [ 1, "&lt;map&gt;", "&lt;/map&gt;" ],
		_default: [ 0, "", "" ]
	},
	safeFragment = createSafeFragment( document );

wrapMap.optgroup = wrapMap.option;
wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
wrapMap.th = wrapMap.td;

// IE can't serialize &lt;link&gt; and &lt;script&gt; tags normally
if ( !jQuery.support.htmlSerialize ) {
	wrapMap._default = [ 1, "div&lt;div&gt;", "&lt;/div&gt;" ];
}

jQuery.fn.extend({
	text: function( value ) {
		return jQuery.access( this, function( value ) {
			return value === undefined ?
				jQuery.text( this ) :
				this.empty().append( ( this[0] &amp;&amp; this[0].ownerDocument || document ).createTextNode( value ) );
		}, null, value, arguments.length );
	},

	wrapAll: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapAll( html.call(this, i) );
			});
		}

		if ( this[0] ) {
			// The elements to wrap the target around
			var wrap = jQuery( html, this[0].ownerDocument ).eq(0).clone(true);

			if ( this[0].parentNode ) {
				wrap.insertBefore( this[0] );
			}

			wrap.map(function() {
				var elem = this;

				while ( elem.firstChild &amp;&amp; elem.firstChild.nodeType === 1 ) {
					elem = elem.firstChild;
				}

				return elem;
			}).append( this );
		}

		return this;
	},

	wrapInner: function( html ) {
		if ( jQuery.isFunction( html ) ) {
			return this.each(function(i) {
				jQuery(this).wrapInner( html.call(this, i) );
			});
		}

		return this.each(function() {
			var self = jQuery( this ),
				contents = self.contents();

			if ( contents.length ) {
				contents.wrapAll( html );

			} else {
				self.append( html );
			}
		});
	},

	wrap: function( html ) {
		var isFunction = jQuery.isFunction( html );

		return this.each(function(i) {
			jQuery( this ).wrapAll( isFunction ? html.call(this, i) : html );
		});
	},

	unwrap: function() {
		return this.parent().each(function() {
			if ( !jQuery.nodeName( this, "body" ) ) {
				jQuery( this ).replaceWith( this.childNodes );
			}
		}).end();
	},

	append: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 ) {
				this.appendChild( elem );
			}
		});
	},

	prepend: function() {
		return this.domManip(arguments, true, function( elem ) {
			if ( this.nodeType === 1 ) {
				this.insertBefore( elem, this.firstChild );
			}
		});
	},

	before: function() {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			return this.domManip(arguments, false, function( elem ) {
				this.parentNode.insertBefore( elem, this );
			});
		} else if ( arguments.length ) {
			var set = jQuery.clean( arguments );
			set.push.apply( set, this.toArray() );
			return this.pushStack( set, "before", arguments );
		}
	},

	after: function() {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			return this.domManip(arguments, false, function( elem ) {
				this.parentNode.insertBefore( elem, this.nextSibling );
			});
		} else if ( arguments.length ) {
			var set = this.pushStack( this, "after", arguments );
			set.push.apply( set, jQuery.clean(arguments) );
			return set;
		}
	},

	// keepData is for internal use only--do not document
	remove: function( selector, keepData ) {
		for ( var i = 0, elem; (elem = this[i]) != null; i++ ) {
			if ( !selector || jQuery.filter( selector, [ elem ] ).length ) {
				if ( !keepData &amp;&amp; elem.nodeType === 1 ) {
					jQuery.cleanData( elem.getElementsByTagName("*") );
					jQuery.cleanData( [ elem ] );
				}

				if ( elem.parentNode ) {
					elem.parentNode.removeChild( elem );
				}
			}
		}

		return this;
	},

	empty: function() {
		for ( var i = 0, elem; (elem = this[i]) != null; i++ ) {
			// Remove element nodes and prevent memory leaks
			if ( elem.nodeType === 1 ) {
				jQuery.cleanData( elem.getElementsByTagName("*") );
			}

			// Remove any remaining nodes
			while ( elem.firstChild ) {
				elem.removeChild( elem.firstChild );
			}
		}

		return this;
	},

	clone: function( dataAndEvents, deepDataAndEvents ) {
		dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
		deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

		return this.map( function () {
			return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
		});
	},

	html: function( value ) {
		return jQuery.access( this, function( value ) {
			var elem = this[0] || {},
				i = 0,
				l = this.length;

			if ( value === undefined ) {
				return elem.nodeType === 1 ?
					elem.innerHTML.replace( rinlinejQuery, "" ) :
					null;
			}


			if ( typeof value === "string" &amp;&amp; !rnoInnerhtml.test( value ) &amp;&amp;
				( jQuery.support.leadingWhitespace || !rleadingWhitespace.test( value ) ) &amp;&amp;
				!wrapMap[ ( rtagName.exec( value ) || ["", ""] )[1].toLowerCase() ] ) {

				value = value.replace( rxhtmlTag, "&lt;$1&gt;&lt;/$2&gt;" );

				try {
					for (; i &lt; l; i++ ) {
						// Remove element nodes and prevent memory leaks
						elem = this[i] || {};
						if ( elem.nodeType === 1 ) {
							jQuery.cleanData( elem.getElementsByTagName( "*" ) );
							elem.innerHTML = value;
						}
					}

					elem = 0;

				// If using innerHTML throws an exception, use the fallback method
				} catch(e) {}
			}

			if ( elem ) {
				this.empty().append( value );
			}
		}, null, value, arguments.length );
	},

	replaceWith: function( value ) {
		if ( this[0] &amp;&amp; this[0].parentNode ) {
			// Make sure that the elements are removed from the DOM before they are inserted
			// this can help fix replacing a parent with child elements
			if ( jQuery.isFunction( value ) ) {
				return this.each(function(i) {
					var self = jQuery(this), old = self.html();
					self.replaceWith( value.call( this, i, old ) );
				});
			}

			if ( typeof value !== "string" ) {
				value = jQuery( value ).detach();
			}

			return this.each(function() {
				var next = this.nextSibling,
					parent = this.parentNode;

				jQuery( this ).remove();

				if ( next ) {
					jQuery(next).before( value );
				} else {
					jQuery(parent).append( value );
				}
			});
		} else {
			return this.length ?
				this.pushStack( jQuery(jQuery.isFunction(value) ? value() : value), "replaceWith", value ) :
				this;
		}
	},

	detach: function( selector ) {
		return this.remove( selector, true );
	},

	domManip: function( args, table, callback ) {
		var results, first, fragment, parent,
			value = args[0],
			scripts = [];

		// We can't cloneNode fragments that contain checked, in WebKit
		if ( !jQuery.support.checkClone &amp;&amp; arguments.length === 3 &amp;&amp; typeof value === "string" &amp;&amp; rchecked.test( value ) ) {
			return this.each(function() {
				jQuery(this).domManip( args, table, callback, true );
			});
		}

		if ( jQuery.isFunction(value) ) {
			return this.each(function(i) {
				var self = jQuery(this);
				args[0] = value.call(this, i, table ? self.html() : undefined);
				self.domManip( args, table, callback );
			});
		}

		if ( this[0] ) {
			parent = value &amp;&amp; value.parentNode;

			// If we're in a fragment, just use that instead of building a new one
			if ( jQuery.support.parentNode &amp;&amp; parent &amp;&amp; parent.nodeType === 11 &amp;&amp; parent.childNodes.length === this.length ) {
				results = { fragment: parent };

			} else {
				results = jQuery.buildFragment( args, this, scripts );
			}

			fragment = results.fragment;

			if ( fragment.childNodes.length === 1 ) {
				first = fragment = fragment.firstChild;
			} else {
				first = fragment.firstChild;
			}

			if ( first ) {
				table = table &amp;&amp; jQuery.nodeName( first, "tr" );

				for ( var i = 0, l = this.length, lastIndex = l - 1; i &lt; l; i++ ) {
					callback.call(
						table ?
							root(this[i], first) :
							this[i],
						// Make sure that we do not leak memory by inadvertently discarding
						// the original fragment (which might have attached data) instead of
						// using it; in addition, use the original fragment object for the last
						// item instead of first because it can end up being emptied incorrectly
						// in certain situations (Bug #8070).
						// Fragments from the fragment cache must always be cloned and never used
						// in place.
						results.cacheable || ( l &gt; 1 &amp;&amp; i &lt; lastIndex ) ?
							jQuery.clone( fragment, true, true ) :
							fragment
					);
				}
			}

			if ( scripts.length ) {
				jQuery.each( scripts, function( i, elem ) {
					if ( elem.src ) {
						jQuery.ajax({
							type: "GET",
							global: false,
							url: elem.src,
							async: false,
							dataType: "script"
						});
					} else {
						jQuery.globalEval( ( elem.text || elem.textContent || elem.innerHTML || "" ).replace( rcleanScript, "/*$0*/" ) );
					}

					if ( elem.parentNode ) {
						elem.parentNode.removeChild( elem );
					}
				});
			}
		}

		return this;
	}
});

function root( elem, cur ) {
	return jQuery.nodeName(elem, "table") ?
		(elem.getElementsByTagName("tbody")[0] ||
		elem.appendChild(elem.ownerDocument.createElement("tbody"))) :
		elem;
}

function cloneCopyEvent( src, dest ) {

	if ( dest.nodeType !== 1 || !jQuery.hasData( src ) ) {
		return;
	}

	var type, i, l,
		oldData = jQuery._data( src ),
		curData = jQuery._data( dest, oldData ),
		events = oldData.events;

	if ( events ) {
		delete curData.handle;
		curData.events = {};

		for ( type in events ) {
			for ( i = 0, l = events[ type ].length; i &lt; l; i++ ) {
				jQuery.event.add( dest, type, events[ type ][ i ] );
			}
		}
	}

	// make the cloned public data object a copy from the original
	if ( curData.data ) {
		curData.data = jQuery.extend( {}, curData.data );
	}
}

function cloneFixAttributes( src, dest ) {
	var nodeName;

	// We do not need to do anything for non-Elements
	if ( dest.nodeType !== 1 ) {
		return;
	}

	// clearAttributes removes the attributes, which we don't want,
	// but also removes the attachEvent events, which we *do* want
	if ( dest.clearAttributes ) {
		dest.clearAttributes();
	}

	// mergeAttributes, in contrast, only merges back on the
	// original attributes, not the events
	if ( dest.mergeAttributes ) {
		dest.mergeAttributes( src );
	}

	nodeName = dest.nodeName.toLowerCase();

	// IE6-8 fail to clone children inside object elements that use
	// the proprietary classid attribute value (rather than the type
	// attribute) to identify the type of content to display
	if ( nodeName === "object" ) {
		dest.outerHTML = src.outerHTML;

	} else if ( nodeName === "input" &amp;&amp; (src.type === "checkbox" || src.type === "radio") ) {
		// IE6-8 fails to persist the checked state of a cloned checkbox
		// or radio button. Worse, IE6-7 fail to give the cloned element
		// a checked appearance if the defaultChecked value isn't also set
		if ( src.checked ) {
			dest.defaultChecked = dest.checked = src.checked;
		}

		// IE6-7 get confused and end up setting the value of a cloned
		// checkbox/radio button to an empty string instead of "on"
		if ( dest.value !== src.value ) {
			dest.value = src.value;
		}

	// IE6-8 fails to return the selected option to the default selected
	// state when cloning options
	} else if ( nodeName === "option" ) {
		dest.selected = src.defaultSelected;

	// IE6-8 fails to set the defaultValue to the correct value when
	// cloning other types of input fields
	} else if ( nodeName === "input" || nodeName === "textarea" ) {
		dest.defaultValue = src.defaultValue;

	// IE blanks contents when cloning scripts
	} else if ( nodeName === "script" &amp;&amp; dest.text !== src.text ) {
		dest.text = src.text;
	}

	// Event data gets referenced instead of copied if the expando
	// gets copied too
	dest.removeAttribute( jQuery.expando );

	// Clear flags for bubbling special change/submit events, they must
	// be reattached when the newly cloned events are first activated
	dest.removeAttribute( "_submit_attached" );
	dest.removeAttribute( "_change_attached" );
}

jQuery.buildFragment = function( args, nodes, scripts ) {
	var fragment, cacheable, cacheresults, doc,
	first = args[ 0 ];

	// nodes may contain either an explicit document object,
	// a jQuery collection or context object.
	// If nodes[0] contains a valid object to assign to doc
	if ( nodes &amp;&amp; nodes[0] ) {
		doc = nodes[0].ownerDocument || nodes[0];
	}

	// Ensure that an attr object doesn't incorrectly stand in as a document object
	// Chrome and Firefox seem to allow this to occur and will throw exception
	// Fixes #8950
	if ( !doc.createDocumentFragment ) {
		doc = document;
	}

	// Only cache "small" (1/2 KB) HTML strings that are associated with the main document
	// Cloning options loses the selected state, so don't cache them
	// IE 6 doesn't like it when you put &lt;object&gt; or &lt;embed&gt; elements in a fragment
	// Also, WebKit does not clone 'checked' attributes on cloneNode, so don't cache
	// Lastly, IE6,7,8 will not correctly reuse cached fragments that were created from unknown elems #10501
	if ( args.length === 1 &amp;&amp; typeof first === "string" &amp;&amp; first.length &lt; 512 &amp;&amp; doc === document &amp;&amp;
		first.charAt(0) === "&lt;" &amp;&amp; !rnocache.test( first ) &amp;&amp;
		(jQuery.support.checkClone || !rchecked.test( first )) &amp;&amp;
		(jQuery.support.html5Clone || !rnoshimcache.test( first )) ) {

		cacheable = true;

		cacheresults = jQuery.fragments[ first ];
		if ( cacheresults &amp;&amp; cacheresults !== 1 ) {
			fragment = cacheresults;
		}
	}

	if ( !fragment ) {
		fragment = doc.createDocumentFragment();
		jQuery.clean( args, doc, fragment, scripts );
	}

	if ( cacheable ) {
		jQuery.fragments[ first ] = cacheresults ? fragment : 1;
	}

	return { fragment: fragment, cacheable: cacheable };
};

jQuery.fragments = {};

jQuery.each({
	appendTo: "append",
	prependTo: "prepend",
	insertBefore: "before",
	insertAfter: "after",
	replaceAll: "replaceWith"
}, function( name, original ) {
	jQuery.fn[ name ] = function( selector ) {
		var ret = [],
			insert = jQuery( selector ),
			parent = this.length === 1 &amp;&amp; this[0].parentNode;

		if ( parent &amp;&amp; parent.nodeType === 11 &amp;&amp; parent.childNodes.length === 1 &amp;&amp; insert.length === 1 ) {
			insert[ original ]( this[0] );
			return this;

		} else {
			for ( var i = 0, l = insert.length; i &lt; l; i++ ) {
				var elems = ( i &gt; 0 ? this.clone(true) : this ).get();
				jQuery( insert[i] )[ original ]( elems );
				ret = ret.concat( elems );
			}

			return this.pushStack( ret, name, insert.selector );
		}
	};
});

function getAll( elem ) {
	if ( typeof elem.getElementsByTagName !== "undefined" ) {
		return elem.getElementsByTagName( "*" );

	} else if ( typeof elem.querySelectorAll !== "undefined" ) {
		return elem.querySelectorAll( "*" );

	} else {
		return [];
	}
}

// Used in clean, fixes the defaultChecked property
function fixDefaultChecked( elem ) {
	if ( elem.type === "checkbox" || elem.type === "radio" ) {
		elem.defaultChecked = elem.checked;
	}
}
// Finds all inputs and passes them to fixDefaultChecked
function findInputs( elem ) {
	var nodeName = ( elem.nodeName || "" ).toLowerCase();
	if ( nodeName === "input" ) {
		fixDefaultChecked( elem );
	// Skip scripts, get other children
	} else if ( nodeName !== "script" &amp;&amp; typeof elem.getElementsByTagName !== "undefined" ) {
		jQuery.grep( elem.getElementsByTagName("input"), fixDefaultChecked );
	}
}

// Derived From: http://www.iecss.com/shimprove/javascript/shimprove.1-0-1.js
function shimCloneNode( elem ) {
	var div = document.createElement( "div" );
	safeFragment.appendChild( div );

	div.innerHTML = elem.outerHTML;
	return div.firstChild;
}

jQuery.extend({
	clone: function( elem, dataAndEvents, deepDataAndEvents ) {
		var srcElements,
			destElements,
			i,
			// IE&lt;=8 does not properly clone detached, unknown element nodes
			clone = jQuery.support.html5Clone || jQuery.isXMLDoc(elem) || !rnoshimcache.test( "&lt;" + elem.nodeName + "&gt;" ) ?
				elem.cloneNode( true ) :
				shimCloneNode( elem );

		if ( (!jQuery.support.noCloneEvent || !jQuery.support.noCloneChecked) &amp;&amp;
				(elem.nodeType === 1 || elem.nodeType === 11) &amp;&amp; !jQuery.isXMLDoc(elem) ) {
			// IE copies events bound via attachEvent when using cloneNode.
			// Calling detachEvent on the clone will also remove the events
			// from the original. In order to get around this, we use some
			// proprietary methods to clear the events. Thanks to MooTools
			// guys for this hotness.

			cloneFixAttributes( elem, clone );

			// Using Sizzle here is crazy slow, so we use getElementsByTagName instead
			srcElements = getAll( elem );
			destElements = getAll( clone );

			// Weird iteration because IE will replace the length property
			// with an element if you are cloning the body and one of the
			// elements on the page has a name or id of "length"
			for ( i = 0; srcElements[i]; ++i ) {
				// Ensure that the destination node is not null; Fixes #9587
				if ( destElements[i] ) {
					cloneFixAttributes( srcElements[i], destElements[i] );
				}
			}
		}

		// Copy the events from the original to the clone
		if ( dataAndEvents ) {
			cloneCopyEvent( elem, clone );

			if ( deepDataAndEvents ) {
				srcElements = getAll( elem );
				destElements = getAll( clone );

				for ( i = 0; srcElements[i]; ++i ) {
					cloneCopyEvent( srcElements[i], destElements[i] );
				}
			}
		}

		srcElements = destElements = null;

		// Return the cloned set
		return clone;
	},

	clean: function( elems, context, fragment, scripts ) {
		var checkScriptType, script, j,
				ret = [];

		context = context || document;

		// !context.createElement fails in IE with an error but returns typeof 'object'
		if ( typeof context.createElement === "undefined" ) {
			context = context.ownerDocument || context[0] &amp;&amp; context[0].ownerDocument || document;
		}

		for ( var i = 0, elem; (elem = elems[i]) != null; i++ ) {
			if ( typeof elem === "number" ) {
				elem += "";
			}

			if ( !elem ) {
				continue;
			}

			// Convert html string into DOM nodes
			if ( typeof elem === "string" ) {
				if ( !rhtml.test( elem ) ) {
					elem = context.createTextNode( elem );
				} else {
					// Fix "XHTML"-style tags in all browsers
					elem = elem.replace(rxhtmlTag, "&lt;$1&gt;&lt;/$2&gt;");

					// Trim whitespace, otherwise indexOf won't work as expected
					var tag = ( rtagName.exec( elem ) || ["", ""] )[1].toLowerCase(),
						wrap = wrapMap[ tag ] || wrapMap._default,
						depth = wrap[0],
						div = context.createElement("div"),
						safeChildNodes = safeFragment.childNodes,
						remove;

					// Append wrapper element to unknown element safe doc fragment
					if ( context === document ) {
						// Use the fragment we've already created for this document
						safeFragment.appendChild( div );
					} else {
						// Use a fragment created with the owner document
						createSafeFragment( context ).appendChild( div );
					}

					// Go to html and back, then peel off extra wrappers
					div.innerHTML = wrap[1] + elem + wrap[2];

					// Move to the right depth
					while ( depth-- ) {
						div = div.lastChild;
					}

					// Remove IE's autoinserted &lt;tbody&gt; from table fragments
					if ( !jQuery.support.tbody ) {

						// String was a &lt;table&gt;, *may* have spurious &lt;tbody&gt;
						var hasBody = rtbody.test(elem),
							tbody = tag === "table" &amp;&amp; !hasBody ?
								div.firstChild &amp;&amp; div.firstChild.childNodes :

								// String was a bare &lt;thead&gt; or &lt;tfoot&gt;
								wrap[1] === "&lt;table&gt;" &amp;&amp; !hasBody ?
									div.childNodes :
									[];

						for ( j = tbody.length - 1; j &gt;= 0 ; --j ) {
							if ( jQuery.nodeName( tbody[ j ], "tbody" ) &amp;&amp; !tbody[ j ].childNodes.length ) {
								tbody[ j ].parentNode.removeChild( tbody[ j ] );
							}
						}
					}

					// IE completely kills leading whitespace when innerHTML is used
					if ( !jQuery.support.leadingWhitespace &amp;&amp; rleadingWhitespace.test( elem ) ) {
						div.insertBefore( context.createTextNode( rleadingWhitespace.exec(elem)[0] ), div.firstChild );
					}

					elem = div.childNodes;

					// Clear elements from DocumentFragment (safeFragment or otherwise)
					// to avoid hoarding elements. Fixes #11356
					if ( div ) {
						div.parentNode.removeChild( div );

						// Guard against -1 index exceptions in FF3.6
						if ( safeChildNodes.length &gt; 0 ) {
							remove = safeChildNodes[ safeChildNodes.length - 1 ];

							if ( remove &amp;&amp; remove.parentNode ) {
								remove.parentNode.removeChild( remove );
							}
						}
					}
				}
			}

			// Resets defaultChecked for any radios and checkboxes
			// about to be appended to the DOM in IE 6/7 (#8060)
			var len;
			if ( !jQuery.support.appendChecked ) {
				if ( elem[0] &amp;&amp; typeof (len = elem.length) === "number" ) {
					for ( j = 0; j &lt; len; j++ ) {
						findInputs( elem[j] );
					}
				} else {
					findInputs( elem );
				}
			}

			if ( elem.nodeType ) {
				ret.push( elem );
			} else {
				ret = jQuery.merge( ret, elem );
			}
		}

		if ( fragment ) {
			checkScriptType = function( elem ) {
				return !elem.type || rscriptType.test( elem.type );
			};
			for ( i = 0; ret[i]; i++ ) {
				script = ret[i];
				if ( scripts &amp;&amp; jQuery.nodeName( script, "script" ) &amp;&amp; (!script.type || rscriptType.test( script.type )) ) {
					scripts.push( script.parentNode ? script.parentNode.removeChild( script ) : script );

				} else {
					if ( script.nodeType === 1 ) {
						var jsTags = jQuery.grep( script.getElementsByTagName( "script" ), checkScriptType );

						ret.splice.apply( ret, [i + 1, 0].concat( jsTags ) );
					}
					fragment.appendChild( script );
				}
			}
		}

		return ret;
	},

	cleanData: function( elems ) {
		var data, id,
			cache = jQuery.cache,
			special = jQuery.event.special,
			deleteExpando = jQuery.support.deleteExpando;

		for ( var i = 0, elem; (elem = elems[i]) != null; i++ ) {
			if ( elem.nodeName &amp;&amp; jQuery.noData[elem.nodeName.toLowerCase()] ) {
				continue;
			}

			id = elem[ jQuery.expando ];

			if ( id ) {
				data = cache[ id ];

				if ( data &amp;&amp; data.events ) {
					for ( var type in data.events ) {
						if ( special[ type ] ) {
							jQuery.event.remove( elem, type );

						// This is a shortcut to avoid jQuery.event.remove's overhead
						} else {
							jQuery.removeEvent( elem, type, data.handle );
						}
					}

					// Null the DOM reference to avoid IE6/7/8 leak (#7054)
					if ( data.handle ) {
						data.handle.elem = null;
					}
				}

				if ( deleteExpando ) {
					delete elem[ jQuery.expando ];

				} else if ( elem.removeAttribute ) {
					elem.removeAttribute( jQuery.expando );
				}

				delete cache[ id ];
			}
		}
	}
});




var ralpha = /alpha\([^)]*\)/i,
	ropacity = /opacity=([^)]*)/,
	// fixed for IE9, see #8346
	rupper = /([A-Z]|^ms)/g,
	rnum = /^[\-+]?(?:\d*\.)?\d+$/i,
	rnumnonpx = /^-?(?:\d*\.)?\d+(?!px)[^\d\s]+$/i,
	rrelNum = /^([\-+])=([\-+.\de]+)/,
	rmargin = /^margin/,

	cssShow = { position: "absolute", visibility: "hidden", display: "block" },

	// order is important!
	cssExpand = [ "Top", "Right", "Bottom", "Left" ],

	curCSS,

	getComputedStyle,
	currentStyle;

jQuery.fn.css = function( name, value ) {
	return jQuery.access( this, function( elem, name, value ) {
		return value !== undefined ?
			jQuery.style( elem, name, value ) :
			jQuery.css( elem, name );
	}, name, value, arguments.length &gt; 1 );
};

jQuery.extend({
	// Add in style property hooks for overriding the default
	// behavior of getting and setting a style property
	cssHooks: {
		opacity: {
			get: function( elem, computed ) {
				if ( computed ) {
					// We should always get a number back from opacity
					var ret = curCSS( elem, "opacity" );
					return ret === "" ? "1" : ret;

				} else {
					return elem.style.opacity;
				}
			}
		}
	},

	// Exclude the following css properties to add px
	cssNumber: {
		"fillOpacity": true,
		"fontWeight": true,
		"lineHeight": true,
		"opacity": true,
		"orphans": true,
		"widows": true,
		"zIndex": true,
		"zoom": true
	},

	// Add in properties whose names you wish to fix before
	// setting or getting the value
	cssProps: {
		// normalize float css property
		"float": jQuery.support.cssFloat ? "cssFloat" : "styleFloat"
	},

	// Get and set the style property on a DOM Node
	style: function( elem, name, value, extra ) {
		// Don't set styles on text and comment nodes
		if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
			return;
		}

		// Make sure that we're working with the right name
		var ret, type, origName = jQuery.camelCase( name ),
			style = elem.style, hooks = jQuery.cssHooks[ origName ];

		name = jQuery.cssProps[ origName ] || origName;

		// Check if we're setting a value
		if ( value !== undefined ) {
			type = typeof value;

			// convert relative number strings (+= or -=) to relative numbers. #7345
			if ( type === "string" &amp;&amp; (ret = rrelNum.exec( value )) ) {
				value = ( +( ret[1] + 1) * +ret[2] ) + parseFloat( jQuery.css( elem, name ) );
				// Fixes bug #9237
				type = "number";
			}

			// Make sure that NaN and null values aren't set. See: #7116
			if ( value == null || type === "number" &amp;&amp; isNaN( value ) ) {
				return;
			}

			// If a number was passed in, add 'px' to the (except for certain CSS properties)
			if ( type === "number" &amp;&amp; !jQuery.cssNumber[ origName ] ) {
				value += "px";
			}

			// If a hook was provided, use that value, otherwise just set the specified value
			if ( !hooks || !("set" in hooks) || (value = hooks.set( elem, value )) !== undefined ) {
				// Wrapped to prevent IE from throwing errors when 'invalid' values are provided
				// Fixes bug #5509
				try {
					style[ name ] = value;
				} catch(e) {}
			}

		} else {
			// If a hook was provided get the non-computed value from there
			if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, false, extra )) !== undefined ) {
				return ret;
			}

			// Otherwise just get the value from the style object
			return style[ name ];
		}
	},

	css: function( elem, name, extra ) {
		var ret, hooks;

		// Make sure that we're working with the right name
		name = jQuery.camelCase( name );
		hooks = jQuery.cssHooks[ name ];
		name = jQuery.cssProps[ name ] || name;

		// cssFloat needs a special treatment
		if ( name === "cssFloat" ) {
			name = "float";
		}

		// If a hook was provided get the computed value from there
		if ( hooks &amp;&amp; "get" in hooks &amp;&amp; (ret = hooks.get( elem, true, extra )) !== undefined ) {
			return ret;

		// Otherwise, if a way to get the computed value exists, use that
		} else if ( curCSS ) {
			return curCSS( elem, name );
		}
	},

	// A method for quickly swapping in/out CSS properties to get correct calculations
	swap: function( elem, options, callback ) {
		var old = {},
			ret, name;

		// Remember the old values, and insert the new ones
		for ( name in options ) {
			old[ name ] = elem.style[ name ];
			elem.style[ name ] = options[ name ];
		}

		ret = callback.call( elem );

		// Revert the old values
		for ( name in options ) {
			elem.style[ name ] = old[ name ];
		}

		return ret;
	}
});

// DEPRECATED in 1.3, Use jQuery.css() instead
jQuery.curCSS = jQuery.css;

if ( document.defaultView &amp;&amp; document.defaultView.getComputedStyle ) {
	getComputedStyle = function( elem, name ) {
		var ret, defaultView, computedStyle, width,
			style = elem.style;

		name = name.replace( rupper, "-$1" ).toLowerCase();

		if ( (defaultView = elem.ownerDocument.defaultView) &amp;&amp;
				(computedStyle = defaultView.getComputedStyle( elem, null )) ) {

			ret = computedStyle.getPropertyValue( name );
			if ( ret === "" &amp;&amp; !jQuery.contains( elem.ownerDocument.documentElement, elem ) ) {
				ret = jQuery.style( elem, name );
			}
		}

		// A tribute to the "awesome hack by Dean Edwards"
		// WebKit uses "computed value (percentage if specified)" instead of "used value" for margins
		// which is against the CSSOM draft spec: http://dev.w3.org/csswg/cssom/#resolved-values
		if ( !jQuery.support.pixelMargin &amp;&amp; computedStyle &amp;&amp; rmargin.test( name ) &amp;&amp; rnumnonpx.test( ret ) ) {
			width = style.width;
			style.width = ret;
			ret = computedStyle.width;
			style.width = width;
		}

		return ret;
	};
}

if ( document.documentElement.currentStyle ) {
	currentStyle = function( elem, name ) {
		var left, rsLeft, uncomputed,
			ret = elem.currentStyle &amp;&amp; elem.currentStyle[ name ],
			style = elem.style;

		// Avoid setting ret to empty string here
		// so we don't default to auto
		if ( ret == null &amp;&amp; style &amp;&amp; (uncomputed = style[ name ]) ) {
			ret = uncomputed;
		}

		// From the awesome hack by Dean Edwards
		// http://erik.eae.net/archives/2007/07/27/18.54.15/#comment-102291

		// If we're not dealing with a regular pixel number
		// but a number that has a weird ending, we need to convert it to pixels
		if ( rnumnonpx.test( ret ) ) {

			// Remember the original values
			left = style.left;
			rsLeft = elem.runtimeStyle &amp;&amp; elem.runtimeStyle.left;

			// Put in the new values to get a computed value out
			if ( rsLeft ) {
				elem.runtimeStyle.left = elem.currentStyle.left;
			}
			style.left = name === "fontSize" ? "1em" : ret;
			ret = style.pixelLeft + "px";

			// Revert the changed values
			style.left = left;
			if ( rsLeft ) {
				elem.runtimeStyle.left = rsLeft;
			}
		}

		return ret === "" ? "auto" : ret;
	};
}

curCSS = getComputedStyle || currentStyle;

function getWidthOrHeight( elem, name, extra ) {

	// Start with offset property
	var val = name === "width" ? elem.offsetWidth : elem.offsetHeight,
		i = name === "width" ? 1 : 0,
		len = 4;
	if (val === undefined) {
		val = name === "width" ? elem.getBBox().width : elem.getBBox().height;
	}
	if ( val &gt; 0 ) {
		if ( extra !== "border" ) {
			for ( ; i &lt; len; i += 2 ) {
				if ( !extra ) {
					val -= parseFloat( jQuery.css( elem, "padding" + cssExpand[ i ] ) ) || 0;
				}
				if ( extra === "margin" ) {
					val += parseFloat( jQuery.css( elem, extra + cssExpand[ i ] ) ) || 0;
				} else {
					val -= parseFloat( jQuery.css( elem, "border" + cssExpand[ i ] + "Width" ) ) || 0;
				}
			}
		}

		return val + "px";
	}

	// Fall back to computed then uncomputed css if necessary
	val = curCSS( elem, name );
	if ( val &lt; 0 || val == null ) {
		val = elem.style[ name ];
	}

	// Computed unit is not pixels. Stop here and return.
	if ( rnumnonpx.test(val) ) {
		return val;
	}

	// Normalize "", auto, and prepare for extra
	val = parseFloat( val ) || 0;

	// Add padding, border, margin
	if ( extra ) {
		for ( ; i &lt; len; i += 2 ) {
			val += parseFloat( jQuery.css( elem, "padding" + cssExpand[ i ] ) ) || 0;
			if ( extra !== "padding" ) {
				val += parseFloat( jQuery.css( elem, "border" + cssExpand[ i ] + "Width" ) ) || 0;
			}
			if ( extra === "margin" ) {
				val += parseFloat( jQuery.css( elem, extra + cssExpand[ i ]) ) || 0;
			}
		}
	}

	return val + "px";
}

jQuery.each([ "height", "width" ], function( i, name ) {
	jQuery.cssHooks[ name ] = {
		get: function( elem, computed, extra ) {
			if ( computed ) {
				if ( elem.offsetWidth !== 0 ) {
					return getWidthOrHeight( elem, name, extra );
				} else {
					return jQuery.swap( elem, cssShow, function() {
						return getWidthOrHeight( elem, name, extra );
					});
				}
			}
		},

		set: function( elem, value ) {
			return rnum.test( value ) ?
				value + "px" :
				value;
		}
	};
});

if ( !jQuery.support.opacity ) {
	jQuery.cssHooks.opacity = {
		get: function( elem, computed ) {
			// IE uses filters for opacity
			return ropacity.test( (computed &amp;&amp; elem.currentStyle ? elem.currentStyle.filter : elem.style.filter) || "" ) ?
				( parseFloat( RegExp.$1 ) / 100 ) + "" :
				computed ? "1" : "";
		},

		set: function( elem, value ) {
			var style = elem.style,
				currentStyle = elem.currentStyle,
				opacity = jQuery.isNumeric( value ) ? "alpha(opacity=" + value * 100 + ")" : "",
				filter = currentStyle &amp;&amp; currentStyle.filter || style.filter || "";

			// IE has trouble with opacity if it does not have layout
			// Force it by setting the zoom level
			// style.zoom = 1;

			// if setting opacity to 1, and no other filters exist - attempt to remove filter attribute #6652
		  //    if ( value &gt;= 1 &amp;&amp; jQuery.trim( filter.replace( ralpha, "" ) ) === "" ) {

		  //    	// Setting style.filter to null, "" &amp; " " still leave "filter:" in the cssText
		  //    	// if "filter:" is present at all, clearType is disabled, we want to avoid this
		  //    	// style.removeAttribute is IE Only, but so apparently is this code path...
		  //    	style.removeAttribute( "filter" );

		  //    	// if there there is no filter style applied in a css rule, we are done
		  //    	if ( currentStyle &amp;&amp; !currentStyle.filter ) {
		  //    		return;
		  //    	}
		  //    }

			// otherwise, set new filter values
			style.filter = ralpha.test( filter ) ?
				filter.replace( ralpha, opacity ) :
				filter + " " + opacity;
		}
	};
}

jQuery(function() {
	// This hook cannot be added until DOM ready because the support test
	// for it is not run until after DOM ready
	if ( !jQuery.support.reliableMarginRight ) {
		jQuery.cssHooks.marginRight = {
			get: function( elem, computed ) {
				// WebKit Bug 13343 - getComputedStyle returns wrong value for margin-right
				// Work around by temporarily setting element display to inline-block
				return jQuery.swap( elem, { "display": "inline-block" }, function() {
					if ( computed ) {
						return curCSS( elem, "margin-right" );
					} else {
						return elem.style.marginRight;
					}
				});
			}
		};
	}
});

if ( jQuery.expr &amp;&amp; jQuery.expr.filters ) {
	jQuery.expr.filters.hidden = function( elem ) {
		var width = elem.offsetWidth,
			height = elem.offsetHeight;

		return ( width === 0 &amp;&amp; height === 0 ) || (!jQuery.support.reliableHiddenOffsets &amp;&amp; ((elem.style &amp;&amp; elem.style.display) || jQuery.css( elem, "display" )) === "none");
	};

	jQuery.expr.filters.visible = function( elem ) {
		return !jQuery.expr.filters.hidden( elem );
	};
}

// These hooks are used by animate to expand properties
jQuery.each({
	margin: "",
	padding: "",
	border: "Width"
}, function( prefix, suffix ) {

	jQuery.cssHooks[ prefix + suffix ] = {
		expand: function( value ) {
			var i,

				// assumes a single number if not a string
				parts = typeof value === "string" ? value.split(" ") : [ value ],
				expanded = {};

			for ( i = 0; i &lt; 4; i++ ) {
				expanded[ prefix + cssExpand[ i ] + suffix ] =
					parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
			}

			return expanded;
		}
	};
});




var r20 = /%20/g,
	rbracket = /\[\]$/,
	rCRLF = /\r?\n/g,
	rhash = /#.*$/,
	rheaders = /^(.*?):[ \t]*([^\r\n]*)\r?$/mg, // IE leaves an \r character at EOL
	rinput = /^(?:color|date|datetime|datetime-local|email|hidden|month|number|password|range|search|tel|text|time|url|week)$/i,
	// #7653, #8125, #8152: local protocol detection
	rlocalProtocol = /^(?:about|app|app\-storage|.+\-extension|file|res|widget):$/,
	rnoContent = /^(?:GET|HEAD)$/,
	rprotocol = /^\/\//,
	rquery = /\?/,
	rscript = /&lt;script\b[^&lt;]*(?:(?!&lt;\/script&gt;)&lt;[^&lt;]*)*&lt;\/script&gt;/gi,
	rselectTextarea = /^(?:select|textarea)/i,
	rspacesAjax = /\s+/,
	rts = /([?&amp;])_=[^&amp;]*/,
	rurl = /^([\w\+\.\-]+:)(?:\/\/([^\/?#:]*)(?::(\d+))?)?/,

	// Keep a copy of the old load method
	_load = jQuery.fn.load,

	/* Prefilters
	 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
	 * 2) These are called:
	 *    - BEFORE asking for a transport
	 *    - AFTER param serialization (s.data is a string if s.processData is true)
	 * 3) key is the dataType
	 * 4) the catchall symbol "*" can be used
	 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
	 */
	prefilters = {},

	/* Transports bindings
	 * 1) key is the dataType
	 * 2) the catchall symbol "*" can be used
	 * 3) selection will start with transport dataType and THEN go to "*" if needed
	 */
	transports = {},

	// Document location
	ajaxLocation,

	// Document location segments
	ajaxLocParts,

	// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
	allTypes = ["*/"] + ["*"];

// #8138, IE may throw an exception when accessing
// a field from window.location if document.domain has been set
try {
	ajaxLocation = location.href;
} catch( e ) {
	// Use the href attribute of an A element
	// since IE will modify it given document.location
	ajaxLocation = document.createElement( "a" );
	ajaxLocation.href = "";
	ajaxLocation = ajaxLocation.href;
}

// Segment location into parts
ajaxLocParts = rurl.exec( ajaxLocation.toLowerCase() ) || [];

// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
function addToPrefiltersOrTransports( structure ) {

	// dataTypeExpression is optional and defaults to "*"
	return function( dataTypeExpression, func ) {

		if ( typeof dataTypeExpression !== "string" ) {
			func = dataTypeExpression;
			dataTypeExpression = "*";
		}

		if ( jQuery.isFunction( func ) ) {
			var dataTypes = dataTypeExpression.toLowerCase().split( rspacesAjax ),
				i = 0,
				length = dataTypes.length,
				dataType,
				list,
				placeBefore;

			// For each dataType in the dataTypeExpression
			for ( ; i &lt; length; i++ ) {
				dataType = dataTypes[ i ];
				// We control if we're asked to add before
				// any existing element
				placeBefore = /^\+/.test( dataType );
				if ( placeBefore ) {
					dataType = dataType.substr( 1 ) || "*";
				}
				list = structure[ dataType ] = structure[ dataType ] || [];
				// then we add to the structure accordingly
				list[ placeBefore ? "unshift" : "push" ]( func );
			}
		}
	};
}

// Base inspection function for prefilters and transports
function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR,
		dataType /* internal */, inspected /* internal */ ) {

	dataType = dataType || options.dataTypes[ 0 ];
	inspected = inspected || {};

	inspected[ dataType ] = true;

	var list = structure[ dataType ],
		i = 0,
		length = list ? list.length : 0,
		executeOnly = ( structure === prefilters ),
		selection;

	for ( ; i &lt; length &amp;&amp; ( executeOnly || !selection ); i++ ) {
		selection = list[ i ]( options, originalOptions, jqXHR );
		// If we got redirected to another dataType
		// we try there if executing only and not done already
		if ( typeof selection === "string" ) {
			if ( !executeOnly || inspected[ selection ] ) {
				selection = undefined;
			} else {
				options.dataTypes.unshift( selection );
				selection = inspectPrefiltersOrTransports(
						structure, options, originalOptions, jqXHR, selection, inspected );
			}
		}
	}
	// If we're only executing or nothing was selected
	// we try the catchall dataType if not done already
	if ( ( executeOnly || !selection ) &amp;&amp; !inspected[ "*" ] ) {
		selection = inspectPrefiltersOrTransports(
				structure, options, originalOptions, jqXHR, "*", inspected );
	}
	// unnecessary when only executing (prefilters)
	// but it'll be ignored by the caller in that case
	return selection;
}

// A special extend for ajax options
// that takes "flat" options (not to be deep extended)
// Fixes #9887
function ajaxExtend( target, src ) {
	var key, deep,
		flatOptions = jQuery.ajaxSettings.flatOptions || {};
	for ( key in src ) {
		if ( src[ key ] !== undefined ) {
			( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
		}
	}
	if ( deep ) {
		jQuery.extend( true, target, deep );
	}
}

jQuery.fn.extend({
	load: function( url, params, callback ) {
		if ( typeof url !== "string" &amp;&amp; _load ) {
			return _load.apply( this, arguments );

		// Don't do a request if no elements are being requested
		} else if ( !this.length ) {
			return this;
		}

		var off = url.indexOf( " " );
		if ( off &gt;= 0 ) {
			var selector = url.slice( off, url.length );
			url = url.slice( 0, off );
		}

		// Default to a GET request
		var type = "GET";

		// If the second parameter was provided
		if ( params ) {
			// If it's a function
			if ( jQuery.isFunction( params ) ) {
				// We assume that it's the callback
				callback = params;
				params = undefined;

			// Otherwise, build a param string
			} else if ( typeof params === "object" ) {
				params = jQuery.param( params, jQuery.ajaxSettings.traditional );
				type = "POST";
			}
		}

		var self = this;

		// Request the remote document
		jQuery.ajax({
			url: url,
			type: type,
			dataType: "html",
			data: params,
			// Complete callback (responseText is used internally)
			complete: function( jqXHR, status, responseText ) {
				// Store the response as specified by the jqXHR object
				responseText = jqXHR.responseText;
				// If successful, inject the HTML into all the matched elements
				if ( jqXHR.isResolved() ) {
					// #4825: Get the actual response in case
					// a dataFilter is present in ajaxSettings
					jqXHR.done(function( r ) {
						responseText = r;
					});
					// See if a selector was specified
					self.html( selector ?
						// Create a dummy div to hold the results
						jQuery("&lt;div&gt;")
							// inject the contents of the document in, removing the scripts
							// to avoid any 'Permission Denied' errors in IE
							.append(responseText.replace(rscript, ""))

							// Locate the specified elements
							.find(selector) :

						// If not, just inject the full result
						responseText );
				}

				if ( callback ) {
					self.each( callback, [ responseText, status, jqXHR ] );
				}
			}
		});

		return this;
	},

	serialize: function() {
		return jQuery.param( this.serializeArray() );
	},

	serializeArray: function() {
		return this.map(function(){
			return this.elements ? jQuery.makeArray( this.elements ) : this;
		})
		.filter(function(){
			return this.name &amp;&amp; !this.disabled &amp;&amp;
				( this.checked || rselectTextarea.test( this.nodeName ) ||
					rinput.test( this.type ) );
		})
		.map(function( i, elem ){
			var val = jQuery( this ).val();

			return val == null ?
				null :
				jQuery.isArray( val ) ?
					jQuery.map( val, function( val, i ){
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					}) :
					{ name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
		}).get();
	}
});

// Attach a bunch of functions for handling common AJAX events
jQuery.each( "ajaxStart ajaxStop ajaxComplete ajaxError ajaxSuccess ajaxSend".split( " " ), function( i, o ){
	jQuery.fn[ o ] = function( f ){
		return this.on( o, f );
	};
});

jQuery.each( [ "get", "post" ], function( i, method ) {
	jQuery[ method ] = function( url, data, callback, type ) {
		// shift arguments if data argument was omitted
		if ( jQuery.isFunction( data ) ) {
			type = type || callback;
			callback = data;
			data = undefined;
		}

		return jQuery.ajax({
			type: method,
			url: url,
			data: data,
			success: callback,
			dataType: type
		});
	};
});

jQuery.extend({

	getScript: function( url, callback ) {
		return jQuery.get( url, undefined, callback, "script" );
	},

	getJSON: function( url, data, callback ) {
		return jQuery.get( url, data, callback, "json" );
	},

	// Creates a full fledged settings object into target
	// with both ajaxSettings and settings fields.
	// If target is omitted, writes into ajaxSettings.
	ajaxSetup: function( target, settings ) {
		if ( settings ) {
			// Building a settings object
			ajaxExtend( target, jQuery.ajaxSettings );
		} else {
			// Extending ajaxSettings
			settings = target;
			target = jQuery.ajaxSettings;
		}
		ajaxExtend( target, settings );
		return target;
	},

	ajaxSettings: {
		url: ajaxLocation,
		isLocal: rlocalProtocol.test( ajaxLocParts[ 1 ] ),
		global: true,
		type: "GET",
		contentType: "application/x-www-form-urlencoded; charset=UTF-8",
		processData: true,
		async: true,
		/*
		timeout: 0,
		data: null,
		dataType: null,
		username: null,
		password: null,
		cache: null,
		traditional: false,
		headers: {},
		*/

		accepts: {
			xml: "application/xml, text/xml",
			html: "text/html",
			text: "text/plain",
			json: "application/json, text/javascript",
			"*": allTypes
		},

		contents: {
			xml: /xml/,
			html: /html/,
			json: /json/
		},

		responseFields: {
			xml: "responseXML",
			text: "responseText"
		},

		// List of data converters
		// 1) key format is "source_type destination_type" (a single space in-between)
		// 2) the catchall symbol "*" can be used for source_type
		converters: {

			// Convert anything to text
			"* text": window.String,

			// Text to html (true = no transformation)
			"text html": true,

			// Evaluate text as a json expression
			"text json": jQuery.parseJSON,

			// Parse text as xml
			"text xml": jQuery.parseXML
		},

		// For options that shouldn't be deep extended:
		// you can add your own custom options here if
		// and when you create one that shouldn't be
		// deep extended (see ajaxExtend)
		flatOptions: {
			context: true,
			url: true
		}
	},

	ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
	ajaxTransport: addToPrefiltersOrTransports( transports ),

	// Main method
	ajax: function( url, options ) {

		// If url is an object, simulate pre-1.5 signature
		if ( typeof url === "object" ) {
			options = url;
			url = undefined;
		}

		// Force options to be an object
		options = options || {};

		var // Create the final options object
			s = jQuery.ajaxSetup( {}, options ),
			// Callbacks context
			callbackContext = s.context || s,
			// Context for global events
			// It's the callbackContext if one was provided in the options
			// and if it's a DOM node or a jQuery collection
			globalEventContext = callbackContext !== s &amp;&amp;
				( callbackContext.nodeType || callbackContext instanceof jQuery ) ?
						jQuery( callbackContext ) : jQuery.event,
			// Deferreds
			deferred = jQuery.Deferred(),
			completeDeferred = jQuery.Callbacks( "once memory" ),
			// Status-dependent callbacks
			statusCode = s.statusCode || {},
			// ifModified key
			ifModifiedKey,
			// Headers (they are sent all at once)
			requestHeaders = {},
			requestHeadersNames = {},
			// Response headers
			responseHeadersString,
			responseHeaders,
			// transport
			transport,
			// timeout handle
			timeoutTimer,
			// Cross-domain detection vars
			parts,
			// The jqXHR state
			state = 0,
			// To know if global events are to be dispatched
			fireGlobals,
			// Loop variable
			i,
			// Fake xhr
			jqXHR = {

				readyState: 0,

				// Caches the header
				setRequestHeader: function( name, value ) {
					if ( !state ) {
						var lname = name.toLowerCase();
						name = requestHeadersNames[ lname ] = requestHeadersNames[ lname ] || name;
						requestHeaders[ name ] = value;
					}
					return this;
				},

				// Raw string
				getAllResponseHeaders: function() {
					return state === 2 ? responseHeadersString : null;
				},

				// Builds headers hashtable if needed
				getResponseHeader: function( key ) {
					var match;
					if ( state === 2 ) {
						if ( !responseHeaders ) {
							responseHeaders = {};
							while( ( match = rheaders.exec( responseHeadersString ) ) ) {
								responseHeaders[ match[1].toLowerCase() ] = match[ 2 ];
							}
						}
						match = responseHeaders[ key.toLowerCase() ];
					}
					return match === undefined ? null : match;
				},

				// Overrides response content-type header
				overrideMimeType: function( type ) {
					if ( !state ) {
						s.mimeType = type;
					}
					return this;
				},

				// Cancel the request
				abort: function( statusText ) {
					statusText = statusText || "abort";
					if ( transport ) {
						transport.abort( statusText );
					}
					done( 0, statusText );
					return this;
				}
			};

		// Callback for when everything is done
		// It is defined here because jslint complains if it is declared
		// at the end of the function (which would be more logical and readable)
		function done( status, nativeStatusText, responses, headers ) {

			// Called once
			if ( state === 2 ) {
				return;
			}

			// State is "done" now
			state = 2;

			// Clear timeout if it exists
			if ( timeoutTimer ) {
				clearTimeout( timeoutTimer );
			}

			// Dereference transport for early garbage collection
			// (no matter how long the jqXHR object will be used)
			transport = undefined;

			// Cache response headers
			responseHeadersString = headers || "";

			// Set readyState
			jqXHR.readyState = status &gt; 0 ? 4 : 0;

			var isSuccess,
				success,
				error,
				statusText = nativeStatusText,
				response = responses ? ajaxHandleResponses( s, jqXHR, responses ) : undefined,
				lastModified,
				etag;

			// If successful, handle type chaining
			if ( status &gt;= 200 &amp;&amp; status &lt; 300 || status === 304 ) {

				// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
				if ( s.ifModified ) {

					if ( ( lastModified = jqXHR.getResponseHeader( "Last-Modified" ) ) ) {
						jQuery.lastModified[ ifModifiedKey ] = lastModified;
					}
					if ( ( etag = jqXHR.getResponseHeader( "Etag" ) ) ) {
						jQuery.etag[ ifModifiedKey ] = etag;
					}
				}

				// If not modified
				if ( status === 304 ) {

					statusText = "notmodified";
					isSuccess = true;

				// If we have data
				} else {

					try {
						success = ajaxConvert( s, response );
						statusText = "success";
						isSuccess = true;
					} catch(e) {
						// We have a parsererror
						statusText = "parsererror";
						error = e;
					}
				}
			} else {
				// We extract error from statusText
				// then normalize statusText and status for non-aborts
				error = statusText;
				if ( !statusText || status ) {
					statusText = "error";
					if ( status &lt; 0 ) {
						status = 0;
					}
				}
			}

			// Set data for the fake xhr object
			jqXHR.status = status;
			jqXHR.statusText = "" + ( nativeStatusText || statusText );

			// Success/Error
			if ( isSuccess ) {
				deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
			} else {
				deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
			}

			// Status-dependent callbacks
			jqXHR.statusCode( statusCode );
			statusCode = undefined;

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajax" + ( isSuccess ? "Success" : "Error" ),
						[ jqXHR, s, isSuccess ? success : error ] );
			}

			// Complete
			completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );
				// Handle the global AJAX counter
				if ( !( --jQuery.active ) ) {
					jQuery.event.trigger( "ajaxStop" );
				}
			}
		}

		// Attach deferreds
		deferred.promise( jqXHR );
		jqXHR.success = jqXHR.done;
		jqXHR.error = jqXHR.fail;
		jqXHR.complete = completeDeferred.add;

		// Status-dependent callbacks
		jqXHR.statusCode = function( map ) {
			if ( map ) {
				var tmp;
				if ( state &lt; 2 ) {
					for ( tmp in map ) {
						statusCode[ tmp ] = [ statusCode[tmp], map[tmp] ];
					}
				} else {
					tmp = map[ jqXHR.status ];
					jqXHR.then( tmp, tmp );
				}
			}
			return this;
		};

		// Remove hash character (#7531: and string promotion)
		// Add protocol if not provided (#5866: IE7 issue with protocol-less urls)
		// We also use the url parameter if available
		s.url = ( ( url || s.url ) + "" ).replace( rhash, "" ).replace( rprotocol, ajaxLocParts[ 1 ] + "//" );

		// Extract dataTypes list
		s.dataTypes = jQuery.trim( s.dataType || "*" ).toLowerCase().split( rspacesAjax );

		// Determine if a cross-domain request is in order
		if ( s.crossDomain == null ) {
			parts = rurl.exec( s.url.toLowerCase() );
			s.crossDomain = !!( parts &amp;&amp;
				( parts[ 1 ] != ajaxLocParts[ 1 ] || parts[ 2 ] != ajaxLocParts[ 2 ] ||
					( parts[ 3 ] || ( parts[ 1 ] === "http:" ? 80 : 443 ) ) !=
						( ajaxLocParts[ 3 ] || ( ajaxLocParts[ 1 ] === "http:" ? 80 : 443 ) ) )
			);
		}

		// Convert data if not already a string
		if ( s.data &amp;&amp; s.processData &amp;&amp; typeof s.data !== "string" ) {
			s.data = jQuery.param( s.data, s.traditional );
		}

		// Apply prefilters
		inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

		// If request was aborted inside a prefilter, stop there
		if ( state === 2 ) {
			return false;
		}

		// We can fire global events as of now if asked to
		fireGlobals = s.global;

		// Uppercase the type
		s.type = s.type.toUpperCase();

		// Determine if request has content
		s.hasContent = !rnoContent.test( s.type );

		// Watch for a new set of requests
		if ( fireGlobals &amp;&amp; jQuery.active++ === 0 ) {
			jQuery.event.trigger( "ajaxStart" );
		}

		// More options handling for requests with no content
		if ( !s.hasContent ) {

			// If data is available, append data to url
			if ( s.data ) {
				s.url += ( rquery.test( s.url ) ? "&amp;" : "?" ) + s.data;
				// #9682: remove data so that it's not used in an eventual retry
				delete s.data;
			}

			// Get ifModifiedKey before adding the anti-cache parameter
			ifModifiedKey = s.url;

			// Add anti-cache in url if needed
			if ( s.cache === false ) {

				var ts = jQuery.now(),
					// try replacing _= if it is there
					ret = s.url.replace( rts, "$1_=" + ts );

				// if nothing was replaced, add timestamp to the end
				s.url = ret + ( ( ret === s.url ) ? ( rquery.test( s.url ) ? "&amp;" : "?" ) + "_=" + ts : "" );
			}
		}

		// Set the correct header, if data is being sent
		if ( s.data &amp;&amp; s.hasContent &amp;&amp; s.contentType !== false || options.contentType ) {
			jqXHR.setRequestHeader( "Content-Type", s.contentType );
		}

		// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
		if ( s.ifModified ) {
			ifModifiedKey = ifModifiedKey || s.url;
			if ( jQuery.lastModified[ ifModifiedKey ] ) {
				jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ ifModifiedKey ] );
			}
			if ( jQuery.etag[ ifModifiedKey ] ) {
				jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ ifModifiedKey ] );
			}
		}

		// Set the Accepts header for the server, depending on the dataType
		jqXHR.setRequestHeader(
			"Accept",
			s.dataTypes[ 0 ] &amp;&amp; s.accepts[ s.dataTypes[0] ] ?
				s.accepts[ s.dataTypes[0] ] + ( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
				s.accepts[ "*" ]
		);

		// Check for headers option
		for ( i in s.headers ) {
			jqXHR.setRequestHeader( i, s.headers[ i ] );
		}

		// Allow custom headers/mimetypes and early abort
		if ( s.beforeSend &amp;&amp; ( s.beforeSend.call( callbackContext, jqXHR, s ) === false || state === 2 ) ) {
				// Abort if not done already
				jqXHR.abort();
				return false;

		}

		// Install callbacks on deferreds
		for ( i in { success: 1, error: 1, complete: 1 } ) {
			jqXHR[ i ]( s[ i ] );
		}

		// Get transport
		transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

		// If no transport, we auto-abort
		if ( !transport ) {
			done( -1, "No Transport" );
		} else {
			jqXHR.readyState = 1;
			// Send global event
			if ( fireGlobals ) {
				globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
			}
			// Timeout
			if ( s.async &amp;&amp; s.timeout &gt; 0 ) {
				timeoutTimer = setTimeout( function(){
					jqXHR.abort( "timeout" );
				}, s.timeout );
			}

			try {
				state = 1;
				transport.send( requestHeaders, done );
			} catch (e) {
				// Propagate exception as error if not done
				if ( state &lt; 2 ) {
					done( -1, e );
				// Simply rethrow otherwise
				} else {
					throw e;
				}
			}
		}

		return jqXHR;
	},

	// Serialize an array of form elements or a set of
	// key/values into a query string
	param: function( a, traditional ) {
		var s = [],
			add = function( key, value ) {
				// If value is a function, invoke it and return its value
				value = jQuery.isFunction( value ) ? value() : value;
				s[ s.length ] = encodeURIComponent( key ) + "=" + encodeURIComponent( value );
			};

		// Set traditional to true for jQuery &lt;= 1.3.2 behavior.
		if ( traditional === undefined ) {
			traditional = jQuery.ajaxSettings.traditional;
		}

		// If an array was passed in, assume that it is an array of form elements.
		if ( jQuery.isArray( a ) || ( a.jquery &amp;&amp; !jQuery.isPlainObject( a ) ) ) {
			// Serialize the form elements
			jQuery.each( a, function() {
				add( this.name, this.value );
			});

		} else {
			// If traditional, encode the "old" way (the way 1.3.2 or older
			// did it), otherwise encode params recursively.
			for ( var prefix in a ) {
				buildParams( prefix, a[ prefix ], traditional, add );
			}
		}

		// Return the resulting serialization
		return s.join( "&amp;" ).replace( r20, "+" );
	}
});

function buildParams( prefix, obj, traditional, add ) {
	if ( jQuery.isArray( obj ) ) {
		// Serialize array item.
		jQuery.each( obj, function( i, v ) {
			if ( traditional || rbracket.test( prefix ) ) {
				// Treat each array item as a scalar.
				add( prefix, v );

			} else {
				// If array item is non-scalar (array or object), encode its
				// numeric index to resolve deserialization ambiguity issues.
				// Note that rack (as of 1.0.0) can't currently deserialize
				// nested arrays properly, and attempting to do so may cause
				// a server error. Possible fixes are to modify rack's
				// deserialization algorithm or to provide an option or flag
				// to force array serialization to be shallow.
				buildParams( prefix + "[" + ( typeof v === "object" ? i : "" ) + "]", v, traditional, add );
			}
		});

	} else if ( !traditional &amp;&amp; jQuery.type( obj ) === "object" ) {
		// Serialize object item.
		for ( var name in obj ) {
			buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
		}

	} else {
		// Serialize scalar item.
		add( prefix, obj );
	}
}

// This is still on the jQuery object... for now
// Want to move this to jQuery.ajax some day
jQuery.extend({

	// Counter for holding the number of active queries
	active: 0,

	// Last-Modified header cache for next request
	lastModified: {},
	etag: {}

});

/* Handles responses to an ajax request:
 * - sets all responseXXX fields accordingly
 * - finds the right dataType (mediates between content-type and expected dataType)
 * - returns the corresponding response
 */
function ajaxHandleResponses( s, jqXHR, responses ) {

	var contents = s.contents,
		dataTypes = s.dataTypes,
		responseFields = s.responseFields,
		ct,
		type,
		finalDataType,
		firstDataType;

	// Fill responseXXX fields
	for ( type in responseFields ) {
		if ( type in responses ) {
			jqXHR[ responseFields[type] ] = responses[ type ];
		}
	}

	// Remove auto dataType and get content-type in the process
	while( dataTypes[ 0 ] === "*" ) {
		dataTypes.shift();
		if ( ct === undefined ) {
			ct = s.mimeType || jqXHR.getResponseHeader( "content-type" );
		}
	}

	// Check if we're dealing with a known content-type
	if ( ct ) {
		for ( type in contents ) {
			if ( contents[ type ] &amp;&amp; contents[ type ].test( ct ) ) {
				dataTypes.unshift( type );
				break;
			}
		}
	}

	// Check to see if we have a response for the expected dataType
	if ( dataTypes[ 0 ] in responses ) {
		finalDataType = dataTypes[ 0 ];
	} else {
		// Try convertible dataTypes
		for ( type in responses ) {
			if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[0] ] ) {
				finalDataType = type;
				break;
			}
			if ( !firstDataType ) {
				firstDataType = type;
			}
		}
		// Or just use first one
		finalDataType = finalDataType || firstDataType;
	}

	// If we found a dataType
	// We add the dataType to the list if needed
	// and return the corresponding response
	if ( finalDataType ) {
		if ( finalDataType !== dataTypes[ 0 ] ) {
			dataTypes.unshift( finalDataType );
		}
		return responses[ finalDataType ];
	}
}

// Chain conversions given the request and the original response
function ajaxConvert( s, response ) {

	// Apply the dataFilter if provided
	if ( s.dataFilter ) {
		response = s.dataFilter( response, s.dataType );
	}

	var dataTypes = s.dataTypes,
		converters = {},
		i,
		key,
		length = dataTypes.length,
		tmp,
		// Current and previous dataTypes
		current = dataTypes[ 0 ],
		prev,
		// Conversion expression
		conversion,
		// Conversion function
		conv,
		// Conversion functions (transitive conversion)
		conv1,
		conv2;

	// For each dataType in the chain
	for ( i = 1; i &lt; length; i++ ) {

		// Create converters map
		// with lowercased keys
		if ( i === 1 ) {
			for ( key in s.converters ) {
				if ( typeof key === "string" ) {
					converters[ key.toLowerCase() ] = s.converters[ key ];
				}
			}
		}

		// Get the dataTypes
		prev = current;
		current = dataTypes[ i ];

		// If current is auto dataType, update it to prev
		if ( current === "*" ) {
			current = prev;
		// If no auto and dataTypes are actually different
		} else if ( prev !== "*" &amp;&amp; prev !== current ) {

			// Get the converter
			conversion = prev + " " + current;
			conv = converters[ conversion ] || converters[ "* " + current ];

			// If there is no direct converter, search transitively
			if ( !conv ) {
				conv2 = undefined;
				for ( conv1 in converters ) {
					tmp = conv1.split( " " );
					if ( tmp[ 0 ] === prev || tmp[ 0 ] === "*" ) {
						conv2 = converters[ tmp[1] + " " + current ];
						if ( conv2 ) {
							conv1 = converters[ conv1 ];
							if ( conv1 === true ) {
								conv = conv2;
							} else if ( conv2 === true ) {
								conv = conv1;
							}
							break;
						}
					}
				}
			}
			// If we found no converter, dispatch an error
			if ( !( conv || conv2 ) ) {
				jQuery.error( "No conversion from " + conversion.replace(" "," to ") );
			}
			// If found converter is not an equivalence
			if ( conv !== true ) {
				// Convert with 1 or 2 converters accordingly
				response = conv ? conv( response ) : conv2( conv1(response) );
			}
		}
	}
	return response;
}




var jsc = jQuery.now(),
	jsre = /(\=)\?(&amp;|$)|\?\?/i;

// Default jsonp settings
jQuery.ajaxSetup({
	jsonp: "callback",
	jsonpCallback: function() {
		return jQuery.expando + "_" + ( jsc++ );
	}
});

// Detect, normalize options and install callbacks for jsonp requests
jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

	var inspectData = ( typeof s.data === "string" ) &amp;&amp; /^application\/x\-www\-form\-urlencoded/.test( s.contentType );

	if ( s.dataTypes[ 0 ] === "jsonp" ||
		s.jsonp !== false &amp;&amp; ( jsre.test( s.url ) ||
				inspectData &amp;&amp; jsre.test( s.data ) ) ) {

		var responseContainer,
			jsonpCallback = s.jsonpCallback =
				jQuery.isFunction( s.jsonpCallback ) ? s.jsonpCallback() : s.jsonpCallback,
			previous = window[ jsonpCallback ],
			url = s.url,
			data = s.data,
			replace = "$1" + jsonpCallback + "$2";

		if ( s.jsonp !== false ) {
			url = url.replace( jsre, replace );
			if ( s.url === url ) {
				if ( inspectData ) {
					data = data.replace( jsre, replace );
				}
				if ( s.data === data ) {
					// Add callback manually
					url += (/\?/.test( url ) ? "&amp;" : "?") + s.jsonp + "=" + jsonpCallback;
				}
			}
		}

		s.url = url;
		s.data = data;

		// Install callback
		window[ jsonpCallback ] = function( response ) {
			responseContainer = [ response ];
		};

		// Clean-up function
		jqXHR.always(function() {
			// Set callback back to previous value
			window[ jsonpCallback ] = previous;
			// Call if it was a function and we have a response
			if ( responseContainer &amp;&amp; jQuery.isFunction( previous ) ) {
				window[ jsonpCallback ]( responseContainer[ 0 ] );
			}
		});

		// Use data converter to retrieve json after script execution
		s.converters["script json"] = function() {
			if ( !responseContainer ) {
				jQuery.error( jsonpCallback + " was not called" );
			}
			return responseContainer[ 0 ];
		};

		// force json dataType
		s.dataTypes[ 0 ] = "json";

		// Delegate to script
		return "script";
	}
});




// Install script dataType
jQuery.ajaxSetup({
	accepts: {
		script: "text/javascript, application/javascript, application/ecmascript, application/x-ecmascript"
	},
	contents: {
		script: /javascript|ecmascript/
	},
	converters: {
		"text script": function( text ) {
			jQuery.globalEval( text );
			return text;
		}
	}
});

// Handle cache's special case and global
jQuery.ajaxPrefilter( "script", function( s ) {
	if ( s.cache === undefined ) {
		s.cache = false;
	}
	if ( s.crossDomain ) {
		s.type = "GET";
		s.global = false;
	}
});

// Bind script tag hack transport
jQuery.ajaxTransport( "script", function(s) {

	// This transport only deals with cross domain requests
	if ( s.crossDomain ) {

		var script,
			head = document.head || document.getElementsByTagName( "head" )[0] || document.documentElement;

		return {

			send: function( _, callback ) {

				script = document.createElement( "script" );

				script.async = "async";

				if ( s.scriptCharset ) {
					script.charset = s.scriptCharset;
				}

				script.src = s.url;

				// Attach handlers for all browsers
				script.onload = script.onreadystatechange = function( _, isAbort ) {

					if ( isAbort || !script.readyState || /loaded|complete/.test( script.readyState ) ) {

						// Handle memory leak in IE
						script.onload = script.onreadystatechange = null;

						// Remove the script
						if ( head &amp;&amp; script.parentNode ) {
							head.removeChild( script );
						}

						// Dereference the script
						script = undefined;

						// Callback if not abort
						if ( !isAbort ) {
							callback( 200, "success" );
						}
					}
				};
				// Use insertBefore instead of appendChild  to circumvent an IE6 bug.
				// This arises when a base node is used (#2709 and #4378).
				head.insertBefore( script, head.firstChild );
			},

			abort: function() {
				if ( script ) {
					script.onload( 0, 1 );
				}
			}
		};
	}
});




var // #5280: Internet Explorer will keep connections alive if we don't abort on unload
	xhrOnUnloadAbort = window.ActiveXObject ? function() {
		// Abort all pending requests
		for ( var key in xhrCallbacks ) {
			xhrCallbacks[ key ]( 0, 1 );
		}
	} : false,
	xhrId = 0,
	xhrCallbacks;

// Functions to create xhrs
function createStandardXHR() {
	try {
		return new window.XMLHttpRequest();
	} catch( e ) {}
}

function createActiveXHR() {
	try {
		return new window.ActiveXObject( "Microsoft.XMLHTTP" );
	} catch( e ) {}
}

// Create the request object
// (This is still attached to ajaxSettings for backward compatibility)
jQuery.ajaxSettings.xhr = window.ActiveXObject ?
	/* Microsoft failed to properly
	 * implement the XMLHttpRequest in IE7 (can't request local files),
	 * so we use the ActiveXObject when it is available
	 * Additionally XMLHttpRequest can be disabled in IE7/IE8 so
	 * we need a fallback.
	 */
	function() {
		return !this.isLocal &amp;&amp; createStandardXHR() || createActiveXHR();
	} :
	// For all other browsers, use the standard XMLHttpRequest object
	createStandardXHR;

// Determine support properties
(function( xhr ) {
	jQuery.extend( jQuery.support, {
		ajax: !!xhr,
		cors: !!xhr &amp;&amp; ( "withCredentials" in xhr )
	});
})( jQuery.ajaxSettings.xhr() );

// Create transport if the browser can provide an xhr
if ( jQuery.support.ajax ) {

	jQuery.ajaxTransport(function( s ) {
		// Cross domain only allowed if supported through XMLHttpRequest
		if ( !s.crossDomain || jQuery.support.cors ) {

			var callback;

			return {
				send: function( headers, complete ) {

					// Get a new xhr
					var xhr = s.xhr(),
						handle,
						i;

					// Open the socket
					// Passing null username, generates a login popup on Opera (#2865)
					if ( s.username ) {
						xhr.open( s.type, s.url, s.async, s.username, s.password );
					} else {
						xhr.open( s.type, s.url, s.async );
					}

					// Apply custom fields if provided
					if ( s.xhrFields ) {
						for ( i in s.xhrFields ) {
							xhr[ i ] = s.xhrFields[ i ];
						}
					}

					// Override mime type if needed
					if ( s.mimeType &amp;&amp; xhr.overrideMimeType ) {
						xhr.overrideMimeType( s.mimeType );
					}

					// X-Requested-With header
					// For cross-domain requests, seeing as conditions for a preflight are
					// akin to a jigsaw puzzle, we simply never set it to be sure.
					// (it can always be set on a per-request basis or even using ajaxSetup)
					// For same-domain requests, won't change header if already provided.
					if ( !s.crossDomain &amp;&amp; !headers["X-Requested-With"] ) {
						headers[ "X-Requested-With" ] = "XMLHttpRequest";
					}

					// Need an extra try/catch for cross domain requests in Firefox 3
					try {
						for ( i in headers ) {
							xhr.setRequestHeader( i, headers[ i ] );
						}
					} catch( _ ) {}

					// Do send the request
					// This may raise an exception which is actually
					// handled in jQuery.ajax (so no try/catch here)
					xhr.send( ( s.hasContent &amp;&amp; s.data ) || null );

					// Listener
					callback = function( _, isAbort ) {

						var status,
							statusText,
							responseHeaders,
							responses,
							xml;

						// Firefox throws exceptions when accessing properties
						// of an xhr when a network error occured
						// http://helpful.knobs-dials.com/index.php/Component_returned_failure_code:_0x80040111_(NS_ERROR_NOT_AVAILABLE)
						try {

							// Was never called and is aborted or complete
							if ( callback &amp;&amp; ( isAbort || xhr.readyState === 4 ) ) {

								// Only called once
								callback = undefined;

								// Do not keep as active anymore
								if ( handle ) {
									xhr.onreadystatechange = jQuery.noop;
									if ( xhrOnUnloadAbort ) {
										delete xhrCallbacks[ handle ];
									}
								}

								// If it's an abort
								if ( isAbort ) {
									// Abort it manually if needed
									if ( xhr.readyState !== 4 ) {
										xhr.abort();
									}
								} else {
									status = xhr.status;
									responseHeaders = xhr.getAllResponseHeaders();
									responses = {};
									xml = xhr.responseXML;

									// Construct response list
									if ( xml &amp;&amp; xml.documentElement /* #4958 */ ) {
										responses.xml = xml;
									}

									// When requesting binary data, IE6-9 will throw an exception
									// on any attempt to access responseText (#11426)
									try {
										responses.text = xhr.responseText;
									} catch( _ ) {
									}

									// Firefox throws an exception when accessing
									// statusText for faulty cross-domain requests
									try {
										statusText = xhr.statusText;
									} catch( e ) {
										// We normalize with Webkit giving an empty statusText
										statusText = "";
									}

									// Filter status for non standard behaviors

									// If the request is local and we have data: assume a success
									// (success with no data won't get notified, that's the best we
									// can do given current implementations)
									if ( !status &amp;&amp; s.isLocal &amp;&amp; !s.crossDomain ) {
										status = responses.text ? 200 : 404;
									// IE - #1450: sometimes returns 1223 when it should be 204
									} else if ( status === 1223 ) {
										status = 204;
									}
								}
							}
						} catch( firefoxAccessException ) {
							if ( !isAbort ) {
								complete( -1, firefoxAccessException );
							}
						}

						// Call complete if needed
						if ( responses ) {
							complete( status, statusText, responses, responseHeaders );
						}
					};

					// if we're in sync mode or it's in cache
					// and has been retrieved directly (IE6 &amp; IE7)
					// we need to manually fire the callback
					if ( !s.async || xhr.readyState === 4 ) {
						callback();
					} else {
						handle = ++xhrId;
						if ( xhrOnUnloadAbort ) {
							// Create the active xhrs callbacks list if needed
							// and attach the unload handler
							if ( !xhrCallbacks ) {
								xhrCallbacks = {};
								jQuery( window ).unload( xhrOnUnloadAbort );
							}
							// Add to list of active xhrs callbacks
							xhrCallbacks[ handle ] = callback;
						}
						xhr.onreadystatechange = callback;
					}
				},

				abort: function() {
					if ( callback ) {
						callback(0,1);
					}
				}
			};
		}
	});
}




var elemdisplay = {},
	iframe, iframeDoc,
	rfxtypes = /^(?:toggle|show|hide)$/,
	rfxnum = /^([+\-]=)?([\d+.\-]+)([a-z%]*)$/i,
	timerId,
	fxAttrs = [
		// height animations
		[ "height", "marginTop", "marginBottom", "paddingTop", "paddingBottom" ],
		// width animations
		[ "width", "marginLeft", "marginRight", "paddingLeft", "paddingRight" ],
		// opacity animations
		[ "opacity" ]
	],
	fxNow;

jQuery.fn.extend({
	show: function( speed, easing, callback ) {
		var elem, display;

		if ( speed || speed === 0 ) {
			return this.animate( genFx("show", 3), speed, easing, callback );

		} else {
			for ( var i = 0, j = this.length; i &lt; j; i++ ) {
				elem = this[ i ];

				if ( elem.style ) {
					display = elem.style.display;

					// Reset the inline display of this element to learn if it is
					// being hidden by cascaded rules or not
					if ( !jQuery._data(elem, "olddisplay") &amp;&amp; display === "none" ) {
						display = elem.style.display = "";
					}

					// Set elements which have been overridden with display: none
					// in a stylesheet to whatever the default browser style is
					// for such an element
					if ( (display === "" &amp;&amp; jQuery.css(elem, "display") === "none") ||
						!jQuery.contains( elem.ownerDocument.documentElement, elem ) ) {
						jQuery._data( elem, "olddisplay", defaultDisplay(elem.nodeName) );
					}
				}
			}

			// Set the display of most of the elements in a second loop
			// to avoid the constant reflow
			for ( i = 0; i &lt; j; i++ ) {
				elem = this[ i ];

				if ( elem.style ) {
					display = elem.style.display;

					if ( display === "" || display === "none" ) {
						elem.style.display = jQuery._data( elem, "olddisplay" ) || "";
					}
				}
			}

			return this;
		}
	},

	hide: function( speed, easing, callback ) {
		if ( speed || speed === 0 ) {
			return this.animate( genFx("hide", 3), speed, easing, callback);

		} else {
			var elem, display,
				i = 0,
				j = this.length;

			for ( ; i &lt; j; i++ ) {
				elem = this[i];
				if ( elem.style ) {
					display = jQuery.css( elem, "display" );

					if ( display !== "none" &amp;&amp; !jQuery._data( elem, "olddisplay" ) ) {
						jQuery._data( elem, "olddisplay", display );
					}
				}
			}

			// Set the display of the elements in a second loop
			// to avoid the constant reflow
			for ( i = 0; i &lt; j; i++ ) {
				if ( this[i].style ) {
					this[i].style.display = "none";
				}
			}

			return this;
		}
	},

	// Save the old toggle function
	_toggle: jQuery.fn.toggle,

	toggle: function( fn, fn2, callback ) {
		var bool = typeof fn === "boolean";

		if ( jQuery.isFunction(fn) &amp;&amp; jQuery.isFunction(fn2) ) {
			this._toggle.apply( this, arguments );

		} else if ( fn == null || bool ) {
			this.each(function() {
				var state = bool ? fn : jQuery(this).is(":hidden");
				jQuery(this)[ state ? "show" : "hide" ]();
			});

		} else {
			this.animate(genFx("toggle", 3), fn, fn2, callback);
		}

		return this;
	},

	fadeTo: function( speed, to, easing, callback ) {
		return this.filter(":hidden").css("opacity", 0).show().end()
					.animate({opacity: to}, speed, easing, callback);
	},

	animate: function( prop, speed, easing, callback ) {
		var optall = jQuery.speed( speed, easing, callback );

		if ( jQuery.isEmptyObject( prop ) ) {
			return this.each( optall.complete, [ false ] );
		}

		// Do not change referenced properties as per-property easing will be lost
		prop = jQuery.extend( {}, prop );

		function doAnimation() {
			// XXX 'this' does not always have a nodeName when running the
			// test suite

			if ( optall.queue === false ) {
				jQuery._mark( this );
			}

			var opt = jQuery.extend( {}, optall ),
				isElement = this.nodeType === 1,
				hidden = isElement &amp;&amp; jQuery(this).is(":hidden"),
				name, val, p, e, hooks, replace,
				parts, start, end, unit,
				method;

			// will store per property easing and be used to determine when an animation is complete
			opt.animatedProperties = {};

			// first pass over propertys to expand / normalize
			for ( p in prop ) {
				name = jQuery.camelCase( p );
				if ( p !== name ) {
					prop[ name ] = prop[ p ];
					delete prop[ p ];
				}

				if ( ( hooks = jQuery.cssHooks[ name ] ) &amp;&amp; "expand" in hooks ) {
					replace = hooks.expand( prop[ name ] );
					delete prop[ name ];

					// not quite $.extend, this wont overwrite keys already present.
					// also - reusing 'p' from above because we have the correct "name"
					for ( p in replace ) {
						if ( ! ( p in prop ) ) {
							prop[ p ] = replace[ p ];
						}
					}
				}
			}

			for ( name in prop ) {
				val = prop[ name ];
				// easing resolution: per property &gt; opt.specialEasing &gt; opt.easing &gt; 'swing' (default)
				if ( jQuery.isArray( val ) ) {
					opt.animatedProperties[ name ] = val[ 1 ];
					val = prop[ name ] = val[ 0 ];
				} else {
					opt.animatedProperties[ name ] = opt.specialEasing &amp;&amp; opt.specialEasing[ name ] || opt.easing || 'swing';
				}

				if ( val === "hide" &amp;&amp; hidden || val === "show" &amp;&amp; !hidden ) {
					return opt.complete.call( this );
				}

				if ( isElement &amp;&amp; ( name === "height" || name === "width" ) ) {
					// Make sure that nothing sneaks out
					// Record all 3 overflow attributes because IE does not
					// change the overflow attribute when overflowX and
					// overflowY are set to the same value
					opt.overflow = [ this.style.overflow, this.style.overflowX, this.style.overflowY ];

					// Set display property to inline-block for height/width
					// animations on inline elements that are having width/height animated
					if ( jQuery.css( this, "display" ) === "inline" &amp;&amp;
							jQuery.css( this, "float" ) === "none" ) {

						// inline-level elements accept inline-block;
						// block-level elements need to be inline with layout
						if ( !jQuery.support.inlineBlockNeedsLayout || defaultDisplay( this.nodeName ) === "inline" ) {
							this.style.display = "inline-block";

						} else {
							this.style.zoom = 1;
						}
					}
				}
			}

			if ( opt.overflow != null ) {
				this.style.overflow = "hidden";
			}

			for ( p in prop ) {
				e = new jQuery.fx( this, opt, p );
				val = prop[ p ];

				if ( rfxtypes.test( val ) ) {

					// Tracks whether to show or hide based on private
					// data attached to the element
					method = jQuery._data( this, "toggle" + p ) || ( val === "toggle" ? hidden ? "show" : "hide" : 0 );
					if ( method ) {
						jQuery._data( this, "toggle" + p, method === "show" ? "hide" : "show" );
						e[ method ]();
					} else {
						e[ val ]();
					}

				} else {
					parts = rfxnum.exec( val );
					start = e.cur();

					if ( parts ) {
						end = parseFloat( parts[2] );
						unit = parts[3] || ( jQuery.cssNumber[ p ] ? "" : "px" );

						// We need to compute starting value
						if ( unit !== "px" ) {
							jQuery.style( this, p, (end || 1) + unit);
							start = ( (end || 1) / e.cur() ) * start;
							jQuery.style( this, p, start + unit);
						}

						// If a +=/-= token was provided, we're doing a relative animation
						if ( parts[1] ) {
							end = ( (parts[ 1 ] === "-=" ? -1 : 1) * end ) + start;
						}

						e.custom( start, end, unit );

					} else {
						e.custom( start, val, "" );
					}
				}
			}

			// For JS strict compliance
			return true;
		}

		return optall.queue === false ?
			this.each( doAnimation ) :
			this.queue( optall.queue, doAnimation );
	},

	stop: function( type, clearQueue, gotoEnd ) {
		if ( typeof type !== "string" ) {
			gotoEnd = clearQueue;
			clearQueue = type;
			type = undefined;
		}
		if ( clearQueue &amp;&amp; type !== false ) {
			this.queue( type || "fx", [] );
		}

		return this.each(function() {
			var index,
				hadTimers = false,
				timers = jQuery.timers,
				data = jQuery._data( this );

			// clear marker counters if we know they won't be
			if ( !gotoEnd ) {
				jQuery._unmark( true, this );
			}

			function stopQueue( elem, data, index ) {
				var hooks = data[ index ];
				jQuery.removeData( elem, index, true );
				hooks.stop( gotoEnd );
			}

			if ( type == null ) {
				for ( index in data ) {
					if ( data[ index ] &amp;&amp; data[ index ].stop &amp;&amp; index.indexOf(".run") === index.length - 4 ) {
						stopQueue( this, data, index );
					}
				}
			} else if ( data[ index = type + ".run" ] &amp;&amp; data[ index ].stop ){
				stopQueue( this, data, index );
			}

			for ( index = timers.length; index--; ) {
				if ( timers[ index ].elem === this &amp;&amp; (type == null || timers[ index ].queue === type) ) {
					if ( gotoEnd ) {

						// force the next step to be the last
						timers[ index ]( true );
					} else {
						timers[ index ].saveState();
					}
					hadTimers = true;
					timers.splice( index, 1 );
				}
			}

			// start the next in the queue if the last step wasn't forced
			// timers currently will call their complete callbacks, which will dequeue
			// but only if they were gotoEnd
			if ( !( gotoEnd &amp;&amp; hadTimers ) ) {
				jQuery.dequeue( this, type );
			}
		});
	}

});

// Animations created synchronously will run synchronously
function createFxNow() {
	setTimeout( clearFxNow, 0 );
	return ( fxNow = jQuery.now() );
}

function clearFxNow() {
	fxNow = undefined;
}

// Generate parameters to create a standard animation
function genFx( type, num ) {
	var obj = {};

	jQuery.each( fxAttrs.concat.apply([], fxAttrs.slice( 0, num )), function() {
		obj[ this ] = type;
	});

	return obj;
}

// Generate shortcuts for custom animations
jQuery.each({
	slideDown: genFx( "show", 1 ),
	slideUp: genFx( "hide", 1 ),
	slideToggle: genFx( "toggle", 1 ),
	fadeIn: { opacity: "show" },
	fadeOut: { opacity: "hide" },
	fadeToggle: { opacity: "toggle" }
}, function( name, props ) {
	jQuery.fn[ name ] = function( speed, easing, callback ) {
		return this.animate( props, speed, easing, callback );
	};
});

jQuery.extend({
	speed: function( speed, easing, fn ) {
		var opt = speed &amp;&amp; typeof speed === "object" ? jQuery.extend( {}, speed ) : {
			complete: fn || !fn &amp;&amp; easing ||
				jQuery.isFunction( speed ) &amp;&amp; speed,
			duration: speed,
			easing: fn &amp;&amp; easing || easing &amp;&amp; !jQuery.isFunction( easing ) &amp;&amp; easing
		};

		opt.duration = jQuery.fx.off ? 0 : typeof opt.duration === "number" ? opt.duration :
			opt.duration in jQuery.fx.speeds ? jQuery.fx.speeds[ opt.duration ] : jQuery.fx.speeds._default;

		// normalize opt.queue - true/undefined/null -&gt; "fx"
		if ( opt.queue == null || opt.queue === true ) {
			opt.queue = "fx";
		}

		// Queueing
		opt.old = opt.complete;

		opt.complete = function( noUnmark ) {
			if ( jQuery.isFunction( opt.old ) ) {
				opt.old.call( this );
			}

			if ( opt.queue ) {
				jQuery.dequeue( this, opt.queue );
			} else if ( noUnmark !== false ) {
				jQuery._unmark( this );
			}
		};

		return opt;
	},

	easing: {
		linear: function( p ) {
			return p;
		},
		swing: function( p ) {
			return ( -Math.cos( p*Math.PI ) / 2 ) + 0.5;
		}
	},

	timers: [],

	fx: function( elem, options, prop ) {
		this.options = options;
		this.elem = elem;
		this.prop = prop;

		options.orig = options.orig || {};
	}

});

jQuery.fx.prototype = {
	// Simple function for setting a style value
	update: function() {
		if ( this.options.step ) {
			this.options.step.call( this.elem, this.now, this );
		}

		( jQuery.fx.step[ this.prop ] || jQuery.fx.step._default )( this );
	},

	// Get the current size
	cur: function() {
		if ( this.elem[ this.prop ] != null &amp;&amp; (!this.elem.style || this.elem.style[ this.prop ] == null) ) {
              if (this.elem[ this.prop ].baseVal) {
			return this.elem[ this.prop ].baseVal.value;
              }
			return this.elem[ this.prop ];
		}

		var parsed,
			r = jQuery.css( this.elem, this.prop );
		// Empty strings, null, undefined and "auto" are converted to 0,
		// complex values such as "rotate(1rad)" are returned as is,
		// simple values such as "10px" are parsed to Float.
		return isNaN( parsed = parseFloat( r ) ) ? !r || r === "auto" ? 0 : r : parsed;
	},

	// Start an animation from one number to another
	custom: function( from, to, unit ) {
		var self = this,
			fx = jQuery.fx;

		this.startTime = fxNow || createFxNow();
		this.end = to;
		this.now = this.start = from;
		this.pos = this.state = 0;
		this.unit = unit || this.unit || ( jQuery.cssNumber[ this.prop ] ? "" : "px" );

		function t( gotoEnd ) {
			return self.step( gotoEnd );
		}

		t.queue = this.options.queue;
		t.elem = this.elem;
		t.saveState = function() {
			if ( jQuery._data( self.elem, "fxshow" + self.prop ) === undefined ) {
				if ( self.options.hide ) {
					jQuery._data( self.elem, "fxshow" + self.prop, self.start );
				} else if ( self.options.show ) {
					jQuery._data( self.elem, "fxshow" + self.prop, self.end );
				}
			}
		};

		if ( t() &amp;&amp; jQuery.timers.push(t) &amp;&amp; !timerId ) {
			timerId = setInterval( fx.tick, fx.interval );
		}
	},

	// Simple 'show' function
	show: function() {
		var dataShow = jQuery._data( this.elem, "fxshow" + this.prop );

		// Remember where we started, so that we can go back to it later
		this.options.orig[ this.prop ] = dataShow || jQuery.style( this.elem, this.prop );
		this.options.show = true;

		// Begin the animation
		// Make sure that we start at a small width/height to avoid any flash of content
		if ( dataShow !== undefined ) {
			// This show is picking up where a previous hide or show left off
			this.custom( this.cur(), dataShow );
		} else {
			this.custom( this.prop === "width" || this.prop === "height" ? 1 : 0, this.cur() );
		}

		// Start by showing the element
		jQuery( this.elem ).show();
	},

	// Simple 'hide' function
	hide: function() {
		// Remember where we started, so that we can go back to it later
		this.options.orig[ this.prop ] = jQuery._data( this.elem, "fxshow" + this.prop ) || jQuery.style( this.elem, this.prop );
		this.options.hide = true;

		// Begin the animation
		this.custom( this.cur(), 0 );
	},

	// Each step of an animation
	step: function( gotoEnd ) {
		var p, n, complete,
			t = fxNow || createFxNow(),
			done = true,
			elem = this.elem,
			options = this.options;

		if ( gotoEnd || t &gt;= options.duration + this.startTime ) {
			this.now = this.end;
			this.pos = this.state = 1;
			this.update();

			options.animatedProperties[ this.prop ] = true;

			for ( p in options.animatedProperties ) {
				if ( options.animatedProperties[ p ] !== true ) {
					done = false;
				}
			}

			if ( done ) {
				// Reset the overflow
				if ( options.overflow != null &amp;&amp; !jQuery.support.shrinkWrapBlocks ) {

					jQuery.each( [ "", "X", "Y" ], function( index, value ) {
						elem.style[ "overflow" + value ] = options.overflow[ index ];
					});
				}

				// Hide the element if the "hide" operation was done
				if ( options.hide ) {
					jQuery( elem ).hide();
				}

				// Reset the properties, if the item has been hidden or shown
				if ( options.hide || options.show ) {
					for ( p in options.animatedProperties ) {
						jQuery.style( elem, p, options.orig[ p ] );
						jQuery.removeData( elem, "fxshow" + p, true );
						// Toggle data is no longer needed
						jQuery.removeData( elem, "toggle" + p, true );
					}
				}

				// Execute the complete function
				// in the event that the complete function throws an exception
				// we must ensure it won't be called twice. #5684

				complete = options.complete;
				if ( complete ) {

					options.complete = false;
					complete.call( elem );
				}
			}

			return false;

		} else {
			// classical easing cannot be used with an Infinity duration
			if ( options.duration == Infinity ) {
				this.now = t;
			} else {
				n = t - this.startTime;
				this.state = n / options.duration;

				// Perform the easing function, defaults to swing
				this.pos = jQuery.easing[ options.animatedProperties[this.prop] ]( this.state, n, 0, 1, options.duration );
				this.now = this.start + ( (this.end - this.start) * this.pos );
			}
			// Perform the next step of the animation
			this.update();
		}

		return true;
	}
};

jQuery.extend( jQuery.fx, {
	tick: function() {
		var timer,
			timers = jQuery.timers,
			i = 0;

		for ( ; i &lt; timers.length; i++ ) {
			timer = timers[ i ];
			// Checks the timer has not already been removed
			if ( !timer() &amp;&amp; timers[ i ] === timer ) {
				timers.splice( i--, 1 );
			}
		}

		if ( !timers.length ) {
			jQuery.fx.stop();
		}
	},

	interval: 13,

	stop: function() {
		clearInterval( timerId );
		timerId = null;
	},

	speeds: {
		slow: 600,
		fast: 200,
		// Default speed
		_default: 400
	},

	step: {
		opacity: function( fx ) {
			jQuery.style( fx.elem, "opacity", fx.now );
		},

		_default: function( fx ) {
			if ( fx.elem.style &amp;&amp; fx.elem.style[ fx.prop ] != null ) {
				fx.elem.style[ fx.prop ] = fx.now + fx.unit;
			} else {
				fx.elem[ fx.prop ] = fx.now;
			}
		}
	}
});

// Ensure props that can't be negative don't go there on undershoot easing
jQuery.each( fxAttrs.concat.apply( [], fxAttrs ), function( i, prop ) {
	// exclude marginTop, marginLeft, marginBottom and marginRight from this list
	if ( prop.indexOf( "margin" ) ) {
		jQuery.fx.step[ prop ] = function( fx ) {
			jQuery.style( fx.elem, prop, Math.max(0, fx.now) + fx.unit );
		};
	}
});

if ( jQuery.expr &amp;&amp; jQuery.expr.filters ) {
	jQuery.expr.filters.animated = function( elem ) {
		return jQuery.grep(jQuery.timers, function( fn ) {
			return elem === fn.elem;
		}).length;
	};
}

// Try to restore the default display value of an element
function defaultDisplay( nodeName ) {

	if ( !elemdisplay[ nodeName ] ) {

		var body = document.body,
			elem = jQuery( "&lt;" + nodeName + "&gt;" ).appendTo( body ),
			display = elem.css( "display" );
		elem.remove();

		// If the simple way fails,
		// get element's real default display by attaching it to a temp iframe
		if ( display === "none" || display === "" ) {
			// No iframe to use yet, so create it
			if ( !iframe ) {
				iframe = document.createElement( "iframe" );
				iframe.frameBorder = iframe.width = iframe.height = 0;
			}

			body.appendChild( iframe );

			// Create a cacheable copy of the iframe document on first call.
			// IE and Opera will allow us to reuse the iframeDoc without re-writing the fake HTML
			// document to it; WebKit &amp; Firefox won't allow reusing the iframe document.
			if ( !iframeDoc || !iframe.createElement ) {
				iframeDoc = ( iframe.contentWindow || iframe.contentDocument ).document;
				iframeDoc.write( ( jQuery.support.boxModel ? "&lt;!doctype html&gt;" : "" ) + "&lt;html&gt;&lt;body&gt;" );
				iframeDoc.close();
			}

			elem = iframeDoc.createElement( nodeName );

			iframeDoc.body.appendChild( elem );

			display = jQuery.css( elem, "display" );
			body.removeChild( iframe );
		}

		// Store the correct default display
		elemdisplay[ nodeName ] = display;
	}

	return elemdisplay[ nodeName ];
}




var getOffset,
	rtable = /^t(?:able|d|h)$/i,
	rroot = /^(?:body|html)$/i;

if ( "getBoundingClientRect" in document.documentElement ) {
	getOffset = function( elem, doc, docElem, box ) {
		try {
			box = elem.getBoundingClientRect();
		} catch(e) {}

		// Make sure we're not dealing with a disconnected DOM node
		if ( !box || !jQuery.contains( docElem, elem ) ) {
			return box ? { top: box.top, left: box.left } : { top: 0, left: 0 };
		}

		var body = doc.body,
			win = getWindow( doc ),
			clientTop  = docElem.clientTop  || body.clientTop  || 0,
			clientLeft = docElem.clientLeft || body.clientLeft || 0,
			scrollTop  = win.pageYOffset || jQuery.support.boxModel &amp;&amp; docElem.scrollTop  || body.scrollTop,
			scrollLeft = win.pageXOffset || jQuery.support.boxModel &amp;&amp; docElem.scrollLeft || body.scrollLeft,
			top  = box.top  + scrollTop  - clientTop,
			left = box.left + scrollLeft - clientLeft;

		return { top: top, left: left };
	};

} else {
	getOffset = function( elem, doc, docElem ) {
		var computedStyle,
			offsetParent = elem.offsetParent,
			prevOffsetParent = elem,
			body = doc.body,
			defaultView = doc.defaultView,
			prevComputedStyle = defaultView ? defaultView.getComputedStyle( elem, null ) : elem.currentStyle,
			top = elem.offsetTop,
			left = elem.offsetLeft;

		while ( (elem = elem.parentNode) &amp;&amp; elem !== body &amp;&amp; elem !== docElem ) {
			if ( jQuery.support.fixedPosition &amp;&amp; prevComputedStyle.position === "fixed" ) {
				break;
			}

			computedStyle = defaultView ? defaultView.getComputedStyle(elem, null) : elem.currentStyle;
			top  -= elem.scrollTop;
			left -= elem.scrollLeft;

			if ( elem === offsetParent ) {
				top  += elem.offsetTop;
				left += elem.offsetLeft;

				if ( jQuery.support.doesNotAddBorder &amp;&amp; !(jQuery.support.doesAddBorderForTableAndCells &amp;&amp; rtable.test(elem.nodeName)) ) {
					top  += parseFloat( computedStyle.borderTopWidth  ) || 0;
					left += parseFloat( computedStyle.borderLeftWidth ) || 0;
				}

				prevOffsetParent = offsetParent;
				offsetParent = elem.offsetParent;
			}

			if ( jQuery.support.subtractsBorderForOverflowNotVisible &amp;&amp; computedStyle.overflow !== "visible" ) {
				top  += parseFloat( computedStyle.borderTopWidth  ) || 0;
				left += parseFloat( computedStyle.borderLeftWidth ) || 0;
			}

			prevComputedStyle = computedStyle;
		}

		if ( prevComputedStyle.position === "relative" || prevComputedStyle.position === "static" ) {
			top  += body.offsetTop;
			left += body.offsetLeft;
		}

		if ( jQuery.support.fixedPosition &amp;&amp; prevComputedStyle.position === "fixed" ) {
			top  += Math.max( docElem.scrollTop, body.scrollTop );
			left += Math.max( docElem.scrollLeft, body.scrollLeft );
		}

		return { top: top, left: left };
	};
}

jQuery.fn.offset = function( options ) {
	if ( arguments.length ) {
		return options === undefined ?
			this :
			this.each(function( i ) {
				jQuery.offset.setOffset( this, options, i );
			});
	}

	var elem = this[0],
		doc = elem &amp;&amp; elem.ownerDocument;

	if ( !doc ) {
		return null;
	}

	if ( elem === doc.body ) {
		return jQuery.offset.bodyOffset( elem );
	}

	return getOffset( elem, doc, doc.documentElement );
};

jQuery.offset = {

	bodyOffset: function( body ) {
		var top = body.offsetTop,
			left = body.offsetLeft;

		if ( jQuery.support.doesNotIncludeMarginInBodyOffset ) {
			top  += parseFloat( jQuery.css(body, "marginTop") ) || 0;
			left += parseFloat( jQuery.css(body, "marginLeft") ) || 0;
		}

		return { top: top, left: left };
	},

	setOffset: function( elem, options, i ) {
		var position = jQuery.css( elem, "position" );

		// set position first, in-case top/left are set even on static elem
		if ( position === "static" ) {
			elem.style.position = "relative";
		}

		var curElem = jQuery( elem ),
			curOffset = curElem.offset(),
			curCSSTop = jQuery.css( elem, "top" ),
			curCSSLeft = jQuery.css( elem, "left" ),
			calculatePosition = ( position === "absolute" || position === "fixed" ) &amp;&amp; jQuery.inArray("auto", [curCSSTop, curCSSLeft]) &gt; -1,
			props = {}, curPosition = {}, curTop, curLeft;

		// need to be able to calculate position if either top or left is auto and position is either absolute or fixed
		if ( calculatePosition ) {
			curPosition = curElem.position();
			curTop = curPosition.top;
			curLeft = curPosition.left;
		} else {
			curTop = parseFloat( curCSSTop ) || 0;
			curLeft = parseFloat( curCSSLeft ) || 0;
		}

		if ( jQuery.isFunction( options ) ) {
			options = options.call( elem, i, curOffset );
		}

		if ( options.top != null ) {
			props.top = ( options.top - curOffset.top ) + curTop;
		}
		if ( options.left != null ) {
			props.left = ( options.left - curOffset.left ) + curLeft;
		}

		if ( "using" in options ) {
			options.using.call( elem, props );
		} else {
			curElem.css( props );
		}
	}
};


jQuery.fn.extend({

	position: function() {
		if ( !this[0] ) {
			return null;
		}

		var elem = this[0],

		// Get *real* offsetParent
		offsetParent = this.offsetParent(),

		// Get correct offsets
		offset       = this.offset(),
		parentOffset = rroot.test(offsetParent[0].nodeName) ? { top: 0, left: 0 } : offsetParent.offset();

		// Subtract element margins
		// note: when an element has margin: auto the offsetLeft and marginLeft
		// are the same in Safari causing offset.left to incorrectly be 0
		offset.top  -= parseFloat( jQuery.css(elem, "marginTop") ) || 0;
		offset.left -= parseFloat( jQuery.css(elem, "marginLeft") ) || 0;

		// Add offsetParent borders
		parentOffset.top  += parseFloat( jQuery.css(offsetParent[0], "borderTopWidth") ) || 0;
		parentOffset.left += parseFloat( jQuery.css(offsetParent[0], "borderLeftWidth") ) || 0;

		// Subtract the two offsets
		return {
			top:  offset.top  - parentOffset.top,
			left: offset.left - parentOffset.left
		};
	},

	offsetParent: function() {
		return this.map(function() {
			var offsetParent = this.offsetParent || document.body;
			while ( offsetParent &amp;&amp; (!rroot.test(offsetParent.nodeName) &amp;&amp; jQuery.css(offsetParent, "position") === "static") ) {
				offsetParent = offsetParent.offsetParent;
			}
			return offsetParent;
		});
	}
});


// Create scrollLeft and scrollTop methods
jQuery.each( {scrollLeft: "pageXOffset", scrollTop: "pageYOffset"}, function( method, prop ) {
	var top = /Y/.test( prop );

	jQuery.fn[ method ] = function( val ) {
		return jQuery.access( this, function( elem, method, val ) {
			var win = getWindow( elem );

			if ( val === undefined ) {
				return win ? (prop in win) ? win[ prop ] :
					jQuery.support.boxModel &amp;&amp; win.document.documentElement[ method ] ||
						win.document.body[ method ] :
					elem[ method ];
			}

			if ( win ) {
				win.scrollTo(
					!top ? val : jQuery( win ).scrollLeft(),
					 top ? val : jQuery( win ).scrollTop()
				);

			} else {
				elem[ method ] = val;
			}
		}, method, val, arguments.length, null );
	};
});

function getWindow( elem ) {
	return jQuery.isWindow( elem ) ?
		elem :
		elem.nodeType === 9 ?
			elem.defaultView || elem.parentWindow :
			false;
}




// Create width, height, innerHeight, innerWidth, outerHeight and outerWidth methods
jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
	var clientProp = "client" + name,
		scrollProp = "scroll" + name,
		offsetProp = "offset" + name;

	// innerHeight and innerWidth
	jQuery.fn[ "inner" + name ] = function() {
		var elem = this[0];
		return elem ?
			elem.style ?
			parseFloat( jQuery.css( elem, type, "padding" ) ) :
			this[ type ]() :
			null;
	};

	// outerHeight and outerWidth
	jQuery.fn[ "outer" + name ] = function( margin ) {
		var elem = this[0];
		return elem ?
			elem.style ?
			parseFloat( jQuery.css( elem, type, margin ? "margin" : "border" ) ) :
			this[ type ]() :
			null;
	};

	jQuery.fn[ type ] = function( value ) {
		return jQuery.access( this, function( elem, type, value ) {
			var doc, docElemProp, orig, ret;

			if ( jQuery.isWindow( elem ) ) {
				// 3rd condition allows Nokia support, as it supports the docElem prop but not CSS1Compat
				doc = elem.document;
				docElemProp = doc.documentElement[ clientProp ];
				return jQuery.support.boxModel &amp;&amp; docElemProp ||
					doc.body &amp;&amp; doc.body[ clientProp ] || docElemProp;
			}

			// Get document width or height
			if ( elem.nodeType === 9 ) {
				// Either scroll[Width/Height] or offset[Width/Height], whichever is greater
				doc = elem.documentElement;

				// when a window &gt; document, IE6 reports a offset[Width/Height] &gt; client[Width/Height]
				// so we can't use max, as it'll choose the incorrect offset[Width/Height]
				// instead we use the correct client[Width/Height]
				// support:IE6
				if ( doc[ clientProp ] &gt;= doc[ scrollProp ] ) {
					return doc[ clientProp ];
				}

				return Math.max(
					elem.body[ scrollProp ], doc[ scrollProp ],
					elem.body[ offsetProp ], doc[ offsetProp ]
				);
			}

			// Get width or height on the element
			if ( value === undefined ) {
				orig = jQuery.css( elem, type );
				ret = parseFloat( orig );
				return jQuery.isNumeric( ret ) ? ret : orig;
			}

			// Set the width or height on the element
			jQuery( elem ).css( type, value );
		}, type, value, arguments.length, null );
	};
});




// Expose jQuery to the global object
window.jQuerySvg = window.$vg = jQuery;

// Expose jQuery as an AMD module, but only for AMD loaders that
// understand the issues with loading multiple versions of jQuery
// in a page that all might call define(). The loader will indicate
// they have special allowances for multiple jQuery versions by
// specifying define.amd.jQuery = true. Register as a named module,
// since jQuery can be concatenated with other files that may use define,
// but not use a proper concatenation script that understands anonymous
// AMD modules. A named AMD is safest and most robust way to register.
// Lowercase jquery is used because AMD module names are derived from
// file names, and jQuery is normally delivered in a lowercase file name.
// Do this after creating the global so that if an AMD module wants to call
// noConflict to hide this version of jQuery, it will work.
if ( typeof define === "function" &amp;&amp; define.amd &amp;&amp; define.amd.jQuery ) {
	define( "jquery", [], function () { return jQuery; } );
}



})( window );
</script><script type="text/javascript">// Generated by CoffeeScript 1.6.3
(function() {
  var get_translation, init, padding, r_translation, tooltip_timeout;

  padding = 5;

  tooltip_timeout = 0;

  r_translation = /translate\((\d+)[ ,]+(\d+)\)/;

  get_translation = function($elt) {
    return (r_translation.exec($elt.attr('transform')) || []).slice(1);
  };

  init = function(ctx) {
    var tooltip, untooltip;
    $vg('.text-overlay .series', ctx).hide();
    $vg('.reactive', ctx).hover((function() {
      return $vg(this).addClass('active');
    }), (function() {
      return $vg(this).removeClass('active');
    }));
    $vg('.activate-serie', ctx).hover((function() {
      var num;
      num = this.id.replace('activate-serie-', '');
      $vg('.text-overlay .serie-' + num, ctx).show();
      return $vg('.serie-' + num + ' .reactive', ctx).addClass('active');
    }), function() {
      var num;
      num = this.id.replace('activate-serie-', '');
      $vg('.text-overlay .serie-' + num, ctx).hide();
      return $vg('.serie-' + num + ' .reactive', ctx).removeClass('active');
    });
    $vg('.tooltip-trigger', ctx).hover((function() {
      return tooltip($vg(this));
    }), (function() {
      return untooltip();
    }));
    tooltip = function($elt) {
      var $label, $rect, $text, $tooltip, $value, current_x, current_y, h, target, w, x, x_elt, xlink, y, y_elt, _ref;
      clearTimeout(tooltip_timeout);
      $tooltip = $vg('#tooltip', ctx).css({
        opacity: 1
      });
      $text = $tooltip.find('text');
      $label = $tooltip.find('tspan.label');
      $value = $tooltip.find('tspan.value');
      $rect = $tooltip.find('rect');
      if ($elt.siblings('.tooltip').size()) {
        $label.text($elt.siblings('.tooltip').text());
        $value.text('');
      } else {
        $label.text($elt.siblings('.label').text());
        $value.text($elt.siblings('.value').text());
      }
      xlink = $elt.siblings('.xlink').text() || null;
      target = $elt.parent().attr('target');
      if (xlink) {
        $tooltip.find('a').attr('href', xlink).attr('target', target);
      }
      $text.attr('x', padding);
      $text.attr('y', padding + this.config.tooltip_font_size);
      $value.attr('x', padding);
      $value.attr('dy', $label.text() ? this.config.tooltip_font_size + padding : 0);
      w = $text.width() + 2 * padding;
      h = $text.height() + 2 * padding;
      $rect.attr('width', w);
      $rect.attr('height', h);
      x_elt = $elt.siblings('.x');
      y_elt = $elt.siblings('.y');
      x = parseInt(x_elt.text());
      if (x_elt.hasClass('centered')) {
        x -= w / 2;
      } else if (x_elt.hasClass('left')) {
        x -= w;
      }
      y = parseInt(y_elt.text());
      if (y_elt.hasClass('centered')) {
        y -= h / 2;
      } else if (y_elt.hasClass('top')) {
        y -= h;
      }
      _ref = get_translation($tooltip), current_x = _ref[0], current_y = _ref[1];
      if (current_x === x &amp;&amp; current_y === y) {
        return;
      }
      return $tooltip.attr('transform', "translate(" + x + " " + y + ")");
    };
    return untooltip = function() {
      return tooltip_timeout = setTimeout((function() {
        return $vg('#tooltip', ctx).css({
          opacity: 0
        });
      }), 1000);
    };
  };

  this.init_svg = function(ctx) {
    return init($vg(ctx));
  };

  $vg(function() {
    var $charts;
    $charts = $vg('.pygal-chart');
    if ($charts.size()) {
      return $charts.each(function() {
        return init_svg(this);
      });
    } else {
      return init();
    }
  });

}).call(this);
</script></defs><title>Load</title><g class="graph line-graph vertical"><rect class="background" height="600" width="1000" x="0" y="0"/><g class="plot" transform="translate(143, 46)"><rect class="background" height="499.316955788" width="827.383893384" x="0" y="0"/><g class="axis x"><g class="guides"><path class="major line" d="M15.911229 0.000000 v499.316956"/><text class="major" x="15.9112287189" y="513.316955788" transform="rotate(40 15.911229 513.316956)">00:10:01</text></g><g class="guides"><path class="major guide line" d="M49.526501 0.000000 v499.316956"/><text class="major" x="49.5265006603" y="513.316955788" transform="rotate(40 49.526501 513.316956)">01:10:01</text></g><g class="guides"><path class="major guide line" d="M83.141773 0.000000 v499.316956"/><text class="major" x="83.1417726017" y="513.316955788" transform="rotate(40 83.141773 513.316956)">02:10:01</text></g><g class="guides"><path class="major guide line" d="M116.757045 0.000000 v499.316956"/><text class="major" x="116.757044543" y="513.316955788" transform="rotate(40 116.757045 513.316956)">03:10:01</text></g><g class="guides"><path class="major guide line" d="M150.372316 0.000000 v499.316956"/><text class="major" x="150.372316485" y="513.316955788" transform="rotate(40 150.372316 513.316956)">04:10:01</text></g><g class="guides"><path class="major guide line" d="M183.987588 0.000000 v499.316956"/><text class="major" x="183.987588426" y="513.316955788" transform="rotate(40 183.987588 513.316956)">05:10:01</text></g><g class="guides"><path class="major guide line" d="M217.602860 0.000000 v499.316956"/><text class="major" x="217.602860367" y="513.316955788" transform="rotate(40 217.602860 513.316956)">06:10:01</text></g><g class="guides"><path class="major guide line" d="M251.218132 0.000000 v499.316956"/><text class="major" x="251.218132309" y="513.316955788" transform="rotate(40 251.218132 513.316956)">07:10:01</text></g><g class="guides"><path class="major guide line" d="M284.833404 0.000000 v499.316956"/><text class="major" x="284.83340425" y="513.316955788" transform="rotate(40 284.833404 513.316956)">08:10:01</text></g><g class="guides"><path class="major guide line" d="M318.448676 0.000000 v499.316956"/><text class="major" x="318.448676191" y="513.316955788" transform="rotate(40 318.448676 513.316956)">09:10:01</text></g><g class="guides"><path class="major guide line" d="M352.063948 0.000000 v499.316956"/><text class="major" x="352.063948133" y="513.316955788" transform="rotate(40 352.063948 513.316956)">10:10:01</text></g><g class="guides"><path class="major guide line" d="M385.679220 0.000000 v499.316956"/><text class="major" x="385.679220074" y="513.316955788" transform="rotate(40 385.679220 513.316956)">11:10:01</text></g><g class="guides"><path class="major guide line" d="M419.294492 0.000000 v499.316956"/><text class="major" x="419.294492016" y="513.316955788" transform="rotate(40 419.294492 513.316956)">12:10:01</text></g><g class="guides"><path class="major guide line" d="M452.909764 0.000000 v499.316956"/><text class="major" x="452.909763957" y="513.316955788" transform="rotate(40 452.909764 513.316956)">13:10:01</text></g><g class="guides"><path class="major guide line" d="M486.525036 0.000000 v499.316956"/><text class="major" x="486.525035898" y="513.316955788" transform="rotate(40 486.525036 513.316956)">14:10:01</text></g><g class="guides"><path class="major guide line" d="M520.140308 0.000000 v499.316956"/><text class="major" x="520.14030784" y="513.316955788" transform="rotate(40 520.140308 513.316956)">15:10:01</text></g><g class="guides"><path class="major guide line" d="M553.755580 0.000000 v499.316956"/><text class="major" x="553.755579781" y="513.316955788" transform="rotate(40 553.755580 513.316956)">16:10:01</text></g><g class="guides"><path class="major guide line" d="M587.370852 0.000000 v499.316956"/><text class="major" x="587.370851723" y="513.316955788" transform="rotate(40 587.370852 513.316956)">17:10:02</text></g><g class="guides"><path class="major guide line" d="M620.986124 0.000000 v499.316956"/><text class="major" x="620.986123664" y="513.316955788" transform="rotate(40 620.986124 513.316956)">18:10:01</text></g><g class="guides"><path class="major guide line" d="M654.601396 0.000000 v499.316956"/><text class="major" x="654.601395605" y="513.316955788" transform="rotate(40 654.601396 513.316956)">19:10:01</text></g><g class="guides"><path class="major guide line" d="M688.216668 0.000000 v499.316956"/><text class="major" x="688.216667547" y="513.316955788" transform="rotate(40 688.216668 513.316956)">20:10:01</text></g><g class="guides"><path class="major guide line" d="M721.831939 0.000000 v499.316956"/><text class="major" x="721.831939488" y="513.316955788" transform="rotate(40 721.831939 513.316956)">21:10:01</text></g><g class="guides"><path class="major guide line" d="M755.447211 0.000000 v499.316956"/><text class="major" x="755.44721143" y="513.316955788" transform="rotate(40 755.447211 513.316956)">22:10:01</text></g><g class="guides"><path class="major guide line" d="M789.062483 0.000000 v499.316956"/><text class="major" x="789.062483371" y="513.316955788" transform="rotate(40 789.062483 513.316956)">23:10:01</text></g></g><g class="axis y"><path class="line" d="M0.000000 499.316956 h827.383893"/><g class="logarithmic guides"><path class="major guide line" d="M0.000000 499.316956 h827.383893"/><text class="major" x="-5" y="502.116955788">0.01</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 467.864072 h827.383893"/><text class="" x="-5" y="470.664072496">0.02</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 449.465315 h827.383893"/><text class="" x="-5" y="452.26531523">0.03</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 436.411189 h827.383893"/><text class="" x="-5" y="439.211189204">0.04</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 426.285622 h827.383893"/><text class="" x="-5" y="429.085622407">0.05</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 418.012432 h827.383893"/><text class="" x="-5" y="420.812431938">0.06</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 411.017549 h827.383893"/><text class="" x="-5" y="413.817549065">0.07</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 404.958306 h827.383893"/><text class="" x="-5" y="407.758305912">0.08</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 399.613675 h827.383893"/><text class="" x="-5" y="402.413674673">0.09</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 394.832739 h827.383893"/><text class="major" x="-5" y="397.632739115">0.1</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 363.379856 h827.383893"/><text class="" x="-5" y="366.179855822">0.2</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 344.981099 h827.383893"/><text class="" x="-5" y="347.781098557">0.3</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 331.926973 h827.383893"/><text class="" x="-5" y="334.72697253">0.4</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 321.801406 h827.383893"/><text class="" x="-5" y="324.601405733">0.5</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 313.528215 h827.383893"/><text class="" x="-5" y="316.328215265">0.6</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 306.533332 h827.383893"/><text class="" x="-5" y="309.333332391">0.7</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 300.474089 h827.383893"/><text class="" x="-5" y="303.274089238">0.8</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 295.129458 h827.383893"/><text class="" x="-5" y="297.929457999">0.9</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 290.348522 h827.383893"/><text class="major" x="-5" y="293.148522441">1.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 258.895639 h827.383893"/><text class="" x="-5" y="261.695639149">2.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 240.496882 h827.383893"/><text class="" x="-5" y="243.296881883">3.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 227.442756 h827.383893"/><text class="" x="-5" y="230.242755857">4.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 217.317189 h827.383893"/><text class="" x="-5" y="220.11718906">5.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 209.043999 h827.383893"/><text class="" x="-5" y="211.843998591">6.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 202.049116 h827.383893"/><text class="" x="-5" y="204.849115718">7.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 195.989873 h827.383893"/><text class="" x="-5" y="198.789872565">8.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 190.645241 h827.383893"/><text class="" x="-5" y="193.445241326">9.0</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 185.864306 h827.383893"/><text class="major" x="-5" y="188.664305768">10.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 154.411422 h827.383893"/><text class="" x="-5" y="157.211422475">20.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 136.012665 h827.383893"/><text class="" x="-5" y="138.81266521">30.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 122.958539 h827.383893"/><text class="" x="-5" y="125.758539183">40.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 112.832972 h827.383893"/><text class="" x="-5" y="115.632972386">50.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 104.559782 h827.383893"/><text class="" x="-5" y="107.359781918">60.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 97.564899 h827.383893"/><text class="" x="-5" y="100.364899044">70.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 91.505656 h827.383893"/><text class="" x="-5" y="94.3056558911">80.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 86.161025 h827.383893"/><text class="" x="-5" y="88.9610246523">90.0</text></g><g class="logarithmic guides"><path class="major guide line" d="M0.000000 81.380089 h827.383893"/><text class="major" x="-5" y="84.1800890942">100.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 49.927206 h827.383893"/><text class="" x="-5" y="52.727205802">200.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 31.528449 h827.383893"/><text class="" x="-5" y="34.3284485365">300.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 18.474323 h827.383893"/><text class="" x="-5" y="21.2743225098">400.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 8.348756 h827.383893"/><text class="" x="-5" y="11.1487557129">500.0</text></g><g class="logarithmic guides"><path class="guide line" d="M0.000000 0.075565 h827.383893"/><text class="" x="-5" y="2.87556524435">600.0</text></g></g><g class="series serie-0 color-0"><path class="line reactive" d="M15.911229 499.316956 L15.911229 8.804809 21.513774 8.988521 27.116319 8.713231 32.718865 8.896572 38.321410 8.713231 43.923955 8.804809 49.526501 8.804809 55.129046 8.896572 60.731591 8.988521 66.334137 8.988521 71.936682 8.988521 77.539227 8.804809 83.141773 8.896572 88.744318 8.896572 94.346863 8.804809 99.949409 8.258093 105.551954 8.713231 111.154499 8.713231 116.757045 8.530627 122.359590 8.804809 127.962135 8.713231 133.564681 8.804809 139.167226 8.804809 144.769771 8.896572 150.372316 8.988521 155.974862 8.896572 161.577407 8.167610 167.179952 8.621837 172.782498 8.348756 178.385043 8.621837 183.987588 8.621837 189.590134 8.621837 195.192679 8.621837 200.795224 8.713231 206.397770 8.896572 212.000315 8.896572 217.602860 8.988521 223.205406 8.988521 228.807951 8.988521 234.410496 9.080657 240.013042 8.988521 245.615587 8.988521 251.218132 8.988521 256.820678 9.080657 262.423223 9.080657 268.025768 9.080657 273.628314 9.080657 279.230859 9.080657 284.833404 9.080657 290.435950 9.080657 296.038495 9.080657 301.641040 9.080657 307.243586 10.390598 312.846131 10.485628 318.448676 10.106695 324.051222 8.896572 329.653767 9.730901 335.256312 10.676287 340.858857 8.896572 346.461403 9.080657 352.063948 9.730901 357.666493 8.621837 363.269039 9.637436 368.871584 9.824558 374.474129 8.258093 380.076675 8.258093 385.679220 7.717885 391.281765 7.184032 396.884311 6.656388 402.486856 4.355093 408.089401 5.109298 413.691947 5.962272 419.294492 5.704692 424.897037 6.134808 430.499583 6.481861 436.102128 3.695054 441.704673 4.605104 447.307219 6.569041 452.909764 7.007468 458.512309 6.919443 464.114855 6.919443 469.717400 6.743904 475.319945 6.743904 480.922491 5.448566 486.525036 4.940610 492.127581 4.940610 497.730127 4.438277 503.332672 5.278615 508.935217 5.193877 514.537763 4.605104 520.140308 2.323558 525.742853 2.802896 531.345398 2.883280 536.947944 0.000000 542.550489 1.069539 548.153034 2.963807 553.755580 3.287352 559.358125 3.531533 564.960670 2.323558 570.563216 4.272061 576.165761 5.533781 581.768306 4.521614 587.370852 4.688749 592.973397 4.772547 598.575942 5.024875 604.178488 4.940610 609.781033 5.193877 615.383578 5.193877 620.986124 5.193877 626.588669 3.531533 632.191214 5.962272 637.793760 8.167610 643.396305 8.167610 648.998850 8.167610 654.601396 8.167610 660.203941 8.167610 665.806486 8.077308 671.409032 8.167610 677.011577 8.167610 682.614122 8.167610 688.216668 8.167610 693.819213 8.167610 699.421758 8.077308 705.024304 8.167610 710.626849 8.167610 716.229394 8.167610 721.831939 8.167610 727.434485 8.167610 733.037030 8.167610 738.639575 8.167610 744.242121 8.167610 749.844666 8.167610 755.447211 8.167610 761.049757 8.167610 766.652302 8.167610 772.254847 8.167610 777.857393 8.167610 783.459938 8.167610 789.062483 7.628472 794.665029 8.167610 800.267574 8.167610 805.870119 8.167610 811.472665 8.167610 811.472665 499.316956"/></g><g class="series serie-1 color-1"><path class="line reactive" d="M15.911229 499.316956 L15.911229 411.017549 21.513774 361.165909 27.116319 370.754464 32.718865 436.411189 38.321410 467.864072 43.923955 307.186249 49.526501 499.316956 55.129046 449.465315 60.731591 449.465315 66.334137 382.927457 71.936682 449.465315 77.539227 499.316956 83.141773 499.316956 94.346863 499.316956 99.949409 404.958306 105.551954 285.206016 122.359590 319.157342 133.564681 467.864072 139.167226 368.160791 150.372316 411.017549 155.974862 449.465315 167.179952 353.254289 172.782498 411.017549 178.385043 426.285622 183.987588 467.864072 195.192679 411.017549 217.602860 305.889677 228.807951 399.613675 234.410496 331.926973 240.013042 399.613675 245.615587 418.012432 251.218132 418.012432 256.820678 436.411189 262.423223 394.832739 268.025768 411.017549 273.628314 467.864072 279.230859 353.254289 290.435950 436.411189 296.038495 418.012432 307.243586 467.864072 312.846131 499.316956 318.448676 404.958306 324.051222 379.564666 329.653767 357.037890 335.256312 390.507857 340.858857 320.902824 346.461403 394.832739 352.063948 379.564666 357.666493 365.707387 363.269039 499.316956 368.871584 467.864072 374.474129 307.848697 380.076675 339.301581 385.679220 306.533332 391.281765 344.981099 396.884311 351.474574 402.486856 330.806498 408.089401 342.052539 413.691947 324.609121 419.294492 418.012432 424.897037 449.465315 430.499583 355.106665 436.102128 426.285622 441.704673 346.519447 447.307219 418.012432 452.909764 404.958306 458.512309 394.832739 464.114855 342.052539 469.717400 404.958306 475.319945 365.707387 480.922491 344.981099 486.525036 318.309151 492.127581 467.864072 497.730127 404.958306 503.332672 418.012432 508.935217 386.559549 514.537763 370.754464 520.140308 386.559549 525.742853 399.613675 531.345398 355.106665 536.947944 340.656216 542.550489 373.505423 548.153034 390.507857 553.755580 370.754464 559.358125 330.806498 564.960670 333.075817 570.563216 390.507857 576.165761 299.910394 581.768306 322.718142 587.370852 292.200899 592.973397 292.200899 598.575942 349.762034 604.178488 499.316956 609.781033 467.864072 615.383578 370.754464 620.986124 361.165909 626.588669 300.474089 632.191214 376.433982 637.793760 386.559549 643.396305 499.316956 648.998850 418.012432 654.601396 499.316956 660.203941 418.012432 665.806486 286.856268 671.409032 436.411189 677.011577 467.864072 682.614122 449.465315 688.216668 499.316956 699.421758 357.037890 705.024304 399.613675 710.626849 394.832739 721.831939 386.559549 727.434485 399.613675 733.037030 411.017549 738.639575 467.864072 744.242121 411.017549 749.844666 386.559549 755.447211 499.316956 766.652302 499.316956 772.254847 379.564666 777.857393 404.958306 783.459938 467.864072 789.062483 426.285622 794.665029 404.958306 800.267574 404.958306 805.870119 376.433982 811.472665 467.864072 811.472665 499.316956"/></g><g class="series serie-2 color-2"><path class="line reactive" d="M15.911229 499.316956 L15.911229 404.958306 21.513774 382.927457 27.116319 333.075817 32.718865 394.832739 38.321410 436.411189 43.923955 335.464627 49.526501 399.613675 55.129046 418.012432 60.731591 379.564666 66.334137 399.613675 71.936682 399.613675 77.539227 379.564666 83.141773 449.465315 88.744318 499.316956 94.346863 399.613675 99.949409 404.958306 105.551954 335.464627 111.154499 411.017549 116.757045 436.411189 122.359590 333.075817 127.962135 426.285622 133.564681 394.832739 139.167226 348.111782 144.769771 436.411189 150.372316 426.285622 155.974862 373.505423 161.577407 449.465315 167.179952 399.613675 172.782498 365.707387 178.385043 426.285622 183.987588 436.411189 189.590134 370.754464 195.192679 426.285622 200.795224 449.465315 206.397770 394.832739 212.000315 436.411189 217.602860 363.379856 223.205406 411.017549 228.807951 426.285622 234.410496 330.806498 240.013042 394.832739 245.615587 418.012432 251.218132 361.165909 256.820678 390.507857 262.423223 418.012432 268.025768 357.037890 273.628314 382.927457 279.230859 342.052539 284.833404 363.379856 290.435950 436.411189 296.038495 411.017549 301.641040 382.927457 307.243586 418.012432 312.846131 436.411189 318.448676 386.559549 324.051222 370.754464 329.653767 368.160791 335.256312 361.165909 340.858857 330.806498 346.461403 348.111782 352.063948 361.165909 357.666493 361.165909 363.269039 426.285622 368.871584 343.493197 374.474129 322.718142 380.076675 326.582341 385.679220 322.718142 391.281765 339.301581 396.884311 325.585006 402.486856 353.254289 408.089401 361.165909 413.691947 331.926973 419.294492 340.656216 424.897037 394.832739 430.499583 373.505423 436.102128 337.986216 441.704673 363.379856 447.307219 399.613675 452.909764 411.017549 458.512309 404.958306 464.114855 376.433982 469.717400 386.559549 475.319945 379.564666 480.922491 351.474574 486.525036 318.309151 492.127581 382.927457 497.730127 382.927457 503.332672 379.564666 508.935217 390.507857 514.537763 353.254289 520.140308 370.754464 525.742853 361.165909 531.345398 333.075817 536.947944 346.519447 542.550489 365.707387 548.153034 382.927457 553.755580 390.507857 559.358125 337.986216 564.960670 348.111782 570.563216 361.165909 576.165761 328.645284 581.768306 315.855747 587.370852 312.778166 592.973397 280.222956 598.575942 331.926973 604.178488 394.832739 609.781033 394.832739 615.383578 386.559549 620.986124 376.433982 626.588669 310.599656 632.191214 328.645284 637.793760 386.559549 643.396305 426.285622 648.998850 411.017549 654.601396 386.559549 660.203941 411.017549 665.806486 311.314268 671.409032 382.927457 677.011577 394.832739 682.614122 399.613675 688.216668 436.411189 693.819213 499.316956 699.421758 363.379856 705.024304 404.958306 710.626849 418.012432 716.229394 449.465315 721.831939 404.958306 727.434485 418.012432 733.037030 436.411189 738.639575 426.285622 744.242121 418.012432 749.844666 379.564666 755.447211 449.465315 761.049757 449.465315 766.652302 449.465315 772.254847 418.012432 777.857393 426.285622 783.459938 436.411189 789.062483 418.012432 794.665029 361.165909 800.267574 394.832739 805.870119 411.017549 811.472665 436.411189 811.472665 499.316956"/></g></g><g class="titles"><text class="title" x="500.0" y="26">Load</text></g><g class="plot overlay" transform="translate(143, 46)"><g class="series serie-0 color-0"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="15.9112287189" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">15.9112287189</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.5137740425" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">21.5137740425</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.1163193661" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">27.1163193661</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="32.7188646896" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">32.7188646896</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.3214100132" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">38.3214100132</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="43.9239553368" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">43.9239553368</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="49.5265006603" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">49.5265006603</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="55.1290459839" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">55.1290459839</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="60.7315913075" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">60.7315913075</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="66.334136631" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">66.334136631</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="71.9366819546" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">71.9366819546</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="77.5392272782" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">77.5392272782</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="83.1417726017" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">83.1417726017</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="88.7443179253" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">88.7443179253</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="94.3468632489" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">94.3468632489</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="99.9494085724" cy="8.25809250839" r="1.5"/><desc class="value">501.0</desc><desc class="x ">99.9494085724</desc><desc class="y ">8.25809250839</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="105.551953896" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">105.551953896</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="111.15449922" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">111.15449922</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="116.757044543" cy="8.53062737416" r="1.5"/><desc class="value">498.0</desc><desc class="x ">116.757044543</desc><desc class="y ">8.53062737416</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="122.359589867" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">122.359589867</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="127.96213519" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">127.96213519</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="133.564680514" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">133.564680514</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="139.167225837" cy="8.80480898627" r="1.5"/><desc class="value">495.0</desc><desc class="x ">139.167225837</desc><desc class="y ">8.80480898627</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="144.769771161" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">144.769771161</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="150.372316485" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">150.372316485</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="155.974861808" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">155.974861808</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="161.577407132" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x ">161.577407132</desc><desc class="y ">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="167.179952455" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">167.179952455</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="172.782497779" cy="8.34875571287" r="1.5"/><desc class="value">500.0</desc><desc class="x ">172.782497779</desc><desc class="y ">8.34875571287</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="178.385043102" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">178.385043102</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="183.987588426" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">183.987588426</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="189.590133749" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">189.590133749</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="195.192679073" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">195.192679073</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="200.795224397" cy="8.71323091534" r="1.5"/><desc class="value">496.0</desc><desc class="x ">200.795224397</desc><desc class="y ">8.71323091534</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="206.39776972" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">206.39776972</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="212.000315044" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">212.000315044</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="217.602860367" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">217.602860367</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="223.205405691" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">223.205405691</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="228.807951014" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">228.807951014</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="234.410496338" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">234.410496338</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="240.013041662" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">240.013041662</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="245.615586985" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">245.615586985</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="251.218132309" cy="8.98852145886" r="1.5"/><desc class="value">493.0</desc><desc class="x ">251.218132309</desc><desc class="y ">8.98852145886</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="256.820677632" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">256.820677632</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="262.423222956" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">262.423222956</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="268.025768279" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">268.025768279</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="273.628313603" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">273.628313603</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="279.230858927" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">279.230858927</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="284.83340425" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">284.83340425</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="290.435949574" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">290.435949574</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="296.038494897" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">296.038494897</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="301.641040221" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">301.641040221</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="307.243585544" cy="10.3905975305" r="1.5"/><desc class="value">478.0</desc><desc class="x ">307.243585544</desc><desc class="y ">10.3905975305</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.846130868" cy="10.4856277616" r="1.5"/><desc class="value">477.0</desc><desc class="x ">312.846130868</desc><desc class="y ">10.4856277616</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="318.448676191" cy="10.1066951316" r="1.5"/><desc class="value">481.0</desc><desc class="x ">318.448676191</desc><desc class="y ">10.1066951316</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="324.051221515" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">324.051221515</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.653766839" cy="9.73090069601" r="1.5"/><desc class="value">485.0</desc><desc class="x ">329.653766839</desc><desc class="y ">9.73090069601</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="335.256312162" cy="10.6762873646" r="1.5"/><desc class="value">475.0</desc><desc class="x ">335.256312162</desc><desc class="y ">10.6762873646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.858857486" cy="8.8965722506" r="1.5"/><desc class="value">494.0</desc><desc class="x ">340.858857486</desc><desc class="y ">8.8965722506</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.461402809" cy="9.08065736614" r="1.5"/><desc class="value">492.0</desc><desc class="x ">346.461402809</desc><desc class="y ">9.08065736614</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="352.063948133" cy="9.73090069601" r="1.5"/><desc class="value">485.0</desc><desc class="x ">352.063948133</desc><desc class="y ">9.73090069601</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.666493456" cy="8.6218372918" r="1.5"/><desc class="value">497.0</desc><desc class="x ">357.666493456</desc><desc class="y ">8.6218372918</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.26903878" cy="9.6374363607" r="1.5"/><desc class="value">486.0</desc><desc class="x ">363.26903878</desc><desc class="y ">9.6374363607</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="368.871584104" cy="9.82455794029" r="1.5"/><desc class="value">484.0</desc><desc class="x ">368.871584104</desc><desc class="y ">9.82455794029</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.474129427" cy="8.25809250839" r="1.5"/><desc class="value">501.0</desc><desc class="x ">374.474129427</desc><desc class="y ">8.25809250839</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.076674751" cy="8.25809250839" r="1.5"/><desc class="value">501.0</desc><desc class="x ">380.076674751</desc><desc class="y ">8.25809250839</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="385.679220074" cy="7.71788471466" r="1.5"/><desc class="value">507.0</desc><desc class="x ">385.679220074</desc><desc class="y ">7.71788471466</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.281765398" cy="7.18403245423" r="1.5"/><desc class="value">513.0</desc><desc class="x ">391.281765398</desc><desc class="y ">7.18403245423</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="396.884310721" cy="6.65638791894" r="1.5"/><desc class="value">519.0</desc><desc class="x ">396.884310721</desc><desc class="y ">6.65638791894</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="402.486856045" cy="4.35509328372" r="1.5"/><desc class="value">546.0</desc><desc class="x ">402.486856045</desc><desc class="y ">4.35509328372</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.089401369" cy="5.10929766107" r="1.5"/><desc class="value">537.0</desc><desc class="x ">408.089401369</desc><desc class="y ">5.10929766107</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="413.691946692" cy="5.96227237709" r="1.5"/><desc class="value">527.0</desc><desc class="x ">413.691946692</desc><desc class="y ">5.96227237709</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="419.294492016" cy="5.70469220343" r="1.5"/><desc class="value">530.0</desc><desc class="x left">419.294492016</desc><desc class="y left">5.70469220343</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="424.897037339" cy="6.13480839769" r="1.5"/><desc class="value">525.0</desc><desc class="x left">424.897037339</desc><desc class="y left">6.13480839769</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="430.499582663" cy="6.48186109322" r="1.5"/><desc class="value">521.0</desc><desc class="x left">430.499582663</desc><desc class="y left">6.48186109322</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="436.102127986" cy="3.69505373744" r="1.5"/><desc class="value">554.0</desc><desc class="x left">436.102127986</desc><desc class="y left">3.69505373744</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="441.70467331" cy="4.60510448778" r="1.5"/><desc class="value">543.0</desc><desc class="x left">441.70467331</desc><desc class="y left">4.60510448778</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="447.307218634" cy="6.5690405989" r="1.5"/><desc class="value">520.0</desc><desc class="x left">447.307218634</desc><desc class="y left">6.5690405989</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="452.909763957" cy="7.00746834529" r="1.5"/><desc class="value">515.0</desc><desc class="x left">452.909763957</desc><desc class="y left">7.00746834529</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="458.512309281" cy="6.91944325707" r="1.5"/><desc class="value">516.0</desc><desc class="x left">458.512309281</desc><desc class="y left">6.91944325707</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="464.114854604" cy="6.91944325707" r="1.5"/><desc class="value">516.0</desc><desc class="x left">464.114854604</desc><desc class="y left">6.91944325707</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="469.717399928" cy="6.74390370064" r="1.5"/><desc class="value">518.0</desc><desc class="x left">469.717399928</desc><desc class="y left">6.74390370064</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="475.319945251" cy="6.74390370064" r="1.5"/><desc class="value">518.0</desc><desc class="x left">475.319945251</desc><desc class="y left">6.74390370064</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="480.922490575" cy="5.44856592373" r="1.5"/><desc class="value">533.0</desc><desc class="x left">480.922490575</desc><desc class="y left">5.44856592373</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="486.525035898" cy="4.94061003652" r="1.5"/><desc class="value">539.0</desc><desc class="x left">486.525035898</desc><desc class="y left">4.94061003652</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="492.127581222" cy="4.94061003652" r="1.5"/><desc class="value">539.0</desc><desc class="x left">492.127581222</desc><desc class="y left">4.94061003652</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="497.730126546" cy="4.43827739271" r="1.5"/><desc class="value">545.0</desc><desc class="x left">497.730126546</desc><desc class="y left">4.43827739271</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="503.332671869" cy="5.27861471852" r="1.5"/><desc class="value">535.0</desc><desc class="x left">503.332671869</desc><desc class="y left">5.27861471852</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="508.935217193" cy="5.19387721724" r="1.5"/><desc class="value">536.0</desc><desc class="x left">508.935217193</desc><desc class="y left">5.19387721724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="514.537762516" cy="4.60510448778" r="1.5"/><desc class="value">543.0</desc><desc class="x left">514.537762516</desc><desc class="y left">4.60510448778</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="520.14030784" cy="2.32355801724" r="1.5"/><desc class="value">571.0</desc><desc class="x left">520.14030784</desc><desc class="y left">2.32355801724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="525.742853163" cy="2.80289612326" r="1.5"/><desc class="value">565.0</desc><desc class="x left">525.742853163</desc><desc class="y left">2.80289612326</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="531.345398487" cy="2.88328041132" r="1.5"/><desc class="value">564.0</desc><desc class="x left">531.345398487</desc><desc class="y left">2.88328041132</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="536.947943811" cy="0.0" r="1.5"/><desc class="value">601.0</desc><desc class="x left">536.947943811</desc><desc class="y left">0.0</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="542.550489134" cy="1.06953917993" r="1.5"/><desc class="value">587.0</desc><desc class="x left">542.550489134</desc><desc class="y left">1.06953917993</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="548.153034458" cy="2.96380735126" r="1.5"/><desc class="value">563.0</desc><desc class="x left">548.153034458</desc><desc class="y left">2.96380735126</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="553.755579781" cy="3.28735181465" r="1.5"/><desc class="value">559.0</desc><desc class="x left">553.755579781</desc><desc class="y left">3.28735181465</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="559.358125105" cy="3.53153313257" r="1.5"/><desc class="value">556.0</desc><desc class="x left">559.358125105</desc><desc class="y left">3.53153313257</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="564.960670428" cy="2.32355801724" r="1.5"/><desc class="value">571.0</desc><desc class="x left">564.960670428</desc><desc class="y left">2.32355801724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="570.563215752" cy="4.27206138727" r="1.5"/><desc class="value">547.0</desc><desc class="x left">570.563215752</desc><desc class="y left">4.27206138727</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="576.165761076" cy="5.53378081966" r="1.5"/><desc class="value">532.0</desc><desc class="x left">576.165761076</desc><desc class="y left">5.53378081966</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="581.768306399" cy="4.52161427334" r="1.5"/><desc class="value">544.0</desc><desc class="x left">581.768306399</desc><desc class="y left">4.52161427334</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="587.370851723" cy="4.68874860132" r="1.5"/><desc class="value">542.0</desc><desc class="x left">587.370851723</desc><desc class="y left">4.68874860132</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="592.973397046" cy="4.77254718238" r="1.5"/><desc class="value">541.0</desc><desc class="x left">592.973397046</desc><desc class="y left">4.77254718238</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="598.57594237" cy="5.02487546231" r="1.5"/><desc class="value">538.0</desc><desc class="x left">598.57594237</desc><desc class="y left">5.02487546231</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="604.178487693" cy="4.94061003652" r="1.5"/><desc class="value">539.0</desc><desc class="x left">604.178487693</desc><desc class="y left">4.94061003652</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="609.781033017" cy="5.19387721724" r="1.5"/><desc class="value">536.0</desc><desc class="x left">609.781033017</desc><desc class="y left">5.19387721724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="615.38357834" cy="5.19387721724" r="1.5"/><desc class="value">536.0</desc><desc class="x left">615.38357834</desc><desc class="y left">5.19387721724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="620.986123664" cy="5.19387721724" r="1.5"/><desc class="value">536.0</desc><desc class="x left">620.986123664</desc><desc class="y left">5.19387721724</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="626.588668988" cy="3.53153313257" r="1.5"/><desc class="value">556.0</desc><desc class="x left">626.588668988</desc><desc class="y left">3.53153313257</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="632.191214311" cy="5.96227237709" r="1.5"/><desc class="value">527.0</desc><desc class="x left">632.191214311</desc><desc class="y left">5.96227237709</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="637.793759635" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">637.793759635</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="643.396304958" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">643.396304958</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="648.998850282" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">648.998850282</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="654.601395605" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">654.601395605</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="660.203940929" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">660.203940929</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.806486253" cy="8.07730773242" r="1.5"/><desc class="value">503.0</desc><desc class="x left">665.806486253</desc><desc class="y left">8.07730773242</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="671.409031576" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">671.409031576</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="677.0115769" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">677.0115769</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.614122223" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">682.614122223</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="688.216667547" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">688.216667547</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="693.81921287" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">693.81921287</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.421758194" cy="8.07730773242" r="1.5"/><desc class="value">503.0</desc><desc class="x left">699.421758194</desc><desc class="y left">8.07730773242</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="705.024303518" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">705.024303518</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.626848841" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">710.626848841</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="716.229394165" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">716.229394165</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.831939488" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">721.831939488</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.434484812" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">727.434484812</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.037030135" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">733.037030135</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="738.639575459" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">738.639575459</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.242120783" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">744.242120783</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="749.844666106" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">749.844666106</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="755.44721143" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">755.44721143</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="761.049756753" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">761.049756753</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="766.652302077" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">766.652302077</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="772.2548474" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">772.2548474</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="777.857392724" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">777.857392724</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="783.459938047" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">783.459938047</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="789.062483371" cy="7.62847203796" r="1.5"/><desc class="value">508.0</desc><desc class="x left">789.062483371</desc><desc class="y left">7.62847203796</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="794.665028695" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">794.665028695</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="800.267574018" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">800.267574018</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="805.870119342" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">805.870119342</desc><desc class="y left">8.16761008808</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="811.472664665" cy="8.16761008808" r="1.5"/><desc class="value">502.0</desc><desc class="x left">811.472664665</desc><desc class="y left">8.16761008808</desc></g></g><g class="series serie-1 color-1"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="15.9112287189" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">15.9112287189</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.5137740425" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">21.5137740425</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.1163193661" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x top">27.1163193661</desc><desc class="y top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="32.7188646896" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">32.7188646896</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.3214100132" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">38.3214100132</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="43.9239553368" cy="307.186248962" r="1.5"/><desc class="value">0.69</desc><desc class="x top">43.9239553368</desc><desc class="y top">307.186248962</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="49.5265006603" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">49.5265006603</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="55.1290459839" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">55.1290459839</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="60.7315913075" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">60.7315913075</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="66.334136631" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x top">66.334136631</desc><desc class="y top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="71.9366819546" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">71.9366819546</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="77.5392272782" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">77.5392272782</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="83.1417726017" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">83.1417726017</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="94.3468632489" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">94.3468632489</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="99.9494085724" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x top">99.9494085724</desc><desc class="y top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="105.551953896" cy="285.206015896" r="1.5"/><desc class="value">1.12</desc><desc class="x top">105.551953896</desc><desc class="y top">285.206015896</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="122.359589867" cy="319.157342224" r="1.5"/><desc class="value">0.53</desc><desc class="x top">122.359589867</desc><desc class="y top">319.157342224</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="133.564680514" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">133.564680514</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="139.167225837" cy="368.160791381" r="1.5"/><desc class="value">0.18</desc><desc class="x top">139.167225837</desc><desc class="y top">368.160791381</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="150.372316485" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">150.372316485</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="155.974861808" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">155.974861808</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="167.179952455" cy="353.254289025" r="1.5"/><desc class="value">0.25</desc><desc class="x top">167.179952455</desc><desc class="y top">353.254289025</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="172.782497779" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">172.782497779</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="178.385043102" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">178.385043102</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="183.987588426" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">183.987588426</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="195.192679073" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">195.192679073</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="217.602860367" cy="305.889677362" r="1.5"/><desc class="value">0.71</desc><desc class="x top">217.602860367</desc><desc class="y top">305.889677362</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="228.807951014" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">228.807951014</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="234.410496338" cy="331.92697253" r="1.5"/><desc class="value">0.4</desc><desc class="x top">234.410496338</desc><desc class="y top">331.92697253</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="240.013041662" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">240.013041662</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="245.615586985" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">245.615586985</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="251.218132309" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">251.218132309</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="256.820677632" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">256.820677632</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="262.423222956" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">262.423222956</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="268.025768279" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">268.025768279</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="273.628313603" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">273.628313603</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="279.230858927" cy="353.254289025" r="1.5"/><desc class="value">0.25</desc><desc class="x top">279.230858927</desc><desc class="y top">353.254289025</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="290.435949574" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">290.435949574</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="296.038494897" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">296.038494897</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="307.243585544" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">307.243585544</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.846130868" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">312.846130868</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="318.448676191" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x top">318.448676191</desc><desc class="y top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="324.051221515" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x top">324.051221515</desc><desc class="y top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.653766839" cy="357.037889519" r="1.5"/><desc class="value">0.23</desc><desc class="x top">329.653766839</desc><desc class="y top">357.037889519</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="335.256312162" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x top">335.256312162</desc><desc class="y top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.858857486" cy="320.902823523" r="1.5"/><desc class="value">0.51</desc><desc class="x top">340.858857486</desc><desc class="y top">320.902823523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.461402809" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">346.461402809</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="352.063948133" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x top">352.063948133</desc><desc class="y top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.666493456" cy="365.707387474" r="1.5"/><desc class="value">0.19</desc><desc class="x top">357.666493456</desc><desc class="y top">365.707387474</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.26903878" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">363.26903878</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="368.871584104" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x top">368.871584104</desc><desc class="y top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.474129427" cy="307.848697497" r="1.5"/><desc class="value">0.68</desc><desc class="x top">374.474129427</desc><desc class="y top">307.848697497</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.076674751" cy="339.301580789" r="1.5"/><desc class="value">0.34</desc><desc class="x top">380.076674751</desc><desc class="y top">339.301580789</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="385.679220074" cy="306.533332391" r="1.5"/><desc class="value">0.7</desc><desc class="x top">385.679220074</desc><desc class="y top">306.533332391</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.281765398" cy="344.981098557" r="1.5"/><desc class="value">0.3</desc><desc class="x top">391.281765398</desc><desc class="y top">344.981098557</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="396.884310721" cy="351.474573911" r="1.5"/><desc class="value">0.26</desc><desc class="x top">396.884310721</desc><desc class="y top">351.474573911</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="402.486856045" cy="330.806497855" r="1.5"/><desc class="value">0.41</desc><desc class="x top">402.486856045</desc><desc class="y top">330.806497855</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.089401369" cy="342.052539327" r="1.5"/><desc class="value">0.32</desc><desc class="x top">408.089401369</desc><desc class="y top">342.052539327</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="413.691946692" cy="324.6091209" r="1.5"/><desc class="value">0.47</desc><desc class="x top">413.691946692</desc><desc class="y top">324.6091209</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="419.294492016" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">419.294492016</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="424.897037339" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">424.897037339</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="430.499582663" cy="355.106665354" r="1.5"/><desc class="value">0.24</desc><desc class="x left top">430.499582663</desc><desc class="y left top">355.106665354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="436.102127986" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x left top">436.102127986</desc><desc class="y left top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="441.70467331" cy="346.519446513" r="1.5"/><desc class="value">0.29</desc><desc class="x left top">441.70467331</desc><desc class="y left top">346.519446513</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="447.307218634" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">447.307218634</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="452.909763957" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">452.909763957</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="458.512309281" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">458.512309281</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="464.114854604" cy="342.052539327" r="1.5"/><desc class="value">0.32</desc><desc class="x left top">464.114854604</desc><desc class="y left top">342.052539327</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="469.717399928" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">469.717399928</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="475.319945251" cy="365.707387474" r="1.5"/><desc class="value">0.19</desc><desc class="x left top">475.319945251</desc><desc class="y left top">365.707387474</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="480.922490575" cy="344.981098557" r="1.5"/><desc class="value">0.3</desc><desc class="x left top">480.922490575</desc><desc class="y left top">344.981098557</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="486.525035898" cy="318.309150823" r="1.5"/><desc class="value">0.54</desc><desc class="x left top">486.525035898</desc><desc class="y left top">318.309150823</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="492.127581222" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">492.127581222</desc><desc class="y left top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="497.730126546" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">497.730126546</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="503.332671869" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">503.332671869</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="508.935217193" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">508.935217193</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="514.537762516" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x left top">514.537762516</desc><desc class="y left top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="520.14030784" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">520.14030784</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="525.742853163" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x left top">525.742853163</desc><desc class="y left top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="531.345398487" cy="355.106665354" r="1.5"/><desc class="value">0.24</desc><desc class="x left top">531.345398487</desc><desc class="y left top">355.106665354</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="536.947943811" cy="340.656216272" r="1.5"/><desc class="value">0.33</desc><desc class="x left top">536.947943811</desc><desc class="y left top">340.656216272</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="542.550489134" cy="373.505422619" r="1.5"/><desc class="value">0.16</desc><desc class="x left top">542.550489134</desc><desc class="y left top">373.505422619</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="548.153034458" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x left top">548.153034458</desc><desc class="y left top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="553.755579781" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x left top">553.755579781</desc><desc class="y left top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="559.358125105" cy="330.806497855" r="1.5"/><desc class="value">0.41</desc><desc class="x left top">559.358125105</desc><desc class="y left top">330.806497855</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="564.960670428" cy="333.075816646" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">564.960670428</desc><desc class="y left top">333.075816646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="570.563215752" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x left top">570.563215752</desc><desc class="y left top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="576.165761076" cy="299.910393557" r="1.5"/><desc class="value">0.81</desc><desc class="x left top">576.165761076</desc><desc class="y left top">299.910393557</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="581.768306399" cy="322.718142342" r="1.5"/><desc class="value">0.49</desc><desc class="x left top">581.768306399</desc><desc class="y left top">322.718142342</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="587.370851723" cy="292.20089877" r="1.5"/><desc class="value">0.96</desc><desc class="x left top">587.370851723</desc><desc class="y left top">292.20089877</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="592.973397046" cy="292.20089877" r="1.5"/><desc class="value">0.96</desc><desc class="x left top">592.973397046</desc><desc class="y left top">292.20089877</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="598.57594237" cy="349.762034115" r="1.5"/><desc class="value">0.27</desc><desc class="x left top">598.57594237</desc><desc class="y left top">349.762034115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="604.178487693" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">604.178487693</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="609.781033017" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">609.781033017</desc><desc class="y left top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="615.38357834" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x left top">615.38357834</desc><desc class="y left top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="620.986123664" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x left top">620.986123664</desc><desc class="y left top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="626.588668988" cy="300.474089238" r="1.5"/><desc class="value">0.8</desc><desc class="x left top">626.588668988</desc><desc class="y left top">300.474089238</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="632.191214311" cy="376.433981849" r="1.5"/><desc class="value">0.15</desc><desc class="x left top">632.191214311</desc><desc class="y left top">376.433981849</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="637.793759635" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">637.793759635</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="643.396304958" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">643.396304958</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="648.998850282" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">648.998850282</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="654.601395605" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">654.601395605</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="660.203940929" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">660.203940929</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.806486253" cy="286.856267531" r="1.5"/><desc class="value">1.08</desc><desc class="x left top">665.806486253</desc><desc class="y left top">286.856267531</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="671.409031576" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x left top">671.409031576</desc><desc class="y left top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="677.0115769" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">677.0115769</desc><desc class="y left top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.614122223" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">682.614122223</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="688.216667547" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">688.216667547</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.421758194" cy="357.037889519" r="1.5"/><desc class="value">0.23</desc><desc class="x left top">699.421758194</desc><desc class="y left top">357.037889519</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="705.024303518" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x left top">705.024303518</desc><desc class="y left top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.626848841" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">710.626848841</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.831939488" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">721.831939488</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.434484812" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x left top">727.434484812</desc><desc class="y left top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.037030135" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">733.037030135</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="738.639575459" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">738.639575459</desc><desc class="y left top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.242120783" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">744.242120783</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="749.844666106" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">749.844666106</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="755.44721143" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">755.44721143</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="766.652302077" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">766.652302077</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="772.2548474" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x left top">772.2548474</desc><desc class="y left top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="777.857392724" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">777.857392724</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="783.459938047" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">783.459938047</desc><desc class="y left top">467.864072496</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="789.062483371" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x left top">789.062483371</desc><desc class="y left top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="794.665028695" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">794.665028695</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="800.267574018" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">800.267574018</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="805.870119342" cy="376.433981849" r="1.5"/><desc class="value">0.15</desc><desc class="x left top">805.870119342</desc><desc class="y left top">376.433981849</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="811.472664665" cy="467.864072496" r="1.5"/><desc class="value">0.02</desc><desc class="x left top">811.472664665</desc><desc class="y left top">467.864072496</desc></g></g><g class="series serie-2 color-2"><g class="dots"><circle class="dot reactive tooltip-trigger" cx="15.9112287189" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x top">15.9112287189</desc><desc class="y top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="21.5137740425" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x top">21.5137740425</desc><desc class="y top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="27.1163193661" cy="333.075816646" r="1.5"/><desc class="value">0.39</desc><desc class="x top">27.1163193661</desc><desc class="y top">333.075816646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="32.7188646896" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">32.7188646896</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="38.3214100132" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">38.3214100132</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="43.9239553368" cy="335.464627063" r="1.5"/><desc class="value">0.37</desc><desc class="x top">43.9239553368</desc><desc class="y top">335.464627063</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="49.5265006603" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">49.5265006603</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="55.1290459839" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">55.1290459839</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="60.7315913075" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x top">60.7315913075</desc><desc class="y top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="66.334136631" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">66.334136631</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="71.9366819546" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">71.9366819546</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="77.5392272782" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x top">77.5392272782</desc><desc class="y top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="83.1417726017" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">83.1417726017</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="88.7443179253" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x top">88.7443179253</desc><desc class="y top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="94.3468632489" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">94.3468632489</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="99.9494085724" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x top">99.9494085724</desc><desc class="y top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="105.551953896" cy="335.464627063" r="1.5"/><desc class="value">0.37</desc><desc class="x top">105.551953896</desc><desc class="y top">335.464627063</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="111.15449922" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">111.15449922</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="116.757044543" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">116.757044543</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="122.359589867" cy="333.075816646" r="1.5"/><desc class="value">0.39</desc><desc class="x top">122.359589867</desc><desc class="y top">333.075816646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="127.96213519" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">127.96213519</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="133.564680514" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">133.564680514</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="139.167225837" cy="348.111782481" r="1.5"/><desc class="value">0.28</desc><desc class="x top">139.167225837</desc><desc class="y top">348.111782481</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="144.769771161" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">144.769771161</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="150.372316485" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">150.372316485</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="155.974861808" cy="373.505422619" r="1.5"/><desc class="value">0.16</desc><desc class="x top">155.974861808</desc><desc class="y top">373.505422619</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="161.577407132" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">161.577407132</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="167.179952455" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x top">167.179952455</desc><desc class="y top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="172.782497779" cy="365.707387474" r="1.5"/><desc class="value">0.19</desc><desc class="x top">172.782497779</desc><desc class="y top">365.707387474</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="178.385043102" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">178.385043102</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="183.987588426" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">183.987588426</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="189.590133749" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x top">189.590133749</desc><desc class="y top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="195.192679073" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">195.192679073</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="200.795224397" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x top">200.795224397</desc><desc class="y top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="206.39776972" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">206.39776972</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="212.000315044" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">212.000315044</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="217.602860367" cy="363.379855822" r="1.5"/><desc class="value">0.2</desc><desc class="x top">217.602860367</desc><desc class="y top">363.379855822</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="223.205405691" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">223.205405691</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="228.807951014" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">228.807951014</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="234.410496338" cy="330.806497855" r="1.5"/><desc class="value">0.41</desc><desc class="x top">234.410496338</desc><desc class="y top">330.806497855</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="240.013041662" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x top">240.013041662</desc><desc class="y top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="245.615586985" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">245.615586985</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="251.218132309" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">251.218132309</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="256.820677632" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x top">256.820677632</desc><desc class="y top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="262.423222956" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">262.423222956</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="268.025768279" cy="357.037889519" r="1.5"/><desc class="value">0.23</desc><desc class="x top">268.025768279</desc><desc class="y top">357.037889519</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="273.628313603" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x top">273.628313603</desc><desc class="y top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="279.230858927" cy="342.052539327" r="1.5"/><desc class="value">0.32</desc><desc class="x top">279.230858927</desc><desc class="y top">342.052539327</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="284.83340425" cy="363.379855822" r="1.5"/><desc class="value">0.2</desc><desc class="x top">284.83340425</desc><desc class="y top">363.379855822</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="290.435949574" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">290.435949574</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="296.038494897" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x top">296.038494897</desc><desc class="y top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="301.641040221" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x top">301.641040221</desc><desc class="y top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="307.243585544" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x top">307.243585544</desc><desc class="y top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="312.846130868" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x top">312.846130868</desc><desc class="y top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="318.448676191" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x top">318.448676191</desc><desc class="y top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="324.051221515" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x top">324.051221515</desc><desc class="y top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="329.653766839" cy="368.160791381" r="1.5"/><desc class="value">0.18</desc><desc class="x top">329.653766839</desc><desc class="y top">368.160791381</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="335.256312162" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">335.256312162</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="340.858857486" cy="330.806497855" r="1.5"/><desc class="value">0.41</desc><desc class="x top">340.858857486</desc><desc class="y top">330.806497855</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="346.461402809" cy="348.111782481" r="1.5"/><desc class="value">0.28</desc><desc class="x top">346.461402809</desc><desc class="y top">348.111782481</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="352.063948133" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">352.063948133</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="357.666493456" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">357.666493456</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="363.26903878" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x top">363.26903878</desc><desc class="y top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="368.871584104" cy="343.493197431" r="1.5"/><desc class="value">0.31</desc><desc class="x top">368.871584104</desc><desc class="y top">343.493197431</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="374.474129427" cy="322.718142342" r="1.5"/><desc class="value">0.49</desc><desc class="x top">374.474129427</desc><desc class="y top">322.718142342</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="380.076674751" cy="326.582341291" r="1.5"/><desc class="value">0.45</desc><desc class="x top">380.076674751</desc><desc class="y top">326.582341291</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="385.679220074" cy="322.718142342" r="1.5"/><desc class="value">0.49</desc><desc class="x top">385.679220074</desc><desc class="y top">322.718142342</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="391.281765398" cy="339.301580789" r="1.5"/><desc class="value">0.34</desc><desc class="x top">391.281765398</desc><desc class="y top">339.301580789</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="396.884310721" cy="325.585006227" r="1.5"/><desc class="value">0.46</desc><desc class="x top">396.884310721</desc><desc class="y top">325.585006227</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="402.486856045" cy="353.254289025" r="1.5"/><desc class="value">0.25</desc><desc class="x top">402.486856045</desc><desc class="y top">353.254289025</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="408.089401369" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x top">408.089401369</desc><desc class="y top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="413.691946692" cy="331.92697253" r="1.5"/><desc class="value">0.4</desc><desc class="x top">413.691946692</desc><desc class="y top">331.92697253</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="419.294492016" cy="340.656216272" r="1.5"/><desc class="value">0.33</desc><desc class="x left top">419.294492016</desc><desc class="y left top">340.656216272</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="424.897037339" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">424.897037339</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="430.499582663" cy="373.505422619" r="1.5"/><desc class="value">0.16</desc><desc class="x left top">430.499582663</desc><desc class="y left top">373.505422619</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="436.102127986" cy="337.986215684" r="1.5"/><desc class="value">0.35</desc><desc class="x left top">436.102127986</desc><desc class="y left top">337.986215684</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="441.70467331" cy="363.379855822" r="1.5"/><desc class="value">0.2</desc><desc class="x left top">441.70467331</desc><desc class="y left top">363.379855822</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="447.307218634" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x left top">447.307218634</desc><desc class="y left top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="452.909763957" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">452.909763957</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="458.512309281" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">458.512309281</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="464.114854604" cy="376.433981849" r="1.5"/><desc class="value">0.15</desc><desc class="x left top">464.114854604</desc><desc class="y left top">376.433981849</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="469.717399928" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">469.717399928</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="475.319945251" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x left top">475.319945251</desc><desc class="y left top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="480.922490575" cy="351.474573911" r="1.5"/><desc class="value">0.26</desc><desc class="x left top">480.922490575</desc><desc class="y left top">351.474573911</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="486.525035898" cy="318.309150823" r="1.5"/><desc class="value">0.54</desc><desc class="x left top">486.525035898</desc><desc class="y left top">318.309150823</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="492.127581222" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x left top">492.127581222</desc><desc class="y left top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="497.730126546" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x left top">497.730126546</desc><desc class="y left top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="503.332671869" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x left top">503.332671869</desc><desc class="y left top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="508.935217193" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x left top">508.935217193</desc><desc class="y left top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="514.537762516" cy="353.254289025" r="1.5"/><desc class="value">0.25</desc><desc class="x left top">514.537762516</desc><desc class="y left top">353.254289025</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="520.14030784" cy="370.754464081" r="1.5"/><desc class="value">0.17</desc><desc class="x left top">520.14030784</desc><desc class="y left top">370.754464081</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="525.742853163" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x left top">525.742853163</desc><desc class="y left top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="531.345398487" cy="333.075816646" r="1.5"/><desc class="value">0.39</desc><desc class="x left top">531.345398487</desc><desc class="y left top">333.075816646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="536.947943811" cy="346.519446513" r="1.5"/><desc class="value">0.29</desc><desc class="x left top">536.947943811</desc><desc class="y left top">346.519446513</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="542.550489134" cy="365.707387474" r="1.5"/><desc class="value">0.19</desc><desc class="x left top">542.550489134</desc><desc class="y left top">365.707387474</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="548.153034458" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x left top">548.153034458</desc><desc class="y left top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="553.755579781" cy="390.50785683" r="1.5"/><desc class="value">0.11</desc><desc class="x left top">553.755579781</desc><desc class="y left top">390.50785683</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="559.358125105" cy="337.986215684" r="1.5"/><desc class="value">0.35</desc><desc class="x left top">559.358125105</desc><desc class="y left top">337.986215684</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="564.960670428" cy="348.111782481" r="1.5"/><desc class="value">0.28</desc><desc class="x left top">564.960670428</desc><desc class="y left top">348.111782481</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="570.563215752" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x left top">570.563215752</desc><desc class="y left top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="576.165761076" cy="328.645283746" r="1.5"/><desc class="value">0.43</desc><desc class="x left top">576.165761076</desc><desc class="y left top">328.645283746</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="581.768306399" cy="315.855746916" r="1.5"/><desc class="value">0.57</desc><desc class="x left top">581.768306399</desc><desc class="y left top">315.855746916</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="587.370851723" cy="312.778166473" r="1.5"/><desc class="value">0.61</desc><desc class="x left top">587.370851723</desc><desc class="y left top">312.778166473</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="592.973397046" cy="280.222955644" r="1.5"/><desc class="value">1.25</desc><desc class="x left top">592.973397046</desc><desc class="y left top">280.222955644</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="598.57594237" cy="331.92697253" r="1.5"/><desc class="value">0.4</desc><desc class="x left top">598.57594237</desc><desc class="y left top">331.92697253</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="604.178487693" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">604.178487693</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="609.781033017" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">609.781033017</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="615.38357834" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">615.38357834</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="620.986123664" cy="376.433981849" r="1.5"/><desc class="value">0.15</desc><desc class="x left top">620.986123664</desc><desc class="y left top">376.433981849</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="626.588668988" cy="310.599656035" r="1.5"/><desc class="value">0.64</desc><desc class="x left top">626.588668988</desc><desc class="y left top">310.599656035</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="632.191214311" cy="328.645283746" r="1.5"/><desc class="value">0.43</desc><desc class="x left top">632.191214311</desc><desc class="y left top">328.645283746</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="637.793759635" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">637.793759635</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="643.396304958" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x left top">643.396304958</desc><desc class="y left top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="648.998850282" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">648.998850282</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="654.601395605" cy="386.559548646" r="1.5"/><desc class="value">0.12</desc><desc class="x left top">654.601395605</desc><desc class="y left top">386.559548646</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="660.203940929" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">660.203940929</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="665.806486253" cy="311.31426795" r="1.5"/><desc class="value">0.63</desc><desc class="x left top">665.806486253</desc><desc class="y left top">311.31426795</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="671.409031576" cy="382.927457204" r="1.5"/><desc class="value">0.13</desc><desc class="x left top">671.409031576</desc><desc class="y left top">382.927457204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="677.0115769" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">677.0115769</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="682.614122223" cy="399.613674673" r="1.5"/><desc class="value">0.09</desc><desc class="x left top">682.614122223</desc><desc class="y left top">399.613674673</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="688.216667547" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x left top">688.216667547</desc><desc class="y left top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="693.81921287" cy="499.316955788" r="1.5"/><desc class="value">0.01</desc><desc class="x left top">693.81921287</desc><desc class="y left top">499.316955788</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="699.421758194" cy="363.379855822" r="1.5"/><desc class="value">0.2</desc><desc class="x left top">699.421758194</desc><desc class="y left top">363.379855822</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="705.024303518" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">705.024303518</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="710.626848841" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">710.626848841</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="716.229394165" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">716.229394165</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="721.831939488" cy="404.958305912" r="1.5"/><desc class="value">0.08</desc><desc class="x left top">721.831939488</desc><desc class="y left top">404.958305912</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="727.434484812" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">727.434484812</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="733.037030135" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x left top">733.037030135</desc><desc class="y left top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="738.639575459" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x left top">738.639575459</desc><desc class="y left top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="744.242120783" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">744.242120783</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="749.844666106" cy="379.564665773" r="1.5"/><desc class="value">0.14</desc><desc class="x left top">749.844666106</desc><desc class="y left top">379.564665773</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="755.44721143" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">755.44721143</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="761.049756753" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">761.049756753</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="766.652302077" cy="449.46531523" r="1.5"/><desc class="value">0.03</desc><desc class="x left top">766.652302077</desc><desc class="y left top">449.46531523</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="772.2548474" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">772.2548474</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="777.857392724" cy="426.285622407" r="1.5"/><desc class="value">0.05</desc><desc class="x left top">777.857392724</desc><desc class="y left top">426.285622407</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="783.459938047" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x left top">783.459938047</desc><desc class="y left top">436.411189204</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="789.062483371" cy="418.012431938" r="1.5"/><desc class="value">0.06</desc><desc class="x left top">789.062483371</desc><desc class="y left top">418.012431938</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="794.665028695" cy="361.165908507" r="1.5"/><desc class="value">0.21</desc><desc class="x left top">794.665028695</desc><desc class="y left top">361.165908507</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="800.267574018" cy="394.832739115" r="1.5"/><desc class="value">0.1</desc><desc class="x left top">800.267574018</desc><desc class="y left top">394.832739115</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="805.870119342" cy="411.017549065" r="1.5"/><desc class="value">0.07</desc><desc class="x left top">805.870119342</desc><desc class="y left top">411.017549065</desc></g><g class="dots"><circle class="dot reactive tooltip-trigger" cx="811.472664665" cy="436.411189204" r="1.5"/><desc class="value">0.04</desc><desc class="x left top">811.472664665</desc><desc class="y left top">436.411189204</desc></g></g></g><g class="plot text-overlay" transform="translate(143, 46)"><g class="series serie-0 color-0"><text class="centered" x="23.9112287189" y="19.4714756529">495.0</text><text class="centered" x="29.5137740425" y="19.6551881255">493.0</text><text class="centered" x="35.1163193661" y="19.379897582">496.0</text><text class="centered" x="40.7188646896" y="19.5632389173">494.0</text><text class="centered" x="46.3214100132" y="19.379897582">496.0</text><text class="centered" x="51.9239553368" y="19.4714756529">495.0</text><text class="centered" x="57.5265006603" y="19.4714756529">495.0</text><text class="centered" x="63.1290459839" y="19.5632389173">494.0</text><text class="centered" x="68.7315913075" y="19.6551881255">493.0</text><text class="centered" x="74.334136631" y="19.6551881255">493.0</text><text class="centered" x="79.9366819546" y="19.6551881255">493.0</text><text class="centered" x="85.5392272782" y="19.4714756529">495.0</text><text class="centered" x="91.1417726017" y="19.5632389173">494.0</text><text class="centered" x="96.7443179253" y="19.5632389173">494.0</text><text class="centered" x="102.346863249" y="19.4714756529">495.0</text><text class="centered" x="107.949408572" y="18.9247591751">501.0</text><text class="centered" x="113.551953896" y="19.379897582">496.0</text><text class="centered" x="119.15449922" y="19.379897582">496.0</text><text class="centered" x="124.757044543" y="19.1972940408">498.0</text><text class="centered" x="130.359589867" y="19.4714756529">495.0</text><text class="centered" x="135.96213519" y="19.379897582">496.0</text><text class="centered" x="141.564680514" y="19.4714756529">495.0</text><text class="centered" x="147.167225837" y="19.4714756529">495.0</text><text class="centered" x="152.769771161" y="19.5632389173">494.0</text><text class="centered" x="158.372316485" y="19.6551881255">493.0</text><text class="centered" x="163.974861808" y="19.5632389173">494.0</text><text class="centered" x="169.577407132" y="18.8342767547">502.0</text><text class="centered" x="175.179952455" y="19.2885039585">497.0</text><text class="centered" x="180.782497779" y="19.0154223795">500.0</text><text class="centered" x="186.385043102" y="19.2885039585">497.0</text><text class="centered" x="191.987588426" y="19.2885039585">497.0</text><text class="centered" x="197.590133749" y="19.2885039585">497.0</text><text class="centered" x="203.192679073" y="19.2885039585">497.0</text><text class="centered" x="208.795224397" y="19.379897582">496.0</text><text class="centered" x="214.39776972" y="19.5632389173">494.0</text><text class="centered" x="220.000315044" y="19.5632389173">494.0</text><text class="centered" x="225.602860367" y="19.6551881255">493.0</text><text class="centered" x="231.205405691" y="19.6551881255">493.0</text><text class="centered" x="236.807951014" y="19.6551881255">493.0</text><text class="centered" x="242.410496338" y="19.7473240328">492.0</text><text class="centered" x="248.013041662" y="19.6551881255">493.0</text><text class="centered" x="253.615586985" y="19.6551881255">493.0</text><text class="centered" x="259.218132309" y="19.6551881255">493.0</text><text class="centered" x="264.820677632" y="19.7473240328">492.0</text><text class="centered" x="270.423222956" y="19.7473240328">492.0</text><text class="centered" x="276.025768279" y="19.7473240328">492.0</text><text class="centered" x="281.628313603" y="19.7473240328">492.0</text><text class="centered" x="287.230858927" y="19.7473240328">492.0</text><text class="centered" x="292.83340425" y="19.7473240328">492.0</text><text class="centered" x="298.435949574" y="19.7473240328">492.0</text><text class="centered" x="304.038494897" y="19.7473240328">492.0</text><text class="centered" x="309.641040221" y="19.7473240328">492.0</text><text class="centered" x="315.243585544" y="21.0572641972">478.0</text><text class="centered" x="320.846130868" y="21.1522944283">477.0</text><text class="centered" x="326.448676191" y="20.7733617983">481.0</text><text class="centered" x="332.051221515" y="19.5632389173">494.0</text><text class="centered" x="337.653766839" y="20.3975673627">485.0</text><text class="centered" x="343.256312162" y="21.3429540312">475.0</text><text class="centered" x="348.858857486" y="19.5632389173">494.0</text><text class="centered" x="354.461402809" y="19.7473240328">492.0</text><text class="centered" x="360.063948133" y="20.3975673627">485.0</text><text class="centered" x="365.666493456" y="19.2885039585">497.0</text><text class="centered" x="371.26903878" y="20.3041030274">486.0</text><text class="centered" x="376.871584104" y="20.491224607">484.0</text><text class="centered" x="382.474129427" y="18.9247591751">501.0</text><text class="centered" x="388.076674751" y="18.9247591751">501.0</text><text class="centered" x="393.679220074" y="18.3845513813">507.0</text><text class="centered" x="399.281765398" y="17.8506991209">513.0</text><text class="centered" x="404.884310721" y="17.3230545856">519.0</text><text class="centered" x="410.486856045" y="15.0217599504">546.0</text><text class="centered" x="416.089401369" y="15.7759643277">537.0</text><text class="centered" x="421.691946692" y="16.6289390438">527.0</text><text class="centered" x="427.294492016" y="16.3713588701">530.0</text><text class="centered" x="432.897037339" y="16.8014750644">525.0</text><text class="centered" x="438.499582663" y="17.1485277599">521.0</text><text class="centered" x="444.102127986" y="14.3617204041">554.0</text><text class="centered" x="449.70467331" y="15.2717711544">543.0</text><text class="centered" x="455.307218634" y="17.2357072656">520.0</text><text class="centered" x="460.909763957" y="17.674135012">515.0</text><text class="centered" x="466.512309281" y="17.5861099237">516.0</text><text class="centered" x="472.114854604" y="17.5861099237">516.0</text><text class="centered" x="477.717399928" y="17.4105703673">518.0</text><text class="centered" x="483.319945251" y="17.4105703673">518.0</text><text class="centered" x="488.922490575" y="16.1152325904">533.0</text><text class="centered" x="494.525035898" y="15.6072767032">539.0</text><text class="centered" x="500.127581222" y="15.6072767032">539.0</text><text class="centered" x="505.730126546" y="15.1049440594">545.0</text><text class="centered" x="511.332671869" y="15.9452813852">535.0</text><text class="centered" x="516.935217193" y="15.8605438839">536.0</text><text class="centered" x="522.537762516" y="15.2717711544">543.0</text><text class="centered" x="528.14030784" y="12.9902246839">571.0</text><text class="centered" x="533.742853163" y="13.4695627899">565.0</text><text class="centered" x="539.345398487" y="13.549947078">564.0</text><text class="centered" x="544.947943811" y="10.6666666667">601.0</text><text class="centered" x="550.550489134" y="11.7362058466">587.0</text><text class="centered" x="556.153034458" y="13.6304740179">563.0</text><text class="centered" x="561.755579781" y="13.9540184813">559.0</text><text class="centered" x="567.358125105" y="14.1981997992">556.0</text><text class="centered" x="572.960670428" y="12.9902246839">571.0</text><text class="centered" x="578.563215752" y="14.9387280539">547.0</text><text class="centered" x="584.165761076" y="16.2004474863">532.0</text><text class="centered" x="589.768306399" y="15.18828094">544.0</text><text class="centered" x="595.370851723" y="15.355415268">542.0</text><text class="centered" x="600.973397046" y="15.439213849">541.0</text><text class="centered" x="606.57594237" y="15.691542129">538.0</text><text class="centered" x="612.178487693" y="15.6072767032">539.0</text><text class="centered" x="617.781033017" y="15.8605438839">536.0</text><text class="centered" x="623.38357834" y="15.8605438839">536.0</text><text class="centered" x="628.986123664" y="15.8605438839">536.0</text><text class="centered" x="634.588668988" y="14.1981997992">556.0</text><text class="centered" x="640.191214311" y="16.6289390438">527.0</text><text class="centered" x="645.793759635" y="18.8342767547">502.0</text><text class="centered" x="651.396304958" y="18.8342767547">502.0</text><text class="centered" x="656.998850282" y="18.8342767547">502.0</text><text class="centered" x="662.601395605" y="18.8342767547">502.0</text><text class="centered" x="668.203940929" y="18.8342767547">502.0</text><text class="centered" x="673.806486253" y="18.7439743991">503.0</text><text class="centered" x="679.409031576" y="18.8342767547">502.0</text><text class="centered" x="685.0115769" y="18.8342767547">502.0</text><text class="centered" x="690.614122223" y="18.8342767547">502.0</text><text class="centered" x="696.216667547" y="18.8342767547">502.0</text><text class="centered" x="701.81921287" y="18.8342767547">502.0</text><text class="centered" x="707.421758194" y="18.7439743991">503.0</text><text class="centered" x="713.024303518" y="18.8342767547">502.0</text><text class="centered" x="718.626848841" y="18.8342767547">502.0</text><text class="centered" x="724.229394165" y="18.8342767547">502.0</text><text class="centered" x="729.831939488" y="18.8342767547">502.0</text><text class="centered" x="735.434484812" y="18.8342767547">502.0</text><text class="centered" x="741.037030135" y="18.8342767547">502.0</text><text class="centered" x="746.639575459" y="18.8342767547">502.0</text><text class="centered" x="752.242120783" y="18.8342767547">502.0</text><text class="centered" x="757.844666106" y="18.8342767547">502.0</text><text class="centered" x="763.44721143" y="18.8342767547">502.0</text><text class="centered" x="769.049756753" y="18.8342767547">502.0</text><text class="centered" x="774.652302077" y="18.8342767547">502.0</text><text class="centered" x="780.2548474" y="18.8342767547">502.0</text><text class="centered" x="785.857392724" y="18.8342767547">502.0</text><text class="centered" x="791.459938047" y="18.8342767547">502.0</text><text class="centered" x="797.062483371" y="18.2951387046">508.0</text><text class="centered" x="802.665028695" y="18.8342767547">502.0</text><text class="centered" x="808.267574018" y="18.8342767547">502.0</text><text class="centered" x="813.870119342" y="18.8342767547">502.0</text><text class="centered" x="819.472664665" y="18.8342767547">502.0</text></g><g class="series serie-1 color-1"><text class="centered" x="23.9112287189" y="421.684215732">0.07</text><text class="centered" x="29.5137740425" y="371.832575174">0.21</text><text class="centered" x="35.1163193661" y="381.421130748">0.17</text><text class="centered" x="40.7188646896" y="447.07785587">0.04</text><text class="centered" x="46.3214100132" y="478.530739163">0.02</text><text class="centered" x="51.9239553368" y="317.852915628">0.69</text><text class="centered" x="57.5265006603" y="509.983622455">0.01</text><text class="centered" x="63.1290459839" y="460.131981897">0.03</text><text class="centered" x="68.7315913075" y="460.131981897">0.03</text><text class="centered" x="74.334136631" y="393.59412387">0.13</text><text class="centered" x="79.9366819546" y="460.131981897">0.03</text><text class="centered" x="85.5392272782" y="509.983622455">0.01</text><text class="centered" x="91.1417726017" y="509.983622455">0.01</text><text class="centered" x="102.346863249" y="509.983622455">0.01</text><text class="centered" x="107.949408572" y="415.624972578">0.08</text><text class="centered" x="113.551953896" y="295.872682563">1.12</text><text class="centered" x="130.359589867" y="329.824008891">0.53</text><text class="centered" x="141.564680514" y="478.530739163">0.02</text><text class="centered" x="147.167225837" y="378.827458047">0.18</text><text class="centered" x="158.372316485" y="421.684215732">0.07</text><text class="centered" x="163.974861808" y="460.131981897">0.03</text><text class="centered" x="175.179952455" y="363.920955692">0.25</text><text class="centered" x="180.782497779" y="421.684215732">0.07</text><text class="centered" x="186.385043102" y="436.952289073">0.05</text><text class="centered" x="191.987588426" y="478.530739163">0.02</text><text class="centered" x="203.192679073" y="421.684215732">0.07</text><text class="centered" x="225.602860367" y="316.556344029">0.71</text><text class="centered" x="236.807951014" y="410.280341339">0.09</text><text class="centered" x="242.410496338" y="342.593639197">0.4</text><text class="centered" x="248.013041662" y="410.280341339">0.09</text><text class="centered" x="253.615586985" y="428.679098605">0.06</text><text class="centered" x="259.218132309" y="428.679098605">0.06</text><text class="centered" x="264.820677632" y="447.07785587">0.04</text><text class="centered" x="270.423222956" y="405.499405781">0.1</text><text class="centered" x="276.025768279" y="421.684215732">0.07</text><text class="centered" x="281.628313603" y="478.530739163">0.02</text><text class="centered" x="287.230858927" y="363.920955692">0.25</text><text class="centered" x="298.435949574" y="447.07785587">0.04</text><text class="centered" x="304.038494897" y="428.679098605">0.06</text><text class="centered" x="315.243585544" y="478.530739163">0.02</text><text class="centered" x="320.846130868" y="509.983622455">0.01</text><text class="centered" x="326.448676191" y="415.624972578">0.08</text><text class="centered" x="332.051221515" y="390.231332439">0.14</text><text class="centered" x="337.653766839" y="367.704556186">0.23</text><text class="centered" x="343.256312162" y="401.174523496">0.11</text><text class="centered" x="348.858857486" y="331.56949019">0.51</text><text class="centered" x="354.461402809" y="405.499405781">0.1</text><text class="centered" x="360.063948133" y="390.231332439">0.14</text><text class="centered" x="365.666493456" y="376.374054141">0.19</text><text class="centered" x="371.26903878" y="509.983622455">0.01</text><text class="centered" x="376.871584104" y="478.530739163">0.02</text><text class="centered" x="382.474129427" y="318.515364163">0.68</text><text class="centered" x="388.076674751" y="349.968247456">0.34</text><text class="centered" x="393.679220074" y="317.199999058">0.7</text><text class="centered" x="399.281765398" y="355.647765224">0.3</text><text class="centered" x="404.884310721" y="362.141240578">0.26</text><text class="centered" x="410.486856045" y="341.473164522">0.41</text><text class="centered" x="416.089401369" y="352.719205994">0.32</text><text class="centered" x="421.691946692" y="335.275787567">0.47</text><text class="centered" x="427.294492016" y="428.679098605">0.06</text><text class="centered" x="432.897037339" y="460.131981897">0.03</text><text class="centered" x="438.499582663" y="365.773332021">0.24</text><text class="centered" x="444.102127986" y="436.952289073">0.05</text><text class="centered" x="449.70467331" y="357.186113179">0.29</text><text class="centered" x="455.307218634" y="428.679098605">0.06</text><text class="centered" x="460.909763957" y="415.624972578">0.08</text><text class="centered" x="466.512309281" y="405.499405781">0.1</text><text class="centered" x="472.114854604" y="352.719205994">0.32</text><text class="centered" x="477.717399928" y="415.624972578">0.08</text><text class="centered" x="483.319945251" y="376.374054141">0.19</text><text class="centered" x="488.922490575" y="355.647765224">0.3</text><text class="centered" x="494.525035898" y="328.97581749">0.54</text><text class="centered" x="500.127581222" y="478.530739163">0.02</text><text class="centered" x="505.730126546" y="415.624972578">0.08</text><text class="centered" x="511.332671869" y="428.679098605">0.06</text><text class="centered" x="516.935217193" y="397.226215313">0.12</text><text class="centered" x="522.537762516" y="381.421130748">0.17</text><text class="centered" x="528.14030784" y="397.226215313">0.12</text><text class="centered" x="533.742853163" y="410.280341339">0.09</text><text class="centered" x="539.345398487" y="365.773332021">0.24</text><text class="centered" x="544.947943811" y="351.322882939">0.33</text><text class="centered" x="550.550489134" y="384.172089286">0.16</text><text class="centered" x="556.153034458" y="401.174523496">0.11</text><text class="centered" x="561.755579781" y="381.421130748">0.17</text><text class="centered" x="567.358125105" y="341.473164522">0.41</text><text class="centered" x="572.960670428" y="343.742483313">0.39</text><text class="centered" x="578.563215752" y="401.174523496">0.11</text><text class="centered" x="584.165761076" y="310.577060224">0.81</text><text class="centered" x="589.768306399" y="333.384809008">0.49</text><text class="centered" x="595.370851723" y="302.867565436">0.96</text><text class="centered" x="600.973397046" y="302.867565436">0.96</text><text class="centered" x="606.57594237" y="360.428700782">0.27</text><text class="centered" x="612.178487693" y="509.983622455">0.01</text><text class="centered" x="617.781033017" y="478.530739163">0.02</text><text class="centered" x="623.38357834" y="381.421130748">0.17</text><text class="centered" x="628.986123664" y="371.832575174">0.21</text><text class="centered" x="634.588668988" y="311.140755905">0.8</text><text class="centered" x="640.191214311" y="387.100648516">0.15</text><text class="centered" x="645.793759635" y="397.226215313">0.12</text><text class="centered" x="651.396304958" y="509.983622455">0.01</text><text class="centered" x="656.998850282" y="428.679098605">0.06</text><text class="centered" x="662.601395605" y="509.983622455">0.01</text><text class="centered" x="668.203940929" y="428.679098605">0.06</text><text class="centered" x="673.806486253" y="297.522934197">1.08</text><text class="centered" x="679.409031576" y="447.07785587">0.04</text><text class="centered" x="685.0115769" y="478.530739163">0.02</text><text class="centered" x="690.614122223" y="460.131981897">0.03</text><text class="centered" x="696.216667547" y="509.983622455">0.01</text><text class="centered" x="707.421758194" y="367.704556186">0.23</text><text class="centered" x="713.024303518" y="410.280341339">0.09</text><text class="centered" x="718.626848841" y="405.499405781">0.1</text><text class="centered" x="729.831939488" y="397.226215313">0.12</text><text class="centered" x="735.434484812" y="410.280341339">0.09</text><text class="centered" x="741.037030135" y="421.684215732">0.07</text><text class="centered" x="746.639575459" y="478.530739163">0.02</text><text class="centered" x="752.242120783" y="421.684215732">0.07</text><text class="centered" x="757.844666106" y="397.226215313">0.12</text><text class="centered" x="763.44721143" y="509.983622455">0.01</text><text class="centered" x="774.652302077" y="509.983622455">0.01</text><text class="centered" x="780.2548474" y="390.231332439">0.14</text><text class="centered" x="785.857392724" y="415.624972578">0.08</text><text class="centered" x="791.459938047" y="478.530739163">0.02</text><text class="centered" x="797.062483371" y="436.952289073">0.05</text><text class="centered" x="802.665028695" y="415.624972578">0.08</text><text class="centered" x="808.267574018" y="415.624972578">0.08</text><text class="centered" x="813.870119342" y="387.100648516">0.15</text><text class="centered" x="819.472664665" y="478.530739163">0.02</text></g><g class="series serie-2 color-2"><text class="centered" x="23.9112287189" y="415.624972578">0.08</text><text class="centered" x="29.5137740425" y="393.59412387">0.13</text><text class="centered" x="35.1163193661" y="343.742483313">0.39</text><text class="centered" x="40.7188646896" y="405.499405781">0.1</text><text class="centered" x="46.3214100132" y="447.07785587">0.04</text><text class="centered" x="51.9239553368" y="346.13129373">0.37</text><text class="centered" x="57.5265006603" y="410.280341339">0.09</text><text class="centered" x="63.1290459839" y="428.679098605">0.06</text><text class="centered" x="68.7315913075" y="390.231332439">0.14</text><text class="centered" x="74.334136631" y="410.280341339">0.09</text><text class="centered" x="79.9366819546" y="410.280341339">0.09</text><text class="centered" x="85.5392272782" y="390.231332439">0.14</text><text class="centered" x="91.1417726017" y="460.131981897">0.03</text><text class="centered" x="96.7443179253" y="509.983622455">0.01</text><text class="centered" x="102.346863249" y="410.280341339">0.09</text><text class="centered" x="107.949408572" y="415.624972578">0.08</text><text class="centered" x="113.551953896" y="346.13129373">0.37</text><text class="centered" x="119.15449922" y="421.684215732">0.07</text><text class="centered" x="124.757044543" y="447.07785587">0.04</text><text class="centered" x="130.359589867" y="343.742483313">0.39</text><text class="centered" x="135.96213519" y="436.952289073">0.05</text><text class="centered" x="141.564680514" y="405.499405781">0.1</text><text class="centered" x="147.167225837" y="358.778449147">0.28</text><text class="centered" x="152.769771161" y="447.07785587">0.04</text><text class="centered" x="158.372316485" y="436.952289073">0.05</text><text class="centered" x="163.974861808" y="384.172089286">0.16</text><text class="centered" x="169.577407132" y="460.131981897">0.03</text><text class="centered" x="175.179952455" y="410.280341339">0.09</text><text class="centered" x="180.782497779" y="376.374054141">0.19</text><text class="centered" x="186.385043102" y="436.952289073">0.05</text><text class="centered" x="191.987588426" y="447.07785587">0.04</text><text class="centered" x="197.590133749" y="381.421130748">0.17</text><text class="centered" x="203.192679073" y="436.952289073">0.05</text><text class="centered" x="208.795224397" y="460.131981897">0.03</text><text class="centered" x="214.39776972" y="405.499405781">0.1</text><text class="centered" x="220.000315044" y="447.07785587">0.04</text><text class="centered" x="225.602860367" y="374.046522489">0.2</text><text class="centered" x="231.205405691" y="421.684215732">0.07</text><text class="centered" x="236.807951014" y="436.952289073">0.05</text><text class="centered" x="242.410496338" y="341.473164522">0.41</text><text class="centered" x="248.013041662" y="405.499405781">0.1</text><text class="centered" x="253.615586985" y="428.679098605">0.06</text><text class="centered" x="259.218132309" y="371.832575174">0.21</text><text class="centered" x="264.820677632" y="401.174523496">0.11</text><text class="centered" x="270.423222956" y="428.679098605">0.06</text><text class="centered" x="276.025768279" y="367.704556186">0.23</text><text class="centered" x="281.628313603" y="393.59412387">0.13</text><text class="centered" x="287.230858927" y="352.719205994">0.32</text><text class="centered" x="292.83340425" y="374.046522489">0.2</text><text class="centered" x="298.435949574" y="447.07785587">0.04</text><text class="centered" x="304.038494897" y="421.684215732">0.07</text><text class="centered" x="309.641040221" y="393.59412387">0.13</text><text class="centered" x="315.243585544" y="428.679098605">0.06</text><text class="centered" x="320.846130868" y="447.07785587">0.04</text><text class="centered" x="326.448676191" y="397.226215313">0.12</text><text class="centered" x="332.051221515" y="381.421130748">0.17</text><text class="centered" x="337.653766839" y="378.827458047">0.18</text><text class="centered" x="343.256312162" y="371.832575174">0.21</text><text class="centered" x="348.858857486" y="341.473164522">0.41</text><text class="centered" x="354.461402809" y="358.778449147">0.28</text><text class="centered" x="360.063948133" y="371.832575174">0.21</text><text class="centered" x="365.666493456" y="371.832575174">0.21</text><text class="centered" x="371.26903878" y="436.952289073">0.05</text><text class="centered" x="376.871584104" y="354.159864098">0.31</text><text class="centered" x="382.474129427" y="333.384809008">0.49</text><text class="centered" x="388.076674751" y="337.249007958">0.45</text><text class="centered" x="393.679220074" y="333.384809008">0.49</text><text class="centered" x="399.281765398" y="349.968247456">0.34</text><text class="centered" x="404.884310721" y="336.251672894">0.46</text><text class="centered" x="410.486856045" y="363.920955692">0.25</text><text class="centered" x="416.089401369" y="371.832575174">0.21</text><text class="centered" x="421.691946692" y="342.593639197">0.4</text><text class="centered" x="427.294492016" y="351.322882939">0.33</text><text class="centered" x="432.897037339" y="405.499405781">0.1</text><text class="centered" x="438.499582663" y="384.172089286">0.16</text><text class="centered" x="444.102127986" y="348.65288235">0.35</text><text class="centered" x="449.70467331" y="374.046522489">0.2</text><text class="centered" x="455.307218634" y="410.280341339">0.09</text><text class="centered" x="460.909763957" y="421.684215732">0.07</text><text class="centered" x="466.512309281" y="415.624972578">0.08</text><text class="centered" x="472.114854604" y="387.100648516">0.15</text><text class="centered" x="477.717399928" y="397.226215313">0.12</text><text class="centered" x="483.319945251" y="390.231332439">0.14</text><text class="centered" x="488.922490575" y="362.141240578">0.26</text><text class="centered" x="494.525035898" y="328.97581749">0.54</text><text class="centered" x="500.127581222" y="393.59412387">0.13</text><text class="centered" x="505.730126546" y="393.59412387">0.13</text><text class="centered" x="511.332671869" y="390.231332439">0.14</text><text class="centered" x="516.935217193" y="401.174523496">0.11</text><text class="centered" x="522.537762516" y="363.920955692">0.25</text><text class="centered" x="528.14030784" y="381.421130748">0.17</text><text class="centered" x="533.742853163" y="371.832575174">0.21</text><text class="centered" x="539.345398487" y="343.742483313">0.39</text><text class="centered" x="544.947943811" y="357.186113179">0.29</text><text class="centered" x="550.550489134" y="376.374054141">0.19</text><text class="centered" x="556.153034458" y="393.59412387">0.13</text><text class="centered" x="561.755579781" y="401.174523496">0.11</text><text class="centered" x="567.358125105" y="348.65288235">0.35</text><text class="centered" x="572.960670428" y="358.778449147">0.28</text><text class="centered" x="578.563215752" y="371.832575174">0.21</text><text class="centered" x="584.165761076" y="339.311950413">0.43</text><text class="centered" x="589.768306399" y="326.522413583">0.57</text><text class="centered" x="595.370851723" y="323.44483314">0.61</text><text class="centered" x="600.973397046" y="290.889622311">1.25</text><text class="centered" x="606.57594237" y="342.593639197">0.4</text><text class="centered" x="612.178487693" y="405.499405781">0.1</text><text class="centered" x="617.781033017" y="405.499405781">0.1</text><text class="centered" x="623.38357834" y="397.226215313">0.12</text><text class="centered" x="628.986123664" y="387.100648516">0.15</text><text class="centered" x="634.588668988" y="321.266322702">0.64</text><text class="centered" x="640.191214311" y="339.311950413">0.43</text><text class="centered" x="645.793759635" y="397.226215313">0.12</text><text class="centered" x="651.396304958" y="436.952289073">0.05</text><text class="centered" x="656.998850282" y="421.684215732">0.07</text><text class="centered" x="662.601395605" y="397.226215313">0.12</text><text class="centered" x="668.203940929" y="421.684215732">0.07</text><text class="centered" x="673.806486253" y="321.980934616">0.63</text><text class="centered" x="679.409031576" y="393.59412387">0.13</text><text class="centered" x="685.0115769" y="405.499405781">0.1</text><text class="centered" x="690.614122223" y="410.280341339">0.09</text><text class="centered" x="696.216667547" y="447.07785587">0.04</text><text class="centered" x="701.81921287" y="509.983622455">0.01</text><text class="centered" x="707.421758194" y="374.046522489">0.2</text><text class="centered" x="713.024303518" y="415.624972578">0.08</text><text class="centered" x="718.626848841" y="428.679098605">0.06</text><text class="centered" x="724.229394165" y="460.131981897">0.03</text><text class="centered" x="729.831939488" y="415.624972578">0.08</text><text class="centered" x="735.434484812" y="428.679098605">0.06</text><text class="centered" x="741.037030135" y="447.07785587">0.04</text><text class="centered" x="746.639575459" y="436.952289073">0.05</text><text class="centered" x="752.242120783" y="428.679098605">0.06</text><text class="centered" x="757.844666106" y="390.231332439">0.14</text><text class="centered" x="763.44721143" y="460.131981897">0.03</text><text class="centered" x="769.049756753" y="460.131981897">0.03</text><text class="centered" x="774.652302077" y="460.131981897">0.03</text><text class="centered" x="780.2548474" y="428.679098605">0.06</text><text class="centered" x="785.857392724" y="436.952289073">0.05</text><text class="centered" x="791.459938047" y="447.07785587">0.04</text><text class="centered" x="797.062483371" y="428.679098605">0.06</text><text class="centered" x="802.665028695" y="371.832575174">0.21</text><text class="centered" x="808.267574018" y="405.499405781">0.1</text><text class="centered" x="813.870119342" y="421.684215732">0.07</text><text class="centered" x="819.472664665" y="447.07785587">0.04</text></g></g><g class="plot tooltip-overlay" transform="translate(143, 46)"><g id="tooltip" style="opacity: 0" transform="translate(0 0)"><a><rect height="0" id="tooltip-box" rx="0" ry="0" width="0"/><text class="text"><tspan class="label"/><tspan class="value"/></text></a></g></g><g class="legends" transform="translate(10, 56)"><g class="legend reactive activate-serie" id="activate-serie-0"><rect class="color-0 reactive" height="12" width="12" x="0.0" y="1.0"/><text x="17.0" y="11.2">ldavg-1</text></g><g class="legend reactive activate-serie" id="activate-serie-1"><rect class="color-1 reactive" height="12" width="12" x="0.0" y="22.0"/><text x="17.0" y="32.2">ldavg-5</text></g><g class="legend reactive activate-serie" id="activate-serie-2"><rect class="color-2 reactive" height="12" width="12" x="0.0" y="43.0"/><text x="17.0" y="53.2">ldavg-15</text></g></g><g class="legends" transform="translate(980, 56)"/></g></svg>